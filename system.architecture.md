# system.architecture.md

> **Este arquivo é a fonte da verdade da arquitetura deste sistema.**
> Objetivo: permitir que qualquer IA (ou o próprio autor, em outro ambiente) reconstrua o sistema do zero apenas lendo este documento.
>
> **Regra de manutenção**: toda vez que uma mudança relevante for feita no sistema (nova tabela/coluna, nova rota, novo componente, nova integração, mudança de fluxo de negócio, correção de bug estrutural), este arquivo DEVE ser atualizado no mesmo commit/sessão. Veja a seção [Changelog](#changelog) no final.

---

## 1. Visão geral do produto

SaaS para **missionários** centralizarem sua comunicação com apoiadores/parceiros, hoje fragmentada em Instagram, WhatsApp, PDF, e-mail e Pix.

Módulos principais:
- **Perfil público** do missionário (`/[username]`), com modos de privacidade (público/privado/stealth).
- **Projetos** (antigo "destaques"/highlights) — campanhas de arrecadação com metas financeiras, orçamento por categoria, marcos, equipe.
- **CRM de parceiros** — cadastro, tipos (financeiro/oração/embaixador/ambos), notas, tags.
- **Financeiro multi-conta** — contas (pessoais ou compartilhadas em equipe), transações, categorias hierárquicas, conciliação de ofertas (pledges).
- **Pedidos de oração** bilaterais (missionário ↔ parceiro), com opção de conteúdo privado cifrado (E2EE).
- **Mensagens diretas** cifradas ponta-a-ponta (E2EE) entre missionário e parceiro.
- **Notificações** em tempo real (Supabase Realtime).
- **Dados sensíveis do perfil** (endereço real, itinerário, etc.) cifrados e liberados seletivamente por parceiro.
- **Assinaturas Stripe** (planos free/pro/mission + assentos extras de gestor) — ativas em modo teste desde 2026-09-01, ver seção 7.19. IA copiloto via Claude API funcional (tradução de conteúdo); WhatsApp Business API e conversão de câmbio (ExchangeRate-API) ainda **sem código de integração**; e-mail transacional via Brevo já em produção (seção 7.10).

### Monetização
- **Free**: 2 parceiros, 1 post/mês, sem IA (pay-per-use). Financeiro bloqueado (teaser + CTA pra `/planos`).
- **Pro**: $20/mês ou $200/ano — parceiros/posts/projetos ilimitados + 50 créditos IA + Financeiro + 1 gestor incluso.
- **Missão**: $45/mês ou $450/ano — tudo do Pro + WhatsApp (planejado, sem código ainda) + 150 créditos IA + 2 gestores inclusos.
- **Assentos extras de gestor** (`MANAGER_ADDONS`, `src/lib/pricing.ts`): +2 por R$15/mês, +4 por R$30/mês — únicos itens do catálogo cobrados em BRL fixo (o resto usa USD + Adaptive Pricing da Stripe, ver 7.19), inconsistência conhecida, não corrigida ainda.
- **Superadmin** (`SUPERADMIN_EMAILS`, ver 7.1-bis) ignora todo limite de plano — parceiros, posts/mês, gestores e o gate de Financeiro — em qualquer plano, inclusive Free. Ver seção 7.19.

---

## 2. Stack técnica

| Camada | Tecnologia |
|---|---|
| Framework | Next.js 16.2.7 (App Router), Turbopack |
| Linguagem | TypeScript, `strict: true` |
| UI | React 19.2.4, Tailwind CSS v4 (CSS-first, sem `tailwind.config`), shadcn/ui v4 (`style: "base-nova"`) sobre `@base-ui/react` (NÃO Radix, embora `@radix-ui/*` ainda esteja instalado como resquício) |
| Ícones | `lucide-react` |
| Toasts | `sonner` |
| Dark mode | `next-themes` (`attribute="class"`) |
| i18n | `next-intl` — PT/EN/ES, sem prefixo de URL (ver seção 5.6) |
| Backend/DB | Supabase (Postgres + Auth + Storage + Realtime), acessado via `@supabase/ssr` e `@supabase/supabase-js` |
| Criptografia E2EE | `libsodium-wrappers` (X25519 + XSalsa20-Poly1305 + Argon2id), 100% client-side |
| Compressão de mídia | `browser-image-compression` (imagens → WebP) |
| Geração de PDF | `pdfkit` (comprovante de doações do parceiro, seção 7.1-bis — API imperativa, sem dependência de React) |
| Drag-and-drop | `@dnd-kit/core`/`@dnd-kit/sortable`/`@dnd-kit/utilities` (2026-08-30, editor de linha do tempo da História — ver 7.3-tris/Changelog; suporte a toque de verdade via `PointerSensor`, importante por o app ser mobile-first) |
| Pagamentos | `stripe`, `@stripe/stripe-js` — implementado (Connect Express + Checkout avulso/recorrente, seção 7.11), não mais "planejado" |
| E-mail | `@getbrevo/brevo` (Brevo) — implementado (lembrete de recorrência e e-mail de acompanhamento a parceiro de oração, seções 7.11/7.3-tris), não mais "planejado" — correção de doc, esta linha estava desatualizada
| IA | `@anthropic-ai/sdk` (Anthropic Claude API) — tradução de conteúdo (`claude-haiku-4-5`, seção 5.6.1) implementada; demais usos futuros |
| Câmbio (planejado, não implementado) | ExchangeRate-API |
| Open Finance | Pluggy — `pluggy-js` (server, troca client id/secret por API key) + `react-pluggy-connect`/`pluggy-connect-sdk` (client, widget de conexão) — implementado (seção 7.32), aguardando credenciais reais do usuário |
| Deploy | Vercel (`.vercel/project.json` presente) |

Projeto Supabase: `eqnekupeiehgkacegmgl` (credenciais em `.env.local`, não versionado).

### Variáveis de ambiente (`.env.example`)

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Stripe
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_CONNECT_WEBHOOK_SECRET=
STRIPE_PRICE_PRO_MONTHLY=
STRIPE_PRICE_PRO_YEARLY=
STRIPE_PRICE_MISSION_MONTHLY=
STRIPE_PRICE_MISSION_YEARLY=
STRIPE_PRICE_CREDITS_20=
STRIPE_PRICE_CREDITS_50=
STRIPE_PRICE_CREDITS_100=

# Brevo (e-mail transacional)
BREVO_API_KEY=
BREVO_FROM_EMAIL=

# Anthropic (Claude API)
ANTHROPIC_API_KEY=

# Exchange Rate API
EXCHANGE_RATE_API_KEY=

# Geocoding (cascata de sugestão de localização — ver seção 7.9 e Changelog)
GOOGLE_PLACES_API_KEY=
MAPBOX_ACCESS_TOKEN=

# WhatsApp (Meta)
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_VERIFY_TOKEN=

# Open Finance (Pluggy) — dashboard.pluggy.ai, sandbox grátis (seção 7.32)
PLUGGY_CLIENT_ID=
PLUGGY_CLIENT_SECRET=

# App
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_APP_NAME=

# Superadmin (allowlist de e-mails, ver 7.1-bis — só habilita a barra de
# pré-visualização de papel no dashboard, nunca troca user_role de ninguém)
SUPERADMIN_EMAILS=
```

> **Status real das integrações**: Brevo, ExchangeRate-API e WhatsApp têm **schema de dados pronto** (tabelas, colunas, RLS) mas **nenhum código de integração** foi escrito (`src/lib/exchange/` e `supabase/functions/` estão vazios). Ao reconstruir, essas integrações precisam ser implementadas do zero seguindo o schema descrito abaixo. **Anthropic** deixou de ser "só schema": a primeira integração real (tradução de conteúdo do usuário) foi implementada — ver seção 5.6.1. **Google Places/Mapbox** (`src/lib/geocoding/`): código pronto e já ligado na cascata de sugestão de localização (`google-places.ts`/`mapbox.ts`, gateados por `isConfigured()`), mas sem chave configurada ainda — `googlePlacesApiKey()`/`mapboxAccessToken()` retornam `null` enquanto as env vars acima estiverem vazias, e a cascata (`cascade.ts`) pula esses tiers em silêncio, caindo direto no Photon (grátis, sem chave) — exatamente o comportamento de antes da cascata existir. Também depende da migration `054_geocoding_usage.sql` (tabela `geocoding_usage`, contador de cota) ter sido aplicada — ver nota de risco operacional logo abaixo sobre aplicação manual de migrations.
>
> **Stripe billing** (assinatura de plano + assentos extras de gestor) foi ativado em modo teste em 2026-09-01 — ver seção 7.19 pro detalhe completo (produtos/preços criados, webhook implementado, o que falta pra ir pra produção real).
>
> **Open Finance (Pluggy)**: código de integração completo (schema, rotas, sync, widget) — ver seção 7.32 — mas **desativado de propósito**: Pluggy cobra (~R$2.500/mês) e o usuário decidiu que é inviável nesta fase do produto (ver 7.33). `contas/page.tsx` volta a mostrar o botão desabilitado (`title="Em breve"`) em vez do dialog funcional; reativar é só trocar esse botão de volta e preencher `PLUGGY_CLIENT_ID`/`PLUGGY_CLIENT_SECRET` — `088_open_finance.sql` já está aplicada no Supabase (confirmado 2026-09-04, ver 7.32). Enquanto isso, a alternativa gratuita em produção é a importação de extrato OFX (migration `089_transaction_import.sql`, também já aplicada, seção 7.33) — sem custo, sem credencial nenhuma.

---

## 3. Modelo de dados (Postgres / Supabase)

Extensões: `uuid-ossp`, `pgcrypto`. Todas as PKs são `UUID DEFAULT uuid_generate_v4()`. Migrations em `supabase/migrations/001` a `017` (ordem de aplicação importa — cada uma altera o resultado da anterior).

### 3.1 Tabelas principais

#### `profiles` (hub central — 1:1 com `auth.users`)
- `id` PK, `user_id UUID UNIQUE FK auth.users ON DELETE CASCADE`
- `username TEXT UNIQUE NOT NULL` (único case-insensitive via índice em `LOWER(username)`)
- `display_name`, `bio`, `location`, `show_location BOOLEAN DEFAULT true` (migration 021 — controla se `location` aparece no perfil público; independente de `privacy_mode`), `account_type TEXT DEFAULT 'individual' CHECK IN ('individual','family','organization')` (migration 022 — só personaliza textos de onboarding/bio via `AccountTypeSelector` em `src/components/profile/account-type-selector.tsx`; não afeta plano nem permissões), `user_role TEXT DEFAULT 'partner' CHECK IN ('partner','missionary')` (migration 032 — **este** é o campo que decide papel/permissão de UI, ortogonal a `account_type` acima; ver seção 7.1-bis), `avatar_url`, `cover_url`
- `privacy_mode TEXT DEFAULT 'public' CHECK IN ('public','private','stealth')`
- `plan TEXT DEFAULT 'free' CHECK IN ('free','pro','mission')`
- `stripe_customer_id`, `accent_color TEXT DEFAULT '#34390c'` (cor livre por usuário, aplicada inline — esse hex é também o `--primary` fixo do app, ver seção 11 "Tema"; default trocado do indigo original (`#6366f1`) pro verde-oliva em 2026-08-25, migration `046_brand_accent_color.sql`)
- Links sociais: `website_url`, `instagram_url`, `youtube_url`, `facebook_url`, `tiktok_url`, `whatsapp_group_url` (migration 094, 2026-09-07 — link de convite de grupo do WhatsApp, `chat.whatsapp.com/...`; não depende da API do WhatsApp Business, que o app não integra — mostrado como opção nas telas de parceria pra quem não quer criar conta, ver `AccountUpsellCard` na seção 7.3-sexies/7.19-bis)
- `ai_credits INTEGER DEFAULT 0`
- `mission_start_date DATE` (migration 012)
- `extra_manager_seats INTEGER DEFAULT 0` (migration 023 — assentos de gestor extras comprados; ver `profile_managers` abaixo)
- `locale TEXT DEFAULT 'pt' CHECK IN ('pt','en','es')` (migration 024 — idioma preferido da **UI**; ver seção 5.6)
- `bio_locale TEXT DEFAULT 'pt' CHECK IN ('pt','en','es')` e `bio_translations JSONB DEFAULT '{}'` (migration 025 — idioma **original do conteúdo da bio** e suas traduções geradas/editadas; independente de `locale`, que só controla a interface. Ver seção 5.6.1)
- `email_verified BOOLEAN DEFAULT false`, `email_verification_token TEXT`, `email_verification_token_expires_at TIMESTAMPTZ` (migration 055 — verificação de e-mail obrigatória no cadastro; ver seção 7.1)
- `phone TEXT`, `whatsapp_contact_opt_in BOOLEAN DEFAULT false` (migration 055 — telefone/WhatsApp da **conta**, não do vínculo de parceria (`partners.phone`); serve pra reaproveitar o contato em qualquer parceria nova sem pedir de novo, e dar ao missionário autorização explícita de falar por WhatsApp)
- `birth_date DATE` (migration 080 — data de nascimento da conta, coletada só no `/cadastro` manual; ao virar parceiro(a) fixo(a) via `RecurringPledgeForm`, esse valor é copiado pra `partners.birth_date` do vínculo criado, alimentando `BirthdayReminders` sem exigir cadastro manual pelo missionário — ver seção 7.3. Chegou a existir também auto-preenchimento via Google People API no login OAuth, removido em 2026-09-08 — ver changelog)
- timestamps + trigger `update_updated_at`

Criado automaticamente por trigger `handle_new_user()` (`SECURITY DEFINER`, `AFTER INSERT ON auth.users`): `username = slug(email) + '_' + 6 chars do UUID`, `display_name` = `raw_user_meta_data->>'full_name'` ou prefixo do e-mail, `phone`/`birth_date` = lidos de `raw_user_meta_data` quando presentes (migrations 055/080 — necessário porque com confirmação de e-mail ativa não há sessão de cliente disponível pra um update logo após o cadastro), `email_verified` = `true` se a conta veio de um provider OAuth (Google já verifica o e-mail) e `false` se veio de e-mail/senha (migration 055).

#### `profile_managers` (contas vinculadas — migration 023)
- `profile_id FK profiles ON DELETE CASCADE`, `user_id FK auth.users ON DELETE CASCADE`, `role CHECK IN ('manager','viewer')` DEFAULT `manager`, `invited_by_user_id`, `UNIQUE (profile_id, user_id)`
- Dá a um usuário adicional acesso "estilo Instagram" a outro perfil, sem fundir nenhum dado: `manager` = mesmo nível do dono (posts, projetos, financeiro, oração, parceiros — tudo escopado por `profile_id` continua igual, só quem pode agir muda); `viewer` = leitura (perfil, posts, projetos, prayer_requests, history_blocks — não financeiro/E2EE por padrão nesta rodada).
- `is_profile_owner(p_profile_id)` (função SQL, migration 004, redefinida na 023) passou a checar também `profile_managers.role = 'manager'` — isso propaga automaticamente para toda política de RLS que já usava essa função (`partners`, `pledges`, `project_members`, dados E2EE, `financial_accounts`/`transactions` via `019_fix_financial_rls_recursion.sql`, budget categories). As políticas que ainda faziam o check inline (de antes da função existir — `posts`, `highlights`, `prayer_requests`, `transaction_categories`, `subscriptions`, `ai_credit_transactions`, `whatsapp_config`, `history_blocks`) foram migradas na 023 para usar o helper.
- `is_profile_viewer_or_above(p_profile_id)` — mesma ideia, mas também aceita `role = 'viewer'`; usada nas políticas de leitura de `profiles`, `posts`, `highlights`, `prayer_requests`, `history_blocks`.
- Convite por e-mail via RPC `invite_profile_manager(p_profile_id, p_email, p_role)` (`SECURITY DEFINER`, já que o client não pode consultar `auth.users`) — também aplica o limite de assentos (`planLimits().managersIncluded` + `extra_manager_seats`), retornando `seat_limit_reached` se estourar.
- **Limitação conhecida (decisão consciente, não bug)**: dados E2EE (mensagens diretas, campos sensíveis do perfil, pedidos de oração privados) são cifrados com a chave do usuário real, não do perfil — um gestor não necessariamente consegue decifrar conteúdo cifrado para o dono original. Compartilhamento de chaves E2EE entre gestores fica fora de escopo por ora.
- Perfil "ativo" da sessão de dashboard é resolvido por `getActiveProfile()` (`src/lib/profile/active-profile.ts`), que lê o cookie `active_profile_id` (setado por `setActiveProfile()`, server action em `src/app/dashboard/actions.ts`) e cai de volta ao perfil próprio se o cookie for inválido/ausente. Troca de conta é feita pelo dropdown em `DashboardSidebar` (`src/components/dashboard/sidebar.tsx`). Duas exceções deliberadas continuam presas ao usuário logado, nunca ao perfil "ativo": `/onboarding` (sempre configura o perfil recém-criado do próprio usuário) e `POST /api/account/delete` (exclusão de conta nunca pode ser feita "como" outra pessoa).
- Cobrança: `planLimits()` (`src/lib/utils.ts`) ganhou `managersIncluded` (`free: 0`, `pro: 1`, `mission: 2`); pacotes pagos extras em `MANAGER_ADDONS` (`src/lib/pricing.ts`: +2 por R$15/mês, +4 por R$30/mês) via `POST /api/billing/checkout` (`type: 'manager_addon'`). Desde 2026-09-01 (seção 7.19) o webhook `POST /api/billing/webhook` incrementa/decrementa `extra_manager_seats` de verdade após compra/cancelamento — deixou de ser scaffold. Superadmin (`isSuperAdmin`) ignora o limite de assento inteiro, ver 7.19.

#### `posts` (feed / publicações)
`profile_id FK`, `type CHECK IN ('text','image','video','carousel')`, `content`, `media_urls TEXT[]`, `published_at`, `scheduled_at`, `is_draft BOOLEAN DEFAULT true`, `created_by_user_id FK auth.users ON DELETE SET NULL` (nullable, corrigido na 017), `project_id UUID FK highlights ON DELETE SET NULL` (migration 005 — vincula um post/relatório a um projeto específico), `original_locale TEXT DEFAULT 'pt' CHECK IN ('pt','en','es')` e `translations JSONB DEFAULT '{}'` (migration 025 — idioma em que o post foi escrito e traduções para os outros dois; ver seção 5.6.1), `media_aspect_ratio TEXT DEFAULT '4:5' CHECK IN ('original','1:1','4:5','1.91:1')` e `location TEXT` (migration 038, valor de paisagem trocado de `16:9` pra `1.91:1` na migration 049 — proporção única aplicada a todas as imagens do post/carrossel e localização em texto livre, escolhidas no wizard de composição; ver seção 7.9).

`scheduled_at` agora tem semântica real (migration 038): quando `is_draft=false`, `published_at IS NULL` e `scheduled_at` está no futuro, o post está "agendado" — o cron `GET /api/cron/publish-scheduled-posts` (a cada 5min, `vercel.json`) promove `published_at = now()` assim que a hora chega. Antes disso a coluna existia mas nada a lia.

#### `post_tags`, `post_likes`, `post_comments` (migration 038 — marcação/curtidas/comentários do redesenho do composer)
- **`post_tags`**: `post_id FK posts CASCADE`, `media_index INT` (índice na `media_urls[]` do post), `tagged_profile_id FK profiles CASCADE` (vínculo real a um perfil da plataforma, não texto livre), `position_x`/`position_y NUMERIC` (0–100, ponto na imagem), `created_by_user_id`. RLS leitura via `can_view_post()`; escrita só do dono do post (`is_profile_owner`).
- **`post_likes`**: `post_id FK CASCADE`, `profile_id FK profiles CASCADE`, `UNIQUE(post_id, profile_id)`. RLS leitura via `can_view_post()`; INSERT/DELETE só do próprio `profile_id` (qualquer `user_role` — missionário e parceiro curtem igual).
- **`post_comments`**: `post_id FK CASCADE`, `profile_id FK profiles CASCADE`, `content TEXT`, `deleted_at` (soft delete). RLS leitura via `can_view_post()` + `deleted_at IS NULL`; INSERT de qualquer autenticado; DELETE do autor **ou** do dono do post (moderação).
- **`can_view_post(p_post_id)`** `SECURITY DEFINER STABLE` (migration 038) — helper único reaproveitado pelas 3 tabelas acima, espelha a mesma regra de `posts_public_read` (perfil público, `is_profile_viewer_or_above`, ou parceiro vinculado) usando os helpers centralizados da 023, para não duplicar a condição de visibilidade em cada policy.

Contagens (`like_count`/`comment_count`) e `viewer_has_liked` não são colunas — calculadas em runtime por `enrichWithEngagement()` (`src/lib/posts/enrich-with-engagement.ts`), que busca curtidas/comentários/marcações de um lote de posts numa rodada só e mescla no `PostWithProfile` de cada post. Sem trigger de contagem denormalizada em v1 (volume ainda pequeno).

#### `highlights` (= "Projetos" no produto atual)
- `profile_id FK`, `title`, `description`, `goal_amount NUMERIC(15,2)`, `current_amount NUMERIC(15,2) DEFAULT 0` (mantido por trigger, nunca calculado on-the-fly), `currency TEXT DEFAULT 'BRL'`
- `cover_url`, `cover_position TEXT DEFAULT '50% 50%'` (migration 006 — recorte de imagem estilo CSS `object-position`)
- `is_featured`, `order_index`
- `status CHECK IN ('active','hidden','completed')` (migration 008 adiciona `completed`)
- `slug TEXT` (migration 005, único composto `(profile_id, slug)`), `partner_token UUID DEFAULT uuid_generate_v4()` (único — token de link público)
- `goal_type TEXT[] DEFAULT ARRAY['financial']` (migration 007 — converteu de escalar para array; `financial | prayer | ambassador | volunteer | ongoing`; DB não restringe valores via CHECK, só o TypeScript)
- `category TEXT[] DEFAULT ARRAY[]` (migration 034 — assunto do projeto: `children | health | education | evangelism | community_development | disaster_relief | other`; **ortogonal** a `goal_type` acima — `goal_type` é o tipo de apoio pedido, `category` é o assunto/tema. Sem CHECK, mesma convenção de `goal_type`. Único uso hoje: sinal de afinidade no ranking do feed — ver 7.1-bis; sem UI pública própria ainda)
- `scripture TEXT`, `letter TEXT` (migration 008); `scripture_translations JSONB DEFAULT '{}'`, `letter_translations JSONB DEFAULT '{}'` (migration 047 — mesmo formato/convenção de `title_translations`/`description_translations` da 042, resolvidos com `original_locale` via `resolveLocalizedText`; ver seção 5.6.1)
- `trip_start_date DATE`, `funding_deadline DATE`, `completed_at TIMESTAMPTZ` (migration 016 — trigger `set_highlight_completed_at()` seta/zera `completed_at` quando `status` entra/sai de `'completed'`)

**`project_story_views`** (migration `035_project_story_views.sql`): `user_id FK auth.users`, `highlight_id FK highlights`, `last_viewed_at`, único `(user_id, highlight_id)`. Só guarda "quando esse usuário viu pela última vez as atualizações desse projeto" — não existe tabela de conteúdo de "story" separada, a faixa de stories do feed (ver 7.13) reaproveita os mesmos `posts` com `project_id` setado. RLS só `auth.uid() = user_id`, mesmo padrão de `follows`.

#### `follows` (migration 033 — "seguir" leve, deliberadamente separado de `partners`)
`follower_user_id FK auth.users CASCADE`, `profile_id FK profiles CASCADE`, `UNIQUE(follower_user_id, profile_id)`. RLS: `follows_insert_self`/`follows_self_read`/`follows_self_delete`, todas `auth.uid() = follower_user_id`, **mais `follows_owner_read` (migration 037)** — `is_profile_owner(profile_id)`, permitindo o dono do perfil ler sua própria lista de seguidores (lacuna documentada aqui até 2026-07-22, ver Changelog). `follower_count(p_profile_id)` (033) e `following_count(p_profile_id)` (037), ambas `SECURITY DEFINER STABLE`, dão só contagem sem expor identidade. Pra exibição paginada há `get_followers(p_profile_id, p_limit, p_offset)` e `get_following(p_profile_id, p_limit, p_offset)` (037, `SECURITY DEFINER STABLE`, join com `profiles`) — como são `SECURITY DEFINER` e ignoram a RLS de `follows`/`profiles`, cada uma checa por dentro `privacy_mode = 'public' OR is_profile_owner(id)` antes de retornar qualquer linha, senão o grafo de follows de um perfil `private`/`stealth` ficaria consultável por id direto.

> **Decisão de design**: seguir é de um clique, sem formulário, e não cria/depende de linha em `partners` — `partners` continua sendo só o compromisso formal (financeiro/oração/embaixador/voluntário) via `PartnershipWizard`. O feed (7.1-bis) lê de `follows` **e** de `partners.user_id = auth.uid()` em paralelo (união dos dois conjuntos de `profile_id`) — ver seção 7.1-bis.

#### `feed_events` (migration 033 — exhaust de eventos para o algoritmo de ranking, Fase 2+)
`actor_user_id FK auth.users ON DELETE SET NULL`, `event_type CHECK IN ('post_view','post_click','project_view','project_click','follow','unfollow','pledge_from_feed')`, `profile_id FK profiles CASCADE` (de quem é o evento), `post_id FK posts ON DELETE CASCADE` (nullable), `project_id FK highlights ON DELETE CASCADE` (nullable). RLS: só `feed_events_insert_self` (`auth.uid() = actor_user_id`) — **write-only nesta fase**, sem policy de SELECT pra usuário comum (só service-role vai ler isso quando existir um job de treino/relatório no futuro). Gravado em lote (1 insert por página de feed carregada, não por post) para manter volume de escrita sensato.

#### `milestones` (migration 005)
`highlight_id FK CASCADE`, `profile_id FK CASCADE`, `title`, `title_translations JSONB DEFAULT '{}'` (migration 047, mesma convenção acima — resolvido com `highlights.original_locale`, já que o marco não tem idioma próprio), `is_completed BOOLEAN`, `completed_at`, `order_index`.

#### `post_deliveries` (migration 005)
Rastreio de envio de relatório a parceiro específico. `post_id FK`, `partner_id FK`, `sent_at`, `opened_at`, `UNIQUE(post_id, partner_id)`.

#### `project_budget_categories` (migration 010; `description` migration 044)
`highlight_id FK CASCADE`, `category_type CHECK IN ('airfare','bus','boat','ferry','rideshare','lodging','food','equipment','visa_documentation','insurance','training','shipping','other')`, `custom_label`, `description TEXT` nullable (migration 044 — texto livre tipo "o que essa etapa inclui", pensado pra projetos com etapas fora do domínio de viagem, ex. construção, onde `category_type` fica sempre `other`), `target_amount NUMERIC(15,2)`, `order_index`.

View `project_budget_progress` **`WITH (security_invoker = true)`** (crítico para RLS): agrega `SUM(amount) FILTER (WHERE type='income')` por categoria como `raised_amount`, via LEFT JOIN com `transactions`. `security_invoker=true` garante que a RLS das tabelas base seja avaliada com o papel de quem consulta a view, não do dono dela.

#### `project_gallery_images` (migration 045)
`highlight_id FK CASCADE`, `image_url TEXT NOT NULL`, `order_index`, `created_at`. Fotos avulsas que representam o projeto (ex.: terreno, progresso da obra), a pedido do usuário — separadas tanto da capa única (`highlights.cover_url`) quanto dos posts vinculados via `posts.project_id` (que aparecem em "Atualizações", ver 7.7-bis). RLS no mesmo padrão de `project_budget_categories`: dono via `is_profile_owner`, leitura pública só se `highlights.status = 'active'`. Editada via `GalleryEditor`/`GalleryEditSection` (mesmo padrão de upload client-side + salvar URL no snapshot do `MilestonesEditSection`/`CoverTitleEditSection`, sem crop/bake — só `compressImage`), persistida pelo mesmo delete+reinsert de `/api/highlights` (campo `galleryImages: string[]`).

#### `project_members` (migration 013 — equipe do projeto)
`highlight_id FK CASCADE`, `user_id FK auth.users CASCADE`, `role CHECK IN ('lead','member','viewer')`, `invited_by_user_id FK auth.users ON DELETE SET NULL`, `UNIQUE(highlight_id, user_id)`.

> **Decisão de design**: `project_members` (quem edita o conteúdo do projeto) e `account_members` (quem mexe no dinheiro) são desacoplados propositalmente. A rota de API `/api/projects/members` sincroniza os dois automaticamente (ver seção 5).

#### `partners` (CRM)
`profile_id FK`, `user_id FK auth.users ON DELETE SET NULL` (nullable — nem todo parceiro tem conta), `name`, `email`, `phone` (WhatsApp), `phone_alt` (telefone alternativo opcional, migration 026), `birth_date` (opcional, migration 027 — alimenta os lembretes de aniversário, ver `BirthdayReminders` na seção 7.1), `type CHECK IN ('financial','prayer','both','ambassador')` (migration 007 adiciona `ambassador`), `notes`, `tags TEXT[]`, `joined_at`. Índice único `(profile_id, user_id)` onde `user_id IS NOT NULL` (migration 012).

#### `partner_visibility_grants` (migration 011 — controle granular de visibilidade)
`profile_id FK`, `partner_id FK`, `section CHECK IN ('full_profile','financial_summary','prayer_requests','sensitive_fields','messages')`, `granted_at`, `UNIQUE(profile_id, partner_id, section)`.

> **Mecanismo central**: o missionário decide, por parceiro individual, quais seções de dado sensível ele enxerga. Duas camadas ortogonais de visibilidade: `privacy_mode` do perfil controla acesso "de longe" (descoberta pública); `partner_visibility_grants` controla, por parceiro já vinculado, granularmente. Quando a seção é `sensitive_fields`, a concessão relacional (esta tabela) precisa ser acompanhada de uma concessão criptográfica real via `keyManager.grantAccessToExisting` (ver seção 6).

#### `payment_methods` (migration 029, ampliada na 031 — substitui as antigas `profiles.pix_key`/`paypal_url`/`wise_url`/`external_donation_url`)
`profile_id FK CASCADE`, `type CHECK IN ('pix','mercadopago','paypal','wise','bank_transfer','revolut','zelle','venmo','cashapp','alipay','wechatpay','mpesa','crypto','other','stripe')`, `label TEXT` (nome customizado; obrigatório na UI quando `type='other'` ou `'bank_transfer'` — aí é o titular da conta), `value TEXT NOT NULL` (chave/handle/link; para `bank_transfer` é o número da conta/IBAN; para `stripe` é o `stripe_user_id` da conta conectada), `details TEXT` (opcional — para `bank_transfer` é um bloco `Rótulo: valor` gerado por `formatBankDetails`/`parseBankDetails` em `src/lib/payment-methods/bank-details.ts`, nunca digitado livre; para `crypto`/`other` continua livre), `linked_account_id UUID FK financial_accounts ON DELETE SET NULL` (migration 031, uso estendido pra `type='pix'` em 2026-09-04 — ver 7.11: pra `stripe`, cada cobrança confirmada pelo webhook já entra como `transactions` nessa conta; pra `pix`, é só organizacional — sem automação —, mas obrigatório sem exceção na UI: `PaymentMethodForm` nunca deixa salvar um Pix com esse campo vazio, criando a conta inline se ainda não existir nenhuma), `is_active BOOLEAN DEFAULT true` (para `type='stripe'`, também serve de flag de onboarding completo — `false` enquanto o Account Link não foi concluído), `sort_order INT DEFAULT 0`.

Lista de métodos por perfil (não mais 1 registro fixo por tipo) — permite múltiplos métodos, reordenação e desativar sem apagar. Catálogo de tipos com ícone/agrupamento em `src/lib/payment-methods/catalog.ts` (`PAYMENT_METHOD_CATALOG` completo vs. `MANUAL_PAYMENT_METHOD_CATALOG` que exclui `stripe`), agrupado por contexto (popular/instantâneo, internacional, redes locais dos EUA, Ásia, África, acesso restrito, outro, automático) para cobrir formas de recebimento usadas por missionários fora do eixo Brasil/EUA. Cada entrada do catálogo pode declarar `currency` opcional quando o método só faz sentido numa moeda só (`pix: 'BRL'`, único caso hoje — Pix é exclusivo do Brasil) — `PaymentMethodForm` usa isso pra filtrar o `<select>` de Tipo pela moeda selecionada, ver 1-ter. `stripe` é um tipo especial: a linha é criada por `/api/stripe/connect/start` (Connect Express) e ativada por `/api/stripe/connect/callback`, não pelo formulário genérico — ver seção 7.11.

RLS: `payment_methods_owner_all` via `is_profile_owner()` (dono + gestor `manager`); `payment_methods_public_read` — leitura pública dos métodos `is_active=true` seguindo a mesma regra de `profiles_public_read` (perfil público, dono, ou parceiro vinculado).

Consumido por: `SettingsTabs` (aba Pagamentos, `PaymentMethodsList`/`PaymentMethodForm`/`PaymentMethodCard` pros manuais + `StripeConnectCard` à parte), `[username]/parceria` (monta `paymentOptions` do wizard, filtra `stripe` fora e calcula `stripeAvailable`), `[username]/projetos/[slug]` (link/Pix de doação do projeto), `ProfileHeader` (badge "Pix disponível"), `dashboard` (checklist de configuração pendente) e passo 2 do onboarding (grava Pix/PayPal/Wise diretamente aqui).

#### `pledges` (migration 012, ampliada na 031 — registro de cada oferta; manual (confirmação humana) ou automático via Stripe (confirmado direto pelo webhook))
`highlight_id FK ON DELETE SET NULL`, `profile_id FK CASCADE`, `partner_id FK ON DELETE SET NULL`, `reporter_user_id FK auth.users ON DELETE SET NULL`, `reporter_name TEXT NOT NULL`, `reporter_email`, `reported_amount NUMERIC(15,2)`, `currency DEFAULT 'BRL'`, `payment_method` (mesmo catálogo de tipos de `payment_methods`, agora incluindo `'stripe'`), `reported_at`, `proof_url`, `is_recurring_pledge BOOLEAN`, `recurring_pledge_id FK recurring_pledges ON DELETE SET NULL` (migration 031 — liga a cobrança mensal de volta ao compromisso), `status CHECK IN ('pending','confirmed','rejected')`, `confirmed_transaction_id FK transactions ON DELETE SET NULL`, `reviewed_by_user_id FK ON DELETE SET NULL`, `reviewed_at`, `rejection_reason`.

RLS: `pledges_insert_public` com `WITH CHECK (true)` — **qualquer pessoa, logada ou não, pode registrar uma oferta pontual para qualquer perfil**. A legitimidade é validada 100% manualmente pelo missionário (não há verificação automática de pagamento) — exceto quando `payment_method='stripe'`, aí quem insere é o webhook (`createServiceClient()`, bypassa RLS) já com `status='confirmed'`. Ver fluxo completo na seção 7.2.

#### `recurring_pledges` (migration 031 — o compromisso de recorrência em si, distinto de cada `pledges` gerado mês a mês)
`profile_id FK CASCADE`, `partner_id FK partners ON DELETE CASCADE` **nullable** (migration 093), `reporter_user_id FK auth.users ON DELETE CASCADE` **nullable** (migration 093 — só o caminho **automático via Cartão** continua exigindo conta de verdade, ver seção 7.3-sexies; o manual aceita convidado), `reporter_name`/`reporter_email`/`reporter_phone TEXT` (migration 093, preenchidos só quando `reporter_user_id IS NULL` — mesmo padrão de `pledges`), `amount NUMERIC(15,2)`, `currency`, `payment_method` (mesmo catálogo; `'stripe'` = automático, qualquer outro = manual + lembrete), `highlight_id FK ON DELETE SET NULL` (projeto específico, se veio de lá), `reminder_opt_in BOOLEAN DEFAULT true`, `next_reminder_at DATE` (null quando é Stripe, `pending`, ou opt-out), `stripe_subscription_id TEXT` (setado pelo webhook quando o checkout conclui), `status CHECK IN ('pending','active','paused','cancelled')`, `lapsed_notified_at TIMESTAMPTZ` (migration 078 — ver abaixo).

RLS: `recurring_pledges_owner_all` via `is_profile_owner()`; `recurring_pledges_self_read` com `auth.uid() = reporter_user_id` (só enxerga o que é seu); `recurring_pledges_insert_public` (`WITH CHECK (true)`, migration 093, substituiu `recurring_pledges_insert_self` — ficou redundante assim que insert é público, mesmo estado de `pledges`) — **qualquer pessoa, logada ou não, pode registrar um compromisso manual**.

**Detecção de compromisso que ficou quieto (migration `078_recurring_pledge_lapse_detection.sql`, 2026-09-02).** `recurring-reminders` (cron acima) só lembra o **parceiro** de pagar por fora — nunca verificava se a doação de fato voltou a acontecer, então um compromisso podia ficar `status='active'` pra sempre mesmo sem nenhuma `pledges` chegando. Novo cron diário `GET /api/cron/lapsed-donor-check` (mesmo guard `CRON_SECRET`/`createServiceClient()`) recalcula do zero a cada execução: pra cada `recurring_pledges` ativo, calcula dias desde a `pledges` confirmada mais recente (`recurring_pledge_id`) — ou desde `created_at`, se nunca houve nenhuma — agnóstico a método de pagamento (Pix manual ou Stripe, o sinal é só "nenhuma confirmação chegou"). Mais de 45 dias (compromisso é sempre mensal, ~15 dias de folga) e `lapsed_notified_at IS NULL` → notifica o **missionário** (`notify()` RPC, `type='partner_lapsed'`, área "Financeiro" no sino — `AREA_BY_TYPE` em `notifications-bell.tsx`) e grava a flag; voltou a ficar em dia → zera a flag sozinho, sem precisar de código em nenhum outro lugar (nem em `PledgeReviewCard`, nem no webhook do Stripe) pra "resetar" isso. **Decisão deliberada**: reengajar é sempre manual — nunca um e-mail automático cobrando o parceiro (mesmo tom convidativo já usado no `FundingProjectionCard`, ver 7.7-bis) — só uma notificação in-app pro missionário decidir se/como falar com a pessoa, e um badge sutil "Parece ter parado" (`variant="secondary"`, nunca `"destructive"`) ao lado de cada parceiro afetado em `/dashboard/parceiros` (`PartnersList`, prop `lapsedPartnerIds`).

#### `scheduled_pledges` (migration 086 — lembrete de oferta avulsa futura, não recorrente; ver seção 7.3-sexies)
`profile_id FK CASCADE`, `partner_id FK partners ON DELETE CASCADE` **nullable** (migration 093), `reporter_user_id FK auth.users ON DELETE CASCADE` **nullable** (migration 093 — deixou de exigir conta), `reporter_name`/`reporter_email`/`reporter_phone TEXT` (migration 093, preenchidos só quando `reporter_user_id IS NULL`), `amount NUMERIC(15,2)`/`currency` (ambos nulos juntos — estimativa opcional), `highlight_id FK ON DELETE SET NULL`, `scheduled_date DATE NOT NULL`, `status CHECK IN ('pending','sent','fulfilled','cancelled')`, `reminded_at TIMESTAMPTZ`. RLS: `scheduled_pledges_owner_all` via `is_profile_owner()` (mesmo padrão de `recurring_pledges`, mesmo sem UI nenhuma pro missionário ainda) + `scheduled_pledges_self_all` pro `reporter_user_id` logado + `scheduled_pledges_insert_public` (`WITH CHECK (true)`, migration 093, substituiu `scheduled_pledges_insert_self`) — **qualquer pessoa, logada ou não, pode agendar um lembrete**. `pledges.scheduled_pledge_id` (nova coluna, mesma migration 086) rastreia a oferta real até a intenção que a originou, igual `pledges.recurring_pledge_id`.

**Convidado sem conta em "Ser parceiro fixo"/"Não posso agora" (migration 093, 2026-09-07).** Espelha o padrão que `pledges` já usava pra doação avulsa (`reporter_user_id`/`partner_id` nullable, insert público, sem checagem de duplicidade nem linha em `partners` pro convidado — abrir SELECT filtrável por e-mail informado pelo cliente vazaria "esse e-mail já tem lembrete pendente" pra qualquer chamador anônimo). `ScheduledPledgeForm` e `RecurringPledgeForm` (caminho manual, não-Stripe) pararam de bloquear com `NeedsAccountCard` — agora coletam nome (sempre) + e-mail e/ou WhatsApp (agendamento único aceita qualquer um dos dois; recorrência mensal exige e-mail, já que não existe automação de WhatsApp pra lembrar todo mês). **Cartão automático em "Ser parceiro fixo" continua exigindo conta** — `POST /api/stripe/checkout-recurring` responde `401` sem usuário autenticado, limitação real de infraestrutura de assinatura, não só de UI; convidado só vê métodos manuais, e se o missionário só aceita Cartão, uma tela reduzida mostra só a explicação + a oferta de conta. Novo componente compartilhado `AccountUpsellCard` (`src/components/partners/account-upsell-card.tsx`, namespace i18n `AccountUpsell`) aparece nos 3 fluxos (`ScheduledPledgeForm`, `RecurringPledgeForm`, e o branch oração/embaixador/voluntariado do `PartnershipWizard` — esse último já aceitava convidado via `PartnershipForm`, só não oferecia a opção de conta) como card não-bloqueante, nunca a única opção na tela: "criar conta"/"já tenho conta" + (se o missionário configurou) "entrar no grupo do WhatsApp" (`profiles.whatsapp_group_url`, seção 3.1) — terceiro botão, mais leve que os outros dois. Os dois crons de lembrete (`scheduled-pledge-reminders`, `recurring-reminders`) ganharam fallback `partner?.email ?? sp.reporter_email` (e mesmo pra nome) pra alcançar convidado sem `partners`; de quebra, corrigido bug latente onde uma linha sem e-mail nenhum era marcada `status='sent'` sem nunca ter enviado nada — agora só marca quando o e-mail existe e o envio confirma sucesso. `NeedsAccountCard` não foi removido do repo (fica sem uso, cosmético apagar depois). Namespace `AccountUpsell` + chaves novas em `ScheduledPledge`/`RecurringPledge`/`ProfileForm`, nas 3 línguas.

#### Financeiro
- **`financial_accounts`**: `profile_id FK`, `currency_code`, `name`, `balance NUMERIC(15,2) DEFAULT 0` (mantido por trigger), `account_type CHECK IN ('checking','savings','credit')`, `credit_limit`, `is_shared BOOLEAN`, `created_by_user_id FK ON DELETE SET NULL`, `highlight_id FK highlights ON DELETE SET NULL` (migration 013 — reaproveita contas para "conta de equipe do projeto" em vez de criar conceito paralelo), `closing_day`/`due_day SMALLINT CHECK BETWEEN 1 AND 31`, `card_brand TEXT`, `archived BOOLEAN DEFAULT false` (migration 028, cartão de crédito), `is_open_finance BOOLEAN DEFAULT false` (migration 088 — true enquanto vinculada a um `open_finance_accounts`, ver abaixo e seção 7.32).
- **`account_members`**: `account_id FK`, `user_id FK auth.users ON DELETE CASCADE`, `role CHECK IN ('owner','viewer')`, `UNIQUE(account_id, user_id)`.
- **`transaction_categories`**: hierárquica via `parent_id` self-FK `ON DELETE SET NULL` (2 níveis: categoria/subcategoria). `color`/`icon` existem desde a migration inicial mas só passaram a ser usados em 2026-09-03 (ver 7.26) — bolinha colorida na tela de Categorias; deliberadamente **não** sincronizado com a paleta categórica `chart-3..8` usada em `CategoryBarChart`/`CategoryPanel` (essas continuam coloridas por posição no ranking, não pela cor salva).
- **`transactions`**: `account_id FK`, `profile_id FK`, `created_by_user_id FK ON DELETE SET NULL`, `type CHECK IN ('income','expense','transfer')`, `amount NUMERIC(15,2)`, `currency`, `description`, `category_id`/`subcategory_id` (FK `transaction_categories`), `partner_id FK partners ON DELETE SET NULL`, `source CHECK IN ('manual','whatsapp','api','recurring','open_finance','import')` (`'recurring'` migration 077, `'open_finance'` migration 088 — ver abaixo e seção 7.32, `'import'` migration 089 — ver seção 7.33), `is_credit_purchase BOOLEAN`, `due_date DATE` (nunca usado por nenhuma tela — coluna morta, ver 7.20), `date DATE DEFAULT CURRENT_DATE`, `highlight_id FK ON DELETE SET NULL` (migration 009), `budget_category_id FK project_budget_categories ON DELETE SET NULL` (migration 010), `fatura_date DATE`/`fatura_paid BOOLEAN DEFAULT false` (migration 028 — mês/fatura ao qual uma compra no cartão pertence; sem tabela de fatura separada, é calculado no cliente a partir de `closing_day`), `is_paid BOOLEAN DEFAULT true` (migration 081 — pago/recebido vs. pendente, alimenta o Saldo Previsto; ver 7.20), `pluggy_transaction_id TEXT UNIQUE` (migration 088 — dedup de importação Open Finance entre sincronizações, null pra qualquer outra origem), `import_uid TEXT` (migration 089, `UNIQUE(account_id, import_uid)` — dedup de importação de extrato OFX, ver 7.33).
- **`recurring_transactions`** (migration 077, 2026-09-02): `profile_id FK`, `account_id FK financial_accounts ON DELETE CASCADE`, `created_by_user_id FK ON DELETE SET NULL`, `type CHECK IN ('income','expense')`, `amount`, `currency`, `description`, `category_id FK transaction_categories ON DELETE SET NULL`, `next_due_date DATE`, `is_active BOOLEAN DEFAULT true`. Schema deliberadamente enxuto (sem `partner_id`/`highlight_id`/`budget_category_id`) — caso de uso é conta fixa pessoal (aluguel, assinatura), não fundraising; diferente de `recurring_pledges` (migration 031, compromisso de **parceiro** com lembrete por e-mail, nunca gera `transactions` sozinho), aqui é dinheiro do próprio missionário — o cron `GET /api/cron/generate-recurring-transactions` (diário, `vercel.json`, mesmo guard `CRON_SECRET`/`createServiceClient()` de todos os outros crons) materializa direto em `transactions` (`source='recurring'`) qualquer recorrência ativa com `next_due_date <= hoje`, e avança a data em +1 mês (mesma técnica `addOneMonth()` de `recurring-reminders/route.ts`) — sem confirmação humana no meio, porque não há terceiro envolvido. `update_account_balance()` (trigger existente) cuida do saldo sozinho. UI em `/dashboard/financeiro/recorrentes` (`RecurringTransactionForm`/`NewRecurringTransactionButton`/`RecurringTransactionsList`, mesmas convenções de `transaction-form.tsx`/`category-tree.tsx`). Só frequência mensal, sem notificação in-app quando gerado — fora do escopo desta rodada.
- **`open_finance_items`/`open_finance_accounts`** (migration `088_open_finance.sql`, 2026-09-04, seção 7.32): conexão bancária automatizada via Pluggy. `open_finance_items` — uma conexão/login num banco: `profile_id FK`, `pluggy_item_id TEXT UNIQUE`, `connector_id`/`connector_name`/`connector_image_url`, `status CHECK IN ('UPDATED','UPDATING','WAITING_USER_INPUT','LOGIN_ERROR','OUTDATED')` (espelha o `ItemStatus` da Pluggy), `last_synced_at`; RLS `is_profile_owner(profile_id)`. `open_finance_accounts` — mapeia cada conta Pluggy de um item pra uma `financial_accounts.id` (criada automaticamente na primeira sincronização): `item_id FK open_finance_items ON DELETE CASCADE`, `financial_account_id FK financial_accounts ON DELETE CASCADE`, `pluggy_account_id TEXT UNIQUE`, `pluggy_type TEXT`; RLS via `EXISTS` no item pai. `src/lib/open-finance/sync.ts` é quem popula as duas e importa `transactions`.

**Cartão de crédito** (migration 028, modelo reaproveitado do projeto GranaZen — branch `master` deste repo, histórico não relacionado): `AccountForm` mostra limite/bandeira/dia de fechamento/vencimento quando `account_type='credit'`; `AccountCard` calcula a fatura atual somando `transactions` com `fatura_paid=false` da conta; `TransactionForm` atribui `fatura_date` automaticamente (mês corrente se a compra é antes do `closing_day`, senão mês seguinte) quando a conta selecionada é de crédito. Não há parcelamento (compra única por lançamento) nem baixa de fatura (marcar `fatura_paid=true`) na v1 — ficou de fora para manter o escopo desta rodada.

**Câmbio** (`dashboard/financeiro/cambio`): conversor de moedas client-side, sem tabela própria — consulta cotações ao vivo em `open.er-api.com` (API pública, sem chave). Também portado do GranaZen.

- **`spending_limits`** (migration `082_spending_limits.sql`, 2026-09-02): `profile_id FK`, `category_id FK transaction_categories ON DELETE CASCADE`, `limit_amount NUMERIC(15,2)`, `currency`, `UNIQUE(profile_id, category_id)` — um limite mensal por categoria de topo. Sem coluna de "gasto acumulado": o gasto é somado no cliente a partir de `transactions` do mês corrente (`type='expense'`, pago+não pago). UI em `/dashboard/financeiro/limites`. Ver 7.20.
- **`financial_goals`** (migration `083_financial_goals.sql`, 2026-09-02): `profile_id FK`, `name`, `target_amount`, `current_amount DEFAULT 0`, `currency`, `target_date` (nullable), `achieved_at` (nullable, setado quando `current_amount` cruza `target_amount` pela primeira vez). **Sem vínculo automático com conta/transação** (decisão deliberada, confirmada com o usuário) — `current_amount` só muda editando a meta ou via "Registrar valor" (soma um aporte manual). UI em `/dashboard/financeiro/metas`. Ver 7.20.

**Triggers de saldo agregado**:
- `update_account_balance()` — soma/subtrai `amount` de `financial_accounts.balance` conforme `type` (income soma, expense subtrai), só quando `is_paid=true`. **Migration 081 passou a disparar em UPDATE também** (antes só INSERT/DELETE — ⚠️ risco operacional documentado até aqui: editar valor/tipo/conta de uma transação existente não resincronizava o saldo, a menos que a aplicação forçasse delete+insert; corrigido de brinde ao implementar `is_paid`, que precisava de UPDATE disparando pra "marcar como pago" funcionar — ver 7.20). Lógica agora é delta-based: reverte o efeito da linha OLD e aplica o da linha NEW no mesmo UPDATE, cobrindo os dois casos com uma função só.
- `update_highlight_current_amount()` (migration 009) — quando uma transação `income` tem `highlight_id`, soma/subtrai em `highlights.current_amount`. **Ainda só INSERT/DELETE, e ainda ignora `is_paid`** (deliberado — ver 7.20: fundraising público conta a oferta assim que registrada, independente de já ter sido conciliada como paga no caixa pessoal).

#### E2EE (migration 015)
- **`user_encryption_keys`**: `user_id PK FK auth.users CASCADE`, `public_key TEXT` (claro), `encrypted_private_key TEXT` (formato `"ciphertext:nonce"`), `kdf_salt TEXT` (salt Argon2id).
- **`encrypted_dek_grants`**: `resource_type CHECK IN ('conversation','prayer_request','profile_sensitive_fields')`, `resource_id UUID`, `grantee_user_id FK auth.users CASCADE`, `wrapped_dek TEXT` (DEK cifrada via sealed box para este grantee), `revoked_at TIMESTAMPTZ`, `UNIQUE(resource_type, resource_id, grantee_user_id)`.
- **`profile_sensitive_data`**: `profile_id PK FK profiles CASCADE`, `ciphertext TEXT`, `nonce TEXT`.
- **`prayer_request_replies`**: `prayer_request_id FK CASCADE`, `author_user_id FK auth.users ON DELETE SET NULL`, `ciphertext`/`nonce` (se privado) OU `content TEXT` (se em claro).
- `messages.nonce TEXT`, `prayer_requests.is_private BOOLEAN DEFAULT false`, `prayer_requests.nonce TEXT` adicionados.

Ver seção 6 para o modelo criptográfico completo.

#### Comunicação
- **`prayer_requests`**: `profile_id FK`, `requester_id FK auth.users ON DELETE SET NULL`, `requester_type CHECK IN ('missionary','partner')`, `content TEXT`, `is_answered BOOLEAN`, `answered_at`, `is_private BOOLEAN`, `nonce`.
- **`messages`**: `sender_id`, `recipient_id` (FK auth.users), `profile_id FK` (contexto de qual missionário), `content TEXT`, `is_encrypted BOOLEAN DEFAULT false`, `nonce`. Não existe tabela `conversations` — o ID de conversa é derivado (ver seção 6.3).
- **`notifications`**: `recipient_user_id FK auth.users CASCADE`, `type TEXT`, `payload JSONB DEFAULT '{}'`, `read_at`.

#### Billing / IA / WhatsApp / Câmbio (schema pronto, sem integração)
- **`subscriptions`**: `profile_id UNIQUE FK`, `stripe_subscription_id UNIQUE`, `plan CHECK IN ('free','pro','mission')`, `status TEXT`, `current_period_end`.
- **`ai_credit_transactions`**: `profile_id FK`, `amount INTEGER` (positivo=crédito, negativo=consumo), `reason TEXT`.
- **`whatsapp_config`**: `profile_id UNIQUE FK`, `phone_number_id`, `verify_token`, `notifications_enabled`, `financial_enabled`, `is_verified`.
- **`exchange_rates`**: `base TEXT`, `rates JSONB`, `UNIQUE(base)`.

#### `history_blocks`
Blocos estilo Notion para página `/historia`. `profile_id FK`, `type CHECK IN ('who_we_are','our_calling','timeline','cta','text')`, `content JSONB DEFAULT '{}'`, `order_index`. `who_we_are`/`our_calling` guardam fotos em `content.images: string[]` (galeria, sem legenda — migration `079_history_blocks_image_gallery.sql` migrou o formato antigo de imagem única `image_url`/`image_caption`). Itens de `timeline.items[]` podem ter `post_id` (vincula um post existente, mutuamente exclusivo com o próximo) ou `image_url` (foto avulsa sem post) — ver seção 7.12.

### 3.2 Functions e triggers auxiliares

- `update_updated_at()` — genérico, `BEFORE UPDATE`, aplicado em `profiles`, `posts`, `highlights`, `financial_accounts`, `subscriptions`, `history_blocks`.
- `handle_new_user()` `SECURITY DEFINER` — cria `profiles` no signup.
- `update_account_balance()`, `update_highlight_current_amount()` — ver acima.
- `set_highlight_completed_at()` — `BEFORE UPDATE ON highlights`.
- `is_profile_owner(p_profile_id)` `SECURITY DEFINER STABLE` (migration 004) — `EXISTS (SELECT 1 FROM profiles WHERE id=p_profile_id AND user_id=auth.uid())`. Criada especificamente para **quebrar recursão de RLS** quando policies de tabelas diferentes se referenciam mutuamente.
- `is_authorized_partner(p_profile_id)`, `has_partner_grant(p_profile_id, p_section)` `SECURITY DEFINER STABLE` (migration 011) — mesma técnica anti-recursão, aplicadas a `partners`/`partner_visibility_grants`.
- `notify(p_recipient_user_id, p_type, p_payload)` `SECURITY DEFINER` (migration 014) — helper de inserção em `notifications` (no-op se recipient NULL). **Decisão de design**: notificações disparadas por trigger Postgres, não por código da aplicação, para garantir disparo mesmo de fontes futuras fora do Next.js (app mobile, webhook WhatsApp).
  - `trg_notify_new_pledge`, `trg_notify_pledge_confirmed`, `trg_notify_new_message`, `trg_notify_new_partner`, `trg_notify_highlight_update` (notifica todos os parceiros quando um post vinculado a projeto é publicado), `trg_notify_prayer_reply` (migration 015).
- `consume_ai_credits(p_profile_id, p_amount, p_reason)` `SECURITY DEFINER` (migration 025) — débito atômico de `profiles.ai_credits` (`FOR UPDATE` + checagem via `is_profile_owner`), levanta `insufficient_ai_credits` se saldo insuficiente; registra a transação em `ai_credit_transactions`. Chamada via `supabase.rpc(...)` com o client **anônimo** (não service-role) para que `auth.uid()` exista dentro da função.

### 3.3 RLS — padrões e histórico de bugs

Padrão geral: "dono" = existe `profiles` com `user_id = auth.uid()` cujo `id` bate com o FK da linha (`FOR ALL`). Leitura pública condicionada a `privacy_mode='public'` (ou dono, ou parceiro autorizado via função `SECURITY DEFINER`).

**Bug histórico de recursão** (documentar para não repetir): a versão inicial (migration 002) de `profiles_public_read` consultava `partners`, e `partners_owner_all` consultava `profiles` → loop infinito de RLS. Corrigido na migration 004 removendo a referência cruzada e introduzindo `is_profile_owner()` como função `SECURITY DEFINER` (que "escapa" da RLS internamente, quebrando o ciclo). O mesmo padrão foi reaplicado na migration 011 ao reintroduzir acesso de parceiros a perfis privados/stealth.

**Regra a preservar em uma reimplementação**: sempre que uma policy de RLS precisar checar uma condição que depende de outra tabela que também tem RLS referenciando a primeira, envolver a checagem em uma função `SECURITY DEFINER STABLE`.

Buckets de Storage (migration 003): `media` (público, 500MB, webp/jpeg/png/gif/mp4/webm/quicktime) e `avatars` (público, 5MB, webp/jpeg/png). Policy de escrita: `auth.uid()::text = (storage.foldername(name))[1]` — cada usuário só grava dentro de uma pasta com seu próprio UUID como primeiro segmento do path.

> **⚠️ Risco operacional descoberto em 2026-07-08 — arquivo de migration ≠ estado real do banco**: não há CLI/connection string do Supabase configurada neste ambiente de desenvolvimento, então não existe garantia de que toda migration em `supabase/migrations/` foi de fato `RUN` contra o projeto Supabase real — aplicação é manual (SQL Editor do painel). Encontramos exatamente esse drift: as policies de `encrypted_dek_grants` em produção eram a versão **015** (mais restritiva: INSERT só permitia `grantee_user_id = auth.uid()`, sem a policy de SELECT ampliada da 018), mesmo a 018 já existindo no repositório há tempo — sintoma foi mensagens que o destinatário nunca conseguia decifrar, porque o `insert`/`upsert` do grant falhava silenciosamente (erro engolido sem `try/catch` em `ensureResourceKey`). Corrigido reaplicando as policies certas + criada a migration `030_fix_dek_grants_rls_and_realtime.sql` para deixar o arquivo e o banco consistentes de novo. **Regra a adotar**: sempre que investigar um bug que "não devia existir" dado o código/migration, considerar a hipótese de drift migration-vs-banco antes de assumir que é bug de aplicação — testar direto contra o banco com uma sessão de usuário real (não `service role`, que ignora RLS) é o jeito mais rápido de confirmar.

> **⚠️ Realtime não é habilitado por migration comum**: tabelas só emitem eventos via `supabase.channel(...).on('postgres_changes', ...)` se estiverem na publicação `supabase_realtime` (`ALTER PUBLICATION supabase_realtime ADD TABLE ...`) — isso não acontece automaticamente ao criar uma tabela, mesmo com RLS configurada certinho, e o `CREATE TABLE` original de `messages` (migration 001) não incluía isso. Sintoma: canal assina com `status: SUBSCRIBED` (nenhum erro visível), mas o evento `INSERT` simplesmente nunca dispara. Corrigido na mesma migration 030. Ao adicionar realtime a uma tabela nova, sempre incluir esse `ALTER PUBLICATION` na própria migration que cria a tabela.

### 3.4 Correção de exclusão de conta (migration 017)

FKs para `auth.users` sem `ON DELETE` explícito usam `NO ACTION` — excluir um usuário falhava sempre que ele aparecia como "quem fez X" em linha pertencente a **outro** perfil (ex: parceiro de outro missionário). Corrigido tornando nullable + `ON DELETE SET NULL` em: `partners.user_id`, `financial_accounts.created_by_user_id`, `transactions.created_by_user_id`, `posts.created_by_user_id`, `project_members.invited_by_user_id`, `pledges.reviewed_by_user_id`, `prayer_requests.requester_id`, `prayer_request_replies.author_user_id`; e `ON DELETE CASCADE` em `account_members.user_id`.

---

## 4. Edge Functions (`supabase/functions/`)

Diretório existe mas está **vazio**. Toda lógica server-side hoje mora em API Routes do Next.js (`src/app/api/`) e em triggers/functions SQL no Postgres.

---

## 5. Backend applicativo (`src/lib/`, `src/app/api/`, middleware)

### 5.1 Clientes Supabase

- **`src/lib/supabase/client.ts`**: `createBrowserClient` (`@supabase/ssr`) com `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY` — Client Components, respeita RLS via sessão do cookie.
- **`src/lib/supabase/server.ts`**: dois clientes:
  - `createClient()` — `createServerClient` com **anon key**, usando `cookies()` de `next/headers` — Server Components/Route Handlers, respeita RLS.
  - `createServiceClient()` — mesma estrutura mas com `SUPABASE_SERVICE_ROLE_KEY` — **bypassa RLS**. Usado só em rotas que precisam de operação administrativa (deletar usuário, buscar por e-mail em todos os usuários, escrever em nome de outro usuário).

### 5.2 `src/proxy.ts` — middleware de autenticação (Next 16 renomeou `middleware.ts` → `proxy.ts`)

- Refresh de sessão via `@supabase/ssr` operando sobre cookies da request/response.
- `PROTECTED_ROUTES = ['/dashboard']` → sem `user`, redirect `/login?redirect={pathname}`.
- `AUTH_ROUTES = ['/login', '/cadastro', '/recuperar-senha']` → já autenticado, redirect `/dashboard`. **Exceção (bug corrigido em 2026-09-01)**: `/recuperar-senha/nova-senha` fica de fora dessa regra — ao clicar no link do e-mail de recuperação, o Supabase já autentica uma sessão (de recovery) *antes* do redirect pra essa página; sem a exceção, a regra "rota de auth + já logado → `/dashboard`" capturava esse `startsWith('/recuperar-senha')` e jogava a pessoa pro dashboard antes de ela conseguir ver o formulário de nova senha, tornando o fluxo inteiro de recuperação de senha inutilizável em produção (só descoberto ao testar de verdade pela primeira vez, depois de configurar SMTP/template — nunca tinha sido testado ponta a ponta antes).
- `matcher` exclui `_next/static`, `_next/image`, `favicon.ico`, extensões de imagem.
- **Nota**: só `/dashboard` é protegido pelo middleware. Perfis públicos e demais rotas dependem só de RLS para controle de acesso.
- **`/recuperar-senha`** (form de e-mail → `supabase.auth.resetPasswordForEmail(email, { redirectTo: '/auth/callback?redirect=/recuperar-senha/nova-senha' })`) e **`/recuperar-senha/nova-senha`** (reaproveita o mesmo `GET /auth/callback` do login OAuth para trocar o `code` por sessão via PKCE; a página confere `getSession()` no mount — sem sessão válida mostra "link inválido/expirado" com atalho para pedir um novo; com sessão, formulário de nova senha via `supabase.auth.updateUser({ password })`).

### 5.3 API Routes (`src/app/api/`)

Todas autenticam com `createClient()` (RLS) e usam `createServiceClient()` (bypass RLS) só para as escritas administrativas.

#### `POST /api/account/delete`
Exclusão de conta (LGPD/GDPR-style):
1. Exige usuário autenticado; payload `{ confirm: string }`.
2. Se o usuário tem `profiles.username`, exige `confirm === username`; senão (só parceiro) exige `confirm === 'EXCLUIR'`.
3. Remove recursivamente todos os arquivos do usuário nos buckets `media` e `avatars` (função `collectAllFiles`, paginado 1000/nível).
4. `admin.auth.admin.deleteUser(user.id)` — cascata cuida do resto via FKs (migration 017).

#### `POST` / `DELETE /api/accounts/members`
Gerencia membros de `financial_accounts` compartilhadas.
- Helper `assertOwnsAccount`: confirma `financial_accounts.profile_id → profiles.user_id === solicitante`.
- `POST { accountId, email, role }`: busca usuário por e-mail via `admin.auth.admin.listUsers` (paginado 200/página, loop até achar ou esgotar — **não há índice de e-mail direto**). 404 se não encontrar. Insere em `account_members` (`role` default `'viewer'`).
- `DELETE { memberId, accountId }`: remove de `account_members`.

#### `src/app/api/open-finance/*` — conexão bancária automatizada (Pluggy, seção 7.32)
- `POST connect-token`: gera o `connectToken` de escopo restrito que o widget usa no navegador (nunca a API key completa, que fica só em `src/lib/open-finance/pluggy.ts`). Corpo opcional `{ itemId }` (nosso UUID interno) vira `updateItem` na Pluggy pra reconexão.
- `POST items`: chamado após o `onSuccess` do widget com `{ pluggyItemId }`. Rebusca o `Item` na Pluggy e só aceita se `item.clientUserId === profile.id` (senão 403 — sem essa checagem, um `itemId` reaproveitado linkaria a conexão bancária de outro perfil). Upsert em `open_finance_items` por `pluggy_item_id` (mesma rota cobre criação e reconexão) e dispara `syncOpenFinanceItem`.
- `POST items/[itemId]/sync`: sincronização manual ("Sincronizar agora" no dialog).
- `DELETE items/[itemId]`: desconecta — remove na Pluggy (best-effort), zera `financial_accounts.is_open_finance` das contas vinculadas, mas não apaga contas nem `transactions` já importadas.
- `POST webhook`: recebido da Pluggy sem validação de assinatura confiável — o corpo só serve pra extrair `itemId`; toda mudança de estado vem de rebuscar o item na API da Pluggy dentro de `syncOpenFinanceItem` (`src/lib/open-finance/sync.ts`), a mesma função usada pelas rotas acima e pelo cron `GET /api/cron/open-finance-sync` (diário, `vercel.json`).

#### `POST /api/highlights` — criar/editar projeto + filhos
Faz upsert completo de `highlights` + `milestones` + `project_budget_categories`. Usa `fetch` cru contra o PostgREST (`${SUPABASE_URL}/rest/v1/...`, header `Authorization: Bearer <service_role>`) em vez do client `supabase-js` (helpers `dbPost`/`dbPatch`/`dbDelete`).
- `slugify(text)` recalcula `slug` a cada save a partir do título.
- Se `highlightId` existe → `PATCH`; senão → busca `MAX(order_index)` do profile e `POST` com `order_index+1`.
- Milestones e budget categories: estratégia **"delete all + recreate"** a cada save (não faz diff incremental).
- **Risco de segurança corrigido**: `assertProfileAccess(profileId, user.id)` roda antes de qualquer escrita — confere que `user.id` é dono do `profileId` (via `profiles.user_id`) ou `manager` em `profile_managers`, 403/404 se não. Fecha o gap que existia antes (autorização não era checada no servidor, só assumida do lado do chamador).
- Payload ganhou `category` (migration 034 — array de assunto do projeto, ver seção 3.1 e 7.1-bis) ao lado de `goalTypes`, salvo direto em `highlights.category` sem transformação.

#### `POST` / `DELETE /api/projects/members`
Gerencia `project_members` (equipe) e sincroniza acesso financeiro.
- Helper `getOwnedHighlight`: confirma posse via `highlights.profile_id → profiles.user_id`.
- `POST { highlightId, email, role }`: busca usuário por e-mail (mesmo padrão paginado). Insere em `project_members`. Garante que existe `financial_accounts` vinculada ao `highlight_id` (cria `{ name: 'Equipe — {title}', is_shared: true }` se não existir). Insere o membro também em `account_members`, mapeando `role='lead' → 'owner'`, demais → `'viewer'`.
- `DELETE { memberId, highlightId, memberUserId }`: remove de `project_members` e, se aplicável, de `account_members` do mesmo par.

#### `POST /api/ai/translate` — tradução de conteúdo via IA (migration 025, `src/lib/ai/`)
1. `createClient()` anônimo, `auth.getUser()` — 401 se não autenticado.
2. Recebe `{ profileId, sourceLocale, targetLocales, text }`; valida locales ⊂ `LOCALES` (`pt`/`en`/`es`) e `targetLocales` sem `sourceLocale`.
3. `supabase.rpc('consume_ai_credits', { p_profile_id, p_amount: AI_ACTION_COSTS.translate_content, p_reason: 'translate_content' })` — client anônimo (não service-role), para `auth.uid()` existir dentro da função `SECURITY DEFINER`. `insufficient_ai_credits` → 402; outro erro → 403.
4. `translateContent(...)` (`src/lib/ai/translate.ts`) chama `claude-haiku-4-5` via `@anthropic-ai/sdk` com `output_config.format` (structured outputs/JSON Schema) para devolver `{ [locale]: string }` sem parsing frágil de texto livre. Erro do provedor → 502 (crédito já debitado; sem reembolso automático nesta versão).
5. Retorna `{ translations, remainingCredits }`.

`src/lib/ai/costs.ts` centraliza `AI_ACTION_COSTS` (hoje só `translate_content: 1`) — ponto único para adicionar custos de futuras ações de IA (ex.: melhorar texto, copiloto de criação) sem espalhar números mágicos pelo código.

### 5.4 `src/lib/financial/projection.ts` — projeção de arrecadação

```
daysSinceCreated = max(1, floor((agora - createdAt) / 1 dia))
dailyPace = raisedAmount / daysSinceCreated
daysRemaining = fundingDeadline ? max(0, ceil((deadline - agora)/1 dia)) : null
projectedFinal = daysRemaining !== null ? raisedAmount + dailyPace * daysRemaining : raisedAmount
projectedPct = projectedFinal / goalAmount * 100
status = projectedPct >= 100 ? 'green' : projectedPct >= 80 ? 'yellow' : 'red'  (ou 'neutral' se sem goalAmount)
```
Modelo de extrapolação linear simples (não considera sazonalidade), usado só para um semáforo visual verde/amarelo/vermelho.

### 5.5 `src/lib/media/compress.ts`

- Imagens: `browser-image-compression` — `maxSizeMB: 2`, `maxWidthOrHeight: 2048`, `useWebWorker: true`, converte para `webp`, `initialQuality: 0.85`.
- Vídeos: `VIDEO_MAX_SIZE_MB = 500`, `VIDEO_MAX_DURATION_SECONDS = 30`. `getVideoDuration` lê duração offscreen via `<video>` antes do upload (sem recompressão real — vídeos fora do limite são rejeitados, não recomprimidos).
- `getMediaType(file)`, `formatFileSize(bytes)`.

### 5.5.1 `src/hooks/use-pending-action.ts` — loading state correto pós-`router.refresh()`

`router.refresh()` não retorna uma Promise que espera o Server Component terminar de re-buscar/re-renderizar — é "dispara e esquece". Qualquer código que zere um `useState` de loading logo em seguida (padrão comum antes desta correção) faz o spinner sumir antes da UI de fato mudar, dando sensação de travamento. Isso era especialmente perceptível na troca de idioma (`account-form.tsx`), já que o `NextIntlClientProvider` é montado no `RootLayout` — trocar idioma exige re-buscar a árvore inteira a partir da raiz.

`usePendingAction<T>()` resolve isso envolvendo a ação em `startTransition` (React 19): `run(value, action)` seta `value` imediatamente (spinner aparece no clique) e roda `action` dentro de uma transition; `pendingValue` só é não-nulo enquanto `isPending` (do React) for `true` — nunca precisa de `finally { setX(null) }` manual. `T` identifica qual ação está pendente (id do item numa lista, locale clicado, `'confirm' | 'reject'`, etc.); use o default (`isPending` puro-boolean) quando só existir um "saving" único na tela.

Usado em ~20 componentes que fazem `router.refresh()`/`router.push()` após mutação: troca de idioma (`account-form.tsx`, `language-switcher.tsx`), Configurações (`profile-form.tsx`, `privacy-form.tsx`, `payment-method-form.tsx`, `payment-method-card.tsx`), financeiro (`category-tree.tsx`, `transaction-table.tsx`, `transaction-form.tsx`, `financial/account-form.tsx`, `manage-members-dialog.tsx`, `pledge-review-card.tsx`), projetos (`project-team-panel.tsx`, `highlight-form.tsx`), publicações (`post-editor.tsx`), autenticação (`login/page.tsx`, `recuperar-senha/nova-senha/page.tsx`).

### 5.5.1-bis `src/hooks/use-highlight-section-save.ts` (2026-07-20)
`useHighlightSectionSave()` centraliza o save das 7 ilhas de edição inline de `[username]/projetos/[slug]` (ver 7.12/7.7): faz o `fetch('/api/highlights', { method: 'POST' })`, mostra toast de sucesso/erro (namespace i18n `PublicProject`) e chama `router.refresh()` no sucesso — cada ilha só precisa montar o payload (spread do `HighlightSnapshot` + seu campo alterado) e chamar `save(payload)`. Não usa `usePendingAction` (5.5.1) porque aqui não há necessidade de identificar *qual* ação está pendente (só uma seção por vez fica em modo edição).

### 5.5.2 `src/hooks/use-dashboard-nav.ts` — `useNav`/`useBottomNavItems`/`useSignOut`

Extraído de `sidebar.tsx` (onde vivia antes) porque o novo `AccountMenuDrawer` (seção 10, `dashboard/`) também é montado a partir de `/[username]`, fora da árvore `/dashboard` — importar direto de `sidebar.tsx` acoplaria conceitualmente esse consumidor a `DashboardSidebar`/`MobileHeader`/`MobileBottomNav`, que ele não usa. `useNav(role)` é a fonte única da navegação completa (desktop sidebar + gaveta do `AccountMenuDrawer`); `useBottomNavItems(role)` só serve a `MobileBottomNav`; `useSignOut()` é usado pelos três. Comportamento idêntico ao que existia antes da extração.

### 5.6 Internacionalização (`src/i18n/`, `messages/`) — Fase 1: site público

**Status**: infraestrutura completa + site público 100% traduzido (PT/EN/ES). Tela de **Configurações** (`SettingsTabs` e as 5 abas — Perfil, Pagamentos, Privacidade, Acesso, Conta) 100% traduzida. Restante do dashboard (publicações, projetos, parceiros, financeiro, orações, mensagens) ainda majoritariamente em português; `[username]/*` (perfil público) já usa `getTranslations`/`getLocale` em parte das páginas (`page.tsx`, `profile-header.tsx`) — tradução do resto do dashboard fica para quando for pedida (decisão explícita para não fazer uma mudança gigante de uma vez).

> **Regra permanente**: toda mudança de texto de UI pedida a partir de agora deve nascer usando `next-intl` (chaves em `messages/{pt,en,es}.json`), nunca hardcoded — mesmo em componentes que hoje ainda estão 100% em português.

#### 5.6.1 Tradução de **conteúdo do usuário** (posts, bio) — ortogonal à i18n de UI acima

Migration 025 adiciona um segundo tipo de "idioma" ao sistema, **independente** do `profiles.locale`/`NEXT_LOCALE` acima: o idioma em que o *conteúdo* (post, bio) foi escrito, e suas traduções. Não confundir os dois — `profiles.locale` controla em que idioma a interface aparece para quem está navegando; `original_locale`/`bio_locale` + `translations`/`bio_translations` controlam em que idioma(s) o *texto que o missionário escreveu* existe.

- **Schema**: `posts.original_locale`/`posts.translations` e `profiles.bio_locale`/`profiles.bio_translations` (ambos JSONB no formato `{"en": {"content", "source": "ai"|"human", "translated_at"}, "es": {...}}` — só as línguas que **não** são a original; o texto original continua só em `posts.content`/`profiles.bio`, nunca duplicado no JSONB). Mesmo formato em `highlights` (`original_locale` + `title_translations`/`description_translations`, migration 042, e `scripture_translations`/`letter_translations`, migration 047) e em `milestones` (`title_translations`, migration 047 — resolvido com `highlights.original_locale` do projeto pai, já que o marco não tem idioma próprio). `src/lib/i18n/content-translations.ts` centraliza a lógica client-side comum a todos esses campos (`initialTranslations`/`initialSources`/`buildTranslationsPayload`/`translateContent`), usada por `highlight-form.tsx`, `use-project-composer.ts` e os `*-edit-section.tsx` da página pública do projeto.
- **Geração de tradução via IA**: `src/lib/ai/` (`client.ts`, `costs.ts`, `translate.ts`) + rota `POST /api/ai/translate` (seção 5.3) — usa `claude-haiku-4-5`, débito de 1 `ai_credit` via `consume_ai_credits`. **Sempre opcional**: o usuário pode digitar a tradução manualmente nas abas de idioma sem gastar nenhum crédito; a IA é só um atalho.
- **UI**: `src/components/dashboard/locale-content-tabs.tsx` (`LocaleContentTabs`) — componente compartilhado com abas PT/EN/ES (bandeiras iguais ao `LanguageSwitcher`), usado em `PostEditor` (texto do post) e `ProfileForm` (bio). Cada aba é um textarea sempre editável; o botão "Traduzir com IA" só preenche o textarea, nunca substitui sem revisão. Já traduzido via `next-intl` (namespace `LocaleContentTabs`).
  - **Aba padrão**: abre priorizando `preferredLocale` (idioma da conta, `profiles.locale`) em vez do idioma original do conteúdo — quem navega o painel em EN vê a aba EN primeiro, mesmo que o texto original tenha sido escrito em outro idioma.
  - **Aviso de cobertura de idioma**: abaixo das abas, uma linha discreta (não bloqueante) lista quais idiomas ainda não têm tradução e avisa que parceiros nesses idiomas verão só o texto original — atualiza em tempo real conforme o usuário preenche as abas. Não impede salvar/publicar (decisão de produto: nunca obrigatório ter as 3 versões).
- **Gravação de posts migrou para Server Action**: `src/app/dashboard/publicacoes/actions.ts` (`savePost`) substitui o antigo insert/update direto do client em `post-editor.tsx` — mesmo padrão de `projetos/actions.ts` (`saveHighlight`): client anônimo só para `auth.getUser()`, depois client service-role reverificando dono/gestor manualmente antes de gravar. Perfil (bio) **não** ganhou Server Action nova — `profile-form.tsx` continua gravando via client sob RLS `profiles_owner_all`, já que a parte sensível (chamar IA/débito de crédito) já está isolada em `/api/ai/translate`.
- **Renderização pública**: `src/lib/i18n/resolve-content-locale.ts` (`resolveLocalizedText`) resolve, dado o idioma do visitante (via `getLocale()` do next-intl, já montado em `[username]/*`), qual texto mostrar — tradução se existir, senão o original (nunca string vazia). Usado em `profile-header.tsx` (bio) e `[username]/projetos/[slug]/page.tsx` (seção "Atualizações"). O dashboard (`posts-list.tsx`, etc.) **não** usa esse resolver — o dono sempre vê o conteúdo no idioma original ali.

- **Biblioteca**: `next-intl`, em modo **sem prefixo de URL** (nada de `/en/...`) — plugado via `createNextIntlPlugin` em `next.config.ts`.
- **Resolução de locale** (`src/i18n/request.ts`, roda a cada request server-side): 1) se logado, `profiles.locale` (sempre manda, independente de cookie); 2) senão, cookie `NEXT_LOCALE` se presente (visitante que já trocou antes); 3) senão, header `Accept-Language` do navegador (primeira visita, sem cookie ainda — parse simples do primeiro idioma da lista que bata com `LOCALES`); 4) senão, `pt` (default em `src/i18n/config.ts`).
- **Seletor de idioma discreto no perfil público**: `LanguageSwitcher` ganhou um modo `compact` (texto "PT ▾" num `DropdownMenu`, em vez das 3 bandeiras) usado nas variantes sem abas de `profile-tabs.tsx` (`mensagens`/`oracao`) — motivado por benchmark: nenhum app de peso (Instagram, WhatsApp, TikTok, X) expõe troca de idioma na tela principal, a detecção de `Accept-Language` acima cobre a maioria dos casos sem nenhuma UI, e o modo compacto é o meio-termo pra quem ainda quiser trocar manualmente. Na barra de abas principal, um terceiro modo `submenu` (`DropdownMenuSub`, reaproveitando a mesma `handleSwitch`/`switching`) embute o seletor dentro do `ProfileMoreMenu` ("...", seção 10 `profile/`) em vez de um `DropdownMenu` próprio. `SiteNav` continua com o modo padrão (bandeiras) — lá é uma barra de navegação normal.
- **Arquivos de tradução**: `messages/{pt,en,es}.json`, namespaces `Metadata`, `Nav`, `Footer`, `Modules` (chave = `id` do módulo), `Landing`, `PricingPage`, `Pricing` (chave = `id` do plano), `Auth`.
- **`src/lib/modules.ts`** e **`src/lib/pricing.ts`** foram refatorados para ficarem agnósticos de idioma — só guardam `id`/ícone/cor/preço; todo texto (título, descrição, bullets, nome, tagline, features, cta) vive nos `messages/*.json` sob a chave `id`. Ao criar um módulo ou plano novo: adicionar a entrada em `modules.ts`/`pricing.ts` **e** a tradução correspondente nos 3 `messages/*.json`.
- **Troca de idioma**: `LanguageSwitcher` (`src/components/marketing/language-switcher.tsx`, bandeiras 🇧🇷🇺🇸🇪🇸) — seta o cookie `NEXT_LOCALE` via `document.cookie`, e se houver usuário logado também atualiza `profiles.locale`, depois `router.refresh()`. Presente no `SiteNav` (desktop e painel mobile) e, de forma equivalente (sem o componente compartilhado, para não acoplar dashboard a `components/marketing/`), num card "Idioma" no topo de `AccountForm` (Configurações → Conta) — funcional mesmo com o resto do dashboard ainda em português.
- **`profiles.locale`** (migration 024) é a fonte de verdade por usuário; o cookie é só a otimização para não repetir a query em toda request.

---

## 6. Camada de criptografia E2EE (`src/lib/crypto/`)

Modelo: **X25519** (par de chaves por usuário) + **key-wrapping** (cada DEK de recurso é cifrada individualmente, via sealed box, para cada participante autorizado) + conteúdo cifrado simetricamente (**XSalsa20-Poly1305**) com a DEK do recurso. A chave privada do usuário nunca sai em claro do dispositivo — fica cifrada em repouso com uma chave derivada via **Argon2id** de um código de recuperação que só o usuário possui (nem o servidor consegue recuperar).

### 6.1 `e2ee.ts` — primitivas (libsodium/NaCl puro, sem DOM)
- `generateKeyPair()` → `crypto_box_keypair()` (X25519).
- `generateRecoveryCode()` → 24 chars, 6 grupos de 4, alfabeto sem ambiguidade (`ABCDEFGHJKLMNPQRSTUVWXYZ23456789`, sem 0/O/1/I/L), formato `XXXX-XXXX-XXXX-XXXX-XXXX-XXXX`.
- `generateSalt()`, `deriveKeyFromSecret(secret, salt)` → Argon2id (`crypto_pwhash`, `OPSLIMIT_INTERACTIVE`/`MEMLIMIT_INTERACTIVE`). Genérico: `secret` é a senha de login (caso normal) ou o recovery code aleatório (fallback OAuth, ver 6.2).
- `encryptPrivateKey`/`decryptPrivateKey` → `crypto_secretbox_easy`/`_open_easy`, chave = derivada do recovery code. Erro "Código de recuperação incorreto" se a autenticação Poly1305 falhar.
- `generateDEK()` → `crypto_secretbox_keygen()`, uma por recurso.
- `wrapDEKForRecipient(dek, publicKey)` → `crypto_box_seal` (sealed box — cifra unilateral, sem handshake nem chave privada do remetente).
- `unwrapDEK(wrapped, myPublicKey, myPrivateKey)` → `crypto_box_seal_open`.
- `encryptContent`/`decryptContent` → `crypto_secretbox_easy` com a DEK do recurso.

### 6.2 `key-manager.ts` — orquestração de sessão
Cache em memória (`cachedPrivateKeyB64`, `cachedPublicKeyB64`, `dekCache: Map<"resourceType:resourceId", Uint8Array>`) **+ cache local em `localStorage`** (`go_guide_e2ee_${userId}`, chave privada+pública deste dispositivo) — desbloqueio automático em recarregamentos e em qualquer sessão futura no mesmo navegador, sem pedir nada ao usuário.

**Desbloqueio automático via senha de login** (desde 2026-07-08 — ver Changelog): a chave privada não é mais protegida por um "recovery code" avulso que o usuário precisa copiar e guardar; é derivada (Argon2id) da **própria senha de conta**, chamada automaticamente por `login/page.tsx` e `cadastro/page.tsx` logo após `signInWithPassword`/`signUp`. Isso dá acesso automático em **qualquer dispositivo**, só de fazer login normalmente — sem tela extra, sem código pra guardar (mesmo padrão do Bitwarden/1Password: senha mestra → KDF → chave).
- `setupOrUnlockWithPassword(userId, password)` — chamado no login/cadastro: se `hasKeysConfigured` for false, gera o par + salt e cifra a privada com a senha (`setupKeysWithSecret`, interno); se já existir, deriva e decifra (`unlockWithSecret`, interno). Ambos os caminhos também persistem em `localStorage`.
- `unlockWithPassword(userId, password)` — fallback do `E2EEGate` (ver 7.5) quando a sessão já estava aberta antes da correção, ou quando `localStorage` foi limpo: pede a senha de novo (não um código separado).
- `setupKeysWithRandomRecoveryCode(userId)` — **só** para contas sem senha (login via Google, sem segredo pra derivar): gera um código aleatório (`e2ee.generateRecoveryCode()`) e usa como `secret` no lugar da senha. É o único caso em que ainda existe um "recovery code" mostrado ao usuário (card discreto, não bloqueante, no `E2EEGate`).
- `tryAutoUnlock(userId)` — tenta usar a cópia salva em `localStorage` antes de qualquer chamada de rede; síncrono.
- `hasKeysConfigured(userId)` — SELECT em `user_encryption_keys`.
- `getPublicKeyOf(userId)` — busca chave pública de terceiros (RLS permite leitura pública de qualquer chave pública).
- `ensureResourceKey(resourceType, resourceId, grantees[])` — busca grant não revogado meu; se não existe (primeira vez), gera DEK nova; para cada grantee sem grant ainda e com chave pública já configurada, faz **`insert` simples** (não `upsert`) de `wrapped_dek`, ignorando erro `23505` (unique violation = grant já existe, nada a fazer). **Importante (bug corrigido em 2026-07-08, ver 3.3)**: NÃO usar `upsert`/`ON CONFLICT` aqui — com RLS, o Postgres precisa conseguir "ver" (via policy de SELECT) uma eventual linha conflitante de OUTRA pessoa pra decidir se há conflito; isso trava exatamente ao conceder acesso a alguém que não seja eu mesmo, inclusive na primeiríssima mensagem de uma conversa nova (quando nem eu nem o destinatário temos grant ainda). `insert` simples só depende da policy de INSERT (permissiva) e não tem esse problema.
- **RLS de `encrypted_dek_grants` (endurecida em 2026-09-01, migration `067`)**: INSERT/UPDATE exigem `grantee_user_id = auth.uid()` (bootstrap — inserir/atualizar o próprio grant) **ou** `has_dek_grant(resource_type, resource_id)` (já ter um grant ativo naquele recurso, mesmo critério do self-healing do SELECT) — antes, as duas aceitavam qualquer autenticado (`auth.uid() IS NOT NULL`) sem checar relação nenhuma com o recurso. Isso nunca quebrou confidencialidade de verdade (sem conhecer a DEK real, ninguém forja um `wrapped_dek` que abra — `crypto_box_seal` exige o segredo original), mas permitia negar acesso silenciosamente: UPDATE arbitrário revogava/corrompia o grant de qualquer pessoa em qualquer recurso (mesmo sem nenhuma relação com ele), e como `resource_id` de conversa é hash determinístico sem segredo (`conversation.ts`, abaixo), dava pra "reservar" a linha de outra pessoa **antes** da conversa real existir, bloqueando o grant de verdade pra sempre (colide com a `UNIQUE`, e o app ignora `23505` silenciosamente). A correção fecha de vez o vetor de UPDATE; o de INSERT via auto-grant de bootstrap fica mais restrito mas não 100% fechado (quem planta um grant "de mentirinha" em si mesmo primeiro ainda consegue satisfazer `has_dek_grant` depois) — fechar por completo exigiria validar no banco que o `resource_id` corresponde a uma conversa/pedido real entre as partes; fica como próximo passo em aberto.
- `encryptForResource` / `decryptResource` — API de alto nível.
- `grantAccessToExisting(resourceType, resourceId, newGranteeUserId)` — concede acesso a destinatário novo numa DEK já existente **sem recifrar conteúdo**: desembrulha minha cópia, rewrap para o novo, `upsert` (seguro aqui especificamente porque a função exige que **eu já tenha** um grant nesse recurso antes de rodar — `has_dek_grant` então é `true` pra mim, o que satisfaz a policy de SELECT que o `ON CONFLICT` precisa pra checar a linha do novo grantee; não é o mesmo cenário de risco do `ensureResourceKey`, onde o primeiro grant de alguém pode acontecer sem que eu tenha grant nenhum ainda). Falha se o destinatário ainda não configurou E2EE.
- `revokeGrant(...)` — `UPDATE ... SET revoked_at = now()`, sem afetar os demais grantees.

### 6.3 `conversation.ts`
- `derivedResourceId(...parts)` — SHA-256 determinístico dos parts concatenados por `:`, truncado a 16 bytes, formatado como UUID (bits de versão/variant forçados manualmente). Permite ter um "ID de recurso" **sem precisar de coluna/tabela extra**.
- `conversationId(profileId, userA, userB)` — ordena os dois userIds alfabeticamente (ID estável independente de quem inicia) e chama `derivedResourceId('conversation', profileId, "userA:userB")`.

Recursos cifrados no sistema: `conversation` (DMs), `prayer_request` (pedidos/respostas privados), `profile_sensitive_fields` (dados sensíveis do perfil).

---

## 7. Fluxos de negócio (a preservar em qualquer reimplementação)

### 7.1 Autenticação e onboarding
1. **Cadastro** (`/cadastro`) → `supabase.auth.signUp` com `full_name` em metadata → trigger `handle_new_user()` já cria `profiles` com username auto-gerado → redirect para `/onboarding` (Google OAuth usa `redirectTo: /auth/callback?redirect=/onboarding`; login com Google continua indo para `/dashboard`).
   - **Verificação de e-mail (migration 055, 2026-08-28)** — obrigatória só para contas de e-mail/senha (Google já vem verificado, ver `handle_new_user()` acima). Logo após o `signUp`, o client dispara (fire-and-forget) `POST /api/auth/enviar-verificacao`, que gera um token (`crypto.randomUUID()`, expira em 24h), grava em `profiles.email_verification_token`/`_expires_at` e manda o e-mail via `sendVerificationEmail()` (`src/lib/email/send-verification-email.ts`, reaproveita `sendEmail()` de `src/lib/email/brevo.ts` — mesmo padrão "sem `BREVO_API_KEY` = no-op silencioso"). O link aponta para `/verificar-email/[token]` (Server Component, `createServiceClient()`), que marca `profiles.email_verified=true`, limpa o token e também sincroniza `auth.users.email_confirmed_at` via `supabase.auth.admin.updateUserById(..., { email_confirm: true })` — sem depender do fluxo nativo de confirmação do Supabase (decisão consciente: reaproveitar a infra Brevo já existente, sem mexer em config do painel do Supabase). Link inválido/expirado mostra estado próprio com CTA para `/dashboard` (onde o `EmailVerificationBanner` cobre o reenvio, ver item 5 abaixo).
2. **Onboarding guiado** (`/onboarding` → `OnboardingWizard`, `src/components/onboarding/onboarding-wizard.tsx`) — wizard client-side com estado interno tipado como união de strings (`Step = 'role' | 'profile' | 'payment' | 'project' | 'summary' | 'partnerDone'`, migration 032 em diante — antes era `useState(1..4)` numérico puro):
   - **Passo "role" (novo, migration 032)** — primeira tela, dois cards grandes ("Sou apoiador/parceiro" vs. "Sou missionário/organização", mesmo padrão pessoal→profissional do Instagram). Escolher grava `profiles.user_role` na hora. `partner` → pula direto pro passo `partnerDone` (tela de fim leve, sem recebimento/projeto — não fazem sentido pra parceiro) → `/dashboard` (que agora é o feed, ver 7.1-bis). `missionary` → segue pro passo `profile`, fluxo original abaixo. Fechar o wizard sem escolher deixa `user_role` no default `'partner'` da coluna (fallback mais seguro, nunca bloqueia). A barra de progresso (`STEP_LABELS`) só é renderizada nos 4 passos da trilha de missionário — os passos `role`/`partnerDone` ficam fora dela.
   - **Passo "profile"**: `display_name`/`username` (com checagem de disponibilidade)/`bio`/`location`/`mission_start_date` — update em `profiles`. Obrigatório para avançar (nome e username não podem ficar vazios), mas os valores já vêm pré-preenchidos pelo trigger.
   - **Passo "payment"**: cria linhas em `payment_methods` (tipos `pix`/`paypal`/`wise`, versão reduzida do catálogo completo — demais tipos ficam para a aba Pagamentos em Configurações) — pulável via "Pular por agora".
   - **Passo "project"**: título/descrição/meta/moeda → `POST /api/highlights` com `goalTypes: ['financial']`, `status: 'active'` — pulável.
   - **Passo "summary"**: resumo do que ficou pendente, CTA para `/dashboard`.
   - Nenhum estado de "pulou" é persistido — a pendência é sempre recalculada a partir dos dados reais (ver item 7.10-bis abaixo e `SetupChecklistBanner`).
   - **Reaproveitado pelo fluxo "virar missionário"** (ver 7.1-bis): `OnboardingWizard` computa o passo inicial como `profile.user_role === 'missionary' ? 'profile' : 'role'` — como `becomeMissionary()` já grava `user_role='missionary'` **antes** de redirecionar pra `/onboarding`, a tela pula a pergunta de papel e cai direto no passo `profile`, sem precisar de rota nem componente novo.
3. **Login** (`/login`) → e-mail/senha ou Google OAuth (`redirectTo: /auth/callback?redirect=...`), preserva `?redirect=`.
4. **Callback** (`/auth/callback/route.ts`) → `exchangeCodeForSession`, redirect.
5. **Proteção**: middleware (`proxy.ts`) protege `/dashboard`; layout do dashboard revalida `user`+`profile` (sem fallback para onboarding — assume que o trigger sempre cria o profile). `profile.email_verified=false` **não bloqueia** nada (decisão revista depois da 2026-08-28 original, que redirecionava pra uma página cheia — usuário pediu explicitamente pra não perder acesso) — em vez disso, `EmailVerificationBanner` (`src/components/dashboard/email-verification-banner.tsx`) aparece no topo de `{children}` em `dashboard/layout.tsx`, em **toda** rota do dashboard, até a pessoa confirmar; tem botão de reenvio (mesma rota `/api/auth/enviar-verificacao`) e some sozinho assim que `email_verified` vira `true`, sem lógica de dismiss (reaparece em qualquer visita até resolver de verdade). **Exceção (2026-09-01): `/dashboard/financeiro` bloqueia de verdade** (`financeiro/layout.tsx`) — decisão explícita do usuário só pra essa área, motivo: recibos e avisos de repasse dependem de e-mail real. `!profile.email_verified` substitui `FinanceSubNav`+`{children}` por um card fixo (reaproveita `EmailVerificationBanner` pro botão de reenvio, sem duplicar a lógica de cooldown/toast) — nenhuma subrota de Financeiro (contas, lançamentos, conciliação, categorias, câmbio) escapa, por estarem todas dentro do mesmo layout. Escopo deliberadamente restrito a quem **recebe** (perfil ativo do missionário) — `/dashboard/financeiro-parceiro` (histórico de doações de quem **doa**) não tem esse gate, por decisão do usuário. Resto do dashboard continua sem bloqueio nenhum. `/onboarding` nunca teve gate nenhum, continua igual.
6. **Exclusão de conta**: duas entradas (`/conta/excluir` para quem só é parceiro, exige digitar `EXCLUIR`; `AccountForm` em Configurações para quem tem profile, exige digitar `@username`) → ambas chamam `POST /api/account/delete`.
7. **Alerta de configuração pendente** (`SetupChecklistBanner`, `src/components/dashboard/setup-checklist-banner.tsx`, renderizado em `dashboard/painel/page.tsx` — ver 7.1-bis sobre por que não é mais `dashboard/page.tsx`) — banner no topo da visão de gestão, computado (não armazenado) a partir de 3 condições: perfil incompleto (sem avatar OU username ainda no padrão auto-gerado `nome_xxxxxx`), nenhum método de recebimento configurado, e zero `highlights`. Mostra só os itens pendentes com link direto para resolver; desaparece sozinho quando os 3 estão OK. Dismissable via `localStorage` (`profile-banner-dismissed`, mesmo padrão do banner anterior que substituiu).

8. **Lembretes de aniversário** (`BirthdayReminders`, `src/components/dashboard/birthday-reminders.tsx`, Server Component renderizado logo abaixo do `SetupChecklistBanner` em `dashboard/painel/page.tsx`) — banner rosa, sem persistência própria: a cada carregamento da visão de gestão, busca `partners` do perfil com `birth_date IS NOT NULL` e calcula em memória (`src/lib/partners/birthdays.ts`, `getUpcomingBirthdays`) quem faz aniversário nos próximos 14 dias (comparando só mês/dia, com virada de ano). Não usa `pg_cron`/trigger — é só uma consulta computada na visita à página, então só funciona como lembrete se o missionário abrir `/dashboard/painel`. Cada linha tem um botão de ação: se o parceiro tem `user_id`, linka pro chat interno (`/dashboard/mensagens/[userId]`); senão, se tem `phone`, abre `wa.me` com mensagem de parabéns pré-preenchida (`target="_blank"`); sem nenhum dos dois, a linha aparece sem ação. `birth_date` é preenchido opcionalmente no modal `AddPartnerButton` (admin) e no formulário público `PartnershipForm` (parceiro se cadastra sozinho) — mesma coluna nos dois casos.

### 7.1-bis Papéis (parceiro vs. missionário), feed universal, seguir e conversão de conta (migrations 032-034)

Até aqui, todo cadastro criava um `profiles` idêntico e caía sempre no mesmo `/dashboard` (visão de gestão do missionário), sem distinção nenhuma entre apoiador e missionário. Esta rodada introduz `profiles.user_role` e reestrutura o dashboard em torno dele.

**Reestruturação do dashboard**: `/dashboard/page.tsx` deixou de ser a visão de gestão — agora renderiza sempre o `FeedScreen` (feed estilo Instagram, ver abaixo), **para os dois papéis**, sem branch por `user_role`. Todo o conteúdo antigo de `dashboard/page.tsx` (`SetupChecklistBanner` → `BirthdayReminders` → stats → posts recentes/atalhos) foi recortado, sem mudança de lógica, para `dashboard/painel/page.tsx`. `/dashboard/painel` continua acessível por URL direta por qualquer papel (não é um hard-block — só some do nav de quem é `partner`); como a página consulta sempre o `profile_id` de quem está logado, um parceiro que chegue lá só vê o próprio perfil vazio (stats zerados), sem vazamento de dado de terceiros.

**Nav consciente de papel** (`src/hooks/use-dashboard-nav.ts`, `useNav(role)`/`useBottomNavItems(role)` — fonte única lida tanto por `DashboardSidebar` quanto por `MobileBottomNav`, resolvendo uma duplicação que existia antes de cada componente ter sua própria cópia da lista):
- `role='missionary'`: Feed, Visão Geral (`/dashboard/painel`), Projetos, Parceiros, Orações, Mensagens, Financeiro, IA Copiloto, Configurações. Não há mais item de nav "Publicações" — gerenciar posts (ver, editar, arquivar, excluir) agora é feito só de dentro do próprio perfil público (`/[username]`, ver 7.9 e changelog).
- `role='partner'`: Feed, Mensagens, Meus projetos, Minhas doações, Configurações — deliberadamente enxuto, sem nenhuma ferramenta de gestão de missionário.
- O papel usado é sempre o do **perfil ativo** (`profile.user_role`, resolvido por `getActiveProfile()`), não do usuário logado — importa pro caso de alguém ser `manager` de outro perfil via `profile_managers` (seção 3.1): trocar de conta pelo `AccountSwitcher` já naturalmente troca o nav pro papel daquele perfil, sem código extra.

**Feed** (`src/components/dashboard/feed/`, `src/app/dashboard/feed/actions.ts`) — cross-perfil, paginado por cursor, role-agnóstico no conteúdo:
- `profileIds` = união de quem o usuário segue (`follows.profile_id` — seção 3.1) **e** de quem ele é parceiro (`partners.profile_id` onde `partners.user_id = auth.uid()`) — essa segunda parte já funcionava com a RLS existente sem nenhuma mudança de schema (`partners_self_read`, sem escopo de `profile_id`).
- `getFeedPage(cursor)`: `posts` desses `profileIds`, `is_draft=false`, `order by published_at desc`, cursor = `published_at` do último item da página anterior (a query de feed de um perfil só, em `[username]/page.tsx`, usa `.limit(20)` sem cursor — não é a mesma coisa, e não foi reaproveitada).
- **Heurística de ranking v1** (`src/lib/feed/rank.ts`, `scorePost`) — transparente, pesos nomeados num arquivo só, sem ML: recência (decaimento linear) + segue (+50) + já doou/`pledges` confirmado (+30) + parceiro fixo ativo/`recurring_pledges` (+20) + sobreposição de `highlights.category` com projetos já apoiados/seguidos (+15). Pensada para ser substituída por um modelo treinado a partir de `feed_events` (seção 3.1) no futuro (Fase 2+), sem mudar o resto do pipeline — por isso o feed já grava `post_view` em `feed_events` a cada página carregada (1 insert em lote por página, não por post), mesmo a v1 sendo heurística.
- Estado vazio (`DiscoverMissionaries`) — quando `profileIds` está vazio (conta nova, ainda sem seguir/apoiar ninguém): diretório simples de perfis públicos com `user_role='missionary'`, sem ranking, com botão Seguir inline. Garante que ninguém trava numa tela vazia (princípio de nunca bloquear) — exclui o próprio perfil do visitante da lista.
- **Seguir** (`follow-button.tsx`, `followProfile`/`unfollowProfile` em `feed/actions.ts`) — insert/delete direto em `follows` sob a RLS própria da tabela, **sem** passar pelo padrão "encontra ou promove parceiro" usado em `recurring-pledge-form.tsx`/`checkout-recurring/route.ts`/`pledge-review-card.tsx` — decisão deliberada, seguir não deve criar uma linha de `partners`. Botão só renderizado em perfis públicos (um `private`/`stealth` simplesmente não aparece nos resultados de `posts_public_read`/`highlights_public_read`, mesmo com uma linha de `follows` já existente).

> **Decisão de design (2026-07-22): sem `FollowButton` dentro do card de post do feed.** Cada instância de `FollowButton` guarda `useState(initiallyFollowing)` local — quando o mesmo autor aparece em vários posts (comum, já que `getFeedPage` filtra por `profileIds` = quem o usuário já segue ou é parceiro), clicar em seguir num card não atualizava os outros cards do mesmo autor, ficando com estados divergentes na tela pro mesmo relacionamento. Benchmark rápido: Instagram/X só mostram botão de seguir em conteúdo de descoberta (não no feed principal, que por definição já é só de quem você segue); quando um app mostra o botão dentro do feed principal (LinkedIn), ele lê de um cache global único, nunca de estado local por card. Como o feed daqui já filtra pra quem o usuário segue-ou-apoia, o botão ali era redundante na maior parte do tempo e, no caso raro de parceiro-mas-não-seguidor, ficava inconsistente entre posts do mesmo autor. Removido de `FeedPostCard`/`FeedList`/`FeedScreen` (que também parou de buscar `getFollowedProfileIds()` à toa) — `FollowButton` continua existindo só em contexto de descoberta real: `discover-missionaries.tsx` (estado vazio do feed) e a nova `FollowsList` do perfil (7.12).
- Faixa de stories de projetos (`ProjectStoriesRow`, seção 7.13) renderiza no topo do `FeedScreen`, acima da lista — reaproveita o mesmo `profileIds`.

**Área do parceiro**:
- `/dashboard/mensagens` — **reaproveitado sem nenhuma mudança de código**: a caixa já busca por participação (`sender_id`/`recipient_id = eu`), não por `profile_id`, então um parceiro já vê todas as conversas com qualquer missionário.
- `/dashboard/meus-projetos` (`MyProjectsList`, somente leitura — nunca reaproveita `HighlightsList`, que tem editar/reordenar/excluir) — `highlight_id`s distintos a partir de `pledges` confirmados + `recurring_pledges` ativos do usuário, depois `SELECT highlights WHERE id IN (...)` sob a RLS pública já existente.
- `/dashboard/financeiro-parceiro` (`GivingHistory`/`GivingFilters`) — `SELECT pledges WHERE reporter_user_id = auth.uid()`, com o perfil do missionário e o projeto via join; filtro por missionário client-side (`<select>` nativo, convenção do projeto). RLS de self-read já cobria isso, zero schema novo.
- **Comprovante de doações** (`GET /api/partner/tax-export?year=&format=csv|pdf`) — uma única query em `pledges` (`status='confirmed'`, ano filtrado) já cobre avulsas **e** recorrentes confirmadas, porque toda cobrança recorrente confirmada já vira sua própria linha em `pledges` via `recurring_pledge_id` (seção 3.1) — não precisa de uma segunda query em `recurring_pledges`. CSV é geração de string simples; PDF usa `pdfkit` (nova dependência, API imperativa, sem exigir JSX/React) para um recibo simples por ano com aviso fixo de que **não é documento fiscal oficial**. Botão de exportar fica em `/dashboard/financeiro-parceiro`, com `<select>` dos anos que têm doação confirmada.

**Onboarding v2 e conversão "virar missionário"** — ver passo `role` do `OnboardingWizard` na seção 7.1. Fluxo de conversão (`becomeMissionary()`, Server Action em `src/app/dashboard/actions.ts`): `UPDATE profiles SET user_role='missionary' WHERE user_id=auth.uid()` (nunca em nome de outra pessoa) → `redirect('/onboarding')`, que detecta `user_role` já `missionary` e pula direto pro passo `profile`. Entrada em Configurações → Conta (`AccountForm`, card "Divulgar minha própria missão", só visível pra quem é `partner`) — sem gate de plano (quem converte continua no `plan='free'` como qualquer conta nova).

**Categoria de projeto** (`highlights.category`, migration 034) — multi-select em `HighlightForm` ao lado do `goalTypes` existente, mesmo padrão de interação. Puramente um sinal de ranking do feed nesta rodada — sem badge/UI pública.

**Modo de pré-visualização para superadmin** (`src/lib/auth/superadmin.ts`, `isSuperAdmin(email)`) — allowlist por variável de ambiente `SUPERADMIN_EMAILS` (nunca uma coluna em `profiles`, pra não abrir superfície de RLS nem risco de auto-promoção via client), mesmo padrão de segredo administrativo do `CRON_SECRET`. `SuperadminRoleSwitcher` (barra flutuante, só renderizada quando `isSuperAdmin` é verdadeiro) chama `setPreviewRole(role)` (Server Action, também reconfere `isSuperAdmin` no servidor antes de fazer qualquer coisa), que grava um cookie `preview_role` (`src/lib/profile/role-preview.ts`). `dashboard/layout.tsx` computa `effectiveProfile = previewRole ? { ...profile, user_role: previewRole } : profile` e passa esse objeto pro `DashboardSidebar`/`MobileBottomNav`/`CreateContentFab` — **não** troca `getActiveProfile()` nem qualquer query de dado, só decide qual nav renderizar em cima do próprio perfil real do superadmin. Não é impersonação: nenhuma RLS é contornada, e um superadmin com perfil vazio pode "ver como missionário" e literalmente usar as ferramentas reais de missionário na própria conta pra popular dados de teste.

### 7.2 Pledge (registro manual de oferta) → conciliação
O sistema **não processa pagamentos** — Pix/PayPal/etc. acontecem fora da plataforma.
1. Parceiro/visitante preenche `PledgeForm`: escolhe `payment_method` dentre os configurados pelo missionário, valor, data, nome/e-mail, comprovante opcional (upload comprimido para bucket `media`).
2. Insert em `pledges` com `status='pending'` (qualquer um pode inserir, mesmo anônimo/não-parceiro — RLS `WITH CHECK (true)`).
3. Trigger notifica o missionário (`new_pledge`).
4. Missionário revisa em `PledgeReviewCard` (dashboard → Financeiro → Conciliação):
   - **Confirmar**: promove/cria `partners` a partir do `reporter_user_id`, cria uma `transactions` real (`type: income`) na conta escolhida, atualiza `pledges.status='confirmed'` + `confirmed_transaction_id`. Trigger notifica o reporter (`pledge_confirmed`).
   - **Rejeitar**: atualiza `status='rejected'` + `rejection_reason` opcional. Trigger notifica o reporter (`pledge_rejected`, migration `085_pledge_rejected_notification.sql`) com o motivo informado. Não é definitivo na hora — ver janela de reanálise abaixo.

**Correção (2026-09-03): Conciliação sem conta financeira nenhuma não dava nenhum jeito de agir; depois revisto pra criar a conta automaticamente** — usuário (conta real, não a seed de teste) recebeu 2 ofertas pendentes, entrou em Conciliação e viu só o texto "Crie uma conta financeira antes de confirmar ofertas." — sem link, sem botão, nada clicável (`ReconciliationQueue`, `accounts.length === 0`, ver 7.31 pra origem desse filtro de contas ativas). Primeira correção: `ReconciliationQueue` passou a sempre renderizar os cards das ofertas (nome, valor, comprovante, WhatsApp, botão Rejeitar — nenhum desses precisa de conta pra existir); só a seção "Confirmar depósito" de `PledgeReviewCard` ficava condicionada a ter conta, com um botão "Criar conta" linkando pra `/dashboard/financeiro/contas`.

O usuário então questionou por que isso é necessário, já que ele **já tinha configurado Pix** em Configurações → Pagamentos (`payment_methods`) — o sistema "deveria entender isso" em vez de pedir mais um passo manual. É uma distinção real que vale registrar: `payment_methods` é **como alguém te paga** (informação pública, mostrada no perfil); `financial_accounts` é **onde você rastreia o saldo** internamente (o que alimenta Visão Geral/Lançamentos/relatórios) — não são a mesma coisa, um Pix não é "uma conta", é só o endereço de recebimento. Mas nada impedia o sistema de **criar a conta sozinho** a partir dessa informação, em vez de exigir o passo manual. Existia inclusive precedente direto no próprio schema: `payment_methods.linked_account_id` (migration 031) já faz esse vínculo pra Stripe (webhook deposita automaticamente na conta linkada, `StripeConnectCard` até auto-seleciona a primeira conta disponível) — só nunca tinha sido estendido pra criar a conta quando não existe nenhuma.

**Revisão final**: `handleConfirm()` em `PledgeReviewCard`, quando não há nenhuma `financial_accounts` (`accountId` vazio), cria uma automaticamente (`Conta {método}`, ex. "Conta Pix", na moeda da própria oferta, `account_type` default `checking`) antes de criar a `transactions` — sem pedir nada a mais do usuário. UI: campo "Depositar em" mostra "Nova: Conta Pix" como preview quando não há conta ainda, com nota explicando que ela será criada ao confirmar + link "Gerenciar contas"; botão Confirmar nunca mais fica bloqueado por falta de conta. Depois da primeira confirmação, a conta já existe normalmente pra todo o resto do módulo (Visão Geral, próximas confirmações já oferecem ela no seletor). Verificado num browser real (Playwright) duas vezes: (1) o card aparece completo e Rejeitar funciona ponta a ponta sem conta nenhuma (status vira `rejected` de verdade); (2) Confirmar sem conta nenhuma cria a `financial_accounts` ("Conta Pix", BRL), a `transactions` (saldo atualizado via trigger já existente) e marca o pledge `confirmed` — tudo num clique só. Estado seed restaurado ao final das duas rodadas.

**Janela de reanálise de oferta recusada (2026-09-03)** — a pedido do usuário: recusar não é definitivo na hora, porque a causa mais comum ("não achei essa transação no meu banco") pode ser resolvida depois (o apoiador aparece com o comprovante, ou o missionário acha o lançamento no extrato). `src/lib/financial/pledge-windows.ts` define duas constantes (`PLEDGE_RECONSIDERATION_DAYS=7`, `PLEDGE_ARCHIVE_DAYS=60`) e a janela inteira é calculada em cima de `pledges.reviewed_at` — **sem coluna nova nem job agendado** pra "expirar" nada, só filtro de data nas queries:
- **0–7 dias após a recusa**: a oferta continua na aba "Pendentes" de Conciliação, junto com as pendentes de verdade, numa seção "Recusadas recentemente — ainda dá pra reconsiderar". O card (`PledgeReviewCard`) mostra o motivo da recusa e a data-limite pra reconsiderar; some o botão Rejeitar (já foi usado). **Correção de UX (2026-09-04, a pedido do usuário vendo a tela ao vivo)**: a caixa "Confirmar depósito" (valor + conta) não fica mais aberta de cara numa oferta já recusada — só o botão "Reanalisar" aparece; clicar nele revela a caixa + Confirmar/Cancelar (mesmo padrão "revelar ao clicar" que o botão Rejeitar já usa numa oferta pendente). Evita a impressão de "oferta já recusada com um botão Confirmar pronto pra clicar" — reanalisar passa a ser um gesto consciente. Internamente ainda é o mesmo `handleConfirm()` de sempre, sem nenhuma lógica condicional por status — só a exibição do formulário que ficou atrás de mais um clique.
- **7–60 dias após a recusa**: some da aba "Pendentes" e passa a aparecer só na aba "Arquivadas" (`ReconciliationTabs`, novo — substitui o antigo `ReconciliationQueue`, mesmo padrão pill Ativas/Arquivadas já usado em Contas, ver 7.29), com o mesmo card e a mesma capacidade de reconsiderar/confirmar.
- **Depois de 60 dias**: some das duas abas (query de `conciliacao/page.tsx` já filtra fora) — mas a linha **não é apagada do banco**, só para de aparecer na UI (decisão deliberada: registro financeiro histórico nunca é destruído neste sistema, e depender de um cron pra apagar destruiria dado sem necessidade real).
- **Notificação pro doador**: nova migration `085_pledge_rejected_notification.sql` (trigger `trg_notify_pledge_rejected`, espelha `trg_notify_pledge_confirmed` da migration 014) — dispara `pledge_rejected` com `rejection_reason` no payload pro `reporter_user_id`. `NotificationsBell` roteia pra `/dashboard/financeiro-parceiro` (mesmo destino de `pledge_confirmed`); `notification-emails` cron (migration 068, ver 7.10) ganhou o tipo na lista de e-mail, com o motivo no corpo. Em `/dashboard/financeiro-parceiro` (`GivingHistory`), o motivo agora aparece embaixo do lançamento rejeitado (chaves `PartnerFinance.rejectionReasonLabel`/`rejectionContactHint`, 3 línguas) — antes só existia o badge "Rejeitada", sem explicação nenhuma do porquê.
- **E-mail de recusa pra quem reportou sem conta (2026-09-04, migration `087_pledge_rejection_email_anonymous.sql`)** — o caminho acima (`pledge_rejected` via `notifications`) só alcança quem tem conta, porque `notify()` (migration 014) exige `recipient_user_id NOT NULL`. A pedido do usuário, fechada a lacuna: nova coluna `pledges.rejection_email_sent_at` + índice parcial (mesmo padrão de `notifications.email_sent_at`, migration 068). `GET /api/cron/notification-emails` ganhou uma segunda fase (`sendAnonymousRejectionEmails`, mesmo cron a cada 5min, sem rota nova) que varre `pledges` direto por `status='rejected' AND reporter_user_id IS NULL AND reporter_email IS NOT NULL AND rejection_email_sent_at IS NULL` e manda o e-mail pro `reporter_email` capturado no `PledgeForm` — sem depender de `auth.users`. Quem escolheu doação anônima (`is_anonymous=true`) já tem `reporter_email NULL` desde o insert, então o filtro já respeita essa escolha sem checagem extra; e quem tem conta nunca cai nessa fase (mutuamente exclusivo com a fase 1 por `reporter_user_id`), sem risco de e-mail duplicado. CTA do e-mail linka pro perfil público do missionário (`/${username}`) em vez do dashboard, já que essa pessoa não tem onde logar. **Migration nasceu como `086` e foi renumerada pra `087`** — colisão com trabalho paralelo (outra sessão pegou `086` pra `086_scheduled_pledges.sql`, feature de oferta agendada, sem relação); mesmo tipo de colisão já resolvido antes (ver entradas de renumeração 074-077→075-078 e 079-081→081-083 no Changelog). Só renomeação de arquivo + referências no código/doc — schema/lógica inalterados.
- **Migrations 085 e 087 aplicadas pelo usuário (2026-09-04).** Todo o fluxo de reanálise (rejeitar/reconsiderar, abas Pendentes/Arquivadas, o motivo aparecendo pro doador em `/financeiro-parceiro`, o botão "Reanalisar") já tinha sido verificado num browser real (Playwright) antes disso; só o disparo dos triggers/colunas em si dependia das migrations. **Nota de higiene de teste**: ao testar essa rodada (antes da aplicação), chamar o endpoint do cron direto contra o banco de produção processou também algumas notificações `new_pledge` órfãs, deixadas por testes anteriores desta sessão (pledges de teste já apagados, mas a notificação que o trigger da migration 014 gera nesse insert não tinha sido limpa junto) — resultou em alguns e-mails reais, porém inofensivos, indo pra caixa da conta seed `ana.ferreira@seed.goguide.test` (não é uma pessoa real). As 21 notificações órfãs acumuladas ao longo da sessão foram identificadas e removidas.
- **Envio retroativo (2026-09-04)**: a oferta rejeitada pelo usuário no dia anterior (antes da migration 085 existir) nunca gerou notificação, porque trigger só reage a eventos futuros — instalar a migration não executa retroativamente sobre um `UPDATE` que já aconteceu. Reporter tinha conta (`reporter_user_id` preenchido), então caía no caminho da 085, não no da 087 (a de quem não tem conta). Inserida manualmente a mesma linha de `notifications` que o trigger teria gerado (mesmo `payload`, com `rejection_reason` real) e disparado o cron uma vez — confirmado `checked:1, sent:1` e `email_sent_at` preenchido. Ação confirmada com o usuário antes de executar, por afetar uma pessoa real.
- **Redesign visual do e-mail (2026-09-04)**, a pedido do usuário depois de ver o e-mail de recusa "funcionando mas sem estrutura de design". Os 6 e-mails do cron (`new_message`/`pledge_confirmed`/`pledge_rejected`×2/`new_pledge`/`new_partner`) usavam só `<p>` soltos com uma função `wrap()` minimalista. Novo `src/lib/email/template.ts` (`renderEmailTemplate()`, reutilizável — não ficou preso a esse cron) substitui `wrap()`: layout em tabela (compatibilidade com Outlook/clientes antigos que não leem flexbox/grid), logo real do app (`public/logo.png`) no cabeçalho, card branco com cantos arredondados sobre fundo cinza claro, barra de cor fina no topo do card + botão de CTA na cor de "acento" do tipo de e-mail (`success` verde pra oferta confirmada, `warning` âmbar pra oferta recusada — não `destructive`/vermelho, tom deliberadamente menos alarmante já que a ação ainda pode ser corrigida — `primary` olive nos demais), e um bloco destacado (fundo bege claro) só pro motivo da recusa, pra não competir visualmente com o resto do texto. Cores convertidas de oklch (`globals.css`) pra hex à mão (e-mail não entende `oklch()`/CSS vars) — `#34390c`/`#1ea662`/`#f49f1e`, mesmas usadas no resto do produto. Suporte a `preheader` (texto de prévia que alguns clientes mostram antes de abrir, sem aparecer no corpo). **Deliberadamente fora**: os outros 2 crons que já mandavam e-mail antes desta rodada (`recurring-reminders`, `prayer-partner-updates`) continuam com o template antigo, ainda mais simples que o `wrap()` de antes — não foram pedidos nesta rodada, mas migrar pra `renderEmailTemplate()` depois é direto. Testado enviando um preview de verdade (mesmo código, via o cron real) pra caixa do próprio usuário — não só visualização estática — porque renderização de e-mail varia entre clientes o bastante pra um screenshot de browser não bastar como verificação. **Duplicação conhecida**: trabalho paralelo de outra sessão (mesma janela de tempo, ver commits de "Oferta agendada" no Changelog) criou independentemente `src/lib/email/email-header.ts` (`wrapEmail()`) resolvendo o mesmíssimo problema (e-mails sem moldura nenhuma) de um jeito parecido mas não idêntico (cabeçalho colorido com wordmark em texto, sem logo/acento por tipo, sem suporte a CTA embutido). As duas implementações coexistem por enquanto, cada uma nos crons que sua sessão tocou — consolidar em uma só fica como decisão em aberto pra uma próxima rodada, não resolvida nesta.

**Correção (2026-09-03): notificação de oferta caía na página errada, e a de revisão parecia quebrada** — usuário clicou na notificação de "nova oferta" e caiu em `/dashboard/financeiro/lancamentos` vazia (a oferta pendente não é uma `transactions` ainda, então não aparece lá). Corrigido no `NotificationsBell` pra rotear `new_pledge`→Conciliação e `pledge_confirmed`→`/dashboard/financeiro-parceiro` (quem recebe é o reporter, que não tem acesso à agenda do missionário) em vez de uma única rota fixa "financial"→lançamentos; `partner_lapsed` também saiu dessa área genérica e foi pra `/dashboard/parceiros`, onde o badge "Parece ter parado" realmente mora (ver 7.16-bis). Depois de chegar em Conciliação de verdade, o usuário achou a tela "nada intuitiva, nada clicável" — os botões Confirmar/Rejeitar sempre funcionaram (confirmado testando o clique de ponta a ponta: cria a transação, atualiza o pledge, sem erro nenhum), mas o campo de valor e o seletor de conta em `PledgeReviewCard` nunca tiveram `<Label>` nenhum — só números/dropdown soltos, sem dizer pra que servem. Adicionado label em cada campo + uma linha de instrução no topo da página ("confirmar gera o lançamento; rejeitar descarta sem afetar seu saldo") + o seletor de conta passou a filtrar só contas na moeda da própria oferta (evita depositar sem querer numa conta de outra moeda). Verificado num browser real (inclusive o clique de confirmar de ponta a ponta, de novo, depois da mudança).

### 7.3 Wizard de parceria (`PartnershipWizard`)
Máquina de estados com `choice: 'financial_once' | 'financial_once_general' | 'financial_ongoing' | 'prayer' | 'ambassador' | 'volunteer' | null`.
- Tela inicial oferece até 6 opções (as financeiras só aparecem se o missionário tem métodos de pagamento configurados). Não tem mais link "← Voltar ao perfil" próprio (2026-07-20) — esse botão de voltar virou único e fixo em `ProfileTabs`, ver 7.12. Ao escolher uma opção, o wizard mostra só o "← Voltar" interno de cada sub-tela (volta pro `choice=null`).
- Pode abrir direto numa sub-tela via `?choice=` na URL (`prayer`/`ambassador`/`volunteer`/`financial_once`/`financial_once_general`/`financial_ongoing`) — `page.tsx` valida contra `VALID_CHOICES` e passa `initialChoice` como estado inicial do `useState` do wizard. Usado pelo CTA "Quero ser parceiro de oração" do `PrayerRequestForm` (`?choice=prayer`), pra pular a tela de escolha e ir direto pro formulário — evita 1 clique extra que fazia gente desistir no meio do caminho. Desde 2026-07-20, a página pública de projeto (`[username]/projetos/[slug]`) também passa `choice` em todos os links pro wizard (botão "Faça parte deste projeto" e cada card de "Outras formas de apoiar"/"Como apoiar") — antes nenhum desses links tinha `choice`, então qualquer um deles caía sempre na mesma tela de menu genérico, dando a impressão de que os botões eram redundantes entre si.
- `financial_once` (veio de um link de projeto específico, `?highlight_id=`) → `PledgeForm` com `highlightId` preenchido. Só aparece como botão se houver `highlightId` na URL.
- `financial_once_general` (2026-07-08 — doação avulsa **sem** precisar vir de uma campanha específica) → `PledgeForm` sem `highlightId`, `isRecurring=false`. Sempre visível quando há métodos de pagamento, independente de campanha vinculada — antes só existia a opção de virar parceiro fixo mensal quando não havia `highlight_id` na URL.
- `financial_ongoing` (parceria fixa, recorrente) → **não** usa mais `PledgeForm` (migration 031) — usa `RecurringPledgeForm`, que exige conta (ver 7.11).
- `prayer`/`ambassador`/`volunteer` → `PartnershipForm` (cadastro direto em `partners`, sem exigir login) com `type` mapeado. Campo `phone` usa `PhoneInput` (DDI + máscara, mesmo componente do `AddPartnerButton`); label do campo vem da namespace i18n `PartnershipForm` (`whatsappLabel`). **Título/CTA/placeholder da mensagem reagem ao `type` (2026-08-30)** — `t(\`title${suffix}\`)`/`t(\`cta_${type}\`)`/`t(\`messagePlaceholder${type==='prayer'||type==='both'?'_prayer':''}\`)`, `suffix = defaultType ? \`_${defaultType}\` : ''` — antes o card sempre mostrava o título genérico "Como deseja se envolver?" mesmo quando `defaultType` já vinha fixado (ex. clicar em "Comprometa-se a orar regularmente" e cair numa tela perguntando de novo "como você quer se envolver"), pedido do usuário. Resto do componente segue hardcoded em PT.
- `PledgeForm`, na tela de confirmação, se a oferta **não** foi recorrente (`!isRecurring`), mostra um card convidando a virar parceiro fixo (`onBecomePartner` prop, volta o wizard pra `financial_ongoing`) — funil doação avulsa → parceria contínua.

**7.3-bis Abertura em modal (2026-08-26)** — a pedido do usuário: clicar em "Seja Parceiro" no perfil público não deve dar a impressão de "sair" da página do missionário. A busca de dados (perfil, métodos de pagamento, highlight/orçamento) foi extraída de `parceria/page.tsx` para `src/lib/partners/get-partnership-data.ts` (`getPartnershipData(username, searchParams)`), reaproveitada por dois pontos de entrada que renderizam o mesmíssimo `PartnershipWizard`:
- **Navegação dentro do app** (clique no botão do perfil, mesmo `<Link href="/${username}/parceria">` de sempre — nada mudou em `ProfileCTA`): Next.js **intercepta** a rota via `[username]/@modal/(.)parceria/page.tsx` e renderiza `PartnershipModal` (`src/components/partners/partnership-modal.tsx`), um `Dialog` em caixa centralizada de cantos arredondados (mesmo padrão do `ProjectStoryDialog`, ver Changelog 2026-08-26) por cima do perfil, sem trocar de página nem perder o scroll/estado dela por baixo.
- **Acesso direto** (link compartilhado, retorno do Stripe Checkout, e-mail de lembrete de recorrência via `cron/recurring-reminders`) continua caindo na página cheia normal (`parceria/page.tsx`), sem interceptação — é assim que intercepting routes funcionam (só interceptam client-side navigation partindo de dentro do layout).
`[username]/layout.tsx` ganhou o slot `modal` (parallel route `@modal`, com `@modal/default.tsx` retornando `null` quando não há modal ativo) renderizado ao lado de `{children}`. Fechar o modal (`X`, clique fora, ou o `BackButton` interno do próprio `PartnershipWizard` na tela raiz) chama `router.back()` — não precisou de lógica nova de "fechar", porque `BackButton` (`ui/back-button.tsx`) já preferia `router.back()` a `href` sempre que havia navegação in-app; aqui isso naturalmente volta pra rota interceptada anterior (o perfil), fechando o modal. **Limitação aceita**: o funil financeiro redireciona pro Stripe Checkout e volta via `returnUrl` apontando pra `/${username}/parceria` — esse retorno é sempre um carregamento de página nova (página cheia, não modal), já que a navegação sai do app e volta; a sensação de "não sair da página" vale para o início da escolha e os fluxos de oração/embaixador/voluntário, não elimina esse trecho específico.

**7.3-tris E-mail automático de acompanhamento pra parceiros de oração (migration `060_partner_update_emails.sql`, 2026-08-30).** Pedido do usuário: quem se compromete a orar deveria ficar conectado ao missionário e receber "obrigado por orar / veja as novidades" quando ele publica algo — não só um cadastro solto. A relação persistente já existia (`partners.type='prayer'`, ver acima); faltava o lado de saída. Novo cron diário `GET /api/cron/prayer-partner-updates` (`src/app/api/cron/prayer-partner-updates/route.ts`, `vercel.json` `0 13 * * *`, mesmo guard `CRON_SECRET` do `recurring-reminders`): para cada `partners` com `type IN ('prayer','both')`, `email` preenchido e `update_emails_opt_in=true` (coluna nova, default `true`), compara `last_update_email_sent_at` (coluna nova, `NULL` = nunca) — ou `joined_at` se nunca mandou — contra `posts` (`is_draft=false`, `published_at >`) e `highlights` (`status <> 'hidden'`, `created_at >`) daquele `profile_id`; se houver algo novo, manda e-mail via `sendEmail()` (Brevo, mesmo helper do `recurring-reminders`) com a contagem do que mudou ("publicou N atualizações novas"/"iniciou N projetos novos") e link pro perfil público, e atualiza `last_update_email_sent_at = now()`. Link de descadastro (`GET /api/partners/[id]/unsubscribe-updates`, mesmo padrão sem auth do unsubscribe de `recurring_pledges` — o UUID do parceiro já é o token) zera `update_emails_opt_in`. Alcança parceiro de oração **sem conta** (a maioria dos cadastros via `PartnershipForm` são anônimos/sem login) porque usa só o `email` capturado no formulário — o gatilho de notificação in-app existente (`trg_notify_highlight_update`, seção 5) só alcança quem tem `user_id`, então esse cron cobre exatamente a lacuna que ele deixa. Fora do escopo desta rodada, a pedido do próprio usuário: segmentar o gatilho in-app por tipo de parceiro e disparar notificação in-app também na criação de projeto (hoje só em post publicado).

**7.3-quater Clareza no convite pra parceria fixa, telefone mascarado em `PledgeForm` e data de nascimento no cadastro (migration `080_profile_birth_date.sql`, 2026-09-02).** Três ajustes pedidos pelo usuário no funil doação avulsa → parceria contínua:
- **Copy do convite de parceria** (`PledgeForm.becomePartnerPrompt`/nova chave `becomePartnerNote`, tela de confirmação após oferta avulsa) deixa explícito que virar parceiro(a) fixo(a) significa acompanhar tudo que o missionário publicar (atualizações, pedidos de oração, novidades) — não necessariamente um compromisso mensal automático; a nota extra abaixo do botão reforça que a frequência/valor de apoio financeiro é escolhida no passo seguinte (`RecurringPledgeForm`, que continua exigindo um valor pra concluir — a tela não foi redesenhada, só a comunicação antes dela).
- **Campo de telefone de `PledgeForm`** (seção "Seus dados") trocou de `<Input type="tel">` sem máscara nenhuma pra `PhoneInput` (mesmo componente de DDI + máscara por país já usado em `PartnershipForm`/`AddPartnerButton`) — antes o placeholder sugeria o formato `(00) 00000-0000` mas nada impedia digitar letras ou mais dígitos do que cabem.
- **`profiles.birth_date DATE`** (migration 080) — coletado só no `/cadastro` manual (campo opcional, mesmo padrão do `phone` da migration 055: vai em `raw_user_meta_data` no `signUp()` e `handle_new_user()` lê de lá). Quando essa conta vira parceira fixa (`RecurringPledgeForm`, insert em `partners`), `profiles.birth_date` é copiado pra `partners.birth_date` do vínculo criado — sem isso, `BirthdayReminders` (dashboard do missionário) só enxergava aniversário de parceiros cadastrados manualmente (`AddPartnerButton`/`PartnershipForm`), nunca de quem virou parceiro pelo fluxo de doação → conta. **Auto-preenchimento via Google People API removido em 2026-09-08** (ver changelog) — chegou a existir um escopo extra `https://www.googleapis.com/auth/user.birthday.read` pedido no `signInWithOAuth` de `/cadastro`/`/login`, com `src/app/auth/callback/route.ts` trocando o `provider_token` por uma chamada a `people.googleapis.com/v1/people/me?personFields=birthdays`, mas esse escopo é classificado como "sensível" pelo Google e bloqueava o app de sair do modo "Testando" na tela de consentimento OAuth sem passar pela verificação completa do Google — como login com Google pra qualquer pessoa era mais prioritário que o auto-preenchimento, o escopo e o código que dependia dele foram removidos; quem entra via Google agora preenche `birth_date` manualmente depois, se quiser.

**7.3-quinquies Tela de "pronto" do `PledgeForm` redireciona pro perfil do missionário (2026-09-02).** A pedido do usuário: depois de concluir uma oferta avulsa, a pessoa ganha um botão "Ver perfil de {nome}" (`/${username}`) na tela "Recebemos seu registro!", e um redirecionamento automático pra lá em 8s caso não clique em nada (`redirectSeconds`, `useEffect` com `setTimeout`/cleanup — se a pessoa navegar pra outro lugar antes, ex. clicando em "quero ser parceiro fixo", o cleanup cancela o redirect pendente). `PledgeForm` ganhou a prop `username` (antes só tinha `profileId`) pra montar esse link. De quebra, corrigido um gap real: o retorno do Stripe Checkout (`success_url` em `checkout-once/route.ts`, sempre teve `?...&stripe=success`) não era tratado em lugar nenhum — a pessoa caía de volta no formulário vazio, sem nenhuma confirmação de que o pagamento tinha ido. Agora `PledgeForm` lê esse parâmetro (`useSearchParams`, computado direto no `useState` inicial — não em efeito, pra não disparar set-state síncrono num efeito) e mostra a mesma tela de "pronto", com uma mensagem própria (`doneDescriptionStripe`) já que não há como saber aqui se a doação foi anônima/com nome (esse dado ficou só na sessão do Stripe Checkout, perdida no reload completo da volta).
Tanto o botão quanto o redirect automático usam `window.location.href` (reload completo), não `router.push`/`<Link>` — bug real reportado pelo usuário: essa tela às vezes está dentro do `PartnershipModal` (7.3-bis, intercepting route `@modal/(.)parceria`), cujo `Dialog` fica com `open` sempre `true` e só desmonta quando a rota interceptada deixa de casar; navegação client-side pro perfil não disparava isso de forma confiável, então o modal continuava visível por cima da página pra onde já tinha navegado "por baixo". Um reload completo evita esse acoplamento por completo (mesmo motivo documentado em `/cadastro`/`/login`, seção 7.1, pra redirect pós-login/cadastro).

**7.3-sexies Oferta agendada — "Quero ajudar, mas não agora" (migration `086_scheduled_pledges.sql`, 2026-09-04).** A pedido do usuário: alguém que quer ajudar mas só numa data futura específica ("só posso te ajudar dia 10") hoje simplesmente esquecia, sem nenhum jeito de registrar a intenção. Novo `Choice` no wizard, `'financial_scheduled'` — botão "Quero ajudar, mas não agora" logo depois de "Ser parceiro fixo", visível só quando `hasFinancialOptions`, renderizando `ScheduledPledgeForm` (`src/components/partners/scheduled-pledge-form.tsx`) em tela cheia. Bem mais simples que `PledgeForm`/`RecurringPledgeForm`: sem coleta de pagamento nenhuma, só data futura (`min` = amanhã) e valor opcional (sem seletor de moeda, é só uma estimativa na moeda padrão do perfil). Na origem exigia conta (gate `NeedsAccountCard`, compartilhado com `RecurringPledgeForm`) — **deixou de exigir na migration 093 (2026-09-07, ver seção 3.1)**: convidado sem conta acha/cria `partners` só quando logado (mesmo bloco de find-or-create de `RecurringPledgeForm`); sem conta, pula `partners` e grava `reporter_name`/`reporter_email`/`reporter_phone` direto na linha. Insere em `scheduled_pledges` (`profile_id`, `partner_id`/`reporter_user_id` nullable desde a 093, `amount`/`currency` nulos juntos, `highlight_id`, `scheduled_date`, `status` `pending`→`sent`→`fulfilled`/`cancelled`). RLS: dono do perfil enxerga tudo (`is_profile_owner`, mesmo padrão de `recurring_pledges`, mesmo sem tela nenhuma pro missionário ainda) + self-policies pro `reporter_user_id` logado + insert público (migration 093).

Novo cron `GET /api/cron/scheduled-pledge-reminders` (`vercel.json` `"0 8 * * *"`, mesmo esqueleto `CRON_SECRET` de `recurring-reminders`): dispara 1x quando `scheduled_date <= hoje` e `status='pending'` — notificação in-app via RPC `notify()` (novo `NotificationType` `'scheduled_pledge_reminder'`, roteado em `notifications-bell.tsx` pra `/${username}/parceria?choice=...&scheduled=...`, payload carrega tudo que precisa pra montar o link sem query extra) + e-mail direto via `sendEmail()` (não passa pela fila de `notification-emails` — disparo único diário, não fila), tom leve/sem cobrança ("Hoje é o dia combinado — sem cobrança"), com link de cancelamento público (`GET /api/scheduled-pledges/[id]/cancel`, UUID como token, mesmo padrão do unsubscribe de `recurring_pledges`).

`PledgeForm` ganhou pré-preenchimento: chegando pelo link do lembrete (`?scheduled=&amount=&currency=`), os states `amount`/`currency` (já existentes) partem preenchidos em vez de vazios/moeda padrão. Ao completar manualmente (Pix/transferência — o caminho principal desta feature), o insert em `pledges` grava `scheduled_pledge_id` (nova coluna, mesma rastreabilidade que `pledges.recurring_pledge_id` já tem) e um update fecha o laço marcando `scheduled_pledges.status='fulfilled'`. **Fora do escopo desta rodada**: pagamento via Stripe a partir do link do lembrete não marca `fulfilled` (fica em `status='sent'`, inofensivo — sem tela nenhuma dependendo disso ainda); nenhuma tela pro missionário listar essas ofertas agendadas; nenhum lembrete "faltam N dias" antes da data; nenhuma opção de repetir mensalmente (isso já é o `RecurringPledgeForm`).

Novo helper `src/lib/email/email-header.ts` (`wrapEmail()`) — cabeçalho de e-mail compartilhado, mesmo shell de tabela de `send-verification-email.ts` mas com "go→guide" em texto centralizado no lugar do `<img>` de logo (pedido do usuário: sem logo por ora). Usado só no novo e-mail de lembrete por enquanto — os e-mails já existentes (`recurring-reminders`, `prayer-partner-updates`, `notification-emails`) continuam com o texto solto de sempre, candidatos naturais a adotar o helper depois.

**Entrada também dentro do projeto (2026-09-04).** O botão do wizard (acima) só aparecia no menu geral de parceria do perfil — quem clicava direto em "Apoiar este projeto" numa página de projeto (`[username]/projetos/[slug]`) nunca via essa opção, pulava direto pro `PledgeForm`. A pedido do usuário: link secundário "Não posso agora, mas quero ajudar depois" logo abaixo do botão principal "💰 Apoiar este projeto" (dentro do mesmo bloco `#financial-card`, `[username]/projetos/[slug]/page.tsx`), visível sem rolar e antes de qualquer decisão de pagamento — não relegado à lista "Outras formas de apoiar" mais abaixo. Raciocínio do próprio usuário: quem não consegue ajudar agora tende a simplesmente não clicar em nenhum CTA de pagamento, então o convite "ajudar depois" precisa competir por atenção no mesmo instante que os outros CTAs, não depois. Link já carrega `highlight_id` do projeto, então o lembrete fica vinculado a ele.

### 7.4 Visibilidade granular por parceiro (`VisibilityGrantsDialog`)
Toggle de 5 seções (`full_profile`, `financial_summary`, `prayer_requests`, `sensitive_fields`, `messages`) por parceiro:
- Toggle ON → `INSERT` em `partner_visibility_grants`; se seção = `sensitive_fields`, **também** chama `keyManager.grantAccessToExisting('profile_sensitive_fields', profileId, partnerUserId)` para compartilhar a DEK real (a concessão relacional sozinha não dá acesso ao conteúdo cifrado).
- Toggle OFF → `DELETE` da linha.
- Se o parceiro não configurou E2EE ainda (sem `public_key`), a concessão criptográfica falha e a UI avisa — a concessão relacional fica salva "pendente" até ele configurar.

### 7.5 `E2EEGate` — desbloqueio de conteúdo cifrado
Estados: `checking → needs_password | ready`. **Sem etapa manual de configuração** — o caso comum (conta com senha) já chega desbloqueado, porque login/cadastro já rodaram `setupOrUnlockWithPassword` antes da navegação (ver 6.2).
1. `checking`: `keyManager.isUnlocked()` (memória) → `tryAutoUnlock(userId)` (`localStorage` deste dispositivo) → se nenhum dos dois, `hasKeysConfigured(userId)`.
2. Se `hasKeysConfigured` for `false` (só acontece pra contas sem senha, ex. Google OAuth, que não passaram pelo hook de login): gera chaves automaticamente com `setupKeysWithRandomRecoveryCode` (sem pedir clique nem confirmação) → estado `ready` direto, com um card discreto **não bloqueante** sobre `children` mostrando o código de recuperação gerado (dispensável, com botão copiar) — só existe pra permitir acesso de outro dispositivo depois, já que não há senha pra derivar de novo.
3. Se `hasKeysConfigured` for `true` mas sem chave local (dispositivo novo, ou sessão que já estava aberta antes desta mudança): `needs_password` — pede a senha de login (não um código separado) e chama `unlockWithPassword`.
4. `ready`: libera os `children` (mensagens/orações/dados sensíveis).

**Limitação conhecida e aceita**: se o usuário trocar a senha pelo fluxo "esqueci minha senha" (sem saber a antiga), a chave privada antiga fica irrecuperável — o novo `kdf_salt`/senha nunca vai decifrar o `encrypted_private_key` gravado com a senha anterior. Equivalente à limitação do modelo antigo (perder o recovery code também travava tudo); não há reidratação automática de chave neste fluxo. **Aviso adicionado em 2026-09-01** (`recuperar-senha/nova-senha/page.tsx`): antes de confirmar a nova senha, se `keyManager.hasKeysConfigured(userId)` for `true` (a conta já tem alguma chave E2EE configurada), mostra um card de aviso (`resetE2eeWarning`, 3 línguas) explicando que os dados cifrados ficarão ilegíveis. Não bloqueia o fluxo — é só um aviso, resolver de verdade (2ª cópia da chave privada cifrada por um código de recuperação independente da senha, sobrevivendo a troca) fica como decisão em aberto, sem prioridade definida ainda.

### 7.6 Mensagens diretas
- **Acesso**: qualquer usuário autenticado pode mandar mensagem a qualquer perfil não-`stealth` — não existe (nem nunca precisou existir) exigência de já ser parceiro. A página `[username]/mensagens/page.tsx` tinha uma checagem extra de UI ("precisa ser parceiro pra mandar mensagem") removida em 2026-07-08.
- **RLS de `messages` (corrigida em 2026-09-01, migration `067`)**: SELECT/DELETE permitem `sender_id` ou `recipient_id = auth.uid()`; **INSERT exige `sender_id = auth.uid()`** (antes, uma única policy `FOR ALL` usava `sender_id OR recipient_id` pra tudo, inclusive INSERT — como o Postgres usa a expressão `USING` como `WITH CHECK` quando não há um `WITH CHECK` explícito, qualquer autenticado conseguia inserir uma mensagem com `sender_id` de OUTRA pessoa, bastando `recipient_id = si mesmo`, forjando uma "mensagem recebida" com texto arbitrário em claro sem precisar de nenhum acesso criptográfico — ver Changelog). Não há mais policy de UPDATE (nenhum fluxo do app atualiza mensagem depois de enviada).
- Envio (`MessageComposer`): deriva `conversationId`, cifra com `encryptForResource('conversation', ...)`, insere em `messages` com `is_encrypted: true`. `ensureResourceKey` concede acesso a mim e ao destinatário via `insert` simples (não `upsert` — ver 6.2/3.3).
- Leitura (`MessageThread`): busca por par de usuários + `profile_id`, decifra individualmente (falha silenciosa → `'🔒 Não foi possível decifrar esta mensagem.'` — acontece quando o grant nunca foi concedido, ex. mensagens de teste enviadas durante o bug de RLS descrito em 3.3, que ficaram irrecuperáveis). Cabeçalho da conversa aberta não repete mais "Conversa cifrada ponta-a-ponta" (removido em 2026-09-01, a pedido do usuário) — esse aviso agora só aparece na área externa (lista `/dashboard/mensagens`, subtítulo da página + cada item da `ConversationList`), não dentro de cada conversa individual.
- Realtime: canal `messages-${profileId}-${otherUserId}`, evento INSERT, recarrega tudo (sem append incremental). **Exige** que `messages` esteja na publicação `supabase_realtime` (migration 030) — sem isso o canal assina mas nunca dispara, e uma resposta só aparece recarregando a página manualmente.
- **Caixa de entrada do dashboard** (`/dashboard/mensagens`): uma "conversa" pode pertencer ao **meu** perfil (`profile_id` = meu perfil, alguém me mandando mensagem como missionário) OU ao perfil de **outro** missionário (eu mandando mensagem pra ele como parceiro — `profile_id` da linha é dele, não meu). A listagem busca por participação (`sender_id`/`recipient_id` = eu), não mais só `profile_id = meu perfil` (bug corrigido em 2026-07-08: mensagens que eu mandava como parceiro pra outro missionário nunca apareciam na minha própria caixa). A página de conversa individual (`/dashboard/mensagens/[userId]`) deriva o `profile_id` certo a partir do histórico já trocado com aquele `userId` (`anyMessage?.profile_id ?? profile.id`), em vez de assumir sempre o próprio perfil ativo.

**Nota sobre `[username]/oracao`** (2026-07-08): a página pública é conceitualmente **enviar uma oração de apoio ao missionário**, não pedir que ele ore pelo visitante — decisão de produto: toda essa área pública (parceria + oração) é um funil convidativo pra participar da missão, então "pedido de oração" (parceiro pedindo pro missionário orar por ele) destoava do tom. Mudança foi só de copy/label (`PrayerRequestForm`: "Sua oração"/"Enviar oração"), sem alterar `requester_type: 'partner'` no schema (`prayer_requests`) nem a caixa de entrada do missionário (`PrayerInbox`) — o pedido próprio do missionário (`NewPrayerButton`, `requester_type: 'missionary'`, pra pedir que a rede ore por ele) é uma feature diferente e não foi tocado. Após enviar (`done=true`), `PrayerRequestForm` mostra um segundo card convidando a virar parceiro contínuo de oração ("Quero ser parceiro de oração", link pra `/{username}/parceria`, mesmo padrão do card de `onBecomePartner` do `PledgeForm` na seção 7.3) — funil oração avulsa → parceria de oração recorrente. Componente recebe `username` como prop pra montar esse link.

**Modal (2026-08-26)** — mesmo tratamento e mesmo motivo de 7.3-bis: "Enviar Oração" no `ProfileCTA` continua um `<Link href="/${username}/oracao">` normal; navegação in-app é interceptada por `[username]/@modal/(.)oracao/page.tsx`, que busca o perfil (mesma checagem de `stealth` da página cheia, sem extrair função compartilhada — é só um `getProfile`, não vale a pena) e renderiza `PrayerModal` (`src/components/prayer/prayer-modal.tsx`), mesma caixa centralizada/cantos arredondados do `PartnershipModal`. Diferença: a página cheia de `/oracao` nunca teve cabeçalho fixo próprio (só um `<h1>` normal), então não há um back button interno pra reaproveitar — `PrayerModal` desenha seu próprio botão `X` (mesmo padrão do `PostDetailViewer`, ver 10) que chama `router.back()`. Acesso direto (link/e-mail) continua na página cheia sem interceptação.

### 7.7 Projetos (`HighlightForm`) — o formulário mais complexo do sistema
- Capa com **crop completo estilo composer de post desde 2026-08-30** (ver Changelog) — zoom + arrastar + pinça via `ImageCropEditor` (mesmo componente do post e do wizard "novo projeto"/`step-cover-title.tsx`), recorte "assado" num canvas (`bakeImage()`) na proporção fixa `1.91:1` (mesma da capa em destaque na página pública) antes do upload. Substituiu o `CoverEditor` antigo (drag-only, sem zoom, `objectPosition` calculado em runtime) **só neste form** — a capa continua reposicionável por arraste simples (sem zoom) na ilha de edição inline da página pública (`cover-title-edit-section.tsx`, ver 7.12), que ainda usa `CoverEditor`; ficou como dívida conhecida, não trocada nesta rodada.
- Multi-seleção de `goalTypes`.
- Se `financial`: alterna entre modo "meta única" (`goalAmount`/`currentAmount` diretos) e modo "orçamento por categoria" (soma automática das `project_budget_categories` vira a meta total).
- Marcos (`milestones`) editáveis inline.
- Conteúdo: versículo (`scripture`), carta (`letter`).
- Status (active/completed/hidden) só em modo edição.
- Salva via `POST /api/highlights` (não Supabase client direto — precisa de service role para upsert atômico de milestones/budget categories).
- **2026-07-20**: a lógica antes só existente aqui foi extraída pra módulos reutilizáveis, pra a edição inline na página pública do projeto (ver 7.12) reaproveitar sem duplicar: `src/lib/highlights/currency-mask.ts` (`toMasked`/`fromMasked`/`reformatMasked`/`CURRENCIES`), `src/components/highlights/cover-editor.tsx` (`CoverEditor` — upload+drag, controlado, não faz upload sozinho), `milestones-editor.tsx` (`MilestonesEditor`), `budget-categories-editor.tsx` (`BudgetCategoriesEditor` + `BUDGET_CATEGORY_LABELS`), `support-types-picker.tsx` (`SupportTypesPicker`/`GOAL_TYPE_OPTIONS`, seletor de `goalTypes` — não confundir com o `SUPPORT_TYPES` de `[username]/projetos/[slug]/page.tsx`, que é a lista de CTAs públicos, dado diferente). `HighlightForm` em si não mudou de comportamento, só passou a importar esses módulos em vez de conter a lógica inline.

### 7.7-bis Contribuição por categoria de orçamento (migration 043, 2026-08-08)
`project_budget_categories`/`project_budget_progress` (view, migration 010) existiam desde muito antes mas nunca tinham UI de contribuição de verdade: `raised_amount` só é calculado somando `transactions.budget_category_id`, e **nenhum caminho de código escrevia essa coluna** — nem o webhook do Stripe, nem a conciliação manual. Fechada a lacuna (só pro fluxo manual/Pix por enquanto — o fluxo via Stripe Checkout recorrente ficou deliberadamente de fora nesta rodada, a pedido do usuário, que quer reestruturar recebimento automático depois):
- `pledges.budget_category_id`/`recurring_pledges.budget_category_id` (nova, `ON DELETE SET NULL`) — `NULL` = projeto geral, mesma semântica de `highlight_id IS NULL`.
- `src/components/partners/budget-category-select.tsx` (`BudgetCategorySelect`) — seletor "Onde investir" (Projeto geral + cada categoria, já mostrando quanto falta) em `PledgeForm` e `RecurringPledgeForm` (só o branch manual; o branch Stripe recebe o campo no payload mas a rota ainda não persiste). Sempre formata os valores na **moeda do projeto** (`defaultCurrency`/prop `currency`), não na moeda do método de pagamento escolhido (podem divergir) — bug corrigido na mesma leva.
- `src/components/financial/pledge-review-card.tsx` (conciliação manual): categoria pré-preenchida com a que o apoiador escolheu, editável antes de confirmar — mesmo padrão já usado pra valor/conta.
- `src/lib/highlights/budget-category-labels.ts`: fonte única de `BUDGET_CATEGORY_LABELS`/`resolveBudgetCategoryLabel` — antes duplicado em `budget-categories-editor.tsx` e `budget-breakdown.tsx`.
- `src/components/highlights/budget-breakdown.tsx` (`BudgetBreakdown`) redesenhado: cada categoria mostra "faltam R$X" em destaque + botão "Contribuir" (só quando `contributeBaseHref` é passado, isto é, só na tela pública pra quem não é dono) linkando pra `/parceria?...&category=ID`; `[username]/parceria/page.tsx` lê esse `?category=` e valida que pertence mesmo ao `highlight_id` antes de pré-selecionar (nunca confia cegamente no query param).
- **Segurança conhecida, aceitável**: não há validação server-side de que `budget_category_id` enviado num pledge realmente pertence ao `highlight_id` do mesmo pledge — um cliente malicioso poderia mandar um id de categoria de outro projeto. Como pledge é só uma "reivindicação" que o missionário confirma manualmente (e pode corrigir a categoria na conciliação antes de virar `transactions`), o pior caso é uma categoria errada aparecer na fila de conciliação, não dinheiro sendo desviado — aceito por ora, mas vale endurecer (`CHECK`/trigger) se o volume crescer.

**Fundo restrito: quanto já foi gasto, não só arrecadado (migration `076_budget_category_spent.sql`, 2026-09-02).** Até aqui `raised_amount` (arrecadado) existia, mas nada mostrava **quanto desse dinheiro já foi de fato gasto** — porque `budget_category_id` só era preenchido do lado de receita (`PledgeReviewCard`, acima). Fechado:
- `TransactionForm` (`src/components/financial/transaction-form.tsx`, lançamento geral em `/dashboard/financeiro/lancamentos`) ganhou o seletor "Categoria do orçamento" — aparece pra qualquer tipo de lançamento (não só `expense`) quando um projeto com categorias está selecionado, mesmo mecanismo que `PledgeReviewCard` já usava só pro lado de receita. `lancamentos/page.tsx` monta `highlights` já com `budgetCategories` embutido por highlight (mesmo padrão de `conciliacao/page.tsx`).
- View `project_budget_progress` ganhou `spent_amount` (`COALESCE(SUM(t.amount) FILTER (WHERE t.type='expense'), 0)`, mesma técnica já usada pra `raised_amount`, uma coluna a mais na mesma view — migration `076_budget_category_spent.sql`, mesmo padrão de DROP+CREATE que a 044 já usou pra acrescentar `description`).
- `BudgetBreakdown` ganhou prop opcional `showSpent` — quando ligada, cada categoria mostra "Gasto"/"Disponível" (`raised_amount - spent_amount`, com destaque se `spent_amount > raised_amount`) além do que já mostrava. Desligada por padrão: a página pública de doação (`[username]/projetos/[slug]`) continua idêntica, só meta/arrecadado — "gasto" não ajuda quem tá decidindo contribuir.
- Novo painel "Financeiro do projeto" só do dono, em `/dashboard/projetos/[id]/page.tsx` — busca `project_budget_progress` (a view) além do `project_budget_categories` cru que já alimentava o formulário de edição, e renderiza `<BudgetBreakdown showSpent>` acima do formulário, só quando o projeto tem categoria de orçamento.

Nessa mesma leva (2026-08-08), mudanças pontuais na página pública do projeto (`[username]/projetos/[slug]/page.tsx`), todas a pedido direto do usuário:
- `FundingProjectionCard` reescrito de tom "alarme" (⚠️ vermelho "ritmo atual não deve alcançar a meta") pra tom convidativo/acionável (nunca vermelho-alerta, sempre com um CTA "Contribuir agora" quando abaixo do ritmo) — motivado por feedback direto: a versão antiga soava como cobrança/culpa pro visitante, não como convite.
- Badge de status (🟢 Ativo/✅ Concluído/🔒 Oculto) passou a aparecer sempre ao lado do título — antes só existia dentro do formulário de edição, invisível pro resto do mundo. Depois de feedback adicional ("ainda está escondendo status"), virou **clicável de verdade**: novo `src/components/highlights/status-badge.tsx` (`StatusBadge`) — pra `canEdit`, o badge é o próprio `DropdownMenuTrigger` (`render={<Badge/>}`, padrão `render` em vez de `asChild`); selecionar uma opção salva na hora via `useHighlightSectionSave` (mesmo hook dos outros `*-edit-section.tsx`), sem precisar abrir um formulário. `STATUS_OPTIONS` saiu de `dates-status-edit-section.tsx` (que é `'use client'`) pra `src/lib/highlights/project-status.ts` (módulo neutro) — **lição aprendida**: importar uma constante de um arquivo `'use client'` direto num Server Component passa no `next build` de produção mas quebra em runtime (`next dev`/Turbopack) com `TypeError: X.find is not a function`; só um teste real no navegador pegou isso, o build sozinho não. `DatesStatusEditSection` ficou só com as datas (nome do componente não mudou, pra não precisar tocar em mais lugares).
- "Data da viagem" virou "Data de início" (e o pill de exibição trocou "✈️ Viagem em" por "📅 Início em") — nem todo projeto é uma viagem.
- Bug corrigido: quando as duas datas estão vazias, a seção de edição inline (`.group.relative` + botão de lápis `position:absolute`) colapsava a zero e o lápis ficava "flutuando" solto sem card ao redor — corrigido com um placeholder "Adicionar datas..." pro dono, mesmo padrão já usado em `DescriptionEditSection`/`MilestonesEditSection`.
- Novo `src/components/shared/share-button.tsx` (`ShareButton`, Web Share API + fallback de copiar link, extraído de `profile-owner-actions.tsx` que agora reaproveita) — visível pra **qualquer visitante** (não só o dono, diferente do "Editar perfil"), ícone só, ao lado do título.
- PIX ganhou "nome que aparece na chave" (reaproveita a coluna `label` de `payment_methods`, já existente e genérica — só virou obrigatória e com rótulo específico pra `type='pix'`) — mostrado tanto em `PaymentMethodInstructions` (fluxo de contribuição) quanto no bloco de PIX solo da página do projeto, pra quem for pagar ter certeza do destinatário antes de transferir.

### 7.7-tris Galeria de fotos, posts do projeto em grade, PIX em destaque, cabeçalho recolhível (migration 045, 2026-08-08)

Leva de ajustes na página pública do projeto, todos a pedido direto do usuário:

- **Galeria de fotos** (`project_gallery_images`, migration 045 — ver 3.1): nova seção "Fotos do projeto" entre Marcos e "História por trás", editável via `GalleryEditSection`/`GalleryEditor` (mesmo padrão inline-edit dos outros `*-edit-section.tsx`). Também disponível em `HighlightForm` (dashboard, criação/edição), ao lado de Marcos. **Não** foi adicionada ao wizard `ProjectComposer` (fluxo de criação simplificado) — escopo deixado de fora deliberadamente, adiciona-se fotos depois de o projeto já existir.
- **Caminho de volta post → projeto**: `posts.project_id` já linkava o post ao projeto, mas a página do projeto só mostrava uma lista vertical simples de cards (sem link pro post real). A seção "Atualizações" agora reaproveita `ProfilePostsGrid`/`PostDetailViewer` (mesmo grid 3-colunas estilo Instagram do perfil, `src/components/shared/profile-posts-grid.tsx`) — clicar abre o post completo (curtir/comentar). Isso exigiu trocar a query de `select('id, content, media_urls, published_at, type, original_locale, translations')` pra `select('*')` + `enrichWithEngagement` (mesmo padrão de `[username]/page.tsx`), já que `PostWithProfile` precisa de `profile`/`like_count`/`comment_count`/etc.
- **Chave PIX em destaque**: tanto no bloco solo da página do projeto quanto em `PaymentMethodInstructions` (fluxo de contribuição), o PIX ficava com a mesma cor apagada (`text-muted-foreground`) de qualquer outro texto secundário, sem indicar que é o jeito mais rápido de contribuir. `CopyableValue` ganhou prop `emphasized` (borda/fundo/ícone na cor `support` — mesma usada no botão "Apoiar este projeto") usada nos dois lugares.
- **Cabeçalho recolhível** (`src/hooks/use-hide-on-scroll.ts`, usado em `ProfileTabs`): a barra de abas (`sticky top-0`) some ao rolar pra baixo e volta ao rolar pra cima, padrão Instagram/Twitter. Assimétrico de propósito — mostrar reage a qualquer rolagem pra cima de só 4px (`showThreshold`), esconder exige 12px pra baixo (`hideThreshold`) — depois de feedback de que a primeira versão (mesmo threshold simétrico) só parecia reagir perto do topo da página.

### 7.7-quater Cartões por seção, CTA flutuante, atalho pra história, remoção do card embaixador (2026-08-08)

Redesenho pontual da página pública do projeto, planejado em plan mode com pesquisa de benchmark (GoFundMe, Vakinha, Kickante) a pedido do usuário — motivação: a página é longa e as seções só eram separadas por `<h2>` em negrito, sem hierarquia visual, e quem chegava não via de cara como contribuir nem que existia uma seção de história mais embaixo.

- **Cartões por seção**: "Outras formas de apoiar"/"Como apoiar", Marcos, Fotos do projeto, "A história por trás deste projeto", Atualizações e Trajetória ganharam o mesmo tratamento visual que o bloco financeiro já usava (`rounded-2xl border bg-card`). Mudança só de wrapper — `h2`, condicionais de `canEdit`, placeholders vazios e os `*EditSection` continuam nos mesmos lugares (cada `EditPencilButton` ancora no próprio `.group.relative` interno do `*EditSection`, não no cartão novo, então não teve risco de quebrar o lápis). **Exceção**: "Atualizações" não leva `p-5` uniforme — `ProfilePostsGrid` (`src/components/shared/profile-posts-grid.tsx`) usa `-mx-4` pra sangrar a grade até a borda contra o `px-4` da página; com padding uniforme a grade vazava alguns pixels pra fora dos cantos arredondados. Resolvido com `overflow-hidden` no cartão (sem padding) + um `<div className="px-4">` só ao redor do `ProfilePostsGrid` (o mesmo `-mx-4` cancela esse `px-4` local), deixando a grade sangrar até a borda do cartão em vez da borda da página — mesmo efeito visual do grid do perfil, agora dentro de um cartão com cantos cortados.
- **`FloatingSupportCta`** (`src/components/highlights/floating-support-cta.tsx`) + **`useScrolledPastElement`** (`src/hooks/use-scrolled-past-element.ts`, `IntersectionObserver` genérico — fica `true` só quando o elemento de um `targetId` saiu por cima da viewport, reutilizável por outros CTAs flutuantes futuros): pill fixa (`fixed bottom-4`, estilo copiado de `superadmin-role-switcher.tsx`, único precedente de "pill flutuante" no app) que só aparece depois que o cartão financeiro (`id="financial-card"`) sai da tela ao rolar, e ao clicar rola de volta pra ele — **não** duplica o CTA nem a lista de categorias, só atalha de volta pro card original (pedido explícito do usuário: a área "estratificada" por categoria continua intacta e é o único lugar onde o clique leva). Só renderiza quando `hasFinancial && !canEdit`.
- ~~`ScrollToSectionLink`~~ — **substituído em 2026-08-26** por `ProjectStoryDialog` (modal), ver 7.7-quinquies.
- **Removida a entrada `ambassador`** (📣 "Divulgue e traga apoiadores") do array `SUPPORT_TYPES` local de `page.tsx` — na prática abria um formulário de captura de contato (`PartnershipForm`, tipo `ambassador`) pra follow-up manual, não um "compartilhar" de verdade, e já existe o `ShareButton` ao lado do título. Benchmark confirmou: plataformas maduras não têm um botão de embaixador separado numa página de projeto único. **Escopo deliberadamente restrito**: só a entrada nesse array local foi removida — `GOAL_TYPE_OPTIONS` (editor, `support-types-picker.tsx`), `partnership-form.tsx`/`partnership-wizard.tsx` e o tipo `ambassador` em `partners`/`PartnerType` continuam intactos (é o sistema de CRM/classificação de parceiro, ortogonal a ter ou não um card na página pública). Projetos antigos com `ambassador` no `goal_type` simplesmente deixam de mostrar aquele card, sem erro.
- Nenhum texto novo usa `next-intl` — esse arquivo inteiro já é 100% PT hardcoded (ver 5.6), então os textos novos seguem o mesmo padrão.

### 7.7-quinquies "História por trás do projeto" virou modal (2026-08-26)

A pedido do usuário, a partir de outra iteração do mockup do Stitch (a mesma leva do checkout — ver 7.11): o botão "Conheça a história por trás deste projeto" (antes um `ScrollToSectionLink` que rolava até uma seção sempre presente na página) agora abre `ProjectStoryDialog` (`src/components/highlights/project-story-dialog.tsx`, client component com `Dialog` do shadcn — tela cheia no mobile, modal centrado no desktop, mesmo padrão visual do `EditProfileDialog`). A seção "A história por trás deste projeto" (heading + `LetterEditSection`, incluindo a edição inline pro dono via lápis) foi movida pra dentro do modal como `children` — nada da lógica de edição mudou, só o container deixou de ocupar espaço permanente na página, seguindo pro leitor só quando pedido. `ScrollToSectionLink` ficou sem uso e foi deletado.

**Correção no mesmo dia**: o botão do modal ficou praticamente invisível — usei `text-secondary`/`bg-secondary` (token do design system que hoje é quase branco-sobre-branco, não um "verde" como no mockup original, ver rebrand na seção 3.1) achando que era uma cor de destaque. Trocado pra `text-primary`/`bg-primary` (a cor de marca de verdade) e virou um botão de largura cheia, com borda, em vez de uma pill pequena — usuário pediu "melhor contraste... container que ocupe toda a largura". Aproveitei pra também resolver outro ponto que o usuário achou pequeno/escondido: `StatusBadge` ("Ativo") e `ShareButton` (compartilhar), antes espremidos ao lado do `<h1>` do título, viraram um pill `bg-background/95` sobreposto no canto superior direito da capa (mesmo padrão do mockup) — maior, com fundo opaco de alto contraste contra qualquer foto, e alvo de toque maior (`h-8 w-8` no botão de compartilhar, badge com `h-6`). `StatusBadge` ganhou prop `className` pra permitir esse ajuste de tamanho sem duplicar o componente.

**Segunda correção no mesmo dia**: `ProjectStoryDialog` também copiou o `className` do `EditProfileDialog` (tela cheia no mobile, `fixed inset-0 rounded-none`) — apropriado pra editar perfil (fluxo "de propósito parece uma página nova", já documentado), mas o usuário achou que só ler uma carta dava "sensação de nova página" e queria fechar clicando fora, sem depender do botão de voltar. Primeira tentativa usou o `className` padrão de modal do `ui/dialog.tsx` (centralizado via `top-1/2 left-1/2 -translate-x/y-1/2`, `max-w-lg`) — mas ficou "razoalmente pequeno" (feedback do usuário). **Terceira tentativa (com bug)**: troquei a centralização padrão (`top-1/2`+`translate`) por insets responsivos (`fixed inset-x-3 top-10 bottom-10`, crescendo via `sm:`/`lg:`) + cancelamento manual de `left-1/2`/`-translate-x/y-1/2` (`left-auto translate-x-0 translate-y-0`). **Estourava a tela em mobile real** (reportado pelo usuário com screenshot: modal cortado à direita) — o Tailwind não resolve de forma confiável, dentro de uma única string de `className`, o conflito entre a técnica de `inset-x`/edges e a técnica de `top-1/2`+`translate` que o componente base já usa; cancelar uma na mão é frágil (a ordem das classes no arquivo gerado do Tailwind, não a ordem que você escreve na string, decide qual "ganha"). **Quarta correção (definitiva)**: voltou a usar a centralização padrão do `DialogContent` (não mexe em `top`/`left`/`translate` de jeito nenhum) e só sobrescreve `max-width`/`max-height` responsivos (`max-w-[calc(100%-1.5rem)] max-h-[75vh]` no mobile, crescendo até `lg:max-w-2xl lg:max-h-[85vh]`) — o padrão normal do Tailwind (`w-full` do componente base + `max-w-*`) pra "cresce até um teto", sem nenhum conflito de posicionamento. Validado programaticamente (não só visualmente) em 6 larguras de viewport (360–1280px): `document.documentElement.scrollWidth` e o `getBoundingClientRect()` do modal confirmados dentro da viewport em todas.

**Quinta correção (mesmo dia)**: `StatusBadge`/`ShareButton` sobrepostos na capa (item logo acima) voltaram pra dentro do cabeçalho, ao lado do `<h1>` — usuário reparou que em fotos de pessoas (retrato), o pill opaco no canto superior direito acaba cobrindo rosto/cabeça de quem está na foto, já que a capa é enviada livremente por cada missionário sem controle de enquadramento nessa área. Benchmark rápido (GoFundMe/Kickstarter): esses controles de ação ficam sempre na área de conteúdo, nunca sobrepostos à foto — só elementos de identidade/marca (o pill do avatar+nome, mantido sobreposto) costumam ficar sobre a imagem. `ShareButton` também trocou de `variant="ghost"` pra `"outline"` (sem fundo opaco por trás pra dar contraste, ghost ficaria de novo pouco visível fora da capa).

### 7.7-sexies Pontos de oração por projeto (migration `090_project_prayer_points.sql`, 2026-09-04)

A pedido do usuário: até aqui, oração só existia no nível do missionário (`prayer_requests`, só `profile_id`) — nenhuma estrutura de conteúdo de oração específica de um projeto, ao contrário do lado financeiro (`project_budget_categories`). Nova tabela `project_prayer_points` (`highlight_id` NOT NULL, `budget_category_id` opcional — vincula um ponto a uma necessidade financeira específica, `title`, `description`, `prayer_count` denormalizado, `is_completed`/`completed_at` espelhando `milestones`), RLS idêntica a `project_budget_categories` (dono edita tudo, leitura pública só com projeto `active`).

`prayer_requests` ganhou `highlight_id`/`prayer_point_id` (ambos opcionais) — uma oração continua podendo ser geral (os dois nulos, fluxo antigo intacto), sobre um projeto inteiro (`highlight_id` preenchido, "orar por tudo"), ou sobre um ponto específico (os dois preenchidos). `prayer_count` é mantido por trigger (`increment_prayer_point_count()`, `AFTER INSERT ON prayer_requests`) em vez de contado ao vivo — necessário porque `prayer_requests` tem RLS restrita (só dono/parceiros autorizados leem o conteúdo, potencialmente E2EE) e um visitante público não pode contar linhas ali; o contador público vive só na coluna denormalizada, mesmo padrão de `highlights.current_amount`.

**Vínculo com necessidade financeira, na criação**: `BudgetCategoriesEditor` ganhou um campo opcional "Adicionar um ponto de oração relacionado" por linha — ao criar uma necessidade financeira, o missionário já pode escrever o que orar sobre ela. Como `/api/highlights` salva `project_budget_categories` via delete-tudo-e-reinsere a cada save (IDs novos a cada edição), o vínculo só pode ser resolvido depois do insert de verdade: a rota captura os IDs recém-criados (`Prefer: return=representation` já devolvia isso, só não era usado) e monta os `project_prayer_points` vinculados a partir deles. **Limite aceito**: reeditar a lista de necessidades financeiras depois desreveste o vínculo antigo (`ON DELETE SET NULL`) — o ponto de oração em si não é perdido, só solta do `budget_category_id`.

**Correção de um bug real, na mesma leva**: `hasFinancial` na página pública (`[username]/projetos/[slug]/page.tsx`) dependia só de `goal_type.includes('financial')` — um toggle completamente independente de ter meta/categoria/Pix configurado de verdade. Um projeto podia marcar "Apoio financeiro" e nunca preencher nada, e a seção aparecia mesmo assim publicamente com um botão "Apoiar este projeto" sem nada por trás. Agora `hasFinancial` exige `canEdit || (goal_amount != null || budgetCategories.length > 0 || pixMethods.length > 0 || donationLink)` — o dono sempre vê a seção (pra poder preencher), o público só quando há dado real. Oração não tem esse mesmo problema: o botão geral "🙏 Orar por este projeto" não depende de nenhuma configuração prévia pra funcionar, então `hasPrayerContent` é só o toggle mesmo.

**Abas quando os dois existem**: `SupportSectionTabs` (novo, mesmo padrão de pill-button já usado em `PrayerInbox` — sem componente `Tabs` novo no design system) alterna entre o card financeiro e um novo card de oração só quando `hasFinancial && hasPrayerContent` são ambos verdadeiros; um projeto com só um dos dois mostra só esse, sem aba. O card "Comprometer-me em oração" genérico de "Outras formas de apoiar" (`SUPPORT_TYPES`) é escondido quando `hasPrayerContent` é verdadeiro, pra não duplicar o convite.

**Orar por um ponto ou pelo projeto todo**: `PrayForPointModal` (novo, `src/components/prayer/`) reaproveita a lógica de submit de `PrayerRequestForm` (exige login, insere em `prayer_requests` com `requester_type='partner'`) parametrizada por `highlightId`/`prayerPointId` opcional — sem a opção de oração privada/cifrada do formulário genérico, pra manter o modal leve. Botão geral "Orar por este projeto" tem o mesmo peso visual do CTA financeiro principal (pedido explícito do usuário: "já tem que ser um CTA na cara"); cada ponto específico tem seu próprio botão pequeno, e `BudgetBreakdown` ganhou um botãozinho 🙏 ao lado de "Contribuir" quando a categoria tem um ponto de oração vinculado.

**Contador com tempo verbal**: "N pessoas estão orando por isso" enquanto o ponto está ativo, "N pessoas oraram por esse desafio" quando o missionário marca `is_completed` — pedido explícito do usuário, calibrado junto com a virada de sentido.

`PrayerInbox` (painel do missionário) ganhou um badge de contexto (projeto/ponto) em cada card, via `select('*, highlights(title, slug), project_prayer_points(title)')` — sem aba nova nem filtro novo, só o contexto visível.

**Fora do escopo desta rodada**: pontos de oração são editados só em `HighlightForm` (painel), sem edição inline na página pública (diferente de `FinancialEditSection`/`SupportTypesEditSection`, que têm esse padrão); nenhuma notificação in-app nova por "alguém orou por X" (o missionário já vê no inbox); nenhum limite/meta de "quantas pessoas orando" (só contador crescente).

**Textos ainda hardcoded em PT** — diferente do resto do padrão recente do app (next-intl obrigatório pra toda mudança de texto/UI), os componentes novos desta rodada (`PrayerPointsEditor`, `PrayerPointsBreakdown`, `SupportSectionTabs`, `PrayForPointModal`, os textos novos em `[slug]/page.tsx`) ficaram com string fixa em português, sem passar por `messages/{pt,en,es}.json` — pendência conhecida, não resolvida ainda.

### 7.8 Equipe do projeto (`ProjectTeamPanel` + `/api/projects/members`)
Entrar na equipe (`project_members`) concede automaticamente acesso à conta financeira compartilhada do projeto (`account_members`), com nível de acesso derivado do papel: `lead → owner`, demais → `viewer`.

### 7.9 Publicações — wizard de composição (`src/components/dashboard/post-composer/`, redesenho 2026-07-25)
Substituiu o antigo `PostEditor` (formulário único num dialog) por um wizard em tela cheia inspirado no fluxo do Instagram, mas não-cópia (benchmark, não clone). `PostComposerModal` orquestra 4 passos via `usePostComposer` (`use-post-composer.ts`), atrás da mesma API pública `useComposer().openComposer/closeComposer` (`new-post-triggers.tsx`, `sidebar.tsx`) — e, desde 2026-08-25, também de `shared/post-card.tsx` (menu "Editar", via `useOptionalComposer()`, a variante que não quebra fora do provider), já que a antiga tela `/dashboard/publicacoes` foi removida e editar um post passou a acontecer direto no perfil (ver changelog e 7.1-bis).

- **`type`** (só na criação): `StepTypePicker` — "Post" segue o wizard; "Projeto" fecha o modal e navega para `/dashboard/projetos/novo` (formulário de `highlights` já existente, não duplicado).
- **`media`**: `StepMediaSelect` — seleção de arquivos (até 10, imagem+vídeo, mesmo limite de antes), tirinha de miniaturas com adicionar/remover (carrossel). Cada imagem passa por `ImageCropEditor` (`image-crop-editor.tsx`), que estende o mesmo padrão de drag mouse/touch de `CoverEditor` (§7.7) com zoom (slider), seletor de proporção (1:1/4:5/1.91:1 — sem "Original", igual ao Instagram: todo envio passa por recorte padronizado, pedido explícito do usuário; vira `posts.media_aspect_ratio`) e grade dos terços (visível só durante o arraste).
- **`adjust`** (pulado se só houver vídeo): `StepAdjust` — abas Filtros (6 presets prontos, strings de `filter` CSS) e Ajustes (sliders brilho/contraste/saturação/temperatura), estado por imagem (`MediaDraft.filterPreset`/`adjustments`, ver `types.ts`). Layout lado a lado em telas `md+`, imagem sempre visível (não cobre a imagem como o Instagram mobile faz) — abaixo de `md`, imagem ocupa o topo e o painel de abas fica embaixo sem sobrepor.
- **`details`**: `StepDetails` — `LocaleContentTabs` (legenda PT/EN/ES, igual antes), `TagPeoplePicker` (clique na imagem marca um perfil real via `post_tags`, busca por `searchTaggableProfiles`), campo de localização com autocomplete mundo afora estilo Instagram (`searchLocations`, ver 5.3 e 7.9-bis — cascata Mapbox → Photon) e, quando o campo está vazio, chips de sugestão combinando (nessa ordem) lugares perto de onde o usuário está agora — `navigator.geolocation` no browser + reverse geocoding via `getNearbyLocations` (cascata Google Places → Mapbox → Photon, ver 7.9-bis) — e locais já usados antes (`getLocationSuggestions`); se a geolocalização for negada/indisponível, cai só no histórico, sem erro visível. `PostEditorProjectPicker` (reuso), toggle "Agendar" (escreve `scheduled_at`), botões Rascunho/Publicar/Agendar.

Ao avançar do passo `media`, cada imagem é "assada" (`src/lib/media/bake-image.ts` → `bakeImage()`): reproduz em `<canvas>` o recorte (pan+zoom) e o filtro CSS escolhidos, gerando o arquivo final já cortado/filtrado — não existe crop/filtro guardado separado do arquivo. Só depois disso o resultado passa por `compressImage()` (mesma função de sempre, `src/lib/media/compress.ts`) e é enviado ao bucket `media`. Cabeçalho do modal segue o padrão fullscreen de `EditProfileDialog` (`DialogContent` com className customizada + `BackButton`), com `X`/seta-voltar conforme o passo e confirmação (`DiscardConfirmDialog`) se houver conteúdo não salvo — "Descartar publicação?" ao criar, "Descartar edição?" (`discardTitleEdit`) ao editar um post existente.

`postType` continua derivado automaticamente da mídia final (nenhuma→text, 1 vídeo→video, >1 arquivo→carousel, 1 imagem→image). `savePost` (`src/app/dashboard/publicacoes/actions.ts`) ganhou `mediaAspectRatio`, `location`, `scheduledAt` e `tags[]` (grava em `post_tags` após o upsert do post). Editar um post existente pula direto para `details` (re-recortar mídia já publicada é caso raro fora de escopo do v1; marcação de pessoas em mídia antiga também fica limitada a remover/ver, não reposicionar).

Curtir/comentar/compartilhar (`src/app/dashboard/publicacoes/social-actions.ts` — `toggleLike`/`addComment`/`deleteComment`/`getComments`) e o card de exibição estão descritos na seção 7.1-bis / inventário de componentes (`shared/post-card.tsx`).

### 7.9-bis Geocoding — cascata de sugestão de localização (`src/lib/geocoding/`, 2026-08-27)

Os dois pontos de sugestão de localização do composer (`searchLocations`/`getNearbyLocations`, chamados por `StepDetails`) tentam providers pagos antes de cair no Photon (OpenStreetMap, grátis/sem chave, usado sozinho até então) — motivado pelo usuário notando que um estabelecimento real perto dele não aparecia (não estava cadastrado no OSM; cobertura de comércio local no OpenStreetMap é inconsistente por região).

```
src/lib/geocoding/
  config.ts         — googlePlacesApiKey()/mapboxAccessToken(): string|null, nunca lançam
  usage.ts          — GEOCODING_FREE_CAPS, GEOCODING_USAGE_THRESHOLD (0.9), isOverGeocodingQuota(), incrementGeocodingUsage()
  photon.ts         — searchPhoton()/reverseGeocodePhoton() (lógica antiga, movida de actions.ts, com NEARBY_POI_KEYS)
  google-places.ts  — nearbySearchGoogle() — Places API (New) searchNearby
  mapbox.ts         — reverseGeocodeMapbox()/forwardSearchMapbox() — Search Box API
  cascade.ts        — runCascade() genérico + getNearbyLocationsCascade()/searchLocationsCascade()
```

**Ordem da cascata** (decisão do usuário, com um tier a menos na busca por texto):
- `getNearbyLocationsCascade` (GPS, "perto de você"): Google Places (Nearby Search) → Mapbox (Search Box `/reverse`) → Photon.
- `searchLocationsCascade` (busca digitada): Mapbox (Search Box `/forward`) → Photon — **sem Google**: o tier grátis-ilimitado do Google Autocomplete só vale usando "session tokens" (token de sessão reaproveitado a cada tecla + finalizado com uma chamada de Place Details), e o composer chama a Server Action de forma independente a cada tecla, sem sessão nem Place Details — usar Nearby/Text Search do Google aqui geraria cobrança sem necessidade. Mapbox `/forward` é stateless e já resolve.

**`runCascade`** (`cascade.ts`): pula um tier se a chave não estiver configurada (`isConfigured()`) ou se `isOverGeocodingQuota()` disser que já está em ≥90% da cota grátis do mês; cai pro próximo se a chamada lançar erro; **resultado vazio (sem erro) também cai pro próximo tier**, não só cota/erro — decisão deliberada: o objetivo é achar o lugar, não só respeitar limite (o vazio já conta contra a cota do provider tentado). Sempre termina no Photon, que nunca precisa de chave/cota.

**Cota grátis mensal** (checada em páginas oficiais, ago/2026 — pode mudar, reconferir periodicamente): Google Places Nearby Search, SKU "Pro", 5.000/mês grátis depois US$32/1.000 (fieldMask mantido mínimo — `displayName`/`formattedAddress`/`types`/`primaryType` — de propósito: pedir `rating`/`openingHours`/`photos` reclassifica pro SKU "Enterprise" e derruba a cota grátis pra 1.000/mês). Mapbox Search Box, bucket "Requests", 50.000/mês grátis depois US$1,00/1.000 — **não confundir com a API de Geocoding clássica do Mapbox** (100k grátis/US$0,75/1.000), que só devolve endereço/área administrativa, sem POI — não teria resolvido o problema que motivou a feature.

**Banco**: migration `054_geocoding_usage.sql` — tabela `geocoding_usage` (`provider`, `period` 'YYYY-MM' UTC, `count`), chave primária `(provider, period)`, RLS habilitada sem nenhuma policy (só o client service-role, que bypassa RLS, toca essa tabela — não é dado de usuário). Reset mensal não precisa de cron: mês novo é só uma linha nova (upsert por chave primária). Função `increment_geocoding_usage(p_provider, p_period)` faz `INSERT ... ON CONFLICT DO UPDATE SET count = count + 1` — atômico como statement único, sem precisar de `FOR UPDATE`/`SECURITY DEFINER` (diferente de `consume_ai_credits`, que debita saldo de usuário via client anônimo).

**Env vars sem chave ainda**: `GOOGLE_PLACES_API_KEY`/`MAPBOX_ACCESS_TOKEN` (ver seção 2) — `config.ts` retorna `null` em vez de lançar (padrão `stripe/client.ts`, não `bunny.ts`), então a cascata pula os dois tiers pagos e cai direto no Photon, comportamento idêntico a antes da cascata existir. Testado nesta sessão com chaves falsas via script `tsx` (não commitado): confirmado que uma resposta 401/403 real de cada API cai pro próximo tier sem erro visível, terminando no Photon com resultado — inclusive já rodando contra o Supabase real, antes mesmo da migration `054` ter sido aplicada (a tabela ainda não existe em produção: `isOverGeocodingQuota` degrada pra "não está sobre cota" quando a query falha, sem lançar).

**Ativar de verdade**: aplicar a migration `054` manualmente (SQL Editor do Supabase, mesma limitação de sempre — sem CLI/connection string configurada neste ambiente, ver nota de risco operacional na seção 2), criar conta Google Cloud + billing + Places API habilitada, criar conta Mapbox + token da Search Box API (**não confirmado** se precisa ser token "secret" `sk.*` em vez de "public" `pk.*` — checar no dashboard ao criar), preencher as 2 env vars.

### 7.10 Notificações (`useNotifications` hook)
Carga inicial: não lidas, `LIMIT 20`. Realtime: canal `'notifications'`, filtro `recipient_user_id=eq.${userId}` no Postgres changes, prepend local a cada INSERT. `markAllRead()` marca `read_at=now()` no banco **e limpa a lista local** — não há histórico de lidas visível no dropdown.

**Correção (2026-08-30): a nota abaixo estava desatualizada.** `notifications` **é** produzida por triggers reais em `supabase/migrations/014_notification_triggers.sql`/`040_...`/`085_...` (helper `notify()`, `SECURITY DEFINER`): `trg_notify_new_partner`, `trg_notify_new_pledge`, `trg_notify_pledge_confirmed`, `trg_notify_pledge_rejected` (migration 085, 2026-09-03 — ver 7.2), `trg_notify_new_message`, `trg_notify_new_comment`, `trg_notify_prayer_reply` e `trg_notify_highlight_update` (dispara em `posts` INSERT/UPDATE quando `project_id IS NOT NULL` e o post passa a publicado — notifica todo `partners.user_id` não-nulo daquele `profile_id`, sem distinguir tipo de parceiro). Limitações reais que seguem valendo: só alcança parceiro **com conta** (`user_id IS NOT NULL` — a maioria dos cadastros de oração via `PartnershipForm` é anônima, ver 7.3-tris), não dispara em `highlights` INSERT (projeto novo, só posts de atualização), e o tipo `new_post` existe no union `NotificationType` mas nenhum trigger o produz (reservado/morto).

**E-mail para 5 dos 8 tipos (`GET /api/cron/notification-emails`, migration `068_notification_emails.sql`, 2026-09-01; `pledge_rejected` somado em 2026-09-03, ver 7.2)** — a pedido do usuário, dando continuidade ao plano de colocar e-mail em produção (ver Changelog). Roda a cada 5min (`vercel.json`), varre `notifications` com `email_sent_at IS NULL` (coluna nova, índice parcial) restrito a `new_message`/`pledge_confirmed`/`pledge_rejected`/`new_pledge`/`new_partner` — os outros 3 tipos (`new_comment`, `highlight_update`, `prayer_reply`) ficaram de fora por decisão do usuário (mais frequentes, risco de spam de e-mail), podem ser adicionados depois só estendendo `EMAIL_TYPES` + `buildEmailContent`. Pra cada notificação pendente: resolve o e-mail do destinatário via `supabase.auth.admin.getUserById` (a tabela `notifications` só guarda `recipient_user_id`, não e-mail), monta o conteúdo específico do tipo (`buildEmailContent`) e manda via `sendEmail()` (Brevo). Marca `email_sent_at` **mesmo se o envio falhar** — decisão deliberada pra não ficar retentando pra sempre um endereço permanentemente inválido a cada 5min; falhas reais ficam nos logs do `sendEmail()` (ver seção sobre `brevo.ts` mais abaixo). `new_message` só gera e-mail se `read_at IS NULL` (a pessoa ainda não abriu a conversa no app) — os outros 3 tipos não têm essa checagem, porque são itens acionáveis (revisar oferta, ver confirmação) que valem a pena por e-mail independente de já ter visto o sininho. **Mensagens continuam E2EE** (seção 6) — o e-mail de `new_message` nunca inclui o conteúdo da mensagem, só avisa quem mandou e linka pra conversa, mesmo padrão de app de mensagens cifradas (Signal/WhatsApp também não colocam o texto no push/e-mail).

**Correção (2026-09-03): `new_pledge`/`pledge_confirmed`/`partner_lapsed` levavam pra Lançamentos, que aparecia vazia** — usuário clicou numa notificação de "nova oferta" e caiu em `/dashboard/financeiro/lancamentos` sem ver nada. Causa: `NotificationsBell` agrupava os três tipos numa única área "financial" com um único destino fixo (Lançamentos), mas cada um precisa de rota diferente: `new_pledge` notifica o **missionário** sobre uma oferta **pendente** (`trg_fn_notify_new_pledge`, migration 014) — ela só existe em `pledges` com `status='pending'`, ainda não é `transactions`, então mora em **Conciliação**, não em Lançamentos; `pledge_confirmed` notifica **quem reportou a oferta** (`NEW.reporter_user_id` — o parceiro/doador, não o missionário), que não tem acesso à agenda financeira do missionário e vê o próprio histórico em `/dashboard/financeiro-parceiro`; `partner_lapsed` notifica o missionário mas sobre o parceiro que parou de doar, então pertence a `/dashboard/parceiros` (onde já mostra o badge "Parece ter parado", ver 7.16-bis/lapsed-donor-check) — inclusive `parceiros/page.tsx` já marcava esse tipo como lido junto com `new_partner`, um sinal de que a intenção sempre foi essa. `hrefForGroup()` ganhou casos especiais por `latestType` (mesmo padrão já usado pra `new_comment` abrir o post direto) e `partner_lapsed` mudou de área (`financial`→`partners`) no `AREA_BY_TYPE`. Verificado num browser real lendo o `href` renderizado pra cada tipo (sem depender do fix de realtime acima, que é ortogonal a isso).

**Correção (2026-09-03): sininho só atualizava com F5, nunca em tempo real** — usuário reportou que uma notificação nova só aparecia recarregando a página, mesmo o `useNotifications` já assinando `postgres_changes` (INSERT/UPDATE) corretamente. Causa: a tabela `notifications` nunca tinha sido adicionada à publicação `supabase_realtime` do Postgres — sem isso, o Supabase Realtime nunca recebe o evento de mudança pra retransmitir, então a assinatura do client fica "escutando" um canal que nunca dispara, silenciosamente (sem erro nenhum). **Mesmo bug, já resolvido antes pra `messages`** (migration `030_fix_dek_grants_rls_and_realtime.sql`, mesma causa raiz e mesmo sintoma: "resposta só aparecia recarregando"). Corrigido com a mesma receita: migration `084_notifications_realtime.sql` (idempotente, `IF NOT EXISTS` antes do `ALTER PUBLICATION supabase_realtime ADD TABLE`). **Migration criada mas não aplicada por mim** na hora — não há string de conexão direta do Postgres nem token de management do Supabase disponível neste ambiente (só as chaves REST/service-role, que não executam DDL arbitrário); dependia do usuário rodar esse SQL no SQL Editor do painel do Supabase (mesmo jeito que a 030 deve ter sido aplicada). RLS de `notifications` (`auth.uid() = recipient_user_id`, ver migration 002) já estava correta — não era a causa.

**Confirmado ao vivo (2026-09-03): o `ALTER PUBLICATION` já tinha sido aplicado pelo usuário.** Verificado sem depender de browser: script Node avulso (`@supabase/supabase-js`, não commitado) autenticou como `ana.ferreira@seed.goguide.test`, assinou `postgres_changes` em `notifications` filtrado por `recipient_user_id`, e um `insert` feito via service role chegou no client em ~800ms (evento com o payload completo da linha nova). Notificação de teste apagada ao final. Realtime de notificações está funcionando de ponta a ponta em produção.

**Descoberta importante ao configurar isso**: `CRON_SECRET` (usado por **todas** as 4 rotas de cron, incluindo as 3 pré-existentes — `recurring-reminders`, `prayer-partner-updates`, `publish-scheduled-posts`) nunca esteve configurado em lugar nenhum — nem `.env.local`, nem Vercel Production (confirmado via `vercel env ls production`, antes desta correção). Como cada rota faz `if (!secret || header !== 'Bearer '+secret) return 401`, e `process.env.CRON_SECRET` sempre foi `undefined`, **todo cron desse projeto vinha devolvendo 401 e nunca executando de verdade**, silenciosamente, desde que cada um foi criado — inclusive o agendamento de posts (`publish-scheduled-posts`, feature visível "Agendar" no composer) e os lembretes de recorrência/oração por e-mail. Gerado um valor novo (`openssl rand -hex 32`) e configurado em `.env.local` + Vercel Production. O lembrete de recorrência por e-mail (seção 7.11) e o e-mail de acompanhamento de oração (7.3-tris) eram os únicos produtores de aviso **por e-mail** antes desta rodada — agora somam-se o cron de `notification-emails` acima.

### 7.10-bis Campanhas pra parceiros — Fase 1: envio avulso (migration `069_partner_broadcasts.sql`, 2026-09-01)

A pedido do usuário, primeira metade de uma feature de duas partes: (A) **envio avulso** (esta seção — pronta) e (B) **campanha automática com meta/prazo** (planejada, ainda não implementada — ver nota no fim). Decisão de design deliberada: **não criou uma entidade "campanha" nova** — `highlights` já tem `goal_amount`/`current_amount` (meta) e `funding_deadline` (prazo, migration 016, já editável no `HighlightForm` e exibido na página pública) prontos; a Fase 2 vai só automatizar comunicação em cima do que já existe, em vez de duplicar todo o modelo de arrecadação.

- **`partner_broadcasts`** (o envio em si: `profile_id`, `sender_user_id`, `subject`, `body`, `recipient_filter` — `'all'|'financial'|'prayer'|'both'|'ambassador'`, mesmos valores de `partners.type` + `'all'` —, `recipient_count`) + **`partner_broadcast_recipients`** (fila, 1 linha por parceiro: `broadcast_id`, `partner_id`, `email`, `sent_at`, `error`). RLS: dono do perfil (`is_profile_owner`) gerencia os dois via `FOR ALL`; o cron usa `createServiceClient()` (bypassa RLS) pra marcar `sent_at`/`error`.
- **Criação** (`createBroadcast`, Server Action em `src/app/dashboard/parceiros/actions.ts`): resolve a lista de destinatários **na hora do envio** (não guarda uma lista congelada) — `partners` do perfil ativo com `email` preenchido e `update_emails_opt_in=true` (mesma coluna/flag já usada pelo `prayer-partner-updates`, reaproveitada de propósito: quem já cancelou "e-mails de atualização" daquele fluxo não deve receber estes também, é a mesma categoria do ponto de vista de quem recebe), filtrado por `recipient_filter` se não for `'all'`. Insere o `partner_broadcasts` + um `partner_broadcast_recipients` por destinatário, tudo síncrono no Server Action (sem limite prático de request nesse passo — só enfileira, não manda nada ainda).
- **Envio** (`GET /api/cron/broadcast-sender`, a cada 5min, `vercel.json`, protegida por `CRON_SECRET`): drena até 100 pendentes por execução (`sent_at IS NULL`), busca o nome do parceiro pra personalizar, manda via `sendEmail()` (Brevo) com o mesmo rodapé de descadastro do `prayer-partner-updates` (`GET /api/partners/[id]/unsubscribe-updates`), marca `sent_at` **mesmo em falha** (mesma decisão do `notification-emails`: sem retry infinito, erro real fica salvo em `error` pra debug). Fila em vez de envio síncrono na hora do clique é deliberado — evita qualquer risco de timeout de request numa lista grande, sem precisar reavaliar isso conforme a base de parceiros cresce.
- **UI**: `SendBroadcastButton` (`src/components/partners/send-broadcast-button.tsx`), botão "Enviar atualização" ao lado de "Adicionar parceiro" em `/dashboard/parceiros` — modal com seletor de destinatário (`<select>` nativo, convenção do projeto), assunto, mensagem (texto simples, sem HTML). Sem pré-visualização de contagem de destinatários no v1 (decisão de manter simples); a contagem real aparece no toast de sucesso depois de enviar.
- **Fase 2 (planejada, não implementada)**: cron diário varrendo `highlights` ativos com `funding_deadline`, detectando 3 gatilhos (lançamento, lembrete N dias antes do prazo, resultado ao bater a meta ou vencer o prazo) — cada gatilho vira uma notificação in-app pro missionário com um rascunho pronto, que abre nesta mesma tela de compor já preenchida (decisão do usuário: nada sai sozinho, sempre com revisão antes de confirmar). Reaproveita o motor desta seção como mecanismo de envio; só a parte de "quando e o que sugerir" é nova.

**Blocos inteligentes com IA (2026-09-01)** — a pedido do usuário, pra a atualização não virar um relatório seco de números: dois blocos opcionais no compositor, ambos reaproveitando dados que já existem (nenhuma tabela nova):
- **Prestação de contas** — período fixo (últimos 30 ou 90 dias, `periodRange()` client-side); soma `transactions` (`type IN ('income','expense')`) do perfil no período, agrupado por **moeda** (nunca soma moedas diferentes juntas — evitaria um total sem sentido) via `POST /api/ai/generate-partner-update`, mais as 3 maiores categorias de gasto (join `transaction_categories`).
- **Projetos em destaque** — multi-select dos `highlights` ativos e não-arquivados do perfil (buscados no Server Component `parceiros/page.tsx`, todos pré-marcados por padrão), cada um carregando meta/valor atual/moeda/prazo.
- **Geração** (`src/lib/ai/generate-partner-update.ts`, `output_config.format: json_schema` com `{ body: string }`): o rascunho do missionário (se houver) + os dados dos blocos marcados viram contexto de um único prompt que pede texto corrido, caloroso, pessoal — nunca lista/tabela/markdown. `POST /api/ai/generate-partner-update` (`src/app/api/ai/generate-partner-update/route.ts`) segue exatamente o padrão de `/api/ai/translate` (seção 5.6.1): débito de 1 crédito de IA via `consume_ai_credits` **antes** de chamar Claude (sem reembolso automático em erro do provider), 402 se insuficiente, 403 se não for dono. Resultado preenche a `Textarea` da mensagem, **editável antes de enviar** — nunca envia direto. **Modelo `claude-sonnet-5`, não Haiku** — única chamada de IA do app que foge do Haiku padrão (tradução/moderação): decisão deliberada, aqui o produto é a qualidade da escrita em si (retenção de atenção do parceiro), não velocidade/custo.
- **Cartões de projeto com imagem e link real (2026-09-01, migration `071_broadcast_highlight_cards.sql`)** — a IA só escreve a narrativa em **texto puro**, nunca HTML/link/imagem (evita URL inventada ou markup quebrado). `partner_broadcasts.highlight_ids UUID[]` guarda os projetos escolhidos na composição; o **código**, não a IA, monta um cartão por projeto na hora do envio de verdade (`src/lib/email/partner-update-template.ts`, `buildBroadcastHtml()`, chamado pelo cron `broadcast-sender`) — busca capa/progresso/slug atualizados de `highlights` nesse momento (não os do instante da composição, então reflete progresso real mesmo com o atraso do cron) e monta `<img>` da capa + barra de progresso + botão linkando pra `/{username}/projetos/{slug}` de verdade. O prompt da IA sabe que os cartões vêm automaticamente depois do texto, então é instruído a fechar convidando emocionalmente, sem tentar redizer números ou inventar um link. Cache em memória por `broadcast_id` dentro do próprio cron (`Map`) — evita refazer a mesma busca de projetos/username pra cada destinatário do mesmo broadcast.
- **Bug de escopo corrigido (2026-09-02)**: até aqui os blocos (Prestação de contas/Projetos) ficavam dentro de um `<fieldset disabled={!aiEnabled}>` — como `ANTHROPIC_API_KEY` nunca teve valor real em nenhum ambiente (ver "Descoberto ao configurar" abaixo), isso significava que **nenhum missionário conseguia usar os blocos em produção**, mesmo sem querer texto de IA nenhum. Corrigido: os blocos (e os controles de período/privacidade financeira, seleção de projeto) ficam **sempre habilitados**, em qualquer plano, com ou sem IA configurada — só o botão "Gerar com IA" em si continua condicional.
- **Geração sem IA (`generateTemplateUpdate`, `src/lib/partners/generate-template-update.ts`, 2026-09-02)** — contraparte 100% síncrona/determinística de `generatePartnerUpdate()` (mesma assinatura/tipos), monta o texto por template string em vez de chamar Claude: zero custo, zero crédito de IA, disponível pra qualquer missionário desde já. `POST /api/ai/generate-partner-update` ganhou parâmetro `mode: 'ai' | 'template'` (default `'ai'`) — os dois modos reaproveitam a mesma busca de `transactions`/`highlights` já existente na rota; só em `mode='ai'` a rota chama `consume_ai_credits`. Modo `percent_only` de `financialVisibility` mantém a mesma minimização de dado da Fase 1 (a função nunca recebe/usa valor exato nesse modo, só percentuais). Botão "Montar informativo" (`LayoutTemplate`, sempre visível quando `canGenerate`) fica ao lado de "Gerar com IA" (`Sparkles`, só aparece quando `aiConfigured`; sem `aiPlanIncluded` vira um link "— Pro" pra `/planos`, mesmo padrão de upgrade CTA de sempre). A IA continua existindo como opção **a mais**, não como pré-requisito.

**Landing page pra compartilhar por WhatsApp (2026-09-01, migration `072_broadcast_landing_page.sql`)** — a pedido do usuário: sem integração real de API do WhatsApp ainda (schema pronto, sem código — seção 2), "mandar por WhatsApp" por enquanto é o missionário colar um link. Toda atualização criada em `SendBroadcastButton` gera automaticamente uma página pública em `/{username}/atualizacoes/{broadcastId}` (`src/app/[username]/atualizacoes/[broadcastId]/page.tsx`) — `createBroadcast()` devolve `shareUrl`, mostrado com botão "Copiar" numa tela de sucesso dentro do próprio dialog (não fecha sozinho, dá tempo de copiar antes de "Concluir").
- **"Gerar link" e "mandar e-mail" são decisões independentes (refinado ainda em 2026-09-01, a pedido do usuário: "uma coisa é uma coisa, outra coisa é outra coisa")** — checkbox "Também enviar por e-mail pros parceiros" no topo do formulário (ligado por padrão). Desligado: `createBroadcast(..., sendByEmail=false)` pula a busca de `partners`/filtro por tipo e o insert em `partner_broadcast_recipients` inteiro (`recipient_count=0`, nenhuma linha na fila, o cron `broadcast-sender` nunca vê esse broadcast) — mas a linha em `partner_broadcasts` e a landing page continuam sendo criadas normalmente, então o link sempre existe independente da escolha. Botão/copy da tela mudam conforme a escolha ("Enviar"/"Atualização a caminho!" vs. "Gerar link"/"Página pronta!").
- **Leitura pública seguríssima**: `partner_broadcasts` continua com RLS fechada pro dono (`is_profile_owner`) — a página pública nunca lê a tabela direto, só via `get_public_broadcast(p_id)`, function `SECURITY DEFINER` que devolve exclusivamente `profile_id/subject/body/highlight_ids/financial_snapshot/created_at` (nunca `recipient_count`/`recipient_filter`/`sender_user_id`) pra um `id` específico — mesmo padrão de `resolve_short_link()` que a outra sessão paralela implementou pra links curtos (migration 070), útil ter dois exemplos independentes convergindo pro mesmo padrão de "acesso público só por function, nunca abrindo SELECT geral da tabela". A página confere `broadcast.profile_id === profile.id` (resolvido por `username` da própria URL) antes de renderizar — sem isso, um `id` válido funcionaria sob qualquer username na URL.
- **`partner_broadcasts.financial_snapshot JSONB`** (nova coluna) — o resumo financeiro calculado em `/api/ai/generate-partner-update` (que antes só alimentava o prompt e era descartado) agora também volta na resposta e fica salvo junto do broadcast quando enviado, exatamente pra landing page conseguir mostrar os mesmos números reais em cartões visuais (não só na prosa da IA) sem precisar recalcular nem duplicar lógica de agregação.
- **Chrome própria, sem abas do perfil** — `profile-tabs.tsx` exclui `/atualizacoes/*` (mesmo motivo/padrão de `/parceria`: página feita pra ser aberta por link direto do WhatsApp, não navegada como parte do perfil).
- **Visual**: header (avatar+nome+data), texto em prosa normal, até 2 "stat tiles" (Arrecadado/Investido) quando há `financial_snapshot`, e um cartão por projeto (capa real, barra de progresso — e botão linkando pro projeto de verdade). Mobile-first (a maioria vai abrir pelo WhatsApp no celular).
- **Redesign visual + gráfico de categoria (2026-09-02)** — pedido do usuário: página "com design bonito, gráfico pros valores, foto de projeto e efeito moderno". Componentes novos em `src/components/partners/broadcast/` (`'use client'`, todos importados por um Server Component — página continua buscando/redigindo dados no servidor, só a casca visual virou client): `reveal.tsx` (`Reveal`/`RevealItem`, entrada com stagger, mesmo padrão de `FinancialDashboard`), `stat-tile.tsx` (contagem animada, `useSpring` — mesma técnica de `BalanceSummary`), `category-chart.tsx` (barra horizontal animada — reaproveita a paleta `--chart-1..8` de 11.1, cores começam no slot 3 pra não repetir com entrada/saída), `project-card.tsx` (barra de progresso com grow-in, leve *lift* no hover, e — quando não há `cover_url` — um bloco gradiente na cor de destaque do perfil com a inicial do projeto, em vez de simplesmente omitir a foto). Glow radial sutil no topo da página (`accent_color`, puro CSS). **Stat tiles trocaram `accent_color` (arbitrário, não validado) por `chart-1`/`chart-2` fixos** (mesma cor de entrada/saída do dashboard) — corrige de brinde um problema latente de contraste/CVD que dependia de qual cor cada missionário escolhesse. O gráfico de categoria (antes só existia no modo `percent_only`, como `PercentBreakdown`) agora aparece **sempre** que há despesas, nos dois modos — com valor exato quando autorizado, só percentual quando não. Nenhum componente novo recebe o `financial_snapshot` bruto: cada um só recebe os campos já computados/redigidos pela página (mesma garantia da Fase 1).
- **Descoberto ao configurar**: `ANTHROPIC_API_KEY` estava **vazia** em `.env.local` (nunca teve valor real, nem local nem produção) — ou seja, a feature de tradução por IA (já existente, seção 5.6.1) nunca funcionou de verdade em nenhum ambiente até hoje. Adicionado (vazio, como placeholder) em Vercel Production; falta o usuário colar uma chave real do console.anthropic.com em `.env.local` pra qualquer feature de IA do app funcionar.

### 7.10-quater Privacidade em camadas na prestação de contas + aba "Relatórios" (migration `075_broadcast_financial_visibility.sql`, 2026-09-02)

Até aqui, a landing page de uma atualização (7.10-bis acima) mostrava valores exatos de `financial_snapshot` pra **qualquer pessoa com o link**, sem checar nada — e o grant `partner_visibility_grants.section = 'financial_summary'` (existe desde a migration 011, com toggle pronto em `VisibilityGrantsDialog`) nunca era lido em lugar nenhum do código. Esta mudança finalmente usa esse grant, sem criar nenhuma tabela nova (mesma filosofia de reaproveitamento já usada em toda a seção 7.10-bis):

- **`partner_broadcasts.financial_visibility`** (`'exact' | 'percent_only'`, default `'exact'`) — escolha por atualização, radio no compositor (`SendBroadcastButton`) visível só quando "Prestação de contas" está marcada. Default preserva o comportamento anterior sem mudar nada pra quem já usa a feature.
- **Duas camadas de redação, cada uma resolvendo um jeito diferente do dado exato vazar:**
  1. **A narrativa da IA** — `describeFinancial()`/`generatePartnerUpdate()` (`src/lib/ai/generate-partner-update.ts`) sempre mandavam os valores exatos pro Claude, mesmo que a página depois escondesse os stat tiles — o texto gerado podia citar o valor mesmo assim. Em modo `percent_only`, a nova `describeFinancialPercentOnly()` calcula percentuais em TypeScript a partir da moeda dominante (maior total de `expenseByCurrency`) + top categorias de `topExpenseCategories` + bucket "outras categorias" (resto até 100%) e **nunca manda o valor em moeda pro modelo** — minimização de dado, não só uma instrução de "não fale o valor" (mais seguro, já que o modelo não pode vazar um número que nunca recebeu). Reforço adicional no system prompt (defesa em profundidade).
  2. **A landing page pública** (`[username]/atualizacoes/[broadcastId]/page.tsx`) — checa `has_partner_grant(profile_id, 'financial_summary')` via RPC (retorna `false` com segurança se ninguém estiver logado); `canSeeExactFinancial = financial_visibility !== 'percent_only' || hasGrant`. Se falso, renderiza `PercentBreakdown` (barras de percentual por categoria, sem nenhum valor em moeda) em vez dos `StatTile`s de sempre. A redação acontece em TypeScript dentro do próprio Server Component — seguro porque a página não tem `'use client'` na árvore que recebe o snapshot bruto (`StatTile`/`ProjectCard`/`PercentBreakdown` são funções do mesmo arquivo, só recebem primitivos já formatados), então o JSON bruto nunca é serializado pro navegador. `get_public_broadcast()` só precisou passar a devolver a nova coluna, sem lógica de redação no SQL.
- **Nova aba "Relatórios"** em `/dashboard/financeiro` (`finance-sub-nav.tsx`, rota `src/app/dashboard/financeiro/relatorios/`): lista as `partner_broadcasts` do perfil ativo com `financial_snapshot IS NOT NULL`, mais recentes primeiro, com badge de modo (Exato/Percentual) e `ShareButton` (`src/components/shared/share-button.tsx`, reaproveitado) pra copiar o link público de cada uma. Resolve a lacuna de não existir, antes, nenhum lugar que listasse o histórico — cada prestação de contas só era acessível se o missionário tivesse guardado o link mostrado na hora da criação. **Renomeada pra "Prestações" / `/dashboard/financeiro/prestacoes` em 2026-09-03 (ver 7.24)** — "Relatórios" virou outra coisa (analytics estilo GranaZen), nome colidia.
- **Editor dedicado, sem duplicar o compositor (2026-09-02)**: em vez de um componente novo, `SendBroadcastButton` ganhou prop `mode?: 'campaign' | 'report'` (default `'campaign'`, comportamento de `/dashboard/parceiros` inalterado). Em `mode="report"` (usado pela aba Prestações, ver renomeação acima): bloco financeiro começa ligado e sem checkbox pra desativar (só período + as duas opções de privacidade sempre visíveis), bloco "Projetos em destaque" some inteiro, `sendByEmail` começa desligado (objetivo primário é publicar o link, não disparar e-mail), e o assunto vem pré-preenchido com "Prestação de contas — {Mês Ano}" (editável). Reaproveita 100% da geração por IA/`createBroadcast`/tela de link — só a superfície do formulário muda. `createBroadcast` (`src/app/dashboard/parceiros/actions.ts`) ganhou um segundo `revalidatePath('/dashboard/financeiro/prestacoes')` (antes só revalidava `/dashboard/parceiros`, que nunca lista broadcasts — sem isso, a lista da aba não atualizava sozinha); o botão "Concluir" da tela de sucesso passou a chamar `router.refresh()`, que também nunca tinha sido necessário até essa aba existir.

### 7.11 Stripe Connect (recebimento automático) + recorrência (migration 031)
- **Perfil da plataforma (modo live) exige aceitar responsabilidade por perdas (2026-09-07)**: ao ativar as chaves live pela primeira vez, `stripe.accounts.create(...)` passou a falhar com `StripeInvalidRequestError` ("Please review the responsibilities of managing losses for connected accounts") até completar `https://dashboard.stripe.com/settings/connect/platform-profile` no Dashboard (modelo de negócio "Plataforma", fluxo de fundos direto, e reconhecer manualmente a responsabilidade por saldo negativo — nada disso é configurável por API/código, é um passo único feito direto no Dashboard da conta). Isso não existia em modo teste (Connect vem liberado automaticamente lá). O catch de `GET /api/stripe/connect/start` engolia esse erro sem logar nada (`catch {}` mudo) — corrigido pra `console.error` em ambos os `catch` (accounts.create/accountLinks.create), mudança necessária pra diagnosticar isso em produção sem acesso a `supabase`/Stripe CLI locais.
- **Notificação de risco/contestação pro missionário (2026-09-07)**: aceitar a responsabilidade acima também assume "notificar vendedores quando afetados por risco/prevenção de fraude" — a Stripe só fornece a ferramenta (Radar Standard, ~R$0,25/transação avaliada + R$5/conta conectada), quem repassa o aviso pro missionário é a plataforma. `POST /api/stripe/webhook` ganhou dois handlers novos: `account.updated` (só quando `requirements` mudou *neste* evento via `previous_attributes`, evitando e-mail repetido a cada atualização irrelevante enquanto a conta seguir restrita) manda e-mail quando `requirements.disabled_reason` aparece; `charge.dispute.created` manda e-mail a cada contestação nova. Ambos resolvem a conta conectada (`event.account`) pra `profile_id` via `payment_methods` e o e-mail via `supabase.auth.admin.getUserById` (mesmo padrão do cron de `notification-emails`, seção 7.10), reaproveitando `renderEmailTemplate`/`sendEmail` (Brevo) já existentes — nenhum template/infra novo. **Pendência que depende do usuário**: os dois tipos de evento precisam ser adicionados na assinatura do destino de webhook de Connect já existente no Stripe Dashboard (o mesmo que já escuta `checkout.session.completed`/`invoice.payment_succeeded`/`customer.subscription.deleted`) — sem isso, a Stripe nunca envia esses eventos e o código novo fica sem efeito.
1. **Missionário conecta o Stripe** (Configurações > Pagamentos, `StripeConnectCard`): Connect **Express + Account Links** — a plataforma cria a conta conectada (`stripe.accounts.create({type:'express', country, ...})`, país escolhido num `<select>` antes de conectar, obrigatório na criação e não muda depois) e a Stripe hospeda o formulário de onboarding (dados bancários, identidade) direto em stripe.com; a plataforma nunca vê nem guarda senha/chave/dado bancário do missionário, só recebe de volta o ID da conta — mensagem de segurança fica sempre visível no card (`stripeSecurityNote`). Fluxo: `GET /api/stripe/connect/start` cria a conta (ou reaproveita a existente, pra continuar um onboarding incompleto) com `is_active=false`, gera um `accountLink` (`return_url` → `/api/stripe/connect/callback`, `refresh_url` → o próprio `start`) e redireciona pra lá. `GET /api/stripe/connect/callback` (sem `code`/`state` — não é OAuth) confere `stripe.accounts.retrieve(...).{details_submitted,charges_enabled}`: se completo, marca `is_active=true` (`stripe=connected`); se não, deixa `is_active=false` (`stripe=incomplete`, o card mostra badge "Configuração pendente" com botão "Continuar configuração" que reabre o Account Link). Depois de ativo, vincula automaticamente a primeira `financial_accounts` existente (`linked_account_id`) assim que conectado — sem opção de "não lançar": cobrança via Stripe é confirmação automática (API), então sempre gera lançamento (diferente de métodos manuais tipo Pix, onde o próprio parceiro registra a oferta e não há automação); se não existir nenhuma `financial_accounts` ainda, o card mostra um aviso com link pra criar uma em `/dashboard/financeiro/contas`. "Desconectar" chama `stripe.accounts.del` (best-effort — Express com saldo/atividade pode recusar) e apaga a linha local. Não depende de nenhum client_id de plataforma (diferente do Connect Standard/OAuth, que exigiria o missionário já ter — e logar — numa conta Stripe própria pré-existente).
1-bis. **`linked_account_id` estendido pra Pix, vínculo obrigatório sem exceção (2026-09-04)**, a pedido do usuário: até aqui esse campo só existia pra `type='stripe'` (vínculo automático, sem escolha do usuário). Pra `pix`, o vínculo é manual e só organizacional (Pix é método manual — o próprio parceiro registra a oferta, sem automação nenhuma vinda daqui; ver a distinção `payment_methods` vs. `financial_accounts` na seção 7.2), mas passou a ser **exigido sem exceção** na UI, depois de uma primeira versão que deixava salvar sem vínculo quando não havia nenhuma conta ainda (usuário rejeitou explicitamente esse escape: "não pode ser só um Pix sem vincular a conta nenhuma"). `PaymentMethodForm` (criar/editar, `src/components/dashboard/settings/payment-method-form.tsx`) ganhou o campo "Conta vinculada" quando `type==='pix'`: com pelo menos uma `financial_accounts` no perfil, é um `<select>` obrigatório (bloqueia salvar sem escolha, via `errorLinkedAccountRequired`) com um link "+ Criar nova conta" ao lado; sem nenhuma conta ainda, o mesmo espaço mostra um botão "Criar conta". Os dois abrem o **mesmo `AccountWizard`** de 6 etapas já usado em `/dashboard/financeiro/contas` (`src/components/financial/account-wizard.tsx`) — não um formulário reduzido à parte —, montado como um segundo `<Dialog>` **aninhado dentro** do dialog do método de pagamento (Base UI empilha/escurece o de trás automaticamente, sem código extra de camada). Pedido explícito do usuário depois de ver uma primeira versão com um mini-formulário simplificado (só nome): "quando a pessoa clicar em criar uma nova conta, deve aparecer os mesmos processos das etapas que aparecem [...] em adicionar conta". `AccountWizard` ganhou um prop novo `onCreated?: (account: FinancialAccount) => void` (o `insert` passou a ter `.select('*').single()` pra devolver a linha criada) — `PaymentMethodForm` usa isso pra auto-selecionar a conta recém-criada na hora, sem esperar o `router.refresh()` que o wizard já disparava (mantido intacto, porque `AccountsList` continua dependendo dele). **Bug real achado testando** (Playwright): como o wizard soma a conta nova tanto via `onCreated` (estado local `extraAccounts`) quanto, pouco depois, via `financialAccounts` (prop atualizada pelo `router.refresh()`), a mesma conta aparecia duas vezes na lista mesclada — `<option key={a.id}>` duplicada, warning React de key não-única no console. Corrigido filtrando `extraAccounts` pra excluir qualquer id que já esteja em `financialAccounts` no momento do merge (`accounts = [...financialAccounts, ...extraAccounts.filter(...)]`) — resolve sozinho assim que o refresh alcança o estado local, sem precisar de deduplicação por efeito colateral. Resultado: **não existe caminho de salvar um Pix sem `linked_account_id`** — nem criando, nem editando — e criar a conta usa exatamente o mesmo fluxo (automática/manual, nome, saldo inicial, tipo, moeda/compartilhamento, revisão) de qualquer outra conta do sistema. `PaymentMethodCard` ganhou um aviso âmbar genérico ("Sem conta vinculada. Clique em Editar pra vincular (ou criar) uma conta.") pra qualquer Pix já cadastrado com `linked_account_id IS NULL` (cobre Pix legado, de antes dessa mudança) — clicar em Editar cai no mesmo formulário acima. `PaymentMethodsList` repassa `financialAccounts` (já buscado na página de Configurações, antes só usado por `StripeConnectCard`) pros dois componentes. Sem migration nova — reaproveita a coluna que já existia. Verificado num browser real (Playwright, conta seed `ana.ferreira`, dados de teste restaurados ao final, incluindo as contas duplicadas do bug acima): validação nativa do `<select required>` bloqueia submit sem conta escolhida; wizard aninhado abre por cima do dialog do Pix e passa pelas 6 etapas reais; conta criada auto-seleciona no `<select>` do Pix, sem duplicar e sem erro de console (confirmado nas duas rodadas — com o bug e depois do fix); Pix legado sem vínculo mostra o aviso e, ao editar, resolve corretamente. Ver também 3-sexies (grid de mini-cards que consome esse catálogo na página pública, sem mudança) e 7.29 (`AccountWizard` original).

1-ter. **Tipo de método filtrado por moeda — Pix só aparece em BRL (2026-09-04)**, a pedido do usuário: Pix é exclusivo do Brasil, então não fazia sentido oferecê-lo com qualquer outra moeda selecionada. `PAYMENT_METHOD_CATALOG` (`src/lib/payment-methods/catalog.ts`) ganhou um campo opcional `currency` por entrada — só `pix: 'BRL'` usa por enquanto, os outros 12 tipos manuais continuam sem restrição. `PaymentMethodForm` inverteu a ordem dos dois primeiros campos (Moeda antes de Tipo, pedido explícito do usuário: "acho que dá pra colocar a moeda em primeiro lugar") e o `<select>` de Tipo passou a filtrar suas opções (`methodsForCurrency`) pela moeda corrente — um grupo do catálogo que fique sem nenhuma entrada depois do filtro simplesmente não renderiza seu `<optgroup>`. `handleCurrencyChange()` sincroniza os dois campos nos dois sentidos: trocar pra uma moeda que não bate com o tipo atualmente selecionado troca o tipo sozinho pro primeiro compatível (ex.: BRL→USD com Pix selecionado pula pra Mercado Pago); **voltar pra BRL reseleciona `type='pix'` sozinho**, não só reabre a opção na lista — primeira versão só reabria a opção sem reselecionar, usuário corrigiu explicitamente ("quando volto pra BRL ele não reaparece") depois de notar que o tipo ficava travado no que tinha caído por fallback (ex. Mercado Pago) mesmo voltando pra BRL; BRL é a moeda do Pix, então virou a leitura certa que voltar pra ela deve trazer Pix de volta como seleção, não só como opção disponível. Sem dado legado quebrado (checado direto no banco: nenhuma linha `payment_methods` existente tinha `type='pix'` com moeda diferente de `BRL`). Verificado num browser real (Playwright, conta seed `ana.ferreira`), duas rodadas (BRL→USD→BRL e BRL→EUR→BRL): Moeda aparece antes de Tipo; mudar pra moeda estrangeira tira Pix da lista e troca o tipo selecionado sozinho; voltar pra BRL reseleciona Pix automaticamente nas duas rodadas; sem erro de console. `tsc`/`eslint` limpos.
2. **Recorrência exige conta**: diferente do `PledgeForm` avulso (aceita anônimo), quem clica em "Ser parceiro fixo" precisa estar logado — se não estiver, `RecurringPledgeForm` mostra CTA "Criar conta"/"Já tenho conta" que leva a `/cadastro`/`/login` com `?redirect=` apontando de volta pra `/{username}/parceria?...&choice=financial_ongoing` (`PartnershipWizard` já suportava restaurar `initialChoice` via `?choice=`; `/cadastro` ganhou suporte a `?redirect=` nesta mudança — antes sempre mandava pra `/onboarding` fixo). **Limitação conhecida**: `handle_new_user()` cria uma linha em `profiles` pra **qualquer** signup, então um apoiador que só quer virar parceiro recorrente também ganha um perfil de missionário vazio — não filtrado nesta rodada.
3. **Caminho automático (recorrente)** (só aparece se o missionário tem Stripe conectado): `RecurringPledgeForm` chama `POST /api/stripe/checkout-recurring` (autenticado) → resolve/cria `partners` (mesma lógica de "encontra ou promove" do `PledgeReviewCard`, mas rodando na hora porque já se sabe quem é) → insere `recurring_pledges` com `status='pending'` → cria `stripe.checkout.sessions.create({mode:'subscription', ...}, {stripeAccount: contaConectada})` (preço criado inline via `price_data`, sem produto pré-cadastrado; sem `application_fee` — 100% vai pro missionário) → redireciona pro Checkout.
3-bis. **Caminho automático (avulso, cartão)** — `PledgeForm` (doação única/geral, `financial_once`/`financial_once_general`): se `stripeAvailable`, mostra por padrão um botão "Pagar com cartão agora" (`showManual` inicia `false`) com toggle pra voltar ao formulário manual, e vice-versa (mesmo padrão de toggle do `RecurringPledgeForm`). **Diferente da recorrência, não exige login** — anônimo ou não, o pledge só nasce depois, via webhook. `handleStripeCheckout` chama `POST /api/stripe/checkout-once` (sem auth obrigatória) que valida `stripeMethod` ativo do missionário e cria `stripe.checkout.sessions.create({mode:'payment', ...}, {stripeAccount: contaConectada})` — like o recorrente, preço inline via `price_data`, sem `application_fee`. Como não existe pledge prévio pra anexar metadata (diferente do fluxo recorrente, que já insere `recurring_pledges` antes do Checkout), todos os dados do formulário (profileId, highlight, categoria de orçamento, anônimo, nome, e-mail, mensagem) viajam como `session.metadata` (`pledge_*`) e só viram uma linha de `pledges` quando o webhook confirma o pagamento — existe uma janela onde o checkout foi pago mas a oferta ainda não existe no banco (fica só na Stripe) até o evento chegar.
3-ter. **UI do checkout (`PledgeForm` e `RecurringPledgeForm`)** — redesenhado a partir de um mockup trazido pelo usuário (Google Stitch), mantendo a estrutura (resumo sempre visível, chips de valor, abas de método) mas com os tokens de cor reais do app (não o Material/terracota do mockup) e sem inputs de cartão em texto puro (PCI — o "Cartão" continua sendo o Stripe Checkout hospedado, só que dentro de uma aba). Componentes novos, compartilhados pelos dois formulários: `AmountChips` (`src/components/partners/amount-chips.tsx`, valores predefinidos por moeda, ex. R$50/100/200/500), `PaymentModeTabs` (segmented control Cartão×Outro método, substitui o antigo par botão+link "prefiro cartão"/"prefiro manual"), `DonationSummary` (resumo fixo "R$100,00 para {nome}", só aparece com valor preenchido) e `DonationHero` (foto de contexto no topo — usa `next/image`, por isso os hosts precisam estar em `next.config.ts#remotePatterns`, já cobertos pelo domínio do Supabase Storage). A foto do `DonationHero` é decidida por quem monta o formulário (`PartnershipWizard`), não pelo formulário em si: **capa do projeto (`highlights.cover_url`) quando a doação está vinculada a um `highlight_id`, foto de perfil do missionário (`profiles.avatar_url`) como fallback** — `financial_once_general` (doação sem projeto) sempre usa a foto de perfil, mesmo que a URL tenha um `highlight_id` de outra escolha; `financial_once` e `financial_ongoing` usam a capa do projeto quando aplicável. `parceria/page.tsx` passou a buscar `cover_url` do highlight e repassar `profile.avatar_url` como novas props (`profileAvatarUrl`/`highlightCoverUrl`) pro `PartnershipWizard`, que resolve qual imagem cabe em cada branch antes de repassar um único `heroImageUrl` pros formulários. `DonationHero` também recebe `objectPosition` (reaproveita `highlights.cover_position`, o mesmo foco que o editor de capa do projeto já grava — sem isso, um retrato cairia cortado num card baixo e largo).
3-quater. **Header/footer fixos** — a pedido do usuário (também a partir do mockup do Stitch, citando prática de mercado: CTA fixo no rodapé é padrão comprovado em checkout mobile, ~18% de lift de conversão segundo CXL Institute), `PledgeForm`/`RecurringPledgeForm` passaram de `<Card>` centralizado pra tela cheia própria: `<header className="fixed top-0">` (botão voltar + título dinâmico + `LanguageSwitcher`) e `<footer className="fixed bottom-0">` (CTA que troca entre Stripe e "Registrar oferta"/"Confirmar compromisso" conforme a aba ativa) — o conteúdo do meio (incluindo o `DonationHero`) rola normalmente por trás dos dois. O botão do rodapé fica fora do `<form>` das seções manuais e usa o atributo HTML `form="pledge-manual-form"`/`form="recurring-manual-form"` pra disparar o mesmo `onSubmit` sem precisar duplicar lógica nem içar refs — funciona porque os handlers já leem tudo do state React, não de `FormData`. **Efeito colateral importante**: `/parceria` já tinha uma barra fixa global (`ProfileTabs`, ver 7.12) com botão de voltar + `LanguageSwitcher`, que colidia visualmente com o header novo — `ProfileTabs` ganhou um `if (pathname === '${base}/parceria') return null`, e `PartnershipWizard` passou a montar sua própria barra (mesmos `BackButton`/`LanguageSwitcher`) em **todas** as suas telas (lista inicial, `PartnershipForm` de oração/embaixador/voluntário, e os estados "precisa de conta"/"concluído" de `PledgeForm`/`RecurringPledgeForm`) — `/parceria` ficou 100% autônoma da chrome do perfil.
3-quinquies. **Cabeçalho fixo estendido a toda a jornada (não só ao checkout)** — o usuário reparou que, fora de `PledgeForm`/`RecurringPledgeForm`, as outras telas do wizard (lista inicial de escolha, `PartnershipForm` de oração/embaixador/voluntário, e os estados "precisa de conta"/"concluído") ainda usavam `flex items-center justify-center` — nesse layout o cabeçalho ficava dentro do bloco centralizado verticalmente, então "descia" junto com o conteúdo em vez de ficar fixo no topo. Pesquisa de mercado confirmou o padrão esperado (Material Design "top app bar": cabeçalho fixo, conteúdo começa logo abaixo, nunca centralizado no meio). Extraído `CheckoutHeader` (`src/components/partners/checkout-header.tsx`, `BackButton`+título opcional+`LanguageSwitcher`) reaproveitado por **todas** as telas de `/parceria`; cada uma trocou o wrapper centralizado por `<div className="min-h-screen"><CheckoutHeader/><div className="pt-[72px] ...">` — telas terminais curtas (precisa de conta, concluído) mantiveram o cartão visualmente centrado, mas usando `min-h-[calc(100vh-56px)] flex flex-col justify-center` no container de conteúdo (abaixo do cabeçalho, que agora é de fato fixo) em vez de centralizar a tela inteira incluindo o cabeçalho. **Auditoria do resto do app** (pedido do usuário: "aplica em outras telas, caso estejam repetindo esse comportamento"): não achei outra instância do mesmo bug (header preso dentro de bloco centralizado). `/oracao` e `/mensagens` usam o mesmo `BackButton`+`LanguageSwitcher` fixo, só que via `ProfileTabs` (seção 7.12) — que já era `fixed top-3`, então não sofriam o problema. `(auth)/layout.tsx` (login/cadastro/recuperar-senha) e telas de confirmação isoladas (`conta/excluir`, gate de perfil privado) centralizam verticalmente **sem** nenhum header próprio pra colidir — padrão legítimo e diferente (tela única, sem navegação em passos), não alterado. `OnboardingWizard` também não tem header/voltar fixo hoje (nenhum `fixed`/`sticky`/`BackButton` no componente) — fica de fora por não repetir o padrão, não por ter sido esquecido.
3-sexies. **Seletor de método unificado em mini-cards** — a pedido do usuário, de novo a partir de uma iteração do mockup do Stitch: o antigo par "abas Cartão×Outro método" + `<select>` de método dentro da aba manual virou um grid único de mini-cards (ícone + nome do tipo, `PaymentMethodCards`, `src/components/partners/payment-method-cards.tsx`) — Cartão (Stripe) entra como mais uma opção no mesmo grid (`id: 'stripe'` sintético, prepended em `allOptions` quando `stripeAvailable`), não numa aba separada. Selecionar qualquer card revela o mesmo painel abaixo: Stripe mostra uma nota de segurança + o botão de checkout no rodapé; qualquer outro método mostra `PaymentMethodInstructions` (já existente, sem mudança) + os campos de autorregistro (data, comprovante) só nesse caso. O rótulo do card usa o nome genérico do tipo (`PaymentMethods.type_*`, ex. "Pix"), não o rótulo customizado inteiro que o missionário deu (ex. o nome completo na chave) — esse fica só no painel de detalhes abaixo; motivado por um profile de teste com 2 chaves Pix cujos rótulos customizados (nomes de pessoa) não cabiam no card e ficavam truncados/ambíguos. **Refinado no dia seguinte** (2026-08-27, sugestão do usuário): quando existe mais de uma chave do mesmo tipo, "Pix"/"Pix" repetido continua ambíguo mesmo com o nome genérico — o card passou a complementar com o primeiro nome do rótulo customizado ("Pix Silas"/"Pix Izabela"), mas só quando esse rótulo existe e é diferente do nome genérico do tipo (comparação direta `label !== t('type_'+method)`) — sem essa checagem, o caso comum (missionário com só uma chave, sem rótulo próprio) viraria "Pix Pix" redundante. Formulário ganhou seções com `<h2>` ("Seus dados", "Forma de pagamento") separadas por `border-t`, mais perto da estrutura do mockup. `PaymentModeTabs` (componente da rodada anterior) ficou sem uso e foi deletado. Chaves i18n `methodLabel`/`manualTab` removidas (mortas); novas `sectionYourDataTitle`/`sectionPaymentTitle`/`stripeInlineNote` em `PledgeForm` (reaproveitadas por `RecurringPledgeForm` via `tPledge`), nas 3 línguas.
3-septies. **Passo do wizard sincronizado com a URL (2026-08-26)** — bug reportado pelo usuário: ao escolher Cartão e ir pro Stripe Checkout (navegação de página de verdade, `window.location.href`, sai do domínio do app), clicar em "voltar" lá devolvia pra tela inicial "Faça parte com {nome}" em vez da tela de doação em que o usuário estava. Causa: `choice` (o passo atual do `PartnershipWizard`) só existia em estado React — `setChoice()` nunca tocava a URL, então o histórico do navegador guardava só `/parceria` sem `?choice=`, e voltar do Stripe recarregava essa URL do zero, perdendo o passo. Corrigido com um `useEffect` que sincroniza `choice` pra `?choice=` via `router.replace` (não `push` — não empilha uma entrada de histórico a cada clique dentro do wizard, só mantém a URL atual em sincronia; preserva os demais params existentes como `highlight_id`/`category` via `useSearchParams()`). Como `page.tsx` já lia `choice` da URL pra montar `initialChoice` (usado desde a migration 031, pro redirect de login), o fix reaproveita esse mecanismo já existente — só faltava a mão de volta (client → URL). Validado com teste real de navegação (Playwright: escolhe um passo, navega pra um domínio externo simulando o Stripe, `goBack()`, confirma que a tela certa volta a aparecer). **Faltava a outra metade** (usuário reparou passando o mouse sobre o link de voltar/cancelar de dentro do próprio Stripe Checkout): `success_url`/`cancel_url` das duas sessões de checkout (`checkout-once/route.ts`, `checkout-recurring/route.ts`) são montadas no servidor, num caminho totalmente separado do `router.replace` do client — também não tinham `choice=`. Corrigido nos dois: `checkout-once` deriva `choice` da mesma regra já usada pra decidir o resto da URL (`highlightId ? 'financial_once' : 'financial_once_general'`); `checkout-recurring` sempre usa `financial_ongoing` (única origem possível dessa rota). Validado com teste end-to-end de verdade (Playwright clica até chegar em `checkout.stripe.com` de fato — sessão de teste real — e confere que o link de cancelar da própria página da Stripe já vem com `choice=financial_once`).
3-octies. **`choice` deixou de ser `useState`, virou derivado da URL — e "voltar" virou navegação real (2026-08-26)** — refinamento em cima do item acima, motivado por um caso que ele não cobria: usuário reparou que clicar em "Apoiar este projeto" a partir da **página do projeto** (link direto pra `/parceria?highlight_id=...&choice=financial_once`, pulando a lista de escolha) e depois clicar em "<" dentro do próprio app voltava pra lista "Faça parte com..." em vez de voltar pra página do projeto de onde o usuário realmente veio — porque o botão "<" chamava `onBack={() => setChoice(null)}`, sempre fixo na lista, ignorando de onde o usuário chegou. Correção: `choice` deixou de ser `useState` e passou a ser lido direto de `useSearchParams()` a cada render (a URL é que manda); toda navegação "pra frente" (`goTo()`) usa `router.push` (não `replace`), empilhando uma entrada de histórico real por passo. `PledgeForm`/`RecurringPledgeForm` trocaram a prop `onBack: () => void` por `backHref: string` — sem `onClick` customizado, `CheckoutHeader`/`BackButton` usa a própria lógica que já tinham (`hasInAppNavigation() ? router.back() : router.push(href)`), que agora funciona de verdade: se o usuário veio de dentro do wizard (lista → formulário), volta pra lista; se veio de um link direto de fora (página do projeto, perfil), `router.back()` sai do `/parceria` inteiro e volta pra página de origem real; `backHref` (a lista) só entra como rede de segurança quando não há navegação in-app nenhuma pra voltar (aba nova, link direto). **Nota de depuração**: um teste automatizado inicial parecia mostrar flakiness (às vezes a URL não revertia a tempo) — investigado a fundo (log temporário no `BackButton`) e confirmado que não é bug: `router.push` pro Next App Router precisa buscar o payload RSC da rota no servidor antes de comitar a URL, o que no modo dev leva de 300ms a ~1s; aumentando a espera do teste pra 1.5s, o resultado ficou 100% consistente em repetidas execuções. Comportamento esperado de navegação assíncrona, não uma falha real.
3-nonies. **Grid de método filtrado por moeda (2026-08-27)** — pedido do usuário: "quando a pessoa seleciona outra moeda... mostrar somente as possibilidades de transferência para aquela moeda". Antes, o grid de mini-cards (`allOptions`) mostrava **todos** os métodos manuais configurados, independente da moeda escolhida pro valor — uma chave Pix cadastrada em BRL aparecia igual mesmo se o doador tivesse escolhido USD (e a moeda do campo "Valor enviado" às vezes ficava travada no valor do método selecionado, um comportamento invertido do que fazia sentido). Reescrito em `PledgeForm` e `RecurringPledgeForm`: o seletor de moeda agora fica **sempre visível e editável** (não mais condicional a "método configurado ou não"), e é ele quem decide `visibleOptions = allOptions.filter(o => o.currency === moedaEscolhida)` — cada `PaymentOption` já carrega a própria `currency` (do cadastro em Configurações > Pagamentos). Grid vazio pra aquela moeda mostra um aviso (`noMethodsForCurrency`) em vez de nada. Diferença entre os dois formulários: no avulso (`PledgeForm`), Cartão (Stripe) é isento do filtro — `checkout-once` aceita qualquer moeda via `price_data` dinâmico — então Cartão sempre aparece. Na recorrência (`RecurringPledgeForm`), Cartão **entra no filtro igual aos outros** (currency travada em `projectCurrency`) porque `checkout-recurring` só processa a moeda fixa do projeto (ver 7.11, item 3 e `checkout-recurring/route.ts`) — trocar de moeda ali faz até o Cartão sumir do grid se não for a moeda do projeto, correto porque a API realmente não aceita outra. Ao trocar de moeda com o método atual selecionado ficando inválido, troca automaticamente pro primeiro válido (`handleCurrencyChange`). Nova chave i18n `noMethodsForCurrency` em `PledgeForm` (reaproveitada pelas duas telas), nas 3 línguas. De quebra, `type_bank_transfer` (`PaymentMethods`) encurtado pra "Transf. bancária"/"Transf. bancaria" (PT/ES) — o texto completo estourava a largura do mini-card (`truncate`).

**Refinado no dia seguinte** (2026-08-27, pedido do usuário: "amarrar as duas coisas" — o `<select>` de moeda deveria refletir o que foi cadastrado em Configurações > Pagamentos, não uma lista fixa): o dropdown de moeda em ambos os formulários agora é derivado, não mais o `CURRENCIES` fixo (`src/lib/currency-mask.ts`) direto. `PledgeForm`: se `stripeAvailable`, mostra o `CURRENCIES` completo (Cartão aceita qualquer uma via `price_data` dinâmico); senão, só as moedas que existem de fato entre `paymentOptions` (`Array.from(new Set(...))`), com fallback pro `CURRENCIES` completo apenas se não houver nenhum método cadastrado (evita dropdown vazio). `RecurringPledgeForm`: união das moedas configuradas com `projectCurrency` (só entra se `stripeAvailable`, já que ali o Cartão é travado na moeda do projeto, não em qualquer uma). **Limite conhecido, não implementado**: o usuário deu iene como exemplo de "qualquer moeda que o missionário cadastrar" — hoje isso não é possível porque `CURRENCIES` (usado tanto aqui quanto no formulário de cadastro de método em Configurações, `payment-method-form.tsx`) é uma lista fixa de 7 moedas (BRL/USD/EUR/GBP/CHF/CAD/AUD), sem JPY nem outras. Expandir a lista exige mais que adicionar o código ISO: moedas sem casas decimais (JPY, KRW, etc.) quebrariam o cálculo de centavos usado em todo o sistema pro Stripe (`Math.round(amount * 100)` pressupõe 2 casas decimais sempre) e a máscara de digitação (`CURRENCY_SEPARATORS`, que também assume 2 casas). Fora de escopo desta rodada — fica registrado como próximo passo se o usuário quiser mesmo suportar moedas de zero casas decimais.
4. **Webhook de Connect** (`POST /api/stripe/webhook`, secret **separado** do webhook de billing da plataforma — `STRIPE_CONNECT_WEBHOOK_SECRET`, porque eventos de contas conectadas chegam num endpoint de Connect configurado à parte no Dashboard Stripe): `checkout.session.completed` com `mode='subscription'` ativa o `recurring_pledges` (`status='active'`, grava `stripe_subscription_id`); o mesmo evento com `mode='payment'` e `metadata.pledge_profile_id` (caminho avulso do item 3-bis) cria a linha de `pledges` na hora (`status='confirmed'`, sem revisão humana, `is_recurring_pledge=false`) — resolve/cria `partners` só se não for anônimo e tiver `reporter_user_id` (mesma lógica de "encontra ou promove"), e cria `transactions` (com `partner_id`, `highlight_id`, `budget_category_id`) se o `payment_methods` do missionário tiver `linked_account_id`; `invoice.payment_succeeded` (assinatura recorrente) cria um `pledges` já `status='confirmed'` do mesmo jeito e, se houver `linked_account_id`, também cria `transactions` — replica a lógica de `PledgeReviewCard` (seção 7.2) só que automática; `customer.subscription.deleted` marca `recurring_pledges.status='cancelled'`.
5. **Caminho manual** (sempre disponível, qualquer tipo de `payment_methods` exceto `stripe`): `RecurringPledgeForm` resolve/cria `partners` no client e insere `recurring_pledges` direto (`status='active'`, `stripe_subscription_id=null`). Se `reminder_opt_in`, `next_reminder_at = hoje + 1 mês`. Não existe (nem é possível sem um provedor tipo Plaid/GoCardless, fora de escopo) cobrança automática por transferência bancária — "recorrente" aqui é sempre compromisso + lembrete, nunca débito automático.
6. **Lembrete mensal** (`src/lib/email/brevo.ts` + `GET /api/cron/recurring-reminders`, cron diário via `vercel.json`, protegida por `CRON_SECRET`): busca `recurring_pledges` manuais ativos com `next_reminder_at <= hoje`, manda e-mail via Brevo (texto sempre em PT — não há como saber o idioma do apoiador, que não é dono de `profiles.locale`) com link de descadastro (`GET /api/recurring-pledges/[id]/unsubscribe`, sem auth, `id` como token), avança `next_reminder_at` em 1 mês. Sem chave Brevo configurada, `sendEmail()` só retorna `false` sem lançar erro (mesmo padrão de `getStripeClient()`).

### 7.12 Perfil público: dono vs. visitante (`getProfileViewerContext`)
`src/lib/profile/viewer-context.ts` exporta `getProfileViewerContext(username)` (envolto em `React.cache()` — deduplica entre `layout.tsx` e cada `page.tsx` da árvore `[username]` no mesmo request), retornando `{ viewerUserId, isOwner, canEdit, isViewerRole }`. `canEdit` é `isOwner OR` vínculo `manager` em `profile_managers` (mesma semântica de `is_profile_owner()` no banco — ver 7.11/migration 023) — não é uma comparação literal de `user.id`, porque um gestor de conta vinculada também deve poder editar. Essa é a única fonte de verdade para "quem está olhando"; substitui os fetches de auth ad hoc que existiam antes em `page.tsx`, `mensagens/page.tsx` e `parceria/page.tsx`.

Efeitos de `canEdit=true` (dono ou gestor vendo o próprio perfil público):
- `[username]/page.tsx`: perfil `privacy_mode='private'` deixa de exigir vínculo em `partners` para quem tem `canEdit` (bug pré-existente — sem isso, o próprio dono de um perfil privado ficava trancado do lado de fora do próprio perfil). `ProfileCTA` (Parceria/Oração/Mensagem) é substituído por `ProfileOwnerActions` ("Editar perfil" via `EditProfileDialog`, que abre o `ProfileForm` já usado em Configurações dentro de um `Dialog` — nenhuma lógica de formulário duplicada; e "Compartilhar perfil", Web Share API com fallback de copiar link).
- `[username]/mensagens/page.tsx`: redireciona para `/${username}` antes de montar o chat — dono/gestor não conversa consigo mesmo (cobre acesso direto por URL; o CTA de mensagem já não aparece pra eles em `page.tsx`).
- `[username]/layout.tsx` passa a aceitar `params` e renderizar `ProfileTabs` (barra de abas Perfil/História/Projetos/Trajetória, `usePathname()`) acima de `{children}`, visível para **qualquer** visitante (não só dono) — substitui os links "← Voltar" ad hoc de cada subpágina. Fica oculta em `mensagens`, `oracao`, `parceria` e `projetos/[slug]` (fluxos de ação/conversão ou drill-down, sem chrome de navegação por cima — mesmo padrão do Instagram, que também não mostra a barra de abas do perfil ao compor uma mensagem ou abrir um post).
- **2026-07-22 — `BackToDashboard` (seta "voltar" no início da barra de abas) deixou de depender de `canEdit`, passou a depender só de `viewerUserId` (logado ou não)**, motivado por um efeito colateral direto da mudança do mesmo dia no feed (ver Changelog): ao parar de abrir perfis em nova aba, um parceiro logado que clica no nome de outro missionário no feed navega *dentro* da mesma aba e ficava sem nenhuma forma de voltar — a barra de abas só mostrava o botão pra quem tinha `canEdit` (dono/gestor do próprio perfil), então visitante logado via só Perfil/História/Projetos, sem saída a não ser o botão "voltar" do navegador. `getProfileViewerContext` já expunha `viewerUserId` (não-nulo pra qualquer usuário autenticado, independente de posse); `[username]/layout.tsx` passou a repassar esse campo pra `ProfileTabs`, que troca a condição `canEdit &&` por `viewerUserId &&`. Visitante anônimo (link público compartilhado, sem sessão) continua sem o botão — comportamento preservado, é a mesma página "solta" de sempre pra quem chegou de fora do app.
- **2026-07-20 — botão "voltar" unificado nas 7 páginas do perfil público**, motivado pelo usuário reportar botões de voltar duplicados/inconsistentes. Nas 4 rotas sem barra de abas (`mensagens`, `oracao`, `parceria`, `projetos/[slug]`), `ProfileTabs` agora renderiza um único botão fixo e discreto (`BackToProfile`, ícone só, `fixed top-3 left-3 z-50`, mesmo estilo pill do `LanguageSwitcher` que já ficava em `top-3 right-3`) linkando pra `/${username}` — os links "← Voltar ao perfil de X" que cada página/componente renderizava no próprio conteúdo (`projetos/[slug]/page.tsx`, `mensagens/page.tsx`, `oracao/page.tsx`, tela inicial do `PartnershipWizard`) foram removidos. Nas 3 rotas com barra de abas (`historia`, `trajetoria`, `projetos` lista), a aba "Perfil" já embutida na barra cobre a mesma necessidade — o link inline redundante dessas 3 páginas foi só removido, sem substituto novo. **Desatualizado pra `projetos/[slug]` a partir de 2026-08-08** — ver bullet abaixo: essa rota passou a ter a barra de abas normal, não é mais uma das "sem abas".
- **2026-08-08 — `projetos/[slug]` ganhou a barra de abas normal (Perfil/História/Projetos/Trajetória)**, revertendo o item acima só pra essa rota — pedido direto do usuário, pra quem está vendo um projeto poder navegar pro resto do perfil sem precisar voltar primeiro. `ProfileTabs`: removido o bloco `isProjectDetail` que tratava `projetos/[slug]` como "fluxo de ação sem chrome" (mesma categoria de `mensagens`/`oracao`/`parceria`); a rota agora cai no render padrão de abas, igual `historia`/`trajetoria`/`projetos` (lista). `mensagens`/`oracao`/`parceria` continuam sem abas — são fluxos de conversão de verdade, isso não mudou.
- **2026-07-20 — edição inline por seção em `[username]/projetos/[slug]`**, motivado pelo usuário: dono de perfil visitando a própria página pública de projeto não tinha como editar sem ir pro dashboard. A página (Server Component) passou a chamar `getProfileViewerContext` e a relaxar o filtro `.eq('status','active')` da query do projeto quando `canEdit` (deixa o dono abrir/editar projeto `hidden`/`completed` pela própria URL pública). Os blocos de leitura existentes (capa+título+versículo, descrição, formas de apoio, financeiro, marcos, história, datas/status) foram envolvidos por 7 "ilhas" client-side (`src/components/highlights/*-edit-section.tsx`) que só aparecem com `canEdit=true`; cada uma reaproveita os módulos extraídos do `HighlightForm` (ver 7.7) e mostra um ícone de lápis discreto (hover/focus no desktop, sempre visível no mobile) que troca a visualização por um miniformulário com Salvar/Cancelar, sem modal e sem navegar de rota. Arquitetura: o Server Component monta um `HighlightSnapshot` serializável (todos os campos que `POST /api/highlights` espera) e passa como prop pra cada ilha, junto com o JSX de leitura como `children` — funções não atravessam o boundary Server→Client, então cada ilha define seu próprio `onSave` (client) que mescla seu campo editado com o resto do snapshot e faz o `fetch` direto. Cada save bem-sucedido chama `router.refresh()` (hook compartilhado `useHighlightSectionSave`, `src/hooks/`) pra que todas as ilhas recebam um snapshot atualizado do servidor — sem isso, editar duas seções em sequência sem recarregar a página faria a segunda sobrescrever a primeira com dados desatualizados. Pré-requisito de segurança: `POST /api/highlights` não validava posse do `profileId` no servidor (ver 5.3) — corrigido nesta mesma mudança, já que a rota passou a ser chamada também a partir da página pública, e não só do dashboard privado como antes.

Link "Ver meu perfil" no sidebar do dashboard (`DashboardSidebar`/`MobileBottomNav`) parou de abrir em nova aba (`target="_blank"` removido) — agora é navegação integrada para essa mesma experiência de dono, não mais uma "saída" para uma página pública genérica.

**Nota de design atualizada (migration 033 — ver 7.1-bis): "seguir o missionário" saiu do papel.** A tabela `follows` (seção 3.1) implementa exatamente o "seguir leve" cogitado aqui, deliberadamente separado de `partners` (que continua sendo só o relacionamento formal de CRM/mensagens/visibilidade granular). **O que ainda não existe** é a granularidade mais fina "seguir só um projeto específico" (sem seguir o missionário inteiro) — a peça que falta pra isso continua sendo uma tabela leve tipo `highlight_followers (highlight_id, follower_user_id)`, não duplicar `partners`. Também ainda não existe um produtor de notificação de verdade pra atividade de feed (ver nota da seção 7.10) — o feed de hoje é 100% pull/query-based (paginado sob demanda), não push. `pledges`/`recurring_pledges` já carregam `highlight_id` quando a contribuição nasceu de um projeto específico, e `highlights.category` (migration 034) já dá um sinal de afinidade por assunto — ambos já consumidos pela heurística de ranking do feed (7.1-bis).

**Editor de História (2026-08-08).** Até então `history_blocks` (`who_we_are`/`our_calling`/`timeline`/`cta`/`text`, `content JSONB` livre por tipo — schema em `src/types/history.ts`, **não** em `src/types/database.ts` junto dos outros tipos de tabela) tinha RLS de escrita pra dono (`history_blocks_owner_all`, migration 001/002) mas **nenhuma UI de edição em lugar nenhum** — só existia leitura (`HistoryView`) e a tabela nunca tinha sido populada. Fechada a lacuna com um editor enxuto (não um CRUD genérico de blocos): `src/components/dashboard/settings/history-editor-form.tsx` expõe só os 4 tipos com heading conhecido pelo `HistoryView` como 4 seções fixas (título opcional + texto; "Linha do tempo" com lista de itens ano/texto, adicionar/remover, **reordenar por arrastar desde 2026-08-30 (ver Changelog)**) — sem trocar tipo, sem adicionar blocos repetidos, escopo deliberadamente menor que um CMS de blocos pra não superdimensionar uma feature com zero adoção até aqui. Seção vazia ao salvar = a linha daquele tipo é removida (`DELETE`), não fica um heading vazio publicado. Nova rota `/dashboard/configuracoes/historia`, alcançável por um link "Editar história" no fim do `ProfileForm` (só pra `user_role='missionary'`, mesma regra da aba pública) e por um botão equivalente na própria página pública `[username]/historia` quando `canEdit` (mesmo padrão de `getProfileViewerContext` já usado no resto da seção 7.12). Mesmo dia, mesmo padrão: `[username]/projetos` ganhou um botão "Criar projeto" (→ `/dashboard/projetos/novo`) visível só pra `canEdit`, que essa página pública ainda não checava.

**Tradução PT/EN/ES (migration `061_history_blocks_translations.sql`, 2026-08-30).** Único editor de conteúdo do app sem nenhum suporte a tradução até aqui (posts/bio/projeto já tinham via `LocaleContentTabs`) — reportado pelo usuário direto na tela ("em nenhum lugar tem um botão para traduzir"). Fechado reaproveitando a infra existente sem inventar padrão novo: `history_blocks` ganhou `original_locale` (coluna nova, `default 'pt'`, um só valor por formulário — as 4 seções são editadas juntas, não faria sentido cada uma ter um idioma original diferente); as traduções em si **não** viraram colunas novas, foram direto pra dentro do `content` JSONB que já existia (`title_translations`/`text_translations`, mesmo formato `ContentTranslation` — `{content, source, translated_at}` — usado em `highlights`), já que esse campo já era livre por tipo. `who_we_are`/`our_calling`/`cta` usam `LocaleContentTabs` direto (2 instâncias por seção — título e texto, `rows={1}`/`rows={4}`, mesmo padrão de `highlight-form.tsx`). `timeline` é o caso especial: cada item da lista carrega sua própria tradução (`items[].text_translations`), **não** um array paralelo indexado por posição — decisão direta do reorder por arrastar adicionado horas antes na mesma sessão (um array paralelo ficaria dessincronizado a cada drag). UI dos itens virou compacta, no padrão já usado por `MilestonesEditor` (ícone 🌐 expande um mini-painel com input + botão traduzir por idioma) em vez de `LocaleContentTabs` completo, que não cabe numa linha de lista. Lado público: `HistoryView`/`[username]/historia/page.tsx` não resolviam nenhuma tradução ainda (só liam `content.title`/`content.text` cru) — corrigido com `resolveLocalizedText` (já existente, usado por projetos) por bloco e por item de timeline; `HistoryView` ganhou prop `visitorLocale` (repassada por `profile-content-tabs.tsx` e pela página). De quebra, corrigido: `historia/page.tsx` tinha o `<h1>`/subtítulo hardcoded em PT (`"História de {nome}"`/`"A jornada por trás da missão."`) apesar de já existir a chave i18n certa (`PublicProfile.historiaTitle`/`historiaDescription`, já usada só na `<meta>` de SEO) — agora o texto visível também usa `t(...)`, senão o cabeçalho ficava em português mesmo navegando em EN/ES.

**Conteúdo real traduzido, mesmo dia** — a pedido do usuário ("traduza tudo desta página... de forma natural"), os 4 blocos existentes do perfil "Família Silva" (título/texto de Quem somos e Convite, o texto longo em markdown de Nosso chamado, os 8 itens da linha do tempo) foram traduzidos pra EN/ES e gravados direto via SQL (`source: 'ai'`) — não passou pelo botão "Traduzir com IA" do app (que descontaria créditos de IA da conta), foi uma correção administrativa pontual. Verificado com Playwright nas 3 línguas via cookie `NEXT_LOCALE`: conteúdo troca certo, sem erro de console — a única coisa que não muda de idioma é o corpo do markdown de "Nosso chamado" em si, porque `HistoryView` sempre renderizou esse campo como `<p>` puro, sem parser de markdown (`##`/`**`) — os símbolos aparecem literalmente na tela, em qualquer idioma; isso já acontecia antes desta mudança (o autor colou markdown num campo que nunca soube interpretar) e não fazia parte do pedido, só fica registrado aqui pra não ser confundido com bug da tradução.

**Ajustes de UX na mesma tela, mesmo dia** — dois pedidos do usuário direto na tela: (1) campo de texto longo ("Nosso chamado") ocupava altura excessiva porque a classe base de `ui/textarea.tsx` usa `field-sizing-content` (CSS `field-sizing`, cresce pra caber **todo** o conteúdo, sem scroll nem alça de redimensionar manual) — pra textos curtos isso é o comportamento desejado (usado em todo o resto do app), mas pra um campo de várias telas de altura, virou um bloco enorme. Corrigido só nos 3 campos de texto multi-linha do editor de história (`textareaClassName="field-sizing-fixed resize-y"`, via `tailwind-merge` sobrescrevendo a classe base só ali) — volta ao textarea tradicional (altura inicial pelos `rows`, alça de arrastar no canto pra crescer manualmente), sem tocar o componente compartilhado `Textarea` nem qualquer outro formulário do app. (2) A tela (`/dashboard/configuracoes/historia`) estava alinhada à esquerda em vez de centralizada — `dashboard/layout.tsx` já centraliza `{children}` num `max-w-5xl mx-auto`, mas o `max-w-lg` do formulário (dentro dessa faixa já centralizada) não tinha o próprio `mx-auto`, então ficava colado à esquerda dentro da faixa. Corrigido movendo `max-w-lg mx-auto` pro `<div>` externo da página (cobrindo cabeçalho + formulário juntos, pra não desalinhar um do outro). **Resíduo notado pelo usuário na sequência**: essa rota não tinha `loading.tsx` próprio, então herdava o esqueleto genérico de `configuracoes/loading.tsx` (`<SkHeader/>`+`<SkForm/>` sem largura/centralização nenhuma) — o carregamento piscava colado à esquerda e só "pulava" pro centro quando a página real terminava de montar. Corrigido com um `loading.tsx` próprio pra `configuracoes/historia/`, envolvendo o mesmo esqueleto no mesmo `max-w-lg mx-auto` da página real, sem salto visual.

**Aba Seguidores/Seguindo no perfil (migration 037 — ver Changelog 2026-07-22).** Fechada a lacuna acima: agora existe uma rota `[username]/seguidores/page.tsx` (só visível/acessível quando `user_role='missionary'`, gated na `ProfileTabs` por `isMissionary` e de novo dentro da própria rota — defesa em profundidade, já que a rota é navegável direto por URL), com abas Seguidores/Seguindo via `?tab=` e `FollowsList` (client, "carregar mais" com o mesmo padrão de `feed-list.tsx`). Perfil `private` aplica o mesmo gate dono-ou-parceiro-confirmado da página principal do perfil (reforço deliberado — o grafo de follows é mais sensível que trajetória/projetos, que só bloqueiam `stealth`). A "distinção sutil" pedida entre quem o missionário segue e por quem é seguido: na aba Seguindo, quando o **próprio dono do perfil** está olhando (`isOwnerView`), cada pessoa que também o segue de volta ganha um texto sutil "Segue você" (mútuo, calculado por `getFollowerProfileIds` cruzado em memória — mesmo padrão de `Set` já usado em `discover-missionaries.tsx`, sem N+1 no SQL); na aba Seguidores, o próprio `FollowButton` troca o label pra "Seguir de volta" nesse mesmo caso (`followsViewer` prop, opcional, não quebra os usos existentes do botão). Fora do `isOwnerView` (visitante olhando o perfil de outra pessoa) esse cálculo é pulado — "Segue você" só faz sentido relativo a quem está de fato sendo seguido, então mostrar isso pra um terceiro visitante seria ambíguo.

### 7.13 Faixa de "stories" de projetos seguidos no feed

`getFollowedProjectStories()` (`src/app/dashboard/feed/actions.ts`) monta a faixa horizontal renderizada no topo de `FeedScreen` (`ProjectStoriesRow`, seção 10 `dashboard/feed/`): reaproveita o mesmo cálculo de `profileIds` (seguidos + parceiros) de `getFeedPage`, busca `highlights` ativos desses perfis, busca os `posts` mais recentes desses projetos (`project_id IN (...)`, agrupados em JS por "latest per group" — Supabase não faz isso nativamente) e cruza com `project_story_views` (seção 3.1) pra marcar `hasUnseen`. **Não existe conteúdo de "story" separado** — é literalmente a mesma tabela `posts` já usada no feed principal e nas "Atualizações" de `[username]/projetos/[slug]`, só filtrada por projeto e reordenada cronologicamente pro visualizador.

`ProjectStoryViewer` (client) é tap-to-advance (sem timer automático de avanço, ao contrário do Instagram) — avança entre os posts do projeto atual e, no fim, passa pro próximo projeto da faixa; chama `markProjectStoryViewed(highlightId)` (upsert em `project_story_views`) ao abrir/avançar de projeto. Barra fixa "Ver projeto completo" sempre linka pra `/[username]/projetos/[slug]` — o visualizador é um resumo rápido, não substitui a página completa (marcos, orçamento, formas de apoiar).

### 7.14 Busca (`/dashboard/buscar`, migration 2026-07-23)

Motivado por feedback direto do usuário: não havia nenhum lugar visível pra procurar pessoas e projetos — `DiscoverMissionaries` (7.1-bis) só aparece como estado vazio do feed (some assim que a pessoa segue alguém) e só lista missionários, sem campo de busca por nome. Benchmark rápido: Instagram/TikTok reservam um slot fixo na bottom nav pra busca porque descoberta é central ao produto deles; LinkedIn/X, com bottom nav mais enxuta e especializada (mesma situação daqui — 5 slots já ocupados, ver `useBottomNavItems`), usam um ícone de lupa fixo no cabeçalho em vez disso. Adotado o padrão LinkedIn/X: ícone de busca em `dashboard/layout.tsx` (dentro do `<header>`, ao lado de `ThemeToggle`/`NotificationsBell` — visível em toda tela do dashboard, mobile e desktop, não só na home), linkando pra `/dashboard/buscar`.

`src/app/dashboard/buscar/page.tsx` é Client Component (busca instantânea enquanto digita, sem necessidade de submit) — debounce de 300ms num único `useEffect` (`setTimeout` + `searchDirectory`), com `cancelled` flag no cleanup pra descartar resposta de uma busca anterior que ainda não voltou. **Nota de implementação**: o `setState` só acontece dentro do callback do `setTimeout`/`.then()`, nunca síncrono no corpo do effect — a regra `react-hooks/set-state-in-effect` do eslint barra updates síncronos ali (mesmo o clássico "setLoading(true) antes do fetch"), então o padrão certo neste código é sempre mover o efeito colateral pra dentro de um callback assíncrono. `searchDirectory` (`feed/search-actions.ts`, seção 10) busca em paralelo `profiles` (nome/username/bio) e `highlights` (título/descrição), renderizados em duas seções (Missionários/Projetos). Resultados de missionário reaproveitam `FollowButton` — diferente do feed (7.1-bis), aqui cada pessoa aparece uma única vez por busca, então não existe o risco de estado duplicado que motivou remover o botão dos posts.

**Busca difusa/tolerante a acento (migration `059_fuzzy_directory_search.sql`, 2026-08-30).** `searchDirectory` parou de usar `ILIKE` direto (falhava em "familia" vs "Família" — acento é caractere diferente pro Postgres, sem tokenização de múltiplas palavras) e passou a chamar duas funções SQL, `search_missionaries`/`search_highlights` (`SECURITY INVOKER`, respeitam a RLS normal de `profiles`/`highlights` como as queries antigas já respeitavam), via `supabase.rpc(...)`. As funções usam a extensão `pg_trgm` (similaridade por trigrama) sobre texto normalizado por `unaccent` — como o `unaccent()` de fábrica é `STABLE` (não pode entrar em índice funcional), existe um wrapper `immutable_unaccent()` (`IMMUTABLE`, mesmo workaround padrão do Postgres pra isso) usado tanto nos índices GIN (`idx_profiles_display_name_trgm` etc.) quanto dentro das funções. Cada função ranqueia por `GREATEST(similaridade dos campos, com pesos)`, prioriza match direto (substring já sem acento) e, quando não há nenhum, ainda inclui resultados "parecidos" acima de um piso de similaridade (`0.15`) — pedido explícito do usuário: buscar "familia silva" deve mostrar "Família Oliveira" logo depois de "Família Silva" em vez de "Nada encontrado", do jeito que plataformas grandes fazem. `search_highlights` já devolve os campos do perfil dono via `JOIN` (username/display_name/accent_color) e aplica o filtro de visibilidade (`privacy_mode`/`user_role`/`verification_status`/`account_status`) direto no SQL, então a action não precisa mais filtrar em JS depois do join nem buscar em dobro pra compensar linhas descartadas.

**Bug do painel vazio durante o carregamento (2026-09-07).** Tanto o dropdown de `HeaderSearch` quanto `/dashboard/buscar` mostravam a caixa de resultado (borda/sombra, no dropdown) **vazia** por um instante — nenhuma das condições existentes cobria o estado "já digitou 2+ letras, mas a resposta da busca ainda não voltou" (`loading === true`): não é "sem resultado" (isso exige `!loading`), e as listas de missionários/projetos ainda estão vazias. Corrigido com um spinner (`Loader2`) explícito pra esse estado nos dois lugares — antes o usuário via só uma caixa em branco e achava que era bug de renderização (era, só que de estado faltando, não de CSS).

**Pendência — sugestões personalizadas na busca vazia (antes de digitar)**: pedido do usuário, ainda não implementado. Bench rápido: Instagram/Twitter misturam conteúdo de quem já se segue + "amigos de amigos" (2º grau de rede) + afinidade de tópico; LinkedIn/X pesam quase tudo em 2º grau de rede. Proposta discutida (não implementada): (1) por categoria — `highlights.category` já existe (`ProjectCategory[]`), dá pra sugerir projetos de missionários ainda não seguidos nas mesmas categorias dos projetos que a pessoa já apoia (via `pledges`/`recurring_pledges` → `highlight_id` → `category`); (2) por rede — missionários seguidos por quem a pessoa já segue (`follows` cruzado), que ela ainda não segue. Precisa de query nova + ranking + UI no estado vazio de `buscar/page.tsx`/`HeaderSearch` (hoje só mostra um texto de prompt genérico ali).

### 7.15 `/explorar` — feed público de descoberta, sem login (2026-08-08)

Só existia feed de posts dentro do dashboard (exige login) e páginas de perfil/projeto individuais — nenhum lugar público pra "só olhar o que está rolando" sem cadastro. Motivado por um bug reportado pelo usuário: o `BackButton` (seção 11), ao cair no fallback por falta de histórico real (ex.: link de projeto compartilhado aberto direto numa aba nova), levava pra `/` — a landing de vendas, deslocada do que a pessoa esperava ver depois de "voltar" de um projeto. Decisão (confirmada com o usuário, não substitui a landing): página nova e separada, não uma troca da landing atual.

`src/app/explorar/page.tsx` (Server Component, sem auth) — duas seções, "Projetos que precisam de apoio" (`highlights.status = 'active'`, cards com progress bar, `grid grid-cols-2`) e "Publicações recentes" (reaproveita `ProfilePostsGrid`/`PostDetailViewer`, os mesmos do grid do perfil — já degradam bem pra visitante anônimo: curtir/comentar sem sessão falha silenciosamente com toast de erro e reverte o estado otimista, sem crash nem redirect forçado pra login), organizadas via `ExploreTabs` (`src/app/explorar/explore-tabs.tsx`, `'use client'`) — mesmo seletor estilo Instagram (só ícone+label, troca o conteúdo abaixo sem navegar) do `ProfileContentTabs` do perfil, adaptado aqui porque o conteúdo cruza múltiplos perfis (não dá pra reaproveitar o componente original, que espera dados de um perfil só) — pedido do usuário pra tirar a hierarquia implícita de "projetos sempre antes de posts", deixando a pessoa escolher o que ver primeiro. Conteúdo sempre filtrado a `profiles.privacy_mode = 'public'` (nunca `private`/`stealth`) — busca os ids de perfis públicos numa query separada e depois `in('profile_id', ids)` nas duas tabelas (`highlights`/`posts`), padrão já usado em `getFeedPage` (`dashboard/feed/actions.ts`) em vez de tentar filtrar via `!inner` join (não há precedente disso no código pra coluna de uma tabela relacionada). `SiteNav`/`SiteFooter` (mesmos da landing) dão o "botão pra ir pra tela de contratação" pedido pelo usuário — `Entrar`/`Criar conta` já aparecem em ambos, sem precisar de um CTA extra dedicado.

**Capa obrigatória na criação + fallback pra projetos antigos sem capa (2026-08-08)**: reportado pelo usuário vendo cards vazios/cinza em `/explorar`. `src/components/highlights/project-cover-fallback.tsx` (`ProjectCoverFallback`) preenche o espaço com um gradiente sutil (`from-primary/15 via-muted to-support/15`) + o emoji da primeira categoria do projeto (`highlights.category`, via `src/lib/highlights/project-categories.ts` — `PROJECT_CATEGORIES` foi extraído de dentro de `highlight-form.tsx` pra esse módulo neutro, mesma lição do `project-status.ts` de 2026-08-08: constante `export`ada de um arquivo `'use client'` importada num Server Component quebra em runtime dev, mesmo passando no build) ou um `ImageIcon` genérico quando não há categoria. Aplicado em `/explorar`, no hero do projeto e na grade "Trajetória" (`[username]/projetos/[slug]/page.tsx`), e em `trajectory-timeline.tsx`. **Daqui pra frente, capa passou a ser obrigatória na criação** (não retroativo — projetos antigos sem capa continuam existindo e editáveis, só ganham o fallback visual): validado no client em `highlight-form.tsx`/`use-project-composer.ts` (mesmo padrão do título obrigatório) e como rede de segurança no server em `/api/highlights` (`POST`, só quando `!highlightId` — criação; edição de projeto antigo sem capa não é bloqueada).

`ProfileTabs` (`src/components/profile/profile-tabs.tsx`) — fallback do `BackButton` pra visitante anônimo (`viewerUserId` nulo) trocou de `'/'` pra `'/explorar'`; dono logado continua caindo em `/dashboard`.

**Grade de posts alinhada ao Instagram de verdade (2026-08-08)**: usuário mandou prints lado a lado do Instagram vs. `/explorar`/perfil apontando duas diferenças. `src/components/shared/profile-posts-grid.tsx` (`ProfilePostsGrid`, compartilhado entre `/explorar` e `[username]/page.tsx`): (1) célula da grade era `aspect-[4/5]` — virou `aspect-square`, igual à grade real do Instagram (mesmo reels/carrossel são cortados quadrados ali). (2) Post só-texto (sem `media_urls`) renderizava texto cru sobre um `bg-muted` cinza chapado — agora usa a `accent_color` do perfil como fundo (+ gradiente escuro sutil só pra garantir contraste do texto branco em cima, já que a cor é escolhida livremente) — todo item da grade passa a "ser uma imagem" de verdade, nunca texto solto, mesmo princípio já usado no fallback de capa de projeto. `/explorar/page.tsx` também ganhou breakpoints (`max-w-2xl lg:max-w-4xl` no container, `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4` nos cards de projeto) — antes ficava com a largura fixa de mobile mesmo em desktop, apesar de reaproveitar `SiteNav`/`SiteFooter` (que são responsivos).

**Próximo passo já sinalizado pelo usuário, ainda não implementado**: reestruturar posts pra imagem ser *sempre* obrigatória na criação (não só o fallback visual da grade) — a descrição/texto vira só o que aparece ao abrir o post, nunca a representação principal dele. Anotado, não faz parte desta rodada.

**Gap conhecido, não corrigido nesta rodada**: não existe lista de "palavras reservadas" pro `username` — alguém pode se cadastrar como `explorar`, `login`, `planos` etc. e a rota estática sempre vence sobre `/[username]` (Next.js resolve segments estáticos antes de dinâmicos), então o perfil dessa pessoa fica silenciosamente inacessível por username, sem erro visível. Pré-existente (já valia pra `/login`/`/cadastro`/`/dashboard`/`/planos`), só ficou mais visível ao adicionar `/explorar` — validação em `onboarding-wizard.tsx`/`profile-form.tsx` seria o lugar certo pra uma correção futura.

### 7.16 Confiança e segurança: verificação de missionário, denúncias e moderação (migration 056, 2026-08-30)

Não existia nenhuma infraestrutura de confiança/segurança: `becomeMissionary()` virava missionário na hora sem checagem nenhuma, não havia filtro automático de nudez/discurso de ódio, e não havia botão de denúncia nem fila de revisão. Motivado por pergunta direta do usuário sobre como impedir gente fora do meio missionário de "fazer parte do processo" e como conter nudez/pornografia/discurso de ódio em posts e comentários.

**Schema** (`profiles.verification_status` `'pending'|'approved'|'rejected'` default `'approved'` — o default já fez grandfathering de todo perfil existente; `profiles.account_status` `'active'|'hidden_pending_review'|'suspended'` default `'active'`; `posts.moderation_status` e `post_comments.moderation_status` `'visible'|'hidden_pending_review'|'removed'` default `'visible'`; tabela nova `reports` — `reporter_user_id`, `target_type` (`'post'|'comment'|'profile'`), `target_id`, `target_profile_id` (denormalizado no insert: dono do conteúdo denunciado, ou o próprio perfil quando `target_type='profile'` — existe pra contar denúncias contra um autor por qualquer motivo sem join caro), `reason`, `details`, `status` `'open'|'dismissed'|'actioned'`, `UNIQUE(reporter_user_id, target_type, target_id)` evita denúncia duplicada da mesma pessoa inflando contagem. RLS de `reports`: só INSERT do próprio `reporter_user_id`, sem SELECT/UPDATE pra usuário comum — só service-role lê/atualiza, mesmo padrão write-only de `feed_events`).

**Gate de missionário**: `becomeMissionary()` (`src/app/dashboard/actions.ts`) grava `verification_status='pending'` junto com o flip de `user_role`. Perfil pendente some de `/explorar`, da busca (`search-actions.ts`) e do `DiscoverMissionaries` (`feed/actions.ts`), e a página pública `[username]` fica bloqueada pra quem não é dono (`UnderReviewScreen`, mesmo padrão do `PrivateProfileScreen` já existente) — sem isso a pessoa receberia doação via link direto mesmo sem aprovação. `approveMissionary`/`rejectMissionary` (mesmo arquivo) só funcionam pra `isSuperAdmin`; rejeitar também volta `user_role` pra `'partner'` (evita limbo permanente).

**Moderação automática de texto**: `checkTextModeration()` (`src/lib/ai/moderate-text.ts`) reaproveita o Claude já usado pra tradução (`getAnthropicClient()`, `claude-haiku-4-5`, mesmo padrão de `output_config.format: json_schema` de `translateContent`) em vez de introduzir um vendor novo (OpenAI) só pra isso — classifica em `hate_speech`/`sexual_content`/`harassment`/`violence`/`none`. Fail-open: erro/timeout (inclusive sem `ANTHROPIC_API_KEY` configurada, caso comum em dev) deixa passar em vez de travar publicação, mesmo espírito de `sendEmail()`. Hookado em `savePost()` (`dashboard/publicacoes/actions.ts`) e `addComment()` (`dashboard/publicacoes/social-actions.ts`), lançando `Error('content_flagged')` — client trata pelo código da mensagem (`err.message`), mesmo padrão já usado por `access-managers-form.tsx` com `seat_limit_reached`. Só imagem/vídeo ficam de fora da automação (decisão explícita do usuário) — dependem 100% de denúncia.

**Denúncias e ocultação automática** (`reportContent()`, `src/app/dashboard/publicacoes/report-actions.ts`): 2+ denunciantes distintos no mesmo post/comentário ocultam só aquele item (`moderation_status='hidden_pending_review'`); 2+ denunciantes distintos somando **qualquer** denúncia (post, comentário ou perfil) contra o mesmo autor ocultam a **conta inteira** (`profiles.account_status='hidden_pending_review'`) — decisão explícita do usuário, não só o item. Ambos os níveis são soft: dono continua logando e vendo aviso "em análise" (`ProfileCTA`/`post-card.tsx`/`post-comments-sheet.tsx`), só desaparecem pra terceiros — proteção deliberada contra "brigading" (conluio denunciando conteúdo legítimo), a decisão final de remover/restaurar é sempre humana. `can_view_post()` (redefinida, era da migration 038) passou a checar `moderation_status`/`account_status` com bypass total pra `is_profile_owner`; `posts_public_read` e `post_comments_read` (RLS) redefinidas na mesma linha — dono e gestores de conta sempre veem tudo, só o branch "público" exige item/conta em dia. Toda query de listagem de posts pra exibição (`feed/actions.ts`, `explorar/page.tsx`, `[username]/page.tsx`, `[username]/projetos/[slug]/page.tsx`, `dashboard/painel/page.tsx`) ganhou `.neq('moderation_status', 'removed')` — necessário porque `posts_owner_all` (RLS, não tocada) dá acesso total ao dono independente de status, então "removido" só desaparece de verdade se todo mundo (inclusive o dono) filtrar explicitamente na query.

**Botão de denúncia** (`ReportDialog`, `src/components/shared/report-dialog.tsx`, reaproveitado por post/comentário/perfil): menu "..." do post (`post-card.tsx`) agora sempre renderiza — Editar/Arquivar/Excluir pro dono, "Denunciar" pra visitante; comentários (`post-comments-sheet.tsx`) ganharam o mesmo botão ao lado de Editar/Excluir; perfil ganhou "Denunciar perfil" dentro do `ProfileMoreMenu` (menu "..." da barra de abas do perfil público, só quando `!canEdit` — seção 10, `profile/`), depois que o `ReportProfileButton` avulso que vivia no rodapé de `ProfileCTA` foi removido por ficar redundante (ver Changelog).

**Área de revisão do superadmin** (`/superadmin/moderacao`, pedido explícito do usuário — primeira rota admin real do app, `isSuperAdmin` checado no servidor com `redirect('/dashboard')`, não só escondido no client como o `SuperadminRoleSwitcher`): três abas (`ModerationQueue`, `src/components/superadmin/moderation-queue.tsx`) — Verificações pendentes (Aprovar/Rejeitar), Conteúdo denunciado (Remover definitivamente/Restaurar/Suspender autor, com motivos agregados via `reports.reason`), Contas denunciadas (Suspender definitivamente/Restaurar). Ações em `src/app/superadmin/actions.ts`, todas reconferindo `isSuperAdmin` no servidor antes de escrever, mesmo padrão defensivo de `setPreviewRole`. `SUPERADMIN_EMAILS` (env var, allowlist já existente desde 7.1-bis) ganhou `johnsonwel.assis@gmail.com` além de `silaspereiras@gmail.com`.

**Verificado nesta rodada**: build (`npm run build`) e `tsc --noEmit` limpos; acesso não-autenticado a `/superadmin/moderacao` confirmado redirecionando (`NEXT_REDIRECT;replace;/dashboard;307` → middleware manda pra `/login`); `checkTextModeration` falha (sem `ANTHROPIC_API_KEY` neste ambiente de dev) e cai no fail-open como esperado. **Não verificado end-to-end em navegador** (sem sessão de teste disponível nesta sessão): fluxo completo de denúncia → auto-hide → fila do superadmin → restaurar/remover/suspender, e o gate de missionário pendente na prática. Recomendado testar manualmente antes de considerar a feature pronta pra uso real.

**Extensão — dashboard admin, badge global e gênero (migration 057, mesmo dia)**: pedido de acompanhamento do usuário — botão de acesso à área admin sempre visível (não só dentro de `/superadmin`) e um painel de métricas do sistema além da fila de moderação.

- **Gênero** (`profiles.gender`, `'male'|'female'|'unspecified'` default `'unspecified'`): não existia. Adicionado a pedido do usuário — pergunta opcional no passo `role` do `OnboardingWizard` (3 pills antes dos cards de papel, grava junto com `user_role` num update só em `chooseRole()`), com "prefiro não informar" como opção explícita. **Não editável ainda em Configurações** (só capturado no onboarding) — próximo passo se o usuário quiser. Backfill dos 17 perfis existentes (todos de teste, exceto Johnson/Silas/Izabela confirmados reais) inferido pelo nome cadastrado, a pedido explícito do usuário — hardcoded por `id` na própria migration (não repetível/genérico, é um dado histórico de uma vez só).
- **Timestamps de status** (`profiles.verification_requested_at`, `profiles.account_status_changed_at`): sem eles, "há quantos dias está pendente" teria que usar `updated_at` (muda em qualquer edição de perfil, não só troca de status — métrica errada). Gravados em `becomeMissionary()` (verification) e em `reportContent()`/`suspendAccount()`/`restoreAccount()`/`toggleAccountStatus()` (account). `daysSince()` (novo em `src/lib/utils.ts`) computa o número explícito — diferente de `formatRelativeTime()`, que vira data formatada depois de 7 dias e perde a contagem justo no caso (pendência antiga) que mais importa mostrar.
- **`/superadmin/layout.tsx`** (novo): centraliza o gate `isSuperAdmin` + nav de abas (`SuperadminNav` — Painel/Moderação/Usuários) que antes só existia dentro de `moderacao/page.tsx`; essa página teve o gate duplicado removido.
- **`/superadmin` (Painel, novo)**: cards de estatística (`StatCard`/`StatSection`, `src/components/superadmin/stat-card.tsx`) via contagens `head:true` em paralelo — total de usuários, missionário×parceiro, gênero, visibilidade de perfil (público/privado/discreto — responde diretamente "quantos têm página pública, quantos não"), pendências de moderação (verificação/contas ocultas/suspensas/denúncias abertas), atividade (posts publicados, projetos ativos), novos cadastros (7/30 dias). Duas listas "5 mais antigas" (verificações pendentes e contas sinalizadas) já com `daysSince()`, linkando pra `/superadmin/moderacao`/`/superadmin/usuarios`.
- **`/superadmin/usuarios` (novo)**: lista todos os perfis (até 500, sem paginação real — porte atual do sistema não justifica ainda) com busca client-side por nome/username (`UsersTable`, filtro em memória, sem round-trip); cada linha mostra badges (papel, gênero, visibilidade, verificação, status de conta) e um botão Suspender/Reativar direto (`toggleAccountStatus()`, novo em `dashboard/actions.ts`) — diferente da fila de denúncias (`/superadmin/moderacao`), aqui o superadmin pode agir sobre **qualquer** conta, não só as que já bateram 2 denúncias.
- **Badge global "Área admin"** (`AdminLinkBadge`, `src/components/superadmin/admin-link-badge.tsx`): Server Component montado direto no `RootLayout` (`src/app/layout.tsx`), não em `dashboard/layout.tsx` — por isso aparece em qualquer página (pública, checkout, dashboard, o próprio `/superadmin`), pedido explícito do usuário. `fixed bottom-20 right-4 md:bottom-4`, canto inferior direito: único ponto fixo livre encontrado — centro-inferior já tem `SuperadminRoleSwitcher`/`FloatingSupportCta` (ambos `left-1/2 -translate-x-1/2`) e topo já tem `ProfileTabs`/`CheckoutHeader` (`fixed top-3 inset-x-0`, largura cheia). Só renderiza quando `isSuperAdmin`, mesmo allowlist `SUPERADMIN_EMAILS` de sempre.

**Por que `SUPERADMIN_EMAILS` precisa estar configurada também na Vercel** (dúvida do usuário): `.env.local` só existe na máquina de desenvolvimento local — o `next dev` desta sessão lê esse arquivo, mas o site em produção (`mission-guide.vercel.app` ou domínio custom) roda em servidores da Vercel, que nunca veem esse arquivo (nem deveriam, já que normalmente nem é commitado no git). A Vercel tem seu próprio painel de variáveis de ambiente (Project Settings → Environment Variables) que precisa ser configurado separadamente pra cada ambiente (Production/Preview/Development) — é o mesmo motivo de `SUPABASE_SERVICE_ROLE_KEY`/`ANTHROPIC_API_KEY`/etc. já existirem nos dois lugares.

### 7.17 Estabilidade de link ao renomear (migration 058, 2026-08-30)

Motivado por um 404 real: o usuário editou o título de um projeto, o slug mudou junto (derivado do título em `/api/highlights` a cada save, criação **e** edição), e o link já compartilhado do projeto quebrou. A mesma lacuna existe pro `username` do perfil (editável em Configurações/onboarding, sem histórico) — o link público inteiro de alguém (usado em doação, oração, mensagem) depende dele.

**Decisão**: não trocar URL por ID aleatório (link feio não passa confiança num app que vive de link de doação compartilhado) — em vez disso, manter a URL legível como canônica e redirecionar quem cair no link antigo, mesmo padrão Instagram/Twitter/GitHub.

- **`profiles.slug` de projeto congelado na criação**: `/api/highlights` (`POST`) só grava `slug: slugify(title)` no branch de criação (`!hId`) — editar o título de um projeto existente não toca mais o `slug`. Não existe (nem existia) campo de edição de slug na UI, então esse é o único lugar que o alterava.
- **`profile_username_history`/`highlight_slug_history`** (tabelas novas, RLS `FOR SELECT USING (true)` — mapeamento não é dado sensível, precisa ser lido pelo client anônimo pro fallback funcionar): escritas por **trigger** (`track_username_change`/`track_highlight_slug_change`, `BEFORE UPDATE ... WHEN OLD.<campo> IS DISTINCT FROM NEW.<campo>`, `ON CONFLICT` atualiza o ponteiro em vez de duplicar) em vez de código de aplicação — cobre qualquer caminho de escrita, presente ou futuro (`profile-form.tsx`, `onboarding-wizard.tsx`, e qualquer editor que apareça depois), sem duplicar lógica em cada form. Como o slug de projeto ficou congelado no ponto acima, `highlight_slug_history` fica como rede de segurança (não é alimentada pelo fluxo normal hoje, só protegeria uma mudança futura ou manual) — `profile_username_history` já é ativamente alimentada, já que username continua editável.
- **Leitura com fallback**: `getProfileOrRedirect(username, suffix)` (novo em `src/lib/profile/get-profile.ts`) — tenta `getProfile()` direto (caminho comum, sem custo extra); se não achar, consulta `profile_username_history` e, se achar, `redirect(`/${usernameAtual}${suffix}`)` em vez de deixar a página cair em `notFound()`. Cada uma das 9 `page.tsx` da árvore `[username]/*` (mais `getPartnershipData`, compartilhada entre a página cheia de `/parceria` e o modal interceptado) passa o próprio sufixo de rota (`/historia`, `/projetos/${slug}`, `/seguidores?tab=${tab}` etc.) — só a chamada de `generateMetadata()` de cada arquivo continua usando `getProfile()` puro (metadata não precisa redirecionar, só evita indexar título errado). `[username]/projetos/[slug]/page.tsx` tem o mesmo padrão inline pro `highlight_slug_history`, já que ali há dois identificadores (username E slug) podendo ter mudado.
- **Reivindicar o `@username` antigo de OUTRA pessoa passou a ser bloqueado**: `checkUsernameAvailability()` (novo em `src/lib/username.ts`, usado por `onboarding-wizard.tsx` e `profile-form.tsx` no lugar da query inline que cada um tinha) marca como `'taken'` tanto um `username` ativo de outro perfil quanto um `old_username` de outro perfil no histórico — sem essa checagem, alguém poderia registrar o `@handle` antigo de um missionário que trocou de nome e sequestrar o link de doação dele já espalhado por aí. Reclamar o **próprio** username antigo de volta continua permitido. De quebra, `RESERVED_USERNAMES` (`src/lib/username.ts`) ganhou `explorar`/`superadmin` — faltavam desde que essas rotas estáticas foram criadas (mesmo gap já documentado na seção 7.15, agora fechado pros dois casos novos).

**Verificado nesta rodada, direto contra o banco real** (não só build): renomeei `familia-silva` → `familia-silva-novo` e confirmei `curl /familia-silva` devolvendo `NEXT_REDIRECT;replace;/familia-silva-novo` (e o mesmo preservando o sufixo em `/familia-silva/projetos/<slug>`); renomeei de volta e confirmei que o histórico guarda os dois valores sem conflito. Mesmo teste pro slug de projeto (`highlights.slug` trocado direto no banco → `curl` no slug antigo redireciona pro novo). Confirmei também que o client anônimo (RLS) consegue ler `profile_username_history` (necessário pro fallback funcionar sem sessão). Build e lint limpos.

### 7.18 Links curtos pra bio (migration `070_short_links.sql`, 2026-09-01)

Pedido do usuário: os missionários colocam o link do sistema na bio do Instagram, e `/[username]/projetos/[slug]` fica longo demais pra isso. Decisão de escopo (perguntado ao usuário): reaproveitar o domínio atual do app (sem domínio curto dedicado) e cobrir só perfil/projeto — não um encurtador genérico de qualquer URL.

- **`short_links`** (`id`, `profile_id`, `code` único, `target_type` `'profile'|'project'`, `highlight_id` nullable, `click_count`): resolve o destino por **FK**, não grava o path final — sobrevive a troca de `username`/`slug` de graça, sem precisar duplicar a lógica de histórico da migration 058. Um link por alvo (índice único parcial em `profile_id`/`highlight_id` conforme `target_type`) — pedir de novo devolve o mesmo código em vez de acumular lixo. Trigger `check_short_link_highlight_owner` garante que `highlight_id` (quando presente) realmente pertence a `profile_id` — RLS só valida "profile_id é meu", não a consistência entre as duas colunas.
- **RLS fechada pro dono** (`is_profile_owner`, migration 023 — cobre gestor também): ninguém mais lê a tabela direto, ao contrário de `profile_username_history`/`highlight_slug_history` (058) que são `USING (true)`. Diferença deliberada: aquelas tabelas só expõem um mapeamento slug-antigo→id; `short_links` também carrega `click_count`, e leitura pública liberada exporia contagem de cliques de qualquer perfil/projeto da plataforma pra quem tiver a anon key.
- **Resolução pública via function**: `resolve_short_link(p_code)` (`SECURITY DEFINER`) devolve só `target_type`/`profile_id`/`highlight_id` pro código pedido e já incrementa `click_count` no mesmo `UPDATE ... RETURNING` (atômico, um round-trip). `GET /l/[code]/route.ts` chama essa function, resolve `username`/`slug` com o client normal (respeita RLS de `profiles`/`highlights` — perfil privado/stealth cai em `notFound()`, mesmo comportamento de acessar `/[username]` direto) e faz `NextResponse.redirect()` pro link canônico. `l` virou username reservado (`src/lib/username.ts`) pra não colidir com essa rota.
- **Geração**: `getOrCreateShortLinkCode()` (`src/lib/short-links.ts`) — busca-ou-cria, código de 6 chars sem `0/O/1/l/I`, retry em colisão de `code` (distingue "colisão de código" de "corrida no índice único do alvo", que é sucesso, não erro). Chamada direto do client (mesmo padrão de mutação do resto de `project-card-menu.tsx`, sem API route — só precisou de uma no meio do dia por causa da tentativa de encurtamento externo abaixo, revertida) — não revalida posse à mão: a autorização real é a RLS/trigger acima, que rejeita (`42501`/exceção) se quem chamou não puder mesmo gerar link pra aquele `profileId`/`highlightId`. UI: item "Link curto"/"Copiar link curto" no menu "..." do perfil público (`ProfileMoreMenu`, só quando `canEdit`) e no menu de ações do card de projeto (`ProjectCardMenu`, `src/components/highlights/`).
- **Encurtamento externo — tentado e revertido no mesmo dia (2026-09-01)**: sem domínio curto próprio (produção hoje é o domínio padrão da Vercel, `mission-guide.vercel.app`), cogitou-se embrulhar `/l/[code]` num encurtador público de terceiro. **is.gd** bloqueia `*.vercel.app` de propósito (confirmado em produção via `vercel logs` — falha 100% das vezes com "Error, database insert failed", não é instabilidade; domínio diferente encurta normal). **TinyURL** não bloqueia, mas força link criado sem conta por uma página de preview própria antes do redirect real (`x-tinyurl-redirect-type: preview`, sem opção de desligar na API gratuita) — ruim pra um link de bio, que devia ser um toque só. **Decisão final do usuário**: melhor ficar só com `/l/[code]` (mais longo, mas 100% domínio próprio) do que mandar quem vai doar por um domínio de terceiro, ainda mais com uma tela extra pelo caminho — quebra confiança justo no elo mais sensível do produto (link de doação). `short_links.external_short_url` (migration `073_short_links_external.sql`) ficou como coluna morta no banco, sem código lendo/escrevendo nela — inofensiva, não vale forçar mais uma migration só pra limpar.

> **Nota de numeração**: 7.19 abaixo (Stripe billing) e 7.20-7.23 (dashboard financeiro estilo GranaZen) foram escritas em paralelo por sessões diferentes, ambas originalmente numeradas 7.19 — renumeradas na integração (mesmo tipo de colisão da renumeração de migrations 074-077→075-078); nenhum conteúdo mudou, só os números de seção e as auto-referências internas de cada bloco.

### 7.19 Stripe billing da plataforma (planos + gestores extra) + bypass de superadmin (2026-09-01)

A pedido do usuário, motivado por uma conta pessoal (`familia-silva`, `plan='free'`) sem conseguir adicionar gestores ("0 de 0 gestores usados" em Configurações → Acesso — Free inclui 0 e `extra_manager_seats` era 0, sem nunca ter passado pelo Stripe). Duas frentes decididas juntas: (1) contas superadmin devem ter tudo liberado, sem depender de plano; (2) ativar de vez a cobrança Stripe, fechando a lacuna já documentada (seção 2/7.11-adjacente) entre o que `/planos` promete e o que o código realmente checava.

**Descoberta ao investigar**: Financeiro e mensagens/E2EE já estavam 100% liberados pra qualquer plano (nenhum gate no código, apesar do `/planos` listar como exclusivo do Pro), e `planLimits().postsPerMonth` (Free = 1/mês) nunca era checado em lugar nenhum — só a UI de parceiros/gestores/IA de fato aplicava limite. Corrigido: Financeiro e o limite de post/mês agora refletem o marketing; **mensagens/E2EE ficaram deliberadamente de fora** (decisão de escopo tomada nesta sessão, não confirmada com o usuário) — paywall em cima de segurança/comunicação básica parecia hostil ao produto; revisitar se o usuário discordar.

**Bypass de superadmin** (`isSuperAdmin()`, `SUPERADMIN_EMAILS`, já existente desde a seção 7.1-bis/7.16) passou a ignorar todo limite de plano, sempre checado no servidor:
- **Parceiros** (`add-partner-button.tsx`, prop `isSuperAdmin`) e **IA** (`aiPlanIncluded` calculado em `parceiros/page.tsx`, card de upgrade escondido em `dashboard/ia/page.tsx`): bypass simples, sem migration — o limite nunca foi enforced no banco, só na UI/Server Action.
- **Financeiro** (`financeiro/layout.tsx`): novo gate `profile.plan === 'free'` (card de upgrade, namespace i18n `FinanceGate`) pulado quando `isSuperAdmin(user.email)`.
- **Posts/mês** (`savePost`, `dashboard/publicacoes/actions.ts`): novo count de posts não-rascunho no mês corrente vs. `planLimits().postsPerMonth`, pulado pra superadmin; erro `post_limit_reached` tratado em `use-post-composer.ts` (toast `PostComposer.postLimitReached`, 3 línguas).
- **Gestores** (`profile_managers`) é o único limite hoje enforced **no banco** — RPC `invite_profile_manager` (`SECURITY DEFINER`, migration 023) checa `planLimits().managersIncluded + extra_manager_seats` em SQL, sem noção de superadmin (adicionar isso exigiria migration nova). Em vez de tocar a function, `AccessManagersForm` chama a RPC normalmente e, só quando ela recusa com `seat_limit_reached` **e** quem chamou é superadmin, cai numa Server Action nova (`inviteManagerBypass`, `src/app/dashboard/configuracoes/actions.ts`) que replica a mesma checagem de autorização (dono ou gestor `role='manager'`) e o mesmo lookup de `auth.users` por e-mail (paginando `service.auth.admin.listUsers()`, já que a REST admin API não filtra por e-mail — confirmado testando `?email=` direto, ignorado silenciosamente), mas insere em `profile_managers` via client de service-role sem checar assento. `AccessManagersForm` ganhou prop `isSuperAdmin` (calculada em `configuracoes/page.tsx`, repassada por `SettingsTabs`) só pra mostrar `total` como `∞` e não desabilitar o botão de convite — a autorização de verdade é sempre no servidor.
- **Créditos de IA** (`profiles.ai_credits`) são saldo consumível, não um limite booleano — não dá pra representar "∞" numa coluna numérica sem tocar `consume_ai_credits()` (SQL). Em vez de migration, o saldo da conta `familia-silva` foi ajustado direto pra 100000 via REST (`PATCH /rest/v1/profiles`, service role) — solução pontual pra essa conta, não um mecanismo geral; se surgir outra conta superadmin precisando de saldo, repetir manualmente ou then revisitar com uma migration.

**Ativação do Stripe (modo teste)**: `STRIPE_SECRET_KEY`/`STRIPE_PUBLISHABLE_KEY` já estavam preenchidas em `.env.local` (test mode) mas nenhum Price ID existia. Criados via API (script ad-hoc, não versionado) 6 objetos Price em modo teste: Pro mensal ($20 USD)/anual ($200 USD), Missão mensal ($45 USD)/anual ($450 USD), Gestores +2 (R$15 BRL/mês) e +4 (R$30 BRL/mês) — os dois últimos em BRL fixo, diferente do resto que usa USD + Adaptive Pricing (inconsistência já registrada na seção de Monetização, não corrigida). IDs preenchidos em `STRIPE_PRICE_*` (`.env.local`).

- **Novo webhook de billing da plataforma**: `POST /api/billing/webhook` (`src/app/api/billing/webhook/route.ts`) — separado do webhook de Stripe Connect (`/api/stripe/webhook`, que trata cobrança recebida *pelos* missionários, secret próprio `STRIPE_CONNECT_WEBHOOK_SECRET`). Endpoint registrado na Stripe via API apontando pra `https://mission-guide.vercel.app/api/billing/webhook`, escutando `checkout.session.completed`/`customer.subscription.updated`/`customer.subscription.deleted`; secret gerado (`STRIPE_WEBHOOK_SECRET`, preenchido em `.env.local`) — **ainda falta configurar esse valor e os `STRIPE_PRICE_*` na Vercel (Production)**, já que `.env.local` só existe nesta máquina (mesmo motivo já documentado pra `SUPERADMIN_EMAILS` na seção 7.16).
- `POST /api/billing/checkout` (`src/app/api/billing/checkout/route.ts`) ganhou `subscription_data: { metadata }` nas duas sessões (upgrade de plano e addon de gestor) — metadata de `checkout.session` não propaga sozinha pro objeto `Subscription`, e os eventos `customer.subscription.*` só recebem o `Subscription`, não a sessão original.
- **Lógica do webhook**: em `checkout.session.completed`, grava `profiles.stripe_customer_id` sempre; se `metadata.type==='plan'`, busca a subscription recém-criada (`stripe.subscriptions.retrieve`, `current_period_end` mora em `subscription.items.data[0]` nesta versão da API — não mais na raiz do objeto) e faz upsert em `subscriptions` (`onConflict: profile_id`) + `UPDATE profiles SET plan=...`; se `metadata.type==='manager_addon'`, soma `seats` em `profiles.extra_manager_seats` (lê-e-soma, sem idempotência contra retry duplicado do Stripe — risco aceito por ora, mesmo espírito pragmático de outras integrações do projeto). `customer.subscription.deleted` zera o plano pra `free` (subscriptions de plano) ou subtrai `seats` de volta (addons); `customer.subscription.updated` só resincroniza `status`/`current_period_end` de subscriptions de plano.
- **Não implementado nesta rodada**: idempotência forte do webhook (dedupe por `event.id`), fluxo de downgrade/upgrade de plano existente (trocar de Pro pra Missão sem cancelar antes), e qualquer coisa em modo `live` — tudo isso é modo teste, cartão de teste da Stripe. Também não foi possível testar o fluxo ponta-a-ponta num navegador real nesta sessão (exigiria login como o usuário de verdade, com senha/Google OAuth não disponíveis aqui) — `tsc`/`eslint`/`next build` passam limpos, mas o clique real em "Convidar"/"Assinar Pro" não foi validado.

**Aplicação em produção — passos que dependem do usuário** (nenhum acesso à Vercel nem à conta Stripe de verdade nesta sessão): colar em Vercel → Project Settings → Environment Variables (Production) os mesmos `STRIPE_WEBHOOK_SECRET`/`STRIPE_PRICE_PRO_MONTHLY`/`STRIPE_PRICE_PRO_YEARLY`/`STRIPE_PRICE_MISSION_MONTHLY`/`STRIPE_PRICE_MISSION_YEARLY`/`STRIPE_PRICE_MANAGERS_2`/`STRIPE_PRICE_MANAGERS_4` que já estão em `.env.local`, redeploy, e só então testar um checkout de verdade (cartão de teste `4242 4242 4242 4242`) — o webhook só recebe eventos de verdade depois desse deploy.

### 7.20 Saldo Previsto — navegador de mês no dashboard financeiro (migration `081_transaction_paid_status.sql`, 2026-09-02)

Pedido do usuário: reproduzir no `/dashboard/financeiro` (Visão Geral) o modelo de dashboard financeiro pessoal do **GranaZen** — outro produto do próprio usuário, vive na branch `master` deste repo (histórico git não relacionado), já fonte do cartão de crédito/fatura e do câmbio (seção 3.1) e dos primeiros gráficos da Visão Geral (2026-09-02, seção 11.1). Esta rodada porta o pedaço que faltava: navegador de mês com "Saldo Previsto".

> **Integração com o gate de plano (seção 7.19, trabalho paralelo)**: `financeiro/layout.tsx` passou a exigir Pro+ (superadmin sempre passa) — como o gate vive no layout, todas as rotas de `/dashboard/financeiro/*` desta seção e das 7.21-7.23 (Lançamentos, Metas, Limites de gastos, etc.) já herdam o bloqueio automaticamente, sem precisar de nenhuma mudança nelas.

- **`transactions.is_paid BOOLEAN DEFAULT true`** (migration 081) — até aqui toda transação inserida contava pro saldo da conta na hora, mesmo com `date` no futuro (ex: lançar um aluguel com vencimento no mês que vem já derrubava o saldo hoje); não existia "ainda não paguei"/"ainda não recebi". Default `true` é deliberado: preserva 100% do comportamento anterior pra toda linha existente e todo INSERT que não mexe no campo — inclui os inserts que nunca setam a coluna (webhook Stripe, `PledgeReviewCard`, cron `generate-recurring-transactions`), que continuam confirmando na hora sem gate nenhum, como já documentado nas migrations 031/077.
- **`update_account_balance()` passou a considerar `is_paid`** e a disparar em UPDATE também (não só INSERT/DELETE) — ver correção na seção 3.1/3.2. Isso é o que faz o checkbox "Já paguei/Já recebi" (`TransactionForm`) e o botão "Marcar como pago" (`TransactionTable`, ícone `CheckCircle2`, só aparece em lançamento pendente) moverem dinheiro de verdade quando o estado muda.
- **Fora de escopo, deliberado**: `update_highlight_current_amount()` e a view `project_budget_progress` (migration 076) continuam ignorando `is_paid` — arrecadação pública de projeto é sobre "quanto já foi prometido/lançado", não sobre o fluxo de caixa pessoal reconciliado; misturar os dois faria uma oferta pendente sumir da barra de progresso pública até ser marcada como paga.
- **`src/lib/financial/timeline.ts`** (`buildFinancialTimeline`) — motor novo, separado de `dashboard-aggregation.ts` (que alimenta só `TrendChart`/`CategoryBarChart`, intocado). Ancora em `financial_accounts.balance` (o saldo real AGORA, que só reflete `is_paid=true`) e caminha mês a mês: pra cada mês da janela calcula `saldoAnterior` (saldo acumulado antes do mês), `saldoDisponivel` (saldoAnterior + recebido − pago) e `saldoPrevisto` (saldoDisponivel + a-receber − não-pago), sem precisar buscar histórico anterior à janela — o saldo atual já carrega esse efeito embutido; a soma do que é pago dentro da própria janela "desfaz" até achar o saldo de antes do primeiro mês. Também agrega `fixedIncome`/`fixedExpense` (`source='recurring'`) pras abas "fixas". `page.tsx` amplia a janela de busca pra 12 meses pra trás (`windowTransactions`, sem limite superior — datas futuras já entravam por padrão). **`accountsStartDate`** (parâmetro opcional, `Date | null`, desde 2026-09-08 — ver Changelog): o "desfazer" só é fiel enquanto a conta já existia durante o mês; sem essa data o motor repetia o saldo atual pra todo mês anterior à existência da conta, como se o dinheiro já existisse antes de a conta existir no sistema. Os chamadores (`FinancialDashboard`/`ReportsAnalytics`) passam o menor `accountEffectiveStartDate(account, transactions)` (nova função exportada de `timeline.ts`) entre as contas ativas somadas em `currentBalance`; meses inteiramente antes desse mês voltam `saldoAnterior`/`saldoDisponivel`/`saldoPrevisto` como `null` em vez de um número inventado — `MonthNavigator`/`MonthSummaryCards` mostram `---` (`NO_DATA_MASK`) nesses casos. **`accountEffectiveStartDate`** prioriza a data de um lançamento `transactions.source='opening_balance'` da conta (novo, migration `095_account_opening_transaction.sql`) sobre `financial_accounts.created_at`: o saldo inicial digitado no `AccountWizard` deixou de ser só um número solto em `balance` — agora é um lançamento de verdade (`type` conforme o sinal, `is_paid=true`, editável/excluível como qualquer outro, aparece na lista de Lançamentos com o rótulo "Saldo inicial") datado por um campo novo no wizard ("Data do saldo inicial", default hoje, `max` = hoje, pode ser retroativo — "essa conta já existia antes de eu cadastrar aqui"). `AccountWizard.create()` insere a conta com `balance: 0` e deixa o trigger `trg_update_balance` (migration 081) aplicar o efeito desse lançamento — sem escrever `balance` direto, evita duplicar. Migration 095 também faz backfill: toda conta já cadastrada (saldo ≠ 0, não-cartão) ganha esse lançamento retroativo datado do `created_at`, com o trigger desligado durante o insert pra não somar o valor duas vezes. Cai pra `created_at` só quando não há lançamento `opening_balance` (conta sem saldo inicial, cartão de crédito, ou saldo inicial anterior a 12 meses atrás — fora da janela de busca de `page.tsx`, edge case documentado no Changelog). **Bug do backfill da 095, corrigido por `096_fix_opening_balance_backfill.sql`** (mesmo dia, achado pelo usuário): o valor do lançamento retroativo usava `financial_accounts.balance` (saldo atual, hoje) em vez do saldo do dia da criação — só é o mesmo número pra conta sem nenhuma atividade real desde então; pra conta com atividade real depois, o backfill nascia inflado e distorcia a reconstrução mês a mês. 096 recalcula cada `opening_balance` por diferença (saldo atual menos soma de todas as outras transações pagas da conta), fórmula que não mexe em conta sem atividade real (inclusive as criadas ao vivo pelo `AccountWizard` já corrigido).
- **`MonthNavigator`** (`src/components/financial/month-navigator.tsx`) — abas de métrica (Saldo previsto/Despesas e receitas/Despesas/Despesas fixas/Receitas/Receitas fixas, `TimelineMetric` em `timeline.ts`) + tira horizontal rolável de 13 meses (6 pra trás/6 pra frente), cada cartão com o valor da métrica ativa daquele mês; clicar seleciona, setas `<`/`>` andam um mês, "Mês atual" pula direto. Botão de olho (`Eye`/`EyeOff`) oculta valores monetários (mascarados como `••••`) nesse componente e em `MonthSummaryCards` — não alcança `TrendChart`/`CategoryBarChart` (fora de escopo, exigiria mexer em rótulo de SVG).
- **`MonthSummaryCards`** (`src/components/financial/month-summary-cards.tsx`) — substitui `balance-summary.tsx` (removido, só tinha esse um consumidor): 4 cards do mês selecionado — Saldo Anterior; Receitas (Recebido × A receber); Despesas (Pago × Não pago); Saldo Disponível + Saldo Previsto (com `title` explicando o cálculo). Mesma técnica de contagem animada (`useSpring`) do componente antigo.
- **Visão Geral ganhou botão de lançamento rápido** (`NewTransactionButton`, já existia só em Lançamentos) — `page.tsx` agora busca `partners`/`highlights`/`project_budget_categories` também, mesmo padrão de `lancamentos/page.tsx`.
- **Deliberadamente fora do escopo** (a referência era o GranaZen, mas nem tudo fazia sentido portar): aba "Bancos conectados" (Open Finance — não existe integração bancária no sistema); pills de atalho "Hoje"/"7 dias atrás"/"Esse ano" e date-range picker explícito (o dashboard aqui é organizado por mês, não por intervalo de dias arbitrário); botões flutuantes de WhatsApp/dica/configurações (chrome de suporte do GranaZen, sem relação com dado financeiro — `whatsapp_config` já existe no schema deste app pra outro propósito, notificação, não chat de suporte).

### 7.21 Painel Lançamentos+Gráficos na Visão Geral, Metas e Limites de gastos (migrations `082_spending_limits.sql`/`083_financial_goals.sql`, 2026-09-02)

Segunda rodada do mesmo pedido da 7.20 ("deixar quase idêntico ao GranaZen") — o usuário mandou mais prints mostrando o painel inferior da Visão Geral (Lançamentos do mês + Gráficos lado a lado, cada um com suas próprias abas) e outras áreas do menu superior deles (Meta, Limites de gastos). Escopo desta rodada confirmado com o usuário via pergunta direta antes de implementar (Investimentos e Comunidade/Indicar amigos do GranaZen ficaram de fora, por não terem relação com o financeiro deste app).

- **Painel "Lançamentos" + "Gráficos" substitui o card fixo "Por categoria"** (que a 7.20 tinha criado) — ficou redundante com o novo painel, que é um superconjunto dele:
  - **`MonthTransactionsPanel`** (`src/components/financial/month-transactions-panel.tsx`) — abas Todas/Receitas/Despesas + busca por descrição, sobre a mesma `TransactionTable` de `/dashboard/financeiro/lancamentos` (editar/excluir/marcar como pago funcionam igual), escopado ao mês do `MonthNavigator` em vez de paginado por query string. Precisou que `page.tsx` passasse a buscar `windowTransactions` já com os `join`s de categoria/parceiro (antes só a query de "últimos lançamentos", removida nesta rodada por virar redundante, tinha esses joins).
  - **`CategoryPanel`** (`src/components/financial/category-panel.tsx`) — abas de escopo Todas/Receitas/Despesas/Despesas Não Pagas sobre o `CategoryBarChart` existente (ganhou prop opcional `emptyLabel` pra mensagem de vazio ficar certa em cada escopo). Nova função `aggregateByCategoryScoped` (`dashboard-aggregation.ts`, ao lado da `aggregateByCategory` original — intocada, ainda alimenta nada agora que o card fixo foi removido, mas deixada como está por não ter motivo de mexer) faz a mesma composição por categoria filtrando por `type`/`is_paid` conforme o escopo; "Todas" soma entrada+saída juntas (atividade total da categoria no mês, não separadas por direção).
  - `FinancialDashboard` ganhou props `partners`/`highlights` (repassadas pro `MonthTransactionsPanel`, que precisa delas pro dialog de edição da `TransactionTable`).
- **`spending_limits`** (migration 082) — teto mensal de despesa por categoria de topo, um por categoria (`UNIQUE(profile_id, category_id)`). `SpendingLimitForm`/`SpendingLimitsList`/`NewSpendingLimitButton` (mesmo padrão 3-arquivos de `recurring_transactions`, ver 3.1) em `/dashboard/financeiro/limites`. Gasto do mês é somado no cliente a partir de `transactions` (sem coluna própria de acumulado — mesma filosofia "calcula, não duplica" já usada em `fatura_paid`/`project_budget_progress`); barra de progresso (`Progress`, mesmo componente de `BudgetBreakdown`) fica vermelha e mostra `AlertTriangle` quando o gasto passa do limite.
- **`financial_goals`** (migration 083) — meta de economia: nome, valor alvo, `current_amount`, prazo opcional. **Perguntado ao usuário e confirmado: sem vínculo automático com conta/transação nesta v1** — `current_amount` só muda editando a meta (`GoalForm`) ou via "Registrar valor" (`GoalContributionDialog`, soma um aporte). `achieved_at` grava a primeira vez que `current_amount` cruza `target_amount` (badge "Alcançada" + ícone comemorativo); não é recalculado se a meta for editada depois. `GoalsList`/`NewGoalButton` em `/dashboard/financeiro/metas`.
- **`FinanceSubNav`** ganhou "Metas" e "Limites de gastos", entre "Recorrentes" e "Contas".
- **Ainda fora do escopo** (mesma lista da 7.20, reafirmada): conexão bancária, date-range picker explícito, chrome de suporte flutuante. Adicional desta rodada: Investimentos e Comunidade/Indicar amigos do GranaZen — domínios sem relação com o financeiro deste app (Comunidade/Indicar amigos são conceitos de rede social/referral, não financeiro; Investimentos exigiria um módulo de portfólio inteiro, não mostrado em detalhe em nenhum print).

### 7.22 Fidelidade visual do navegador de mês e dos cards de resumo (2026-09-02)

Terceira rodada — o usuário mandou o HTML real do GranaZen (inspecionado no DevTools) pros dois componentes mais visíveis da 7.20, pedindo pra chegar o mais perto possível do visual original em vez de só reproduzir a função. Só CSS/estrutura — nenhuma mudança de dado/schema.

- **`MonthNavigator`** (`src/components/financial/month-navigator.tsx`) reescrito: agora vive dentro de um `Card` (antes era `div` solto); abas de métrica viraram pill `bg-muted/60` com o item ativo em `bg-background` + sombra (era botão preto sólido); texto de descrição abaixo das abas, um por métrica (`TIMELINE_METRICS` ganhou campo `description` em `timeline.ts`); cartões de mês maiores (`h-24`, antes eram bem mais baixos) com a linha conectora ganhando semântica — sólida colorida (`border-primary/60`) até o mês selecionado, tracejada cinza (`border-muted-foreground/30`) depois, e a borda do próprio cartão também tracejada pros meses futuros (ainda não "aconteceram", só projeção — mesmo raciocínio visual do GranaZen); setas de navegação viraram botões circulares sobrepostos nas bordas (`absolute`, `rounded-full`) em vez de ghost buttons inline; `title` (tooltip nativo do browser, sem componente `Tooltip` novo — não existia um no projeto) mostra "{métrica} em {mês}: {valor}" ao passar o mouse num cartão, mesma informação do tooltip customizado do original.
- **`MonthSummaryCards`** (`src/components/financial/month-summary-cards.tsx`) reescrito: cards ganharam barra de destaque de 4px na borda esquerda + ícone num badge circular colorido (extraído pro helper `StatCard` interno do arquivo, evita repetir a mesma estrutura 4x) — ver convenção nova documentada na seção 11.1. Sublinhas (Recebido/A receber, Pago/Não pago) viraram pílulas `bg-muted/25` com ícone `CircleCheck`/`Clock3` e valor tingido de `text-success`/`text-warning` (helper `SubRow`), substituindo o texto simples de antes. Paleta trocou de `chart-1`/`chart-2` (que são pra entrada/saída em **gráfico**, seção 11.1) pra `primary`/`success`/`destructive` — mapeamento mais próximo do que o card realmente comunica (status, não série de dados) e o que o HTML de referência usava.
- **Sem mudança de comportamento**: `hideValues`, seleção de mês, contagem animada (`useSpring`) — tudo igual à 7.20, só a casca visual mudou.
- **Exceção, achada testando**: `financeiro/page.tsx` tinha um `return` antecipado que substituía a página inteira por um card "Criar minha primeira conta" sempre que `financial_accounts` estava vazio — o dashboard inteiro (navegador de mês, cards, gráficos) nunca chegava a renderizar pra quem ainda não tinha conta, diferente do GranaZen (que mostra tudo zerado). Corrigido: o aviso de criar conta virou uma faixa no topo (mesmo Card, sem mais `return` isolado) e `FinancialDashboard` sempre renderiza, com `accounts=[]` — `buildFinancialTimeline`/`aggregateMonthly`/`aggregateByCategoryScoped` já eram seguras com array vazio (só dão tudo zero), não precisou de guarda nova.

### 7.23 Barra de período, ajustes finos do navegador de mês, e detalhes dos painéis Lançamentos/Gráficos (2026-09-02)

Quarta rodada — mais dois prints/HTML do GranaZen (a barra de filtro do topo, que tinha ficado de fora sem querer, e o par Lançamentos/Gráficos com mais detalhe) mais dois ajustes visuais pontuais pedidos depois de ver o resultado renderizado.

- **`PeriodFilterBar`** (`src/components/financial/period-filter-bar.tsx`, novo) — barra que faltava no topo da Visão Geral: ←/→ trocando o mês selecionado (mesmo `timelinePoints`/`selectedMonth` do `MonthNavigator` logo abaixo, os dois ficam sincronizados por serem o mesmo estado em `FinancialDashboard`), botão "Esse mês", badge somente-leitura com o intervalo de datas do mês selecionado, o toggle de ocultar valores (movido pra cá, saiu do `MonthNavigator`) e "Atualizar" (`router.refresh()`, busca os dados de novo no servidor). **Deliberadamente sem** "Hoje"/"7 dias atrás"/"Esse ano" nem date-range picker editável do original — não fazem sentido num dashboard organizado por mês inteiro, não por intervalo de dias arbitrário; o intervalo aparece só como informação. `MonthNavigator` perdeu o botão "Mês atual" e o toggle de olho (ficariam redundantes, duplicados a poucos pixels de distância da barra nova).
- **`MonthTransactionsPanel`** ganhou os botões de atalho Receita/Despesa (verde/vermelho, `bg-success`/`bg-destructive` sólidos — únicos botões do app com essa cor cheia, os demais usos de `destructive` são sutis via `variant="destructive"` do `Button`) que abrem o mesmo `TransactionForm` já com o tipo certo pré-selecionado (`TransactionForm` ganhou prop `defaultType`); busca passou a casar também pelo valor formatado, não só descrição; empty state ganhou ícone (`FileText`) + subtítulo "Não há transações para exibir em {mês}." (novo `emptyTitle`/`emptyHint` em `TransactionTable`, com default igual ao de sempre pra não mudar a página de Lançamentos); rodapé "Total: N" quando há resultado.
- **`CategoryPanel`** ganhou um heading por escopo ("Todas as despesas", "Todas as receitas" etc.) + subtítulo com o intervalo de data do mês, replicando a mesma estrutura da referência; `CategoryBarChart` ganhou ícone (`BarChart3`) no estado vazio (afeta também o card "Fluxo de caixa"/qualquer outro consumidor, mudança aditiva).
- **Deliberadamente fora**: dropdown de agrupamento ("Sem agrupamento"), ordenação por campo (Data de Vencimento ↑↓), ícone de filtro avançado (conta/categoria — já existe filtro equivalente na página dedicada de Lançamentos) e paginação numérica ("Mostrar 30"/Voltar/Próximo) — o painel já escopa a UM mês por vez, volume de dados naturalmente pequeno o bastante pra não precisar. Se algum desses virar dor real, dá pra portar depois.
- **Dois ajustes finos pedidos depois de ver renderizado**:
  1. Cartões de mês na tira horizontal estavam cortando nome de mês mais longo ("Janeiro '27") e valores reais (a amostra zerada "R$ 0,00" escondia isso) — largura fixa subiu de 116px pra 144px.
  2. Linha conectora e bolinha dessincronizadas: a bolinha (dentro do fluxo flex do botão, posição implícita via `justify-between`) não caía em cima da linha (`top` fixo em pixel); e o trecho sólido ia um mês além do selecionado. Corrigido: botão passou de `justify-between` pra 3 linhas de altura fixa (label 32px / bolinha 16px / valor `flex-1`), então a bolinha sempre cai exatamente em `top-[52px]` — mesmo valor usado pela linha. **Ajuste adicional**: o corte sólido/tracejado (cartão e linha) passou a ser calculado contra o **mês atual real** (`currentIndex`, `new Date()`) em vez do mês selecionado/navegado — antes, navegar pra um mês passado fazia o corte "andar junto" com a navegação (todo mês depois da seleção virava visualmente "futuro", mesmo já tendo acontecido); agora fica fixo em hoje, só a seleção (borda azul) se move.

### 7.24 Aba "Relatórios" (analytics) — renomeação da antiga + gráfico de frequência novo (2026-09-03)

Quinta rodada — o usuário mandou print+HTML da aba "Relatórios" do GranaZen (sub-abas Gráficos/Lançamentos pendentes/Fluxo de caixa/Conta bancária Open Finance; conteúdo de "Gráficos": Despesas por Categoria + Receitas por Categoria + um gráfico de frequência Receitas×Despesas com seletor de agrupamento Diário/Semanal). **Colisão de nome**: `/dashboard/financeiro/relatorios` já existia neste app (seção 7.10-quater) pra outra coisa — histórico de prestação de contas publicada. Perguntado ao usuário como resolver; escolheu renomear a rota antiga.

- **Renomeação**: rota antiga (`RelatoriosPage`, lista de `partner_broadcasts`) movida de `financeiro/relatorios/` pra `financeiro/prestacoes/` (label "Prestações" no `FinanceSubNav`). Só 3 referências no código pra atualizar (`finance-sub-nav.tsx`, `revalidatePath` em `parceiros/actions.ts`, comentário em `send-broadcast-button.tsx`) — nenhum link público quebra, a rota é interna ao dashboard. `/dashboard/financeiro/relatorios` virou a página nova.
- **`ReportsAnalytics`** (`src/components/financial/reports-analytics.tsx`, novo container client) — mesma `PeriodFilterBar` do topo da Visão Geral (7.23), mas com **estado de mês/moeda próprio e independente** (páginas diferentes, sem sincronizar seleção). Duas cards "Despesas por categoria"/"Receitas por categoria" reaproveitam 100% o `CategoryBarChart`+`aggregateByCategoryScoped` já existentes (7.21) — sem duplicar lógica, só fixam o escopo em vez de expor abas. A novidade de verdade é o `FrequencyChart`.
- **`FrequencyChart`** (`src/components/ui/charts/frequency-chart.tsx`, novo) — Receitas×Despesas em colunas pareadas por dia/semana do mês selecionado (seletor de agrupamento no header do card). Nova função pura `aggregateFrequency` (`dashboard-aggregation.ts`) gera os buckets. **Cor deliberadamente `chart-1`/`chart-2`** (azul/laranja), não o verde/vermelho literal do GranaZen — mesma paleta já validada (dataviz skill) e já usada pelo `TrendChart` pra essa exata semântica (entrada/saída), consistência interna > fidelidade pixel-a-pixel numa cor que este app já tem convenção própria (seção 11.1). Hover é por par de colunas (bar/cell — regra da dataviz skill, não crosshair de linha), com toggle "Ver como tabela" e ícone no estado vazio, mesmo padrão dos outros gráficos financeiros.
- **Deliberadamente fora desta rodada**: as outras 3 sub-abas do GranaZen (Lançamentos pendentes — poderia reaproveitar `is_paid=false`, mas não pedido explicitamente; Fluxo de caixa — sobreporia o `TrendChart` já existente na Visão Geral; Conta bancária Open Finance — sem integração bancária, já excluído antes) e "Gerenciar Gráficos" (drag-and-drop pra reordenar/esconder cards, precisaria de estado persistido por usuário) — feature de customização grande o bastante pra ser sua própria rodada, se vier a ser pedida.
- **Verificado num browser de verdade (não só `tsc`/`build`)**: `npm run dev` + Playwright headless, login com conta seed (`scripts/seed-test-accounts.mjs`, já existente no repo) + dados financeiros extras inseridos via service role pra ter algo real pra renderizar. Achou 2 bugs reais que só apareciam com dado de verdade (zero deles pegos por `tsc`/`eslint`/`build`, que só garantem que compila):
  1. **`MonthSummaryCards`**: título/valor no mesmo grid row (`grid-cols-[minmax(0,1fr)_auto]`) truncava o título pra 3-4 letras ("Sald...") em qualquer valor um pouco mais longo — a coluna `auto` do valor comia espaço demais da coluna `1fr` do título. Tentativa 1 (tirar `truncate` do título, deixar quebrar linha) trocou um bug visível por outro pior — valor sobrepondo o título. Fix definitivo: valor saiu da mesma linha do título, foi pra baixo do bloco ícone+título/subtítulo (mesmo card, só empilhado em vez de lado a lado) — título nunca mais disputa largura com o valor.
  2. **`FrequencyChart`**: rótulos do eixo X coincidindo/sobrepondo quando o stride calculado deixava o penúltimo dia colado no último (forçado a aparecer sempre) — mesma classe de bug que o `TrendChart` já tinha corrigido (`shownMonthIndices.delete(data.length-2)`), só que a correção não tinha sido replicada aqui na hora de criar o componente. Aplicada a mesma lógica.

### 7.25 "Visão geral" das Metas — anel de composição + cor por meta (2026-09-03)

Sexta rodada — mais HTML+prints do GranaZen, dessa vez da tela de Metas: card "Visão geral" com anel mostrando o total guardado e a fatia de cada meta nesse total, cada meta com sua própria cor (bolinha no card, também usada na barra de progresso), "Já guardado"/"Valor da meta" lado a lado, "Faltam RX" + percentual.

- **`DonutChart`** (`src/components/ui/charts/donut-chart.tsx`, novo) — primeiro donut/pizza do app. Justificado pela dataviz skill como exceção válida ao "não usar pizza pra comparação" (que é o motivo do `CategoryBarChart` ser barra): aqui não é comparação entre categorias soltas, é **parte de um todo com número-herói no centro** — caso legítimo pra anel. Poucos segmentos (metas ativas são tipicamente poucas), legenda sempre ao lado (nunca só cor), paleta `chart-3`..`chart-8` já validada (mesmos slots categóricos de `CategoryBarChart`). Implementado com `stroke-dasharray`/`stroke-dashoffset` em `<circle>` (técnica clássica de anel SVG) — sem lib de gráfico nova. **Achado pelo eslint, não por olhar renderizado**: a primeira versão calculava o offset acumulado mutando uma variável `let` dentro do `.map()` de render — a regra `react-hooks/immutability` (React Compiler) rejeita isso ("Cannot reassign variable after render completes"); corrigido calculando os offsets via `.map()` puro com `.slice(0,i).reduce(...)` (O(n²), irrelevante pra n pequeno) em vez de mutação.
- **Cor por meta** (`GOAL_COLOR_VARS`, mesmos slots 3-8): atribuída por ordem de criação (`created_at` ascendente, `useGoalColors` em `goals-list.tsx`), estável mesmo com a lista exibida em ordem diferente (mais recente primeiro) ou com metas sendo adicionadas depois — recalcular por posição de exibição faria a cor de uma meta existente mudar toda vez que uma nova fosse criada. A mesma cor aparece na bolinha do nome, na fatia do anel e na barra de progresso da meta.
- **Escopado por moeda**: o card "Visão geral" nunca soma moedas diferentes (regra já estabelecida em toda a Fase 1 financeira) — se há metas em mais de uma moeda, um card "Visão geral" separado é renderizado por moeda (`CurrencyOverview`, agrupado via `Map` em `GoalsList`).
- **Card de meta individual** reestruturado pra bater com a referência: "Já guardado"/"Valor da meta" lado a lado, barra de progresso colorida (`style` inline com a cor da meta — não dá pra usar o `Progress` compartilhado aqui, que tem a cor do indicador fixa em classe Tailwind, não uma prop; barra de meta precisa de cor *dinâmica* por item, então virou `<div>` simples em vez do componente compartilhado, só aqui), "Faltam RX" + percentual (ou "Meta concluída" quando `achieved_at`).
- **Deliberadamente fora**: badge de avatar (quem contribuiu — exigiria buscar perfil/iniciais do `created_by_user_id` de cada meta, baixo valor sem conta compartilhada de verdade pra testar), link "Ver meta" pra página de detalhe dedicada (as ações já existentes — editar, registrar valor, excluir — são inline via dialog, mesmo padrão do resto do dashboard; uma rota nova só pra isso duplicaria funcionalidade), card promocional "Reserva de emergência" (calculadora/wizard — feature separada, não faz parte do CRUD de metas).
- **Verificado num browser real** (mesmo método da 7.24): 3 metas seed com progresso variado (65%/30%/100%, uma alcançada) — texto renderizado, percentuais do anel (47%/9%/44%) e offsets do SVG conferem matematicamente com os valores reais; checagem geométrica (`getBoundingClientRect`) confirma anel/legenda sem sobreposição e nenhum elemento colapsado (`width:0 height:0`) — mesma classe de bug (sobreposição CSS) que a 7.24 achou em `MonthSummaryCards`, não repetida aqui.

### 7.26 Cor por categoria + integração com Limites de gastos (2026-09-03)

Sétima rodada — HTML+prints da tela de Categorias do GranaZen: bolinha colorida por categoria, "Limite de gasto: RX" por categoria (linkando com a feature de Limites de gastos, ver 7.20), busca, botão de relatório por categoria, "Valor total do limite de gastos" + link pra `/dashboard/financeiro/limites`.

- **`transaction_categories.color`** já existia no schema desde a migration `001_initial_schema.sql` (junto com `icon`) — nunca tinha sido usado em nenhuma tela até aqui. Sem migration nova nesta rodada, só passou a ser lido/escrito.
- **`CategoryForm`** (`src/components/financial/category-form.tsx`, novo) — dialog de criar/editar (nome + paleta de 10 cores fixas pra escolher, `COLOR_SWATCHES`). **Deliberadamente uma paleta separada** da categórica `chart-3`..`chart-8` que colora barras em `CategoryBarChart`/`CategoryPanel` — aquelas continuam coloridas por posição no ranking do mês (não por categoria fixa); sincronizar as duas coisas (cor salva no banco virar a cor usada nos gráficos) é um refactor maior nos componentes de gráfico existentes, fora de escopo aqui. A cor de categoria por enquanto só aparece na própria tela de Categorias (bolinha ao lado do nome) e nas pílulas de subcategoria.
- **`CategoryTree`** reescrito: busca por nome (filtra categoria de topo OU alguma subcategoria dela), "Limite de gasto: RX" por categoria de topo (join com `spending_limits` — só categoria de topo tem limite, mesma regra da 7.20), badge "Valor total do limite de gastos" + link "Ver limites de gastos" (só aparece quando existe pelo menos um limite), botão de relatório (`ChartPie`) linkando pra `/dashboard/financeiro/lancamentos?category=ID` (mesmo padrão de navegação que `CategoryBarChart` já usa ao clicar numa barra), editar (abre `CategoryForm`), adicionar subcategoria (idem, com `parentId`), excluir (inalterado).
- **Deliberadamente fora**: arquivar/desarquivar categoria (o GranaZen tem uma aba `/arquivadas` separada — exigiria coluna `archived` nova + filtrar em todo lugar que lista categoria hoje: seletor do `TransactionForm`, `CategoryBarChart`, `CategoryPanel`, `ReportsAnalytics` — feature grande o bastante pra ser sua própria rodada); toggles "Com limite de gasto"/"Com subcategoria" do filtro (a busca por nome já cobre o caso de uso mais comum); menu "Ações" colapsado no mobile (os 4 botões de ícone já cabem soltos numa linha, sem precisar esconder atrás de um menu); texto sobre categorização automática por IA (não existe esse recurso no app).
- **Não verificado num browser de verdade nesta rodada** — usuário estava com o próprio `npm run dev` ativo na porta 3000 rodando testes ao vivo (rodada anterior já tinha causado transtorno matando/reiniciando essa mesma porta pra testes automatizados); Next.js 16 também bloqueia dois `next dev` simultâneos no mesmo diretório de projeto, então não deu pra isolar um servidor de teste em outra porta. `tsc`/`eslint`/`build` limpos, healthcheck HTTP do servidor do usuário sem erro, e o padrão de dialog criar/editar é idêntico ao já provado em `GoalForm`/`SpendingLimitForm` — mas vale um olhar visual de verdade assim que possível.

### 7.27 Toggles de filtro + botão de relatório na tela de Categorias (2026-09-03)

Continuação direta da 7.26 — daquela vez só tínhamos prints (sem HTML) e os dois toggles + o botão preto tinham ficado documentados como "deliberadamente fora". Nesta rodada o usuário mandou o HTML real da tela, e a decisão foi revista.

- **`Switch`** (`src/components/ui/switch.tsx`, novo) — wrapping do primitivo `@base-ui/react/switch` (`Root`+`Thumb`), mesmo padrão de `progress.tsx`. Usa os atributos boolean `data-checked`/`data-unchecked` que o Base UI expõe (a referência, em Radix, usa `data-state="checked"|"unchecked"` — API diferente, mesmo efeito visual).
- **`CategoryTree`**: toggles "Com limite de gasto" (filtra por `limitByCategory.has(cat.id)`) e "Com subcategoria" (checa se a categoria tem algum filho, sem aplicar o filtro de busca a essa checagem) somados ao filtro de busca já existente. Cada toggle vira sua própria pill (`rounded-lg border bg-card px-3 py-2`), igual à estrutura do HTML mandado; a badge "Valor total do limite de gastos" também virou pill e passou a aparecer sempre (não só quando já existe algum limite cadastrado, diferente da 7.26). Link de texto "Ver limites de gastos" virou botão sólido "Relatório limite de gasto" (`buttonVariants({ size: 'sm' })` aplicado a um `<Link>`, ícone `ChartColumn`) — usa nosso `bg-primary`, não o `bg-black/90` fixo do GranaZen.
- **Deliberadamente ainda fora**: a tabela ordenável de desktop do GranaZen (mantemos um único layout em cards pra todas as larguras de tela — decisão já explicada no comentário de `CategoryTree`), arquivar/desarquivar categoria e o menu "Ações" colapsado no mobile (motivos inalterados desde a 7.26).

### 7.28 Calculadora de "Reserva de emergência" na Visão geral das Metas (2026-09-03)

Revisita uma decisão da 7.25, que tinha deixado esse card "deliberadamente fora" — o usuário pediu de volta nesta rodada e mandou prints do card de entrada mais as 6 etapas do wizard real do GranaZen.

- **`EmergencyFundCard`** (`src/components/financial/emergency-fund-wizard.tsx`, novo) — card no topo da tela de Metas ("Crie sua reserva de emergência" + botão "Criar reserva"), só aparece enquanto o usuário não tiver nenhuma `financial_goals` com o nome exato "Reserva de emergência" (`EMERGENCY_FUND_GOAL_NAME`); uma vez criada, vira um card de meta normal na lista, sem tratamento especial.
- **`EmergencyFundWizard`** — dialog de 6 passos (nosso `Dialog`/`DialogFooter`, `Progress` reaproveitado como barra de progresso do topo) reproduzindo a sequência do GranaZen: (1) intro com checklist do que será calculado, (2) custo fixo mensal, (3) tipo de renda — "Salário fixo (CLT)" → reserva de 6 meses, ou "Renda variável (PJ ou autônomo)" → 12 meses (a escolha em si não é salva em lugar nenhum, só decide o multiplicador), (4) valor já guardado, (5) quanto consegue guardar por mês, (6) revisão + criar. Painel lateral "Sua proteção" (`hidden lg:block`, igual à referência) recalcula em tempo real a cada resposta: meses já protegidos, %, valor guardado/alvo e prazo estimado.
- **Sem tabela nem coluna nova** — ao confirmar, cria uma `FinancialGoal` comum: `target_amount` = custo fixo mensal × meses (6 ou 12), `current_amount` = valor já guardado, `target_date` = hoje + prazo estimado em meses (só quando há valor/mês informado; sem esse valor, meta fica sem prazo, igual à referência). Reaproveita 100% da infra de metas da 7.25/7.21 — aparece na "Visão geral" (anel + legenda), ganha cor própria da paleta `chart-3..8`, barra de progresso, e os botões inline já existentes (Registrar valor/editar/excluir).
- **Decisão revista desde a 7.25**: aquela rodada tinha listado esse card como "deliberadamente fora"; confirmado com o usuário que agora entra, com o wizard completo (6 telas) como referência.
- **Deliberadamente fora**: seletor de moeda dentro do wizard (usa `currencies[0]`, mesma regra do resto do módulo — a própria referência também não pergunta moeda); qualquer fluxo de edição dedicado da reserva depois de criada (usa os mesmos botões inline de qualquer meta — a 7.25 já tinha decidido não ter página de detalhe "Ver meta", reconfirmado nesta rodada).
- **Não verificado num browser de verdade nesta rodada** — mesmo motivo das rodadas anteriores desta sessão (usuário com `npm run dev` próprio ativo na porta 3000). `tsc`/`eslint`/`build` limpos.

### 7.29 Tela de Contas bancárias: cabeçalho, abas Ativas/Arquivadas, wizard de nova conta (2026-09-03)

Nona rodada — usuário mandou prints + HTML da tela "Contas bancárias" do GranaZen (empty state) e do fluxo "Adicionar conta" (5 etapas: automática/manual, nome, saldo inicial+sinal, "conta padrão" pro WhatsApp, revisão).

- **`financial_accounts.archived`** já existia no schema desde sempre — nenhuma tela usava. `ContasPage` continuava buscando todas as contas sem filtrar; agora `AccountsList` (novo, client) filtra por essa coluna e mostra em abas.
- **`AccountsList`** (`src/components/financial/accounts-list.tsx`, novo) — substitui a renderização direta de `AccountCard` que vivia em `page.tsx`. Abas "Ativas"/"Arquivadas" no mesmo padrão pill já usado no `MonthNavigator` (`bg-muted/60` + botão ativo com `bg-background`/`shadow`, não um primitivo de Tabs novo). Empty state por aba (ícone, título, texto, CTA "Adicionar conta" só na aba Ativas).
- **`AccountCard`** ganhou botão Arquivar/Reativar (ícones `Archive`/`ArchiveRestore`, `update({ archived: !account.archived })` inline, mesmo padrão de pending state que `CategoryTree`/outros já usam).
- **`AccountWizard`** (`src/components/financial/account-wizard.tsx`, novo) — substitui `AccountForm` como ponto de entrada de **criação** de conta (o `AccountForm` de sempre continua sendo usado só pra **editar**, sem mudança). 6 etapas: (1) automática (desabilitada, badge "Em breve" — mesma decisão do botão "Gerenciar Open Finance") / manual, (2) nome, (3) saldo inicial + escolha Positivo/Negativo (aplica o sinal no valor final), (4) tipo de conta — corrente/poupança/cartão, com os campos de cartão condicionais que já existiam no `AccountForm`, (5) moeda + conta compartilhada, (6) revisão + criar. **Nota pós-7.32**: essa etapa 1 continua com "Conta automática" desabilitada mesmo depois da integração Open Finance ficar funcional — o fluxo de conexão real vive só no `OpenFinanceManageDialog` (cabeçalho da página); redirecionar esta etapa pra lá exigiria levantar estado compartilhado entre `AccountsList` e o dialog (hoje são componentes irmãos independentes em `page.tsx`), não fazia parte do pedido original e ficou deliberadamente de fora — inconsistência de UX pequena, documentada aqui pra não surpreender numa próxima rodada.
  - **Etapas 1-3 e a revisão seguem a referência de perto** (inclusive textos como "Esta conta será criada sem conexão bancária.", que também é verdade aqui).
  - **Etapas 4 e 5 são nossas, não do GranaZen**: a etapa 4 real do GranaZen pergunta "Esta será sua conta padrão?" pra lançamentos via WhatsApp — não existe bot de WhatsApp neste app, então essa etapa não tinha pra onde ir. No lugar, duas etapas cobrem campos que o `AccountForm` já exigia e o GranaZen nem pergunta nesse fluxo (tipo de conta, moeda, campos de cartão, compartilhada). Confirmado com o usuário antes de implementar.
- **Botão "Gerenciar Open Finance"** no cabeçalho da página existia só visualmente, desabilitado (`disabled`, `title="Em breve"`) — conexão bancária automatizada (Open Finance/Pluggy no GranaZen) estava fora de escopo, mesma decisão já tomada em 7.20/7.21. **Revertido na 7.32**: o usuário pediu a integração de verdade; o botão foi trocado por `OpenFinanceManageDialog`, funcional.
- **Não verificado num browser de verdade nesta rodada** — mesmo motivo de sempre (servidor de dev do usuário ativo na 3000). `tsc`/`eslint`/`build` limpos.

### 7.30 Verificação end-to-end das rodadas 7.27-7.29 num browser real (2026-09-03)

Décima rodada — a pedido do usuário ("faça ficar tudo funcional"), primeira verificação de ponta a ponta das três rodadas anteriores desta sessão (toggles de Categorias, wizard de Reserva de emergência, tela de Contas + wizard de nova conta), que até aqui só tinham passado por `tsc`/`eslint`/`build`.

- **Método**: script Playwright avulso (não commitado) rodando contra o `npm run dev` do próprio usuário na porta 3000 (sem reiniciar nada), autenticado como `ana.ferreira@seed.goguide.test` (plano já era `mission`, sem precisar mexer). Cada fluxo foi dirigido de verdade (clicar, preencher, avançar etapas) e o resultado verificado tanto na UI quanto direto no banco via service role. A meta "Reserva de emergência" e a conta já existentes nos dados seed foram temporariamente renomeadas/preservadas e restauradas ao estado original ao final (goals/contas criadas pelo teste foram apagadas depois).
- **Confirmado funcionando**: os dois toggles de Categorias (filtram corretamente, `data-checked`/`data-unchecked` batendo com a contagem exibida), o wizard de Reserva de emergência completo (matemática do alvo, `target_date` e criação da meta corretas, card some depois de criada), as abas Ativas/Arquivadas de Contas (arquivar/reativar reflete corretamente após o toggle) e o wizard de Nova conta completo pra conta corrente e cartão de crédito (campos de cartão só são gravados pro tipo certo, saldo inicial descartado pra cartão como esperado).
- **Bug real encontrado e corrigido**: em `CategoryTree`, a mensagem de lista vazia só considerava o termo de busca (`term ? 'Nenhuma categoria encontrada.' : 'Nenhuma categoria ainda.'`) — quando um dos toggles ("Com limite de gasto"/"Com subcategoria") zerava a lista sem nenhum termo digitado, mostrava "Nenhuma categoria ainda.", que sugere que o usuário não tem categoria nenhuma (falso). Corrigido pra também considerar `onlyWithLimit`/`onlyWithSub` na condição.
- **Achado de UX, não corrigido**: os inputs de valor mascarados (`toMasked`/`fromMasked`, padrão já usado em `GoalForm`/`TransactionForm`/etc.) reconstroem o valor a partir dos dígitos do campo inteiro a cada tecla — funciona certo pra digitação normal, mas colar um valor de uma vez (ex. "1000" via paste) é interpretado como centavos ("R$ 10,00", não "R$ 1.000,00"). Não é um bug desta rodada — é o comportamento de todo campo de valor no módulo financeiro — só documentado aqui porque foi o que quase confundiu o próprio script de teste.
- `tsc`/`eslint`/`build` seguem limpos após a correção do empty-state.

### 7.31 Correção: conta arquivada continuava ativa em todo o resto do módulo financeiro (2026-09-03)

Décima primeira rodada — a pedido do usuário ("revise toda essa parte de financeiro... coloque tudo de maneira funcional e com lógica"), rodei o skill `/code-review` (nível `high`, `--fix`) apontado pro módulo financeiro inteiro (`src/components/financial/`, `src/app/dashboard/financeiro/`, `src/lib/financial/`).

- **Corrigido automaticamente pelo review**: `AccountWizard` (7.29) tinha seu próprio `toMasked`/`fromMasked`/`CURRENCIES` local com separador pt-BR fixo, ignorando a moeda escolhida na etapa 5 — se o usuário trocasse pra USD depois de já ter digitado o saldo inicial, o valor ficava formatado/interpretado errado. Trocado pelos helpers de `src/lib/currency-mask.ts` (já usados em outros lugares do app pra moeda de verdade), com `reformatMasked` reaplicado quando a moeda muda.
- **Achado maior, corrigido manualmente**: arquivar uma conta (7.29) só filtrava a própria tela de Contas — o resto do módulo continuava tratando a conta arquivada como ativa: `FinancialDashboard` somava o saldo dela no "saldo atual"/previsão da Visão Geral, e o seletor de conta pra **lançamento novo** (avulso, recorrente, confirmação de oferta pendente) continuava oferecendo ela. Nada disso tinha sido pensado quando arquivar foi implementado, porque a feature nasceu isolada na tela de Contas.
  - Padrão aplicado: qualquer tela/componente que **lista ou edita dado já existente** (tabela de lançamentos, lista de recorrentes, filtros) continua recebendo a lista completa de contas — precisa achar a conta de um lançamento antigo mesmo já arquivada. Qualquer seletor que decide **onde alocar algo novo** (novo lançamento avulso ou recorrente, confirmar oferta pendente, moeda de novo limite/meta, saldo/moeda agregados da Visão Geral e de Relatórios) passou a considerar só contas ativas.
  - Arquivos ajustados: `financial-dashboard.tsx` e `month-transactions-panel.tsx` (cada um ganhou seu próprio `activeAccounts` — o segundo porque um único componente mistura tabela de lançamentos, que precisa da lista completa, com o atalho de lançamento rápido, que não); `financeiro/page.tsx` (banner "criar minha primeira conta" agora considera só ativas — antes só desaparecia se existisse *qualquer* conta, arquivada ou não), `lancamentos/page.tsx`, `recorrentes/page.tsx`, `conciliacao/page.tsx`, `relatorios/page.tsx`, `limites/page.tsx`, `metas/page.tsx`.
- **Verificado num browser real** (Playwright, `ana.ferreira`): com a única conta arquivada, o banner "criar minha primeira conta" volta a aparecer na Visão Geral e "Novo lançamento" fica desabilitado em Lançamentos; reativando a conta, ambos voltam ao normal. Estado seed restaurado ao final.
- `tsc`/`eslint`/`build` limpos.

### 7.32 Open Finance de verdade (Pluggy) — conexão bancária automatizada (migration `088_open_finance.sql`, 2026-09-04)

Décima segunda rodada — o usuário pediu pra reverter a decisão de escopo mantida desde 7.20/7.21/7.29: "integrar automaticamente as contas, pro botão gerenciar open finance ficar clicável e utilizável". Open Finance no Brasil exige um agregador certificado (não dá pra integrar direto com os bancos) — confirmado com o usuário: **Pluggy** (mesmo provedor já citado nas notas do GranaZen), sem credenciais ainda — implementado pronto pra uso, com `PLUGGY_CLIENT_ID`/`PLUGGY_CLIENT_SECRET` vazios em `.env.local` até o usuário criar a conta em `dashboard.pluggy.ai` (sandbox grátis).

- **Schema novo** (`088_open_finance.sql`): `open_finance_items` (uma conexão/login num banco via widget Pluggy Connect — `profile_id`, `pluggy_item_id` único, `connector_id`/`connector_name`/`connector_image_url`, `status` espelhando o `ItemStatus` da Pluggy — `UPDATED`/`UPDATING`/`WAITING_USER_INPUT`/`LOGIN_ERROR`/`OUTDATED` —, `last_synced_at`; RLS `is_profile_owner(profile_id)`) e `open_finance_accounts` (mapeia cada conta Pluggy de um item pra uma `financial_accounts.id`; RLS via `EXISTS` no item pai). `financial_accounts` ganhou `is_open_finance BOOLEAN DEFAULT false` (flag denormalizada, evita join no `AccountCard` — mostra badge "Open Finance"). `transactions` ganhou `pluggy_transaction_id TEXT UNIQUE` (dedup entre sincronizações) e `'open_finance'` foi somado ao `CHECK` de `source` (mesmo padrão `DROP CONSTRAINT IF EXISTS`/`ADD CONSTRAINT` da migration 077). **Migration aplicada no Supabase** (confirmado em 2026-09-04 — usuário tentou reaplicar depois do deploy de código e levou `42P07: relation "open_finance_items" already exists"`, ou seja já tinha rodado numa passada anterior sem isso ter sido registrado aqui; conferido direto via `service_role` que as duas tabelas, as duas colunas novas e o `CHECK` atualizado — inclusive aceitando `source='open_finance'` — estão todos presentes e corretos). Só falta o usuário criar a conta na Pluggy e preencher `PLUGGY_CLIENT_ID`/`PLUGGY_CLIENT_SECRET` (ainda vazios em `.env.local`) pra reativar o botão de verdade.
- **`src/lib/open-finance/pluggy.ts`** — wrapper do SDK server-side `pluggy-js` (não confundir com `pluggy-connect-sdk`/`react-pluggy-connect`, usados só no cliente pro widget). `isPluggyConfigured()` checa as duas env vars; `getPluggyClient()` troca `PLUGGY_CLIENT_ID`/`PLUGGY_CLIENT_SECRET` por uma API key via `POST https://api.pluggy.ai/auth` (a própria Pluggy documenta validade de ~2h — cacheada em memória do processo com 5min de folga) e devolve um client `Pluggy` autenticado; **a API key nunca sai do servidor** — o navegador só recebe um `connectToken` de escopo restrito (`pluggy.createConnectToken`). `pluggyWebhookUrl()` só registra webhook se `NEXT_PUBLIC_APP_URL` for uma URL pública (não `localhost`) — em dev, sincronização segue funcionando via botão manual e via cron.
- **`src/lib/open-finance/sync.ts`** — `syncOpenFinanceItem(itemRowId)`, função única reaproveitada pelos 4 caminhos que precisam sincronizar (primeira conexão, botão "Sincronizar agora", webhook, cron diário), pra nunca divergir o que "sincronizar" significa. Por item: rebusca o `Item` na Pluggy (nunca confia em nada que não seja re-perguntado à API), atualiza status/conector; se `LOGIN_ERROR`/`WAITING_USER_INPUT`, para aí (nada novo pra buscar até reconectar). Por conta Pluggy dentro do item: cria a `financial_accounts` vinculada na primeira vez (mapeando `CHECKINGS_ACCOUNT`/`SAVINGS_ACCOUNT`/`CREDIT_CARD` → `checking`/`savings`/`credit`, `creditData.creditLimit`/`brand`/dia de `balanceCloseDate`/`balanceDueDate` pros campos de cartão) — nunca sobrescreve o **nome** depois de criada, só os campos "administrados" pela Pluggy; importa transações novas (paginado, `from` = `last_synced_at` menos 3 dias de folga, ou 90 dias no primeiro sync) com upsert por `pluggy_transaction_id`; e termina com uma reconciliação final de saldo (`UPDATE financial_accounts SET balance = <saldo real da Pluggy>` direto, sem passar por `transactions` — seguro porque não recursiona no trigger `update_account_balance()`, corrige qualquer deriva). Contas de crédito mantêm `balance=0`, mesma convenção das contas manuais (fatura = soma de `transactions` com `fatura_paid=false`); `fatura_date` calculado com a mesma regra de `defaultFaturaDate()` do `TransactionForm` (duplicada aqui de propósito, mesmo padrão de `addOneMonth()` já duplicado entre crons). Sinal do valor: conta `BANK` usa o sinal direto da Pluggy (negativo=despesa); conta `CREDIT` inverte (positivo=compra=despesa), batendo com a matemática de fatura que `contas/page.tsx` já fazia.
- **Rotas novas** (`src/app/api/open-finance/`): `POST connect-token` (gera o `connectToken` restrito pro widget; aceita `itemId` opcional pra reconexão, que vira `updateItem` na Pluggy); `POST items` (chamado pelo `onSuccess` do widget — busca o `Item` de volta na Pluggy e só aceita se `item.clientUserId === profile.id`, senão 403: sem essa checagem, qualquer usuário autenticado poderia vincular à força a conexão bancária de outro perfil só reaproveitando um `itemId`; depois faz upsert em `open_finance_items` por `pluggy_item_id` — mesma rota cobre criação e reconexão — e chama `syncOpenFinanceItem`); `POST items/[itemId]/sync` (botão manual); `DELETE items/[itemId]` (desconecta: remove o item na Pluggy best-effort, `financial_accounts.is_open_finance` volta a `false` pras contas vinculadas, mas **a conta e o histórico de lançamentos já importados não são apagados** — o usuário continua com a conta pra lançar manualmente dali pra frente); `POST webhook` (Pluggy avisa eventos do item — sem forma confiável de validar assinatura aqui, então o corpo nunca é tratado como verdade além do `itemId`: toda mudança de estado vem de rebuscar o item na API da Pluggy dentro de `syncOpenFinanceItem`, o que também neutraliza forjar um evento pra um item que não é nosso). Novo cron `GET /api/cron/open-finance-sync` (`vercel.json`, diário às 5h, mesmo guard `CRON_SECRET` dos outros crons) resincroniza todo item — rede de segurança pro webhook não disparar (dev sem URL pública, entrega perdida).
- **`OpenFinanceManageDialog`** (`src/components/financial/open-finance-manage-dialog.tsx`, novo, substitui o botão desabilitado em `contas/page.tsx`) — lista os bancos conectados (logo via `Avatar`/`AvatarImage`, nome do conector, badge de status, "sincronizado há Xh" via `formatRelativeTime`), botão "Sincronizar agora"/"Desconectar" por item (`confirm()` nativo antes de desconectar, mesmo padrão de outras exclusões do módulo), "Reconectar" quando `status='LOGIN_ERROR'`, e "Conectar novo banco" no fim. Sem credenciais configuradas, mostra aviso explicando as env vars em vez de qualquer coisa quebrar. Usa o widget oficial `react-pluggy-connect`/`pluggy-connect-sdk` (npm, adicionado às dependências) — **carregado via `next/dynamic({ ssr: false })`**: o SDK escreve `window.__REACT_PLUGGY_CONNECT_SDK_VERSION` no escopo do módulo, o que quebra a passada de SSR do Next se importado no topo do arquivo (`window is not defined`) — bug real encontrado e corrigido ainda nesta rodada, ver verificação abaixo. `AccountCard` ganhou badge "Open Finance" (`variant="outline"`) ao lado da badge "Compartilhada" quando `is_open_finance`.
- **Verificado num browser real** (Playwright, `ana.ferreira`, servidor de dev do próprio usuário na porta 3000, sem reiniciar nada): login → `/dashboard/financeiro/contas` → botão "Gerenciar Open Finance" habilitado (antes `disabled`) → clique abre o dialog → mostra corretamente o aviso de "integração não configurada" (`PLUGGY_CLIENT_ID`/`PLUGGY_CLIENT_SECRET` vazios) → resto da página (cards de conta, abas Ativas/Arquivadas) sem regressão. `console --errors` acusou o bug de SSR acima na primeira passada; corrigido com `next/dynamic`, reverificado limpo. **Não verificado**: o fluxo real de conectar um banco (widget Pluggy, `onSuccess`, importação de contas/transações) — exige credenciais Pluggy que o usuário ainda não tem, e a migration `088` ainda não foi aplicada no Supabase (nenhuma das duas depende de código, só de ação do usuário fora desta sessão). `tsc`/`eslint`/`next build` limpos.
- **Pendências que dependem do usuário**: (1) aplicar `supabase/migrations/088_open_finance.sql` no SQL editor do Supabase; (2) criar conta em `dashboard.pluggy.ai` e preencher `PLUGGY_CLIENT_ID`/`PLUGGY_CLIENT_SECRET` em `.env.local` (e nas env vars de produção na Vercel, mesmo lembrete operacional já registrado pra Stripe/`SUPERADMIN_EMAILS` na 7.16/7.19); (3) em produção, `NEXT_PUBLIC_APP_URL` precisa apontar pro domínio público de verdade pra `pluggyWebhookUrl()` registrar o webhook — sem isso, sincronização automática só acontece pelo cron diário (5h) e pelo botão manual, nunca em tempo quase real.
- **Deliberadamente fora desta rodada**: etapa "Conta automática" do `AccountWizard` (7.29) continua desabilitada — ver nota na 7.29; parcelamento/baixa de fatura pra cartões importados (mesma limitação já existente pra cartões manuais, 3.1); qualquer UI de reatribuir uma conta Open Finance existente pra outro `financial_accounts.id` (se o usuário desconectar e reconectar o mesmo banco, uma nova conta é criada — a conta antiga fica órfã, com `is_open_finance=false`, mas não é apagada nem mesclada automaticamente).

### 7.33 Open Finance desativado por custo + importação de extrato OFX como alternativa gratuita (migration `089_transaction_import.sql`, 2026-09-04)

Décima terceira rodada, ainda no mesmo dia da 7.32 — o usuário pesquisou o preço real da Pluggy (~R$2.500/mês) e decidiu que é inviável nesta fase do produto (sem receita ainda). Pediu pra **manter todo o código da 7.32 pronto pra ativar no futuro, mas desativado agora**, e implementar uma alternativa gratuita.

- **Open Finance voltou a ficar desabilitado**: `contas/page.tsx` voltou a renderizar o botão estático (`disabled`, `title="Em breve"`) em vez do `OpenFinanceManageDialog` — só essa troca de duas linhas. **Nada da 7.32 foi removido**: schema (`088_open_finance.sql`), `src/lib/open-finance/`, as 5 rotas em `src/app/api/open-finance/`, o cron `open-finance-sync` e o componente `OpenFinanceManageDialog` continuam no repo intactos — reativar no futuro é só trocar o botão de volta pelo dialog em `contas/page.tsx` (e, aí sim, aplicar a migration + configurar `PLUGGY_CLIENT_ID`/`PLUGGY_CLIENT_SECRET`).
- **Alternativa gratuita: importação de extrato OFX** — toda instituição financeira brasileira permite exportar o extrato em OFX (Open Financial Exchange, formato padrão do setor, usado por gerenciadores financeiros desde sempre) direto do internet banking. Sem custo, sem API, sem credencial nenhuma.
  - **`transactions.import_uid TEXT`** (migration 089) — guarda o `FITID` do OFX (id único que o próprio banco atribui a cada lançamento), com `UNIQUE(account_id, import_uid)` — permite reimportar o mesmo período sem duplicar (mesma ideia do `pluggy_transaction_id` da 7.32, formato de dedup diferente porque a fonte é um arquivo, não uma API). `'import'` somado ao `CHECK` de `source`.
  - **`src/lib/statement-import/ofx.ts`** — parser client-safe (sem dependência nova; tentei `ofx-js`/`node-ofx-parser` no npm mas pareciam antigos/não mantidos, e o formato que precisamos extrair — só `STMTTRN`/`DTPOSTED`/`TRNAMT`/`FITID`/`NAME`/`MEMO` — é simples o bastante pra um parser próprio por regex, tolerante tanto a OFX 1.x (SGML, tags sem fechamento) quanto 2.x (XML). `readOfxFile()` lê o cabeçalho do arquivo (sempre ASCII puro） pra achar `CHARSET:` antes de decodificar o corpo inteiro — bancos brasileiros costumam exportar em Windows-1252/ISO-8859-1, não UTF-8, e decodificar direto como UTF-8 quebraria acentuação.
  - **`ImportStatementDialog`** (`src/components/financial/import-statement-dialog.tsx`, novo, botão "Importar extrato" ao lado de "Adicionar conta" em `AccountsList`) — fluxo de 2 telas: (1) escolher conta + arquivo `.ofx`/`.qfx` (parseado no navegador, sem round-trip ao servidor); consulta os `import_uid` já existentes daquela conta pra calcular "N novos / M já importados" antes de mostrar qualquer coisa; (2) preview de todos os lançamentos parseados (data, descrição, valor colorido por tipo, os já importados esmaecidos) + botão "Importar N lançamentos", que insere só os novos via `supabase.from('transactions').insert(...)` direto do cliente (mesmo padrão de `TransactionForm`/`AccountWizard` — RLS de `transactions_owner_all` já cobre, sem precisar de rota nova), em lotes de 200. Contas de **crédito ficam de fora do seletor** — o sinal de `TRNAMT` em OFX de fatura de cartão varia entre emissores (alguns exportam compra como negativo, outros como positivo), então implementar exigiria confirmar convenção por banco; arriscar lançamento com sinal trocado em dado financeiro real não valia a pena pra esta rodada. **Mini-manual embutido** (a pedido do usuário, mesmo dia): `<details>`/`<summary>` nativo (sem dependência nova de accordion/collapsible, que não existe em `components/ui/`) recolhido por padrão na tela 1, explicando o caminho geral (Extrato → exportar/baixar → formato OFX, às vezes rotulado "Money"/"Quicken") + dicas específicas pra Nubank/Itaú/Bradesco/Banco do Brasil/Santander/Caixa/Banco Inter — texto hedged ("os passos exatos variam conforme a versão do app"), já que caminho de menu de app de banco muda com frequência e não dá pra confirmar em tempo real; aviso final de que bancos digitais que só oferecem PDF/CSV não têm como ser usados aqui ainda.
  - **Sinal assumido para conta corrente/poupança** (único caso suportado): `TRNAMT` negativo = despesa, positivo = receita — convenção universal do OFX pra contas bancárias, sem exceção entre bancos.
- **Verificado num browser real de ponta a ponta** (Playwright, `ana.ferreira`): criado um arquivo OFX sintético (3 lançamentos — 1 receita, 2 despesas, um deles com vírgula decimal pra testar o parser) e importado de verdade contra a conta seed — preview mostrou os 3 corretamente, confirmar importação criou as `transactions`, saldo da conta atualizou de R$4.230,00 pra R$5.594,60 (bate com a soma esperada, confirma que o trigger `update_account_balance()` disparou certo), toast de sucesso, sem erro de console. Dados de teste apagados depois (`DELETE ... WHERE import_uid IN (...)`, saldo restaurado a R$4.230,00 — confirmado). Botão "Gerenciar Open Finance" confirmado desabilitado de novo. `tsc`/`eslint`/`next build` limpos.
- **Pendente do usuário**: nenhuma — a migration `089_transaction_import.sql` já foi aplicada no Supabase (2026-09-04, confirmado pelo próprio usuário rodando o SQL no editor), então a feature já funciona em produção sem depender de mais nenhum passo manual.
- **Deliberadamente fora**: suporte a CSV (formato varia por banco, sem padrão — ficaria pra uma rodada própria se for pedido, provavelmente exigindo uma UI de mapear colunas); importação de fatura de cartão de crédito (ver acima); qualquer automação de "baixar o extrato sozinho" — o usuário sempre faz o download manual no banco, só a leitura do arquivo é automática.

---

## 8. Frontend — estrutura de rotas (`src/app/`)

```
src/app/
├── layout.tsx                        # Root: ThemeProvider + Toaster (sonner) + fonte Inter
├── page.tsx                          # Landing (redirect /dashboard se autenticado) — hero (2 colunas, foto em public/hero-missionario.png) + diferenciais + "como funciona" + bento grid de módulos + deep-dive + teaser de planos; usa SiteNav/SiteFooter
├── planos/page.tsx                   # Server — página de preços pública; PricingToggle (client) chama /api/billing/checkout; usa SiteNav/SiteFooter
├── globals.css
├── (auth)/                           # Route group sem prefixo de URL
│   ├── layout.tsx                    # Card centralizado, sem nav
│   ├── login/page.tsx                # Client, Suspense (useSearchParams)
│   └── cadastro/page.tsx             # Client — signUp → redirect /onboarding
├── auth/callback/route.ts            # Route Handler — troca code OAuth por sessão
├── l/[code]/route.ts                  # Redirect de link curto (perfil/projeto) — ver 7.18
├── onboarding/page.tsx                # Server — busca profile, renderiza OnboardingWizard (client; passo "role" + 4 passos de missionário — ver 7.1/7.1-bis); também usado pelo fluxo "virar missionário"
├── conta/excluir/page.tsx             # Exclusão para quem só é parceiro
├── [username]/                        # Perfil público
│   ├── layout.tsx                     # LanguageSwitcher flutuante + ProfileTabs (barra de abas, oculta em mensagens/oracao/parceria/projetos/[slug]) + slot @modal — envolve todas as subrotas
│   ├── @modal/default.tsx             # null (nenhum modal ativo) — ver 7.3-bis
│   ├── @modal/(.)parceria/page.tsx    # Intercepta navegação in-app pra /parceria e abre em modal (PartnershipModal) — ver 7.3-bis
│   ├── @modal/(.)oracao/page.tsx      # Intercepta navegação in-app pra /oracao e abre em modal (PrayerModal) — ver nota de modal em 7.6
│   ├── page.tsx                       # Server, generateMetadata; lógica de privacidade + canEdit (ver 7.12); ProfileOwnerActions no lugar de ProfileCTA quando é o dono/gestor
│   ├── historia/page.tsx              # Blocos "nossa história"
│   ├── trajetoria/page.tsx            # Timeline de projetos concluídos
│   ├── parceria/page.tsx              # Entrada do PartnershipWizard (página cheia — acesso direto/deep link; ver 7.3-bis pro modal)
│   ├── oracao/page.tsx                # Enviar oração de apoio ao missionário (página cheia — acesso direto/deep link; ver nota de modal em 7.6); não é o parceiro pedindo que orem por ele — ver nota abaixo de 7.6
│   ├── mensagens/page.tsx             # Chat 1:1 (qualquer autenticado, não exige ser parceiro) + E2EEGate; redireciona pra / quando quem acessa é o próprio dono/gestor (ver 7.12)
│   └── projetos/
│       ├── page.tsx                   # Lista pública (ativos + concluídos)
│       └── [slug]/page.tsx            # Página pública de projeto (fundraising completo)
└── dashboard/
    ├── layout.tsx                     # Shell autenticado: sidebar + bell + mobile nav + CreateContentFab + SuperadminRoleSwitcher (só pra SUPERADMIN_EMAILS — ver 7.1-bis)
    ├── page.tsx                       # Feed universal (parceiro e missionário) — ver 7.1-bis; NÃO é mais a visão de gestão
    ├── painel/page.tsx                # Visão de gestão do missionário (stats/checklist/atalhos) — conteúdo que antes vivia em dashboard/page.tsx
    ├── feed/actions.ts                # getFeedPage, followProfile/unfollowProfile, getDiscoverMissionaries, getFollowedProjectStories (ver 7.1-bis/7.13)
    ├── meus-projetos/page.tsx         # Parceiro: projetos em que é parceiro (somente leitura)
    ├── financeiro-parceiro/page.tsx   # Parceiro: histórico de doações + comprovante para IR
    ├── destaques/                     # LEGADO — page.tsx redireciona para /dashboard/projetos
    │   ├── page.tsx                   # redirect('/dashboard/projetos')
    │   ├── novo/page.tsx              # ainda funcional, desconectado
    │   └── [id]/page.tsx              # ainda funcional, desconectado
    ├── publicacoes/
    │   ├── page.tsx
    │   └── nova/page.tsx
    ├── projetos/                      # rota "atual" (substituiu destaques)
    │   ├── page.tsx
    │   ├── novo/page.tsx
    │   ├── actions.ts                 # Server Action legada 'use server' — código morto
    │   └── [id]/
    │       ├── page.tsx
    │       └── equipe/page.tsx
    ├── parceiros/page.tsx
    ├── ia/page.tsx                     # Copiloto IA: saldo de créditos, feature de tradução, histórico (ai_credit_transactions)
    ├── oracoes/page.tsx                # com E2EEGate condicional
    ├── mensagens/
    │   ├── page.tsx
    │   └── [userId]/page.tsx          # com E2EEGate
    ├── financeiro/
    │   ├── layout.tsx                 # FinanceSubNav
    │   ├── page.tsx                   # Visão geral
    │   ├── contas/page.tsx
    │   ├── lancamentos/page.tsx
    │   ├── categorias/page.tsx
    │   └── conciliacao/page.tsx       # Fila de pledges pendentes
    └── configuracoes/
        ├── page.tsx                   # SettingsTabs
        └── actions.ts                 # inviteManagerBypass (convite de gestor sem limite, só superadmin — 7.19)
└── api/
    ├── account/delete/route.ts
    ├── accounts/members/route.ts
    ├── projects/members/route.ts
    ├── highlights/route.ts
    ├── partner/tax-export/route.ts    # GET ?year=&format=csv|pdf — comprovante agregado de doações (ver 7.1-bis)
    └── billing/
        ├── checkout/route.ts          # Cria Stripe Checkout Session (plano ou manager_addon) — 501 se STRIPE_SECRET_KEY não configurado
        └── webhook/route.ts           # Sincroniza profiles.plan/extra_manager_seats/subscriptions após checkout (7.19) — secret próprio, diferente do Connect (/api/stripe/webhook)
```

> **Gaps/inconsistências conhecidas (documentadas para não serem "redescobertas" como bugs)**:
> - `destaques` (nome legado) vs `projetos` (nome atual) — migração de nomenclatura incompleta; `destaques/novo` e `destaques/[id]` ainda funcionam mas ficaram órfãos de navegação.
> - Duas implementações paralelas de salvar highlight: `dashboard/projetos/actions.ts` (Server Action, não usada por nenhum componente) e `POST /api/highlights` (efetivamente usada) — a Server Action é código morto.

### Renderização
Todas as páginas de rota (exceto formulários) são **Server Components** assíncronos que buscam dados via `createClient()` de `@/lib/supabase/server` e passam como props para Client Components de interação. Padrão de mutação client-side: `setSaving(true)` → `supabase.from(table).insert/update/delete()` → toast de erro (`sonner`) ou `router.refresh()`/`router.push()`.

### Lógica notável de páginas específicas
- **`[username]/page.tsx`**: `stealth` → 404 real (`notFound()`) + `robots: noindex`; `private` → exige login + vínculo em `partners` (a menos que `canEdit`, ver 7.12), senão `PrivateProfileScreen`.
- **`[username]/projetos/[slug]/page.tsx`**: aceita slug OU UUID (`.or('slug.eq.X,id.eq.X')`); busca em paralelo milestones, posts do projeto, `project_budget_progress`, projetos concluídos anteriores, contagem de pledges confirmados; monta lista de formas de apoio filtrando por `goal_type` e métodos de pagamento disponíveis.
- **`dashboard/financeiro/*`**: RLS decide visibilidade de contas compartilhadas — queries não filtram `profile_id` explicitamente para `financial_accounts`.

---

## 9. Layouts

- **`app/layout.tsx`**: fonte `Inter` (`next/font/google`, var `--font-sans`), `ThemeProvider` (`next-themes`, `attribute="class"`, `defaultTheme="system"`), `<Toaster richColors position="top-right" />` global, `<html lang="pt-BR" suppressHydrationWarning>`.
- **`(auth)/layout.tsx`**: `min-h-screen flex items-center justify-center bg-muted/30`, `max-w-md`, sem nav.
- **`dashboard/layout.tsx`**: guarda de auth (Server) — sem `user` ou sem `profile` → `redirect('/login')`. `flex h-screen overflow-hidden`: `DashboardSidebar` (desktop), header com `MobileHeader` + `NotificationsBell`, `<main className="max-w-5xl mx-auto">`, `MobileBottomNav` fixo (mobile).
- **`dashboard/financeiro/layout.tsx`**: guarda de auth + título "Financeiro" + `FinanceSubNav`.

### 9.1-bis Convenção de carregamento/streaming (2026-07-25)

Motivado por relato real de usuário (parceiro via a página pública travar em branco, sem indicador nenhum, ao abrir pelo celular). Investigação achou o mesmo bug estrutural repetido em vários pontos do app — regras abaixo existem pra não reintroduzi-lo em rotas novas:

- **`loading.tsx` só envolve `page.tsx`, nunca o `layout.tsx` da mesma pasta.** Se um `layout.tsx` tem `await`s bloqueantes antes de `{children}`, nenhum skeleton aparece enquanto eles não resolverem — era exatamente o caso de `[username]/layout.tsx` (3 queries sequenciais antes da barra de abas). Layout não pode ter `{children}` atrás de `await` bloqueante, **exceto quando for gate de autenticação real** (ex.: `dashboard/layout.tsx`, que precisa saber quem é o usuário antes de mostrar qualquer coisa do dashboard). Nesse caso de exceção, ao menos paralelizar o que for independente do usuário (ex.: `getTranslations`) e confiar no `loading.tsx` mais próximo acima na árvore.
- **`src/app/loading.tsx`** (novo, raiz do app) existe como rede de segurança genérica — cobre qualquer layout que precise legitimamente bloquear, pra nunca mostrar tela branca.
- **Queries independentes sempre em `Promise.all`, nunca `await` sequencial** quando uma não depende do resultado da outra. Inclui efeitos colaterais que não bloqueiam o que a tela precisa mostrar (ex.: `markNotificationTypesRead()` em `dashboard/oracoes`/`parceiros`/`publicacoes` entra no mesmo `Promise.all` das queries de leitura, em vez de rodar antes).
- **Seções pesadas/abaixo da dobra usam `<Suspense>` + componente-wrapper `async` próprio**, não entram no fetch bloqueante principal — padrão em `[username]/page.tsx` (`ProjectsSectionAsync`/`PublicationsFeedAsync`) e `[username]/historia/page.tsx` (`HistoryBlocksAsync`). `notFound()`/checagens de privacidade continuam sendo os primeiros statements síncronos após seus `await`s obrigatórios, **antes** de qualquer JSX com `<Suspense>` — nunca mover checagem de existência/acesso pra dentro de um filho streamado (senão o status HTTP de 404 vira "soft 404", ver docs do Next sobre streaming).
- **Entidade buscada por várias rotas da mesma árvore usa helper `cache()` compartilhado.** `getProfile(username)` (`src/lib/profile/get-profile.ts`, mesmo padrão de `getProfileViewerContext`) substitui a query solta de `profiles` que cada rota sob `[username]/*` fazia por conta própria.
- **Imagem LCP usa `preload`** (não `priority`, obsoleto no Next 16) — ex.: avatar em `profile-header.tsx`.
- **`src/proxy.ts` só chama `supabase.auth.getUser()` em rotas que realmente precisam** (protegidas ou de auth) — antes rodava em toda rota, inclusive páginas públicas, atrasando o TTFB antes do Next.js sequer começar a responder.

---

## 10. Inventário de componentes (`src/components/`)

### `dashboard/`
| Componente | Papel |
|---|---|
| `sidebar.tsx` | `DashboardSidebar` (nav desktop), `MobileHeader`, `MobileBottomNav` (tab bar mobile; 5º slot é uma miniatura de avatar linkando pra `/[username]`, não mais um botão "Menu" — ver 10 `account-menu-drawer.tsx` e changelog) — nav consciente de papel via `useNav(role)`/`useBottomNavItems(role)` (`src/hooks/use-dashboard-nav.ts`, seção 5.5.2), lê `profile.user_role` do perfil ativo (ver 7.1-bis) |
| `superadmin-role-switcher.tsx` | `SuperadminRoleSwitcher` — barra flutuante só renderizada quando `isSuperAdmin(user.email)` (allowlist `SUPERADMIN_EMAILS`); alterna `preview_role` (cookie) pra ver o nav de missionário/parceiro com a própria conta, sem trocar `user_role` real nem contornar RLS (ver 7.1-bis) |
| `account-menu-drawer.tsx` | `AccountMenuDrawer` — gaveta de navegação completa (extraída de `MobileBottomNav`), self-contained (`open` + trigger próprios, ícone `Menu`); montada a partir de `/[username]` (via `ProfileTabs`, só quando `canEdit`) em vez do rodapé do dashboard |
| `create-content-fab.tsx` | `CreateContentFab` — botão flutuante circular (estilo X/Twitter) com `DropdownMenu` "Nova publicação"/"Novo projeto"; oculto para `role='partner'` |
| `notifications-bell.tsx` | Dropdown com badge de não lidas, usa `useNotifications` |
| `locale-content-tabs.tsx` | `LocaleContentTabs` — abas PT/EN/ES compartilhadas (post + bio), tradução manual sempre livre + botão opcional "Traduzir com IA" (seção 5.6.1) |
| `setup-checklist-banner.tsx` | `SetupChecklistBanner` — checklist dismissível (localStorage + `CustomEvent`/`useSyncExternalStore`) com até 3 itens pendentes: perfil incompleto, sem método de recebimento, sem projeto criado (ver 7.1) |
| `birthday-reminders.tsx` | `BirthdayReminders` — Server Component, banner de aniversários de parceiros nos próximos 14 dias, sem persistência própria (ver 7.1 item 8) |
| `settings/settings-tabs.tsx` | Tabs client-side (Perfil/Pagamentos/Privacidade/Conta) |
| `settings/profile-form.tsx` | Avatar, checagem de username disponível (debounce 500ms), redes sociais; bio via `LocaleContentTabs` |
| `settings/payment-methods-list.tsx` | `PaymentMethodsList` — grid de métodos de recebimento cadastrados + botão "Novo método" |
| `settings/payment-method-form.tsx` | `PaymentMethodForm` — dialog criar/editar (`account?` opcional, mesmo padrão de `financial/account-form.tsx`), tipo selecionado no catálogo (`src/lib/payment-methods/catalog.ts`) |
| `settings/payment-method-card.tsx` | `PaymentMethodCard` — card com ícone/label/valor (copiar), badge inativo, editar/excluir |
| `settings/stripe-connect-card.tsx` | `StripeConnectCard` — conectar/continuar onboarding/desconectar Stripe Express, escolher `linked_account_id`, toasts por `?stripe=` (ver fluxo 7.11) |
| `settings/privacy-form.tsx` | 3 modos de privacidade + `SensitiveDataForm` dentro de `E2EEGate` |
| `settings/sensitive-data-form.tsx` | Dados sensíveis cifrados (blob único JSON) |
| `settings/account-form.tsx` | Trocar senha; card "Divulgar minha própria missão" (só visível pra `user_role='partner'`, chama `becomeMissionary()` — ver 7.1-bis); zona de perigo (exclusão) |

### `dashboard/post-composer/` (wizard de criação/edição de posts, ver 7.9)
| Componente | Papel |
|---|---|
| `post-composer-provider.tsx` | `PostComposerProvider`/`useComposer()` — contexto que abre/fecha o `PostComposerModal`; API pública inalterada desde o antigo `PostEditor` |
| `post-composer-modal.tsx` | `PostComposerModal` — shell fullscreen (`DialogContent` customizada, padrão de `EditProfileDialog`), cabeçalho X/voltar + "Avançar", roteia entre os 4 passos |
| `use-post-composer.ts` | `usePostComposer` — estado consolidado do wizard (mídia, legenda/traduções, tags, localização, agendamento) + `handleSave` (upload, `bakeImage`, `savePost`) |
| `types.ts` | `MediaDraft`/`TagDraft`/`MediaAdjustments`, `resolveCssFilter()` (combina preset + sliders numa string de `filter` CSS) |
| `step-type-picker.tsx` | Post vs. Projeto (Projeto navega pra `/dashboard/projetos/novo`) |
| `step-media-select.tsx` | Seleção de arquivos + tirinha de miniaturas do carrossel |
| `image-crop-editor.tsx` | `ImageCropEditor` — pan/zoom/proporção/grade dos terços, estende o padrão de drag de `CoverEditor` (7.7) |
| `step-adjust.tsx` | Abas Filtros/Ajustes, imagem sempre visível (lado a lado em `md+`) |
| `tag-people-picker.tsx` | `TagPeoplePicker` — clique na imagem busca e marca um perfil real (`searchTaggableProfiles`) |
| `step-details.tsx` | Legenda, marcação, localização, projeto, agendar, botões salvar |
| `discard-confirm-dialog.tsx` | "Descartar publicação?" |

### `dashboard/feed/`
| Componente | Papel |
|---|---|
| `feed-screen.tsx` | `FeedScreen` — Server Component raiz de `/dashboard`, busca a primeira página + `follows` + stories em paralelo, decide entre `FeedList` e `DiscoverMissionaries` (ver 7.1-bis) |
| `feed-list.tsx` | `FeedList` — client, "carregar mais" via Server Action (`getFeedPage`, cursor por `published_at`) |
| `discover-missionaries.tsx` | `DiscoverMissionaries` — estado vazio do feed: perfis públicos `user_role='missionary'`, sem ranking, exclui o próprio perfil do visitante |
| `follow-button.tsx` | `FollowButton` — toggle otimista (`usePendingAction`) de `follows`, grava `feed_events` (`follow`/`unfollow`); prop opcional `followsViewer` troca o label pra "Seguir de volta" (migration 037, ver 7.12) |
| `follows-list-actions.ts` | Server actions de leitura só: `getFollowers`/`getFollowing` (`get_followers`/`get_following` RPC, paginado), `getFollowCounts`, `getFollowerProfileIds` (ids de quem segue de volta, usado só pra calcular o badge de mútuo) |
| `search-actions.ts` | `searchDirectory(query)` — busca difusa (`pg_trgm`/`unaccent`, migration 059, ver 7.14) via `supabase.rpc('search_missionaries'/'search_highlights', ...)`, ranqueada por similaridade e tolerante a acento; `sanitizeQuery` remove `%`/`_` do termo pra não virarem coringa dentro do `LIKE` que as funções montam internamente. Usada por `dashboard/buscar/page.tsx` (ver 7.14). |
| `project-stories-row.tsx` | `ProjectStoriesRow` — faixa horizontal estilo Instagram de projetos ativos de quem o usuário segue/apoia com atualização recente; anel vira gradiente quando `hasUnseen` (ver 7.13) |
| `project-story-viewer.tsx` | `ProjectStoryViewer` — visualizador em tela cheia, tap-to-advance entre as atualizações de um projeto, "Ver projeto completo" fixo embaixo |

### `dashboard/partner/`
| Componente | Papel |
|---|---|
| `my-projects-list.tsx` | `MyProjectsList` — lista somente-leitura de projetos em que o usuário é parceiro (financeiro confirmado ou recorrente ativo); nunca reaproveita `HighlightsList` (tem editar/reordenar/excluir) |
| `giving-history.tsx` | `GivingHistory` — histórico de `pledges` do usuário entre missionários, com badge de status e export de comprovante (CSV/PDF, `GET /api/partner/tax-export`) |
| `giving-filters.tsx` | `GivingFilters` — `<select>` nativo de filtro por missionário, só renderizado quando há mais de um |

### `financial/`
| Componente | Papel |
|---|---|
| `account-card.tsx` | Card de conta, badge compartilhada + badge "Open Finance" (`is_open_finance`, ver 7.32), editar/gerenciar membros |
| `account-form.tsx` | Dialog criar/editar conta |
| `category-panel.tsx` | `CategoryPanel` — abas de escopo (Todas/Receitas/Despesas/Despesas Não Pagas) sobre `CategoryBarChart`, painel "Gráficos" da Visão Geral (ver 7.21) |
| `category-form.tsx` | Dialog criar/editar categoria — nome + paleta de 10 cores (ver 7.26) |
| `category-tree.tsx` | Árvore 2 níveis de categorias, cor + "Limite de gasto" por categoria, busca, link pra lançamentos filtrados (ver 7.26) |
| `finance-sub-nav.tsx` | Tabs de navegação (`usePathname`) |
| `financial-dashboard.tsx` | Container da Visão Geral — dono do filtro de período/moeda, do mês selecionado e da métrica da linha do tempo, compõe `MonthNavigator` + `MonthSummaryCards` + `TrendChart` + `MonthTransactionsPanel` + `CategoryPanel` (ver 11.1, 7.20, 7.21) |
| `goal-contribution-dialog.tsx` | `GoalContributionDialog` — aporte manual, soma em `financial_goals.current_amount` (ver 7.21) |
| `goal-form.tsx` | Dialog criar/editar meta de economia (ver 7.21) |
| `goals-list.tsx` | `GoalsList` — "Visão geral" (anel de composição por moeda) + cards de meta com cor própria, progresso, "Registrar valor", editar/excluir (ver 7.21, 7.25) |
| `import-statement-dialog.tsx` | `ImportStatementDialog` — importa extrato OFX pra uma conta corrente/poupança (parse no navegador, dedup por `import_uid`, sem custo/API), alternativa gratuita ao Open Finance desativado (ver 7.33) |
| `month-navigator.tsx` | `MonthNavigator` — abas de métrica (Saldo previsto/Despesas e receitas/Despesas/Despesas fixas/Receitas/Receitas fixas) + linha do tempo horizontal de meses (setas, "Mês atual", toggle ocultar valores) (ver 7.20) |
| `month-summary-cards.tsx` | `MonthSummaryCards` — Saldo Anterior/Receitas (recebido×a receber)/Despesas (pago×não pago)/Saldo Disponível+Previsto do mês selecionado, contagem animada; substitui o antigo `balance-summary.tsx` (ver 7.20) |
| `month-transactions-panel.tsx` | `MonthTransactionsPanel` — abas Todas/Receitas/Despesas + busca sobre `TransactionTable`, escopado ao mês selecionado; painel "Lançamentos" da Visão Geral (ver 7.21) |
| `manage-members-dialog.tsx` | Add/remove membro de conta compartilhada via API |
| `new-goal-button.tsx` | Abre `GoalForm` |
| `new-recurring-transaction-button.tsx` | Abre `RecurringTransactionForm` |
| `new-spending-limit-button.tsx` | Abre `SpendingLimitForm`; desabilitado quando toda categoria já tem limite |
| `new-transaction-button.tsx` | Abre `TransactionForm` — usado em Lançamentos e, desde 7.20, também na Visão Geral |
| `open-finance-manage-dialog.tsx` | `OpenFinanceManageDialog` — substitui o antigo botão desabilitado "Gerenciar Open Finance"; lista bancos conectados, conecta/reconecta via widget Pluggy (`react-pluggy-connect`, `next/dynamic({ssr:false})`), sincronizar/desconectar (ver 7.32) |
| `period-filter-bar.tsx` | `PeriodFilterBar` — ←/→, "Esse mês", intervalo de data, ocultar valores, Atualizar; topo da Visão Geral (7.23) e da aba Relatórios (7.24), cada uma com seu próprio estado |
| `pledge-review-card.tsx` | Confirmar/rejeitar pledge (ver fluxo 7.2) |
| `reconciliation-queue.tsx` | Lista de `PledgeReviewCard` |
| `recurring-transaction-form.tsx` | Dialog criar/editar recorrência |
| `recurring-transactions-list.tsx` | Lista de recorrências, pausar/retomar/editar/excluir |
| `reports-analytics.tsx` | `ReportsAnalytics` — container da aba "Relatórios": Despesas/Receitas por categoria + `FrequencyChart` (ver 7.24) |
| `spending-limit-form.tsx` | Dialog criar/editar limite de gastos — um por categoria (ver 7.21) |
| `spending-limits-list.tsx` | `SpendingLimitsList` — progresso gasto×limite por categoria, aviso visual ao estourar (ver 7.21) |
| `transaction-filters.tsx` | Filtros via query string |
| `transaction-form.tsx` | Dialog criar/editar transação, máscara BRL |
| `transaction-table.tsx` | Lista, editar inline, excluir (`window.confirm`) |

### `ui/charts/`
| Componente | Papel |
|---|---|
| `trend-chart.tsx` | `TrendChart` — linha/área 2 séries (entradas/saídas) mês a mês, SVG responsivo, crosshair+tooltip, mês clicável, toggle de tabela |
| `category-bar-chart.tsx` | `CategoryBarChart` — barras horizontais de composição por categoria, clique navega pra Lançamentos filtrado, toggle de tabela |
| `frequency-chart.tsx` | `FrequencyChart` — colunas pareadas (entradas/saídas) por dia/semana dentro do mês, hover por par, toggle de tabela (ver 7.24) |
| `donut-chart.tsx` | `DonutChart` — anel de composição (parte de um todo, número-herói no centro), SVG `stroke-dasharray`, cor por fatia vem de fora (ver 7.25) |

Ver 11.1 pra convenções de gráfico (paleta, marcas, interação).

### `highlights/` (projetos)
| Componente | Papel |
|---|---|
| `budget-breakdown.tsx` | Barra de progresso por categoria de orçamento |
| `funding-projection-card.tsx` | Usa `computeFundingProjection` |
| `highlight-form.tsx` | Ver fluxo 7.7 |
| `highlights-list.tsx` | Reordenar, toggle ativo/oculto, excluir |
| `project-team-panel.tsx` | Ver fluxo 7.8 |
| `cover-editor.tsx` | `CoverEditor` — upload+drag de capa, extraído do `HighlightForm` (7.7), controlado via `onChange` |
| `milestones-editor.tsx` | `MilestonesEditor` — lista add/remove/toggle de marcos, extraído do `HighlightForm` |
| `budget-categories-editor.tsx` | `BudgetCategoriesEditor` — categorias de orçamento + toggle single/detailed, extraído do `HighlightForm` |
| `support-types-picker.tsx` | `SupportTypesPicker`/`GOAL_TYPE_OPTIONS` — seletor de `goalTypes`, extraído do `HighlightForm` |
| `section-types.ts` | Tipo `HighlightSnapshot` compartilhado pelas 7 ilhas de edição inline (ver 7.12) |
| `edit-section-chrome.tsx` | `EditPencilButton`/`EditActions` — chrome visual compartilhado (ícone de lápis, botões Salvar/Cancelar) das ilhas de edição |
| `cover-title-edit-section.tsx`, `description-edit-section.tsx`, `support-types-edit-section.tsx`, `financial-edit-section.tsx`, `milestones-edit-section.tsx`, `letter-edit-section.tsx`, `dates-status-edit-section.tsx` | As 7 ilhas de edição inline da página pública de projeto (ver 7.12), uma por seção — só renderizam UI de edição quando `canEdit` |

### `messages/`
| Componente | Papel |
|---|---|
| `conversation-list.tsx` | Lista de conversas (derivada em memória, sem tabela própria) |
| `e2ee-gate.tsx` | Ver fluxo 7.5 |
| `message-composer.tsx` | Ver fluxo 7.6 |
| `message-thread.tsx` | Ver fluxo 7.6 |

### `partners/`
| Componente | Papel |
|---|---|
| `add-partner-button.tsx` | Adicionar parceiro manual, respeita limite do plano (free=2); WhatsApp/telefone via `PhoneInput` (DDI + máscara), data de nascimento opcional |
| `partners-list.tsx` | Filtro, badge por tipo, `VisibilityGrantsDialog`, link de mensagem |
| `partnership-form.tsx` | Form público "quero ser parceiro", grava direto sem login |
| `partnership-wizard.tsx` | Ver fluxo 7.3 |
| `pledge-form.tsx` | Ver fluxo 7.2 |
| `recurring-pledge-form.tsx` | `RecurringPledgeForm` — recorrência exige conta (CTA cadastro/login se deslogado), assinatura Stripe ou compromisso manual + lembrete (ver fluxo 7.11) |
| `visibility-grants-dialog.tsx` | Ver fluxo 7.4 |

### `prayer/`
| Componente | Papel |
|---|---|
| `new-prayer-button.tsx` | Missionário cria pedido próprio (sempre em claro) |
| `prayer-inbox.tsx` | Tabs (Todos/Meus/De parceiros/Respondidos), decifra on-demand |
| `prayer-request-form.tsx` | Form público, checkbox "privado (cifrado)", exige `keyManager.isUnlocked()` se marcado |

### `profile/`
| Componente | Papel |
|---|---|
| `history-view.tsx` | Renderiza `history_blocks` |
| `profile-cta.tsx` | Botões de ação do perfil público (Parceria/Oração/Mensagem) — só renderizado quando o visitante **não** tem `canEdit` |
| `profile-owner-actions.tsx` | `ProfileOwnerActions` — substitui `profile-cta.tsx` quando `canEdit=true`: "Editar perfil" (`EditProfileDialog`) + "Compartilhar perfil" (Web Share API / copiar link) |
| `edit-profile-dialog.tsx` | `EditProfileDialog` — abre o `ProfileForm` (de `dashboard/settings/`) dentro de um `Dialog` por cima do perfil público, sem duplicar a lógica de edição; container em tela cheia no mobile (seta voltar + título centralizado, estilo Instagram), popup central no desktop — `ProfileForm` em si não muda |
| `profile-tabs.tsx` | `ProfileTabs` — barra de abas persistente (Perfil/História/Projetos/Trajetória) renderizada pelo `[username]/layout.tsx`; oculta em `mensagens`/`oracao`/`parceria`/`projetos/[slug]` (ver 7.12), onde mostra em vez disso um único botão fixo `BackToProfile` (canto superior esquerdo) + o `LanguageSwitcher` em modo `compact` (canto superior direito). Na variante com abas, o canto superior direito mostra o `ProfileMoreMenu` (menu "..." estilo Instagram, substituiu o `LanguageSwitcher` compact solto que ficava ali antes); quando `canEdit`, o trigger de `AccountMenuDrawer` (`dashboard/`) aparece entre as abas e o `ProfileMoreMenu` — é o único lugar de onde a gaveta de navegação completa é acionada no mobile |
| `profile-more-menu.tsx` | `ProfileMoreMenu` — menu "..." (estilo Instagram) na barra de abas principal do perfil público: "Compartilhar perfil" (Web Share API/copiar link), submenu "Idioma" (`LanguageSwitcher` em modo `submenu`, ver 5.6.1) e, só quando `!canEdit`, "Denunciar perfil" (`ReportDialog`) — consolida num único menu o que antes eram um ícone de compartilhar + um botão "Denunciar perfil" soltos no rodapé de `profile-cta.tsx` (ambos removidos de lá por ficarem redundantes, ver Changelog) |
| `profile-header.tsx` | Avatar/nome/localização/bio/redes — condicionado a `privacy_mode` |
| `projects-section.tsx` | Projetos do perfil público como "destaques" estilo Instagram: faixa horizontal com scroll (`overflow-x-auto snap-x`), capa circular com anel na `accent_color`, título + barra de progresso/valor arrecadado logo abaixo de cada bolinha |
| `trajectory-timeline.tsx` | Timeline vertical de projetos concluídos |

### `onboarding/`
| Componente | Papel |
|---|---|
| `onboarding-wizard.tsx` | `OnboardingWizard` — wizard client, passo `role` (novo, migration 032) + 4 passos de missionário (perfil/recebimento/primeiro projeto/concluído) com estado interno tipado como união de strings; reaproveitado também pelo fluxo "virar missionário" (pula `role` quando `user_role` já é `missionary`) — ver fluxo 7.1/7.1-bis |

### `pricing/`
| Componente | Papel |
|---|---|
| `pricing-toggle.tsx` | `PricingToggle` — cards Free/Pro/Missão a partir de `src/lib/pricing.ts`, toggle mensal/anual, botão "Assinar" chama `POST /api/billing/checkout` |

### `marketing/`
| Componente | Papel |
|---|---|
| `site-nav.tsx` | `SiteNav` — nav sticky com mega-menu "Módulos" (grid com ícone+descrição a partir de `src/lib/modules.ts` + `messages/*.json`), `LanguageSwitcher` embutido, usado em `/` e `/planos` |
| `site-footer.tsx` | `SiteFooter` — rodapé com colunas agrupadas (módulos, conta), gerado a partir de `src/lib/modules.ts` |
| `language-switcher.tsx` | `LanguageSwitcher` — bandeiras PT/EN/ES, troca cookie `NEXT_LOCALE` (ver seção 5.6); prop `compact` troca as bandeiras por um gatilho de texto discreto ("PT ▾") num `DropdownMenu`, usado no perfil público |

> **`src/lib/modules.ts`** é a fonte única dos 7 módulos do produto (`id`, ícone, cor — texto vive em `messages/*.json`) — consumida pela landing (bento grid + deep-dive), pelo mega-menu do `SiteNav` e pelo `SiteFooter`. Ao lançar um módulo novo, adicionar a entrada aqui + a tradução nos 3 `messages/*.json` já propaga para o site público inteiro — convenção adotada para a página pública não ficar defasada conforme o produto cresce.

### `shared/` (componentes reaproveitados por mais de uma área — dashboard + perfil público)
| Componente | Papel |
|---|---|
| `post-card.tsx` | `PostCard` — card de post unificado (substituiu `feed-post-card.tsx` e `publications-feed.tsx` em 2026-07-25, ver 7.9/Changelog): mídia em destaque na proporção `media_aspect_ratio`, bolinhas de carrossel, pinos de marcação sob demanda (`post_tags`), legenda truncada (`line-clamp-2` + "...mais"), curtir/comentar/compartilhar. Usado tanto por `dashboard/feed/feed-list.tsx` quanto por `[username]/page.tsx` — mesma tela para missionário e parceiro. `post.location` (quando presente) é um link próprio (`target="_blank"`) pra busca no Google Maps, separado do link de avatar/nome pro perfil — ver Changelog 2026-08-27 |
| `post-comments-sheet.tsx` | `PostCommentsSheet` — dialog com lista de `post_comments` + input, via `getComments`/`addComment` |
| `post-project-link.tsx` | `PostProjectLink` — pílula de link pro projeto quando o post tem `project_id`, usada por `PostCard` |

### `ui/` (shadcn v4 sobre `@base-ui/react`)
`avatar`, `badge`, `button`, `card`, `dialog`, `dropdown-menu`, `input`, `label`, `progress`, `sonner`, `textarea`.
- **`phone-input.tsx`** (não é shadcn, componente próprio): `<select>` de DDI (bandeira emoji + código, lista curada de ~20 países) + `<input>` mascarado por país (`mask` com `#` por dígito). Componente não-controlado — inicializa a partir de `defaultValue` (string `"+55 11 99999-9999"`) no mount e só se comunica pra fora via `onChange(value)`; não resincroniza se o `defaultValue` mudar depois (assume que o form pai desmonta o campo para resetar, ex: fechar um `Dialog`). Usado em `AddPartnerButton` (campos WhatsApp/Telefone) e em `PartnershipForm` (campo WhatsApp/Telefone do formulário público de parceria).

---

## 11. Convenções de UI (shadcn v4 + `@base-ui/react`)

- `components.json`: `"style": "base-nova"`, `"rsc": true`, `"iconLibrary": "lucide"`, `"baseColor": "neutral"`, `"cssVariables": true`, sem prefixo Tailwind.
- Primitivos vêm de `@base-ui/react/*`, **não** Radix (apesar de `@radix-ui/*` ainda instalado como resquício não usado nos componentes lidos).
- Padrão de composição: **prop `render`** em vez de `asChild`:
  ```tsx
  <DialogTrigger render={<Button variant="ghost" size="icon-sm"><ShieldCheck /></Button>} />
  ```
- **Botão dentro de `<Link>`**: como `next/link` não aceita `render`, o padrão universal do projeto é `className={cn(buttonVariants({ variant, size }))}` direto no `<Link>` — nunca envolver `<Link>` num `<Button>`.
- Variantes via `cva` + atributos `data-slot` para estilização granular; animações via `data-open`/`data-closed`/`data-[side=...]` (CSS puro, `tw-animate-css`).
- `Button`: variantes `default|outline|secondary|ghost|destructive|support|link` (`support` nova, 2026-07-23 — cor `--support`, só para CTAs de doação/apoio), tamanhos `default|xs|sm|lg|icon|icon-xs|icon-sm|icon-lg`. `Badge` ganhou a mesma lógica com `success` (cor `--success`, meta atingida).
- Forms: **sem** `react-hook-form`/`zod` — sempre `useState` por campo + validação inline no `onSubmit` + toast de erro. Padrão: `Label` + `Input`/`Textarea` em `space-y-2`, grupos em `space-y-4`, campos lado a lado em `grid grid-cols-2 gap-3`.
- `<select>` nativo do HTML (não há componente `Select` do shadcn em uso), estilizado manualmente igual ao `Input`.
- Toasts: `sonner`, sempre em português (`toast.error`/`toast.success`).
- Ícones: `lucide-react` exclusivamente.

### 11.1 Gráficos (dashboard financeiro, 2026-09-02)

Primeiro gráfico do app — `--chart-1` a `--chart-5` existiam em `globals.css` desde o scaffold do shadcn mas eram **placeholders cinza nunca usados em lugar nenhum** (sem matiz, `oklch(_ 0 0)`). Preenchidos com uma paleta categórica de 8 matizes validada com o validador de acessibilidade de cor da skill de dataviz (`validate_palette.js` — PASS em light e dark; WARN só de contraste em 3 slots claros, mitigado pelos itens abaixo) e estendidos até `--chart-8` (`@theme inline` já mapeia todos os 8 pra `bg-chart-N`/`text-chart-N` via Tailwind):

- **Slots 1-2 são semânticos e fixos**: `chart-1` (azul) = entradas, `chart-2` (laranja) = saídas — deliberadamente **não** `--success`/`--destructive` (já comprometidos com outro significado no resto do app — confirmar ação / erro) nem vermelho-alarme pra despesa (repetiria o erro já corrigido no `FundingProjectionCard`, ver 7.7-bis: despesa não é "ruim", é só saída).
- **Slots 3-8** são pra identidade de categoria (ex. composição de gastos) — ordem fixa, nunca ciclada. "Outros"/residual nunca leva matiz categórico — cinza neutro (`--muted-foreground`), porque não é uma categoria de verdade.
- **Componentes**: `src/components/ui/charts/trend-chart.tsx` (`TrendChart`, linha/área) e `category-bar-chart.tsx` (`CategoryBarChart`, barra horizontal) — SVG com `vector-effect="non-scaling-stroke"` (2px de linha/grid continuam 2px em qualquer tamanho de tela, já que o `viewBox` escala tudo proporcionalmente), legenda sempre presente, labels diretos só no essencial, toggle "Ver como tabela" em cada um (par de acessibilidade). `TrendChart` usa um crosshair único (não hit-target por ponto — é linha) que também seleciona o mês ao clicar; `CategoryBarChart` usa a própria barra como alvo de clique (sem crosshair — é bar/cell), navegando pra `/dashboard/financeiro/lancamentos?category=ID`.
- **Animação**: `framer-motion` (dependência do projeto desde sempre, mas nunca usada em nenhum componente até aqui) — entrada com fade+stagger, linha "desenhando" (`pathLength` 0→1), barras crescendo, contagem animada nos KPIs de `MonthSummaryCards` (`useSpring`, ver 7.20 — sucessor do `BalanceSummary` original). Sutil, nunca decorativo — a régua seguida foi "os dados são a única coisa que pode ser chamativa".
- **Agregação**: `src/lib/financial/dashboard-aggregation.ts` (funções puras, mesmo espírito de `src/lib/financial/projection.ts`) — `aggregateMonthly`/`aggregateByCategory`/`aggregateByCategoryScoped` (7.21)/`aggregateFrequency` (dia/semana dentro do mês, 7.24), sempre escopadas a uma moeda só (nunca somar moedas diferentes, regra já estabelecida na Fase 1 de prestação de contas); `src/lib/financial/timeline.ts` (`buildFinancialTimeline`, 7.20) segue o mesmo espírito de função pura pra série mensal com saldo projetado.
- **Cards de status (`MonthSummaryCards`, 2026-09-02, ver 7.22)**: paleta semântica separada da paleta categórica acima — `--primary`/`--success`/`--destructive`/`--warning` (já existiam em `globals.css`, usados esporadicamente em badges; passaram a ganhar um padrão visual fixo pra "card de KPI com status"): barra de destaque de 4px na borda esquerda (`absolute left-0 top-4 h-9 w-1 rounded-r-full bg-{cor}`) + ícone num badge circular/arredondado na mesma cor a 10% de opacidade (`bg-{cor}/10 text-{cor}`) + valor em destaque à direita na cor cheia; sub-linhas dentro do card (ex. Recebido/A receber) usam fundo neutro `bg-muted/25` com o valor tingido de `text-success` (realizado) ou `text-warning` (pendente) — nunca `text-destructive` pra "pendente", fica reservado a erro/estouro de verdade (ex. `SpendingLimitsList`, ver 7.21, usa `text-destructive` só quando o gasto passa do limite).

### Modais roláveis — scroll com gutter simétrico (padrão obrigatório)
Quando o conteúdo de um `Dialog` pode ultrapassar a altura da viewport (formulários longos, principalmente no mobile), o próprio scroll nativo do container precisa de espaço reservado para a barra — senão ela sobrepõe os últimos ~15-20px do conteúdo (inputs, botões, bordas de card). Duas decisões fixadas depois de testar na prática:
- **`scrollbar-gutter: stable` não é confiável** — mesmo com suporte no browser, não reservou espaço de forma consistente. Preferir sempre o fallback manual: `padding` fixo (não depende de feature detection).
- **O padding do gutter deve ser SIMÉTRICO** (mesmo valor nos dois lados), nunca só à direita — um `pr-5` sem `pl-5` correspondente deixa o conteúdo com peso visual deslocado pra esquerda, parecendo descentralizado, mesmo a barra só existindo de um lado.
- Isso está implementado no primitivo `DialogContent` (`src/components/ui/dialog.tsx`), **não** repetido por tela: `max-h-[85vh] overflow-y-auto` + `px-5 py-4` (era `p-4`) por padrão. Qualquer `Dialog` novo herda automaticamente — só usar `<DialogContent className="max-w-sm">` como os outros. `DialogFooter` acompanha com `-mx-5` (era `-mx-4`) pra continuar colando nas bordas.
- Exceção: `EditProfileDialog` (fullscreen no mobile, estilo Instagram) tem layout próprio — header fixo + região de scroll separada — e cancela o `max-h`/`overflow` do primitivo explicitamente (`max-h-full overflow-y-hidden`, com `md:max-h-[85vh]` restaurando o cap só no desktop). Outros modais com necessidade de header fixo devem seguir esse mesmo modelo em vez de reinventar.

### Botão de voltar (`src/components/ui/back-button.tsx`, padrão obrigatório)
`BackButton` sempre volta pra tela anterior de verdade (`router.back()`, histórico do navegador) — `href` **não** é mais o destino usado no clique normal, é só uma rede de segurança (`router.push(href)`) pro caso raro de não haver histórico pra voltar (ex.: aba aberta direto num link compartilhado). Motivado por bug reportado pelo usuário: o botão de voltar da tela de Seguidores tinha um destino fixo (`/dashboard`/`/`) que ignorava de onde a pessoa realmente veio (perfil), então "voltar" levava pro lugar errado. Antes disso o componente decidia entre `<Link href>` (destino fixo sempre) ou `onClick` (callback do chamador); qualquer novo uso de `BackButton` para navegação deve deixar o comportamento padrão cuidar do "voltar" e só passar `href` como fallback — `onClick` continua existindo separadamente para casos que não são navegação de verdade (fechar um dialog, voltar um passo de wizard).

**`window.history.length` não é confiável pra decidir isso (2026-08-08) — usar `hasInAppNavigation()`.** A primeira versão checava `window.history.length > 1` pra decidir entre `router.back()` e o fallback `href`. Bug reportado pelo usuário: abrir um link de projeto compartilhado (WhatsApp etc.) numa aba nova às vezes já chega com `history.length === 2` (o navegador conta uma entrada em branco antes da página carregada), então o botão tentava `router.back()` mesmo sem nenhuma página nossa pra voltar — resultado: tela em branco (`about:blank`). Corrigido substituindo essa checagem por `hasInAppNavigation()` (`src/lib/navigation-tracker.ts`): uma flag de módulo, `false` até a primeira troca de rota client-side de verdade dentro do app, marcada por `NavigationTracker` (`src/components/navigation-tracker.tsx`, `usePathname()` + `useEffect`, ignora o pathname inicial) montado uma vez em `src/app/layout.tsx` (raiz, sobrevive a navegações client-side, reseta em qualquer load de página cheio — exatamente o que queremos: só considera "tem pra onde voltar" quem navegou de verdade dentro do app nesta carga).

**Corolário (2026-08-08): navegação por abas dentro de uma mesma seção precisa usar `<Link replace>`, nunca `push` (padrão).** Voltar de verdade via `router.back()` só funciona bem se cada entrada do histórico representa uma navegação "real" — trocar de aba (ex.: `ProfileTabs`: Perfil/História/Projetos/Trajetória) não é isso, é só mudar de visão dentro da mesma seção. Bug reportado pelo usuário: como cada clique numa aba empilhava uma entrada nova (`push`, padrão do `<Link>`), "voltar" ficava saltando de aba em aba (Projetos → História → Perfil) em vez de sair direto pra onde a pessoa realmente veio (ex.: dashboard) — precisava de um clique por aba visitada. Corrigido em `profile-tabs.tsx` com `<Link replace>` nos links da barra de abas: trocar de aba agora substitui a entrada atual do histórico em vez de empilhar uma nova, então não importa quantas abas a pessoa visite, um único "voltar" sai direto da seção. Qualquer nova barra de abas/sub-navegação dentro de uma mesma área deve seguir esse mesmo padrão.

### Sem deslocamento lateral de página (padrão obrigatório)
`html` (só `html`, **não** `body` — ver nota abaixo) tem `overflow-x: hidden` + `overscroll-behavior-x: none` (`src/app/globals.css`) — a página nunca desliza/balança lateralmente ao arrastar o dedo, só rolagem vertical. Não usar `touch-action: pan-y` no `html`/`body` pra isso: por spec, `touch-action` de um ancestral restringe também os descendentes (interseção na cadeia), então bloquearia até áreas de scroll horizontal intencionais (carrossel de mídia do post, etc.). `overscroll-behavior` não tem esse problema — é escopado ao próprio container de scroll, não herda pros filhos. Qualquer componente com scroll horizontal de propósito (carrossel, tabs) deve gerenciar seu próprio `overscroll-behavior`/`touch-action` local, sem depender de nada no nível global.

**Armadilha real (2026-08-08): nunca aplicar esse `overflow-x: hidden` em `body`.** A regra original tinha `html, body { overflow-x: hidden }`. Pelo CSS Overflow Module, definir `overflow-x` sem `overflow-y` explícito faz o navegador recomputar `overflow-y: visible` como `auto` (não dá pra ter "hidden" num eixo e "visible" no outro). Em `body` isso o transforma num **scroll container próprio** — e todo `position: sticky` dentro dele (`ProfileTabs`, a barra de abas do perfil) passa a calcular "grudado" em relação ao `scrollTop` do `body`, que fica sempre `0` (quem rola de verdade é o `html`, `document.scrollingElement`), então o sticky nunca gruda de verdade — some assim que o conteúdo acima dele sai da tela, só reaparecendo perto do topo real do documento. Diagnosticado com Playwright headless comparando `getBoundingClientRect().top` do elemento sticky contra `window.scrollY` durante o scroll (crescia linearmente junto — prova de que não estava "grudando"). Corrigido movendo a regra só pra `html` (que já é o `document.scrollingElement` de fato).

**Isso não é suficiente sozinho** — só protege o `<html>`/`<body>`, não qualquer container interno com scroll próprio. Regra que pegou vários componentes de surpresa (2026-08-08): pelo CSS Overflow Module, se um elemento define `overflow-y: auto`/`scroll` mas **não** define `overflow-x` explicitamente, o valor `visible` do eixo X é recomputado para `auto` (não fica realmente "visible") — ou seja, **qualquer** `div` com `overflow-y-auto` vira arrastável lateralmente na hora que algum filho fica alguns pixels mais largo que ele (imagem escalada, texto sem `truncate`, badge sem `shrink-0` etc.), e diferente do bounce do `body`, esse scroll fica "preso" onde o dedo soltar, sem voltar. **Todo `overflow-y-auto`/`overflow-y-scroll` do sistema precisa vir acompanhado de `overflow-x-hidden` explícito no mesmo elemento** — já aplicado no primitivo `DialogContent` (`src/components/ui/dialog.tsx`, herdado por todo `Dialog` novo automaticamente) e no `<main>` de `dashboard/layout.tsx` (cobre toda página do dashboard), além de todos os outros containers de scroll vertical do app (sheets, drawers, dropdowns, listas). Exceção: containers que já usam `overflow-x-auto` de propósito (carrosséis/tabs) mantêm como estão.

### Editor de recorte de mídia (`src/components/shared/media-editor/`)
`ImageCropEditor` (post, projeto, capa de perfil, avatar) — preview em tela = resultado salvo (WYSIWYG), um único modelo contínuo de zoom, **sem alternar entre modos** (evita descontinuidade/"salto"): sempre `object-fit: contain` como base (`zoom=1` = foto inteira visível, sobra preenchida com a cor média dela), `object-position` sempre fixo em `50% 50%` — o posicionamento é feito só por `transform`. `transform: translate(x%, y%) scale(zoom)`: o `scale` (a partir do centro do quadro) cresce contínuo — a sobra vai sendo empurrada pra fora (cortada pelo `overflow-hidden`) até desaparecer exatamente no `scaleRatio = max(n/f, f/n)` (`n` = proporção natural da imagem, `f` = proporção alvo), ponto em que a imagem passa a cobrir o quadro inteiro; a partir daí o zoom recorta a própria foto. O `translate` é um deslocamento **livre nos dois eixos, em pixels fixos do quadro, aplicado depois do scale** (por isso não é afetado pela escala — mesma composição de matriz que `translate(...) scale(...)` em CSS) — a imagem segue o cursor 1:1 em qualquer direção, em qualquer zoom (`position.x`/`position.y`, `50` = centralizado, sem deslocamento; **não** é a semântica antiga de "posição dentro da sobra do object-fit:contain", que travava um eixo quando não havia folga). O alcance máximo do arrastar (`clampPosition`/`panRange`) **cresce com o zoom**: em vez de um limite fixo (que ficava curto justo com zoom alto, quando mais se precisa mover), cada eixo tem seu próprio "ponto de referência" — o eixo sem sobra desde `zoom=1` (`localRatio=1`) já ganha alcance assim que o zoom passa de 1; o eixo com sobra só passa a ter alcance quando o zoom ultrapassa `scaleRatio` (ponto em que aquele eixo também passa a estourar o quadro) — o alcance em cada eixo é proporcional a `|zoom/localRatio - 1|`. Mudar o zoom (botão/scroll/pinça) sempre reenquadra a posição atual dentro do novo alcance junto (`setZoom`), pra não deixar a posição "presa" fora do limite ao dar zoom out. `bakeImage()` (`src/lib/media/bake-image.ts`) usa exatamente a mesma fórmula (`drawX = (canvas.width-drawWidth)/2 + panX`, mesma composição scale-depois-translate), então não há branch condicional (`contain` vs `cover`) nem checagem de tolerância de proporção — uma fórmula só cobre "mostrar tudo" e "recortado". Teto de zoom: `maxZoom = scaleRatio * 3` (preserva o teto `3` de antes quando a proporção já bate). Passo do zoom (`zoomStep`/`wheelStep`) também é proporcional a `scaleRatio` (`max(0.1, scaleRatio*0.15)` no botão, `max(0.08, scaleRatio*0.1)` no scroll) em vez de fixo — numa foto bem estreita/alta (ex.: flyer), cobrir o quadro exige zoom até `scaleRatio` inteiro, e um passo fixo de 10% levava dezenas de cliques pra sumir com a sobra (2026-08-26, ver Changelog). Controles: botões +/- e scroll do mouse (`wheel`, listener nativo no `frameRef` — não via `onWheel` do JSX, que o React trata como passive e bloquearia o `preventDefault()`) no desktop, pinça de dois dedos no touch. O listener de `touchmove` no `window` só chama `preventDefault()` quando o usuário está de fato arrastando/pinçando dentro do editor; fora disso o toque tem que passar livre pra rolagem da página. Prop `round` (círculo em vez de cantos arredondados, usado pelo avatar): os botões de zoom saem do overlay interno (ficariam cortados nos cantos do círculo inscrito) e viram uma fileira centralizada abaixo do quadro. `AvatarCropDialog` (`src/components/dashboard/settings/`) reaproveita esse mesmo componente + `bakeImage()` (aspect `1:1`, `round`) em vez de manter uma implementação própria de crop — API pública (`file`/`open`/`onOpenChange`/`onApply`) não mudou. **Armadilha pra quem consome `ImageCropEditor`** (2026-08-26, ver Changelog): `setZoom()` chama `onZoomChange()` e `onPositionChange()` em sequência síncrona a cada mudança de zoom — quem guarda o `MediaDraft` numa lista (não um único objeto solto) **precisa** repassar essas duas chamadas pro `setState` do array usando a forma funcional (`setState(prev => prev.map(...))`), nunca `setState(arrayDaClosure.map(...))` direto: as duas chamadas do mesmo clique partem do mesmo array "fechado" na render atual, e a segunda (posição) sobrescreve de volta o zoom que a primeira acabara de setar — bug real no `StepMediaSelect` do composer de post (zoom nunca avançava, em nenhuma imagem), que `AvatarCropDialog`/`step-cover-title.tsx` nunca tiveram por já usarem a forma funcional num objeto único.

**Preview consistente nas 3 etapas do composer de post (2026-08-27, ver Changelog)** — `StepAdjust` (etapa "Ajustar") e `TagPeoplePicker` (foto na etapa "Detalhes") mostram o mesmíssimo recorte que `ImageCropEditor` (etapa "Selecionar mídia"), porque replicam exatamente a mesma técnica (`object-contain` + `object-position: 50% 50%` + `transform: translate(x-50%, y-50%) scale(zoom)`) sobre os mesmos `position`/`zoom`/`bgColor` do `MediaDraft` — nenhuma das duas reimplementa a matemática do crop. `MediaDraft.bgColor` (cor de fundo da sobra) é calculado **uma única vez**, em `StepMediaSelect.handleFiles`, logo após `createMediaDraft` (`averageColorFromUrl()` nova em `bake-image.ts`, mesma conta de `averageColor()` a partir de uma `Image()` carregada ali mesmo) — e não via `onLoad` de cada `<Image fill>` nas etapas seguintes: esse evento não dispara de forma confiável quando o navegador já tem aquela blob URL em cache (a mesma imagem já foi montada na etapa de corte), deixando o fundo preso no fallback preto. `ImageCropEditor` continua com seu próprio `imgInfo` local via `onLoad` (não migrado pra `bgColor`) porque também precisa da proporção natural da imagem pro `scaleRatio`/zoom, e é a primeira montagem daquela blob URL (sem risco de cache) — mas o valor final bate exato com `MediaDraft.bgColor`, já que os dois chamam a mesma função sobre os mesmos bytes.

### Tema (`src/app/globals.css`)
- Tailwind v4 CSS-first (`@import "tailwindcss"`), sem `tailwind.config.js`.
- `@custom-variant dark (&:is(.dark *));` (compatível com `next-themes attribute="class"`).
- Cores em **OKLCH**. Até 2026-07-23 a paleta era monocromática "carbon" (`--primary` quase preto/branco, único chroma real em `--destructive`); trocada por uma paleta intencional (estudo de psicologia de cores + benchmark de apps modernos, pedido do usuário — ver changelog da mesma data). Em 2026-08-25 o `--primary` em si foi trocado de novo — indigo/violeta genérico de SaaS não carregava nenhum significado próprio para uma plataforma de missionários; ver changelog dessa data para o estudo completo (`design/paleta-missiologica.{html,pdf}`).
  - `--primary` (marca): **verde-oliva terroso** ("Colheita", tom Oliva — `oklch(0.33 0.065 116)` claro, `oklch(0.68 0.085 116)` escuro), raiz missiológica em Mt 9:37 ("a seara é grande, mas os trabalhadores são poucos") — usado em toda ação do dia a dia (botão padrão, links, foco, `--sidebar-primary`). Substituiu o indigo/violeta original (`oklch(0.53 0.21 277)`), que não tinha significado próprio — era só o hue mais comum de SaaS recente (Stripe, Linear, etc.).
  - `--support` (**novo**): coral/terracota (`oklch(0.64 0.17 42)` claro / `oklch(0.7 0.11 42)` escuro) — reservado **só** para ações de doação/apoio (`Button variant="support"`), nunca usado em ações neutras, pra não passar a sensação de "app pedindo dinheiro o tempo todo".
  - `--success` (**novo**): verde (`oklch(0.64 0.15 155)` claro / `oklch(0.72 0.1 155)` escuro) — progresso de arrecadação (`ProgressIndicator`, era `bg-primary`) e badge de meta atingida (`Badge variant="success"`).
  - `--warning` (**novo**, reservado, sem uso ainda): âmbar (`oklch(0.77 0.16 70)` claro / `oklch(0.78 0.11 70)` escuro).
  - `--destructive`: inalterado. Escala neutra (`background`/`foreground`/`card`/`muted`/`border`/`chart-*`): inalterada — a base cinza "carbon" continua sendo o fundo, só ganhou 3 cores de propósito por cima.
  - Todos os tokens novos são mapeados em `@theme inline` (`--color-success`, `--color-support`, `--color-warning` + `-foreground`) e têm par light/dark com chroma reduzido no escuro (mais sóbrio à noite).
- `--radius`: `0.625rem` → **`1.125rem`** (mesma data) — como toda a escala (`sm/md/lg/xl/2xl/3xl/4xl`) deriva de `calc(var(--radius) * N)`, a mudança é só essa variável; nenhum componente precisou de classe nova de radius. Efeito prático: botões `h-8`/`h-9` ficam com formato pílula naturalmente.
- `--font-sans` = Inter, `--font-heading` = alias de `--font-sans` (sem fonte de display separada).
- `.scrollbar-hide` utilitário customizado (tabs horizontais sem barra visível).
- `profile.accent_color`: cor hex livre por usuário (default `#34390c`, hex do novo `--primary` — antes `#6366f1`; migration `046_brand_accent_color.sql` também atualizou o valor de todo perfil que nunca tinha customizado, isto é, ainda estava no default antigo), aplicada via `style={{ backgroundColor }}` inline (fora do design system CSS) — continua sendo a personalização **por perfil** na página pública (avatar-fallback, cor de link, cover do card na grade de descoberta), separada da marca fixa do produto (`--primary`).
- Logomarca: pernas cruzadas em passo (silhueta cortada na cintura, sem tronco/cabeça — interpretação própria, não cópia do "Striding Man" da Johnnie Walker), com "GO" encaixado onde os pés se cruzam e "guide" ao lado. Gerada por ferramenta externa de imagem (não código/SVG) a partir de prompt escrito nesta sessão, recortada em PNG com fundo transparente (`python3`/PIL, sem ImageMagick disponível no ambiente). Arquivos em `public/`: `logo.png` (lockup na cor de marca, uso padrão), `logo-white.png` (lockup com traço branco, fundos escuros/fotos), `logo-black.png` (traço preto, fundos claros sem cor de marca — **estes dois continuam monocromáticos por design, não seguem `--primary`**), `logo-pill.png` (badge fundo colorido + traço branco), `icon-mark.png` (ícone "squircle" na cor de marca, usado no header mobile do dashboard). `src/app/icon.png`/`apple-icon.png`/`favicon.ico` e `public/icons/icon-{192,512}.png` (PWA) derivam do mesmo ícone. Usada em `site-nav.tsx` (troca `logo.png`/`logo-white.png` via `dark:`) e `dashboard/sidebar.tsx` (`MobileHeader`, que deixou de depender de `profile.accent_color` — o quadrado "M" provisório virou o ícone real).
  - **Recolorindo a logomarca sem redesenhar (2026-08-25)**: como é PNG/WebP de traço fino sobre fundo transparente (não SVG/código), trocar a cor por CSS não é possível — mas dá pra recolorir por script preservando a arte exatamente. Receita (ImageMagick): (1) extrai o canal alpha original; (2) achata a imagem sobre branco, converte pra escala de cinza e usa `-negate -level 2%,15%` como "máscara de tinta" (traço colorido → quase branco na máscara mesmo sendo um traço fino de 1-2px com antialiasing; preenchimento branco intencional do desenho → quase preto, fica de fora); (3) multiplica essa máscara pelo alpha original; (4) gera uma camada 100% na cor nova (`-fill "#34390c" -colorize 100%`, preserva o alpha original) e troca o alpha dela pela máscara combinada (`-compose CopyOpacity`); (5) compõe essa camada por cima da imagem original (`-compose Over`) — onde a máscara é baixa (preenchimento branco ou fundo transparente), a imagem original aparece por baixo sem alteração; onde é alta (traço), a cor nova cobre. Duas armadilhas que quebraram tentativas anteriores: `-colorize 100%` sozinho (sem máscara) recolore *todo* pixel não-transparente, inclusive o preenchimento branco interno do desenho, virando um blob sólido; e usar `-opaque` com fuzz numa cor de referência falha porque o traço fino tem antialiasing que varia demais de tom pra um único fuzz pegar tudo. `logo-black.png`/`logo-white.png` foram propositalmente deixados de fora (são variantes monocromáticas, não carregam a cor de marca).

---

## 12. Configurações de projeto

- **`components.json`**: aliases `@/components`, `@/lib`, `@/hooks`, `@/components/ui`, `@/lib/utils`.
- **`next.config.ts`**: `images.remotePatterns` liberando o hostname do bucket Supabase Storage (`eqnekupeiehgkacegmgl.supabase.co`) para `next/image`.
- **`tsconfig.json`**: `target: ES2017`, `strict: true`, `moduleResolution: bundler`, `jsx: react-jsx`, alias único `"@/*": ["./src/*"]`, inclui `.next/types`/`.next/dev/types` (Next 16).

---

## 13. Passo a passo para recriar o sistema do zero

1. `create-next-app` (App Router, TypeScript, Tailwind v4) → renomear `middleware.ts` para `proxy.ts` (convenção Next 16 usada aqui).
2. Instalar shadcn/ui v4 com `@base-ui/react` (`style: base-nova`, `baseColor: neutral`), configurar `components.json` conforme seção 12.
3. Criar projeto Supabase; aplicar as migrations 001→017 **na ordem exata** (seção 3), respeitando os padrões anti-recursão de RLS (`SECURITY DEFINER STABLE` functions) desde o início para não repetir os bugs das migrations 004/011.
4. Configurar buckets de Storage (`media`, `avatars`) com as policies de pasta-por-usuário.
5. Implementar `src/lib/supabase/{client,server}.ts` com os dois clientes (anon/service role).
6. Implementar `src/proxy.ts` com as rotas protegidas/de auth.
7. Implementar a camada E2EE (`src/lib/crypto/{e2ee,key-manager,conversation}.ts`) **antes** de qualquer feature que dependa dela (mensagens, dados sensíveis, orações privadas).
8. Construir as rotas na ordem: auth → onboarding → dashboard shell (layout+sidebar) → perfil público → projetos → parceiros → financeiro → mensagens/orações (E2EE) → notificações.
9. Ao chegar em Stripe/IA/WhatsApp/câmbio: o schema já existe (seção 3), falta escrever toda a camada de integração (`src/lib/stripe/`, `src/lib/ai/`, `src/lib/exchange/`, webhooks, Edge Functions) — não há código de referência no sistema atual para essas partes.
10. Corrigir de saída as inconsistências já conhecidas (seção 8) em vez de reproduzi-las: unificar nomenclatura `destaques`→`projetos`, remover a Server Action morta, adicionar checagem de posse em `/api/highlights`, e considerar mover os triggers de saldo agregado para também reagir a `UPDATE`. (`/recuperar-senha` já foi implementado — ver seção 5.2.)

---

- **2026-09-07** — "Ser parceiro" deixou de exigir conta em "Ser parceiro fixo" (caminho manual) e "Não posso agora" (migration `093_guest_scheduled_and_recurring_pledges.sql`), a pedido do usuário — antes as duas telas bloqueavam com `NeedsAccountCard`, sem alternativa. Espelha o padrão que `pledges` já usava pra doação avulsa (`reporter_user_id`/`partner_id` nullable, insert público, sem `partners` pro convidado). Nome sempre obrigatório; agendamento único aceita e-mail ou WhatsApp, recorrência mensal exige e-mail (não existe automação de WhatsApp pra lembrar todo mês). Cartão automático em "Ser parceiro fixo" continua exigindo conta (limitação real do `checkout-recurring`, que responde `401` sem auth) — convidado só vê métodos manuais. Novo componente compartilhado `AccountUpsellCard` (não-bloqueante, nunca a única opção) aparece nos 3 fluxos, inclusive oração/embaixador/voluntariado (que já aceitava convidado mas nunca oferecia conta), com CTA honesto sobre o ganho real (feed, informativo, progresso dos projetos). Nova coluna `profiles.whatsapp_group_url` (migration `094`, configurável em Configurações → Perfil) dá uma terceira opção — link de convite de grupo, sem depender da API do WhatsApp Business que o app não integra — mostrada como botão extra no `AccountUpsellCard` e no perfil público. Os dois crons de lembrete ganharam fallback pra e-mail/nome do convidado quando não há `partners`, e um bug latente foi corrigido: uma linha sem e-mail nenhum não é mais marcada `sent` sem ter enviado nada. `NeedsAccountCard` ficou sem uso (não removido). Seções 3.1 e 7.3-sexies atualizadas.
- **2026-09-08** — Correção de bug real na migration 095 (entrada abaixo), achado pelo usuário na conta de Silas logo depois de aplicá-la: o backfill de lá usava `financial_accounts.balance` (saldo ATUAL, hoje) como se fosse o saldo do dia da criação da conta — só bate pra conta sem nenhuma transação real desde então. Pra conta com atividade real depois (o caso comum — ex.: uma receita recebida depois da criação, como na conta de Silas), o lançamento `opening_balance` backfillado nasceu inflado, e `buildFinancialTimeline` passou a contar esse valor errado junto com as transações reais na reconstrução mês a mês, produzindo números fantasiosos ("Saldo Anterior" negativo numa conta que nunca esteve negativa). **`financial_accounts.balance` em si nunca foi alterado** por nenhuma das duas migrations (a 095 já desligava o trigger durante o backfill) — o dinheiro real sempre esteve certo, só a "documentação" histórica (o lançamento) e o gráfico derivado dela ficaram errados. Corrigido por `096_fix_opening_balance_backfill.sql`: recalcula cada lançamento `opening_balance` por diferença (saldo atual da conta menos a soma de todas as outras transações pagas dela) — fórmula idempotente e segura tanto pra linha errada da 095 quanto pra uma linha certa criada ao vivo pelo `AccountWizard` já corrigido (não muda nada numa conta sem atividade real desde a criação, já que o trigger de saldo não olha `date`, só soma toda transação paga não importa quando aconteceu). Quando o valor correto dá zero, o lançamento é removido. **Verificado de ponta a ponta num Postgres local** (`brew services start postgresql@18`, banco descartável, sem relação com o Supabase de produção — desligado e apagado ao final): schema mínimo de `financial_accounts`/`transactions`/`trg_update_balance` recriado à mão, dados sintéticos reproduzindo o cenário relatado (conta com saldo inicial negativo digitado antes das duas migrations + uma receita real recebida depois), as duas migrations (095 e 096, os arquivos reais do repo) rodadas em sequência via `psql`. Confirmado: 095 sozinha reproduz o sintoma exato (lançamento `opening_balance` nasce com o saldo atual em vez do saldo do dia 1); 096 corrige pro valor certo sem tocar `financial_accounts.balance`; conta de controle sem nenhuma atividade real desde a criação sai 100% intocada; reaplicar 096 duas vezes é idempotente (mesmo resultado). Ainda assim, o usuário precisa aplicar as duas migrations (095, se ainda não tiver aplicado, e 096) no Supabase de produção e conferir a conta de Silas depois — o teste local confirma a lógica, não substitui a aplicação real. Ver seção 7.20.
- **2026-09-08** — Correção: "Saldo Previsto" (navegador de mês do dashboard financeiro, 7.20) mostrava o mesmo saldo inicial da conta em TODOS os meses anteriores à criação dela — reportado pelo usuário vendo o próprio dashboard (conta nova em setembro/2026 "vazando" saldo pros meses de julho/agosto, que não tinham motivo pra ter o mesmo valor). Causa: `buildFinancialTimeline` reconstrói o saldo de meses passados "desfazendo" transações pagas dentro da janela a partir do saldo atual da conta — mas o saldo inicial informado no `AccountWizard` não é uma transação, então pra uma conta recém-criada (sem transações antes dela existir) não havia nada pra desfazer, e o mesmo número se repetia indefinidamente pra trás. Corrigido com um novo parâmetro opcional `accountsStartDate` em `buildFinancialTimeline` (menor `created_at` das contas ativas somadas no saldo, calculado em `FinancialDashboard`/`ReportsAnalytics`): meses inteiramente antes desse mês agora voltam `null` em `saldoAnterior`/`saldoDisponivel`/`saldoPrevisto` em vez do valor inventado. A pedido do usuário, a UI (`MonthNavigator`/`MonthSummaryCards`) mostra `---` (`NO_DATA_MASK`) nesses meses em vez de "R$ 0,00", pra não passar a impressão de saldo zerado quando na verdade é "sem dado". Limitação conhecida quando escrita, **fechada na mesma sessão pela entrada logo abaixo** (lançamento `opening_balance` com data própria): contas antigas já cadastradas antes desta correção tinham `created_at` = data de cadastro no sistema, não a data real de abertura da conta — resolvido com um lançamento retroativo por conta em vez de um corte só por `created_at`. A limitação de "corte usa a conta mais antiga entre várias, não uma linha do tempo por conta" continua de pé (fora de escopo, documentada na entrada abaixo). Ver seção 7.20.
- **2026-09-08** — Saldo inicial da conta virou um lançamento de verdade (`transactions.source='opening_balance'`), a pedido do usuário logo depois da correção acima ("deveria ter no histórico as datas de cada transação de valor, inclusive a data de abertura da conta, pro usuário ter controle de quando foi feito o lançamento"). Antes, o saldo inicial digitado no `AccountWizard` era só um número escrito direto em `financial_accounts.balance`, sem data nem rastro — a correção anterior (entrada acima) precisou inferir "quando a conta passou a existir" a partir de `created_at` (timestamp de cadastro no sistema, não escolhido pelo usuário) por falta de dado melhor. Migration `095_account_opening_transaction.sql` adiciona `'opening_balance'` ao `CHECK` de `transactions.source` e faz backfill: toda conta já cadastrada com saldo ≠ 0 (exceto cartão de crédito) ganha esse lançamento retroativo datado do próprio `created_at`, com o trigger `trg_update_balance` desligado durante o insert pra não somar o valor duas vezes em cima do `balance` que já estava correto (não é um `UPDATE` de saldo, só documentação histórica). `AccountWizard` ganhou um campo novo na etapa "Saldo inicial" — **Data do saldo inicial** (default hoje, `max` = hoje, editável pra trás: "essa conta já existia antes de eu cadastrar aqui") — e `create()` passou a inserir a conta com `balance: 0` e um lançamento `opening_balance` (`type` conforme o sinal, `is_paid=true`) na data escolhida, deixando o trigger aplicar o efeito sozinho, em vez de escrever `balance` direto (mesmo raciocínio do backfill: evita duplicar). Esse lançamento aparece normal na lista de Lançamentos, com rótulo "· Saldo inicial" (`TransactionTable`), editável/excluível como qualquer transação — corrigir valor ou data depois é só editar ali, sem tela especial. Nova função `accountEffectiveStartDate(account, transactions)` (`timeline.ts`) substitui o cálculo anterior (só `created_at`): agora prioriza a data desse lançamento quando existe, então uma conta com saldo inicial retroativo mostra números reais desde essa data (não `null`) — só cai pra `created_at` quando não há lançamento `opening_balance` pra aquela conta. **Limitação conhecida**: se o usuário escolher uma data de saldo inicial mais de 12 meses no passado, o lançamento pode ficar fora da janela de busca de `page.tsx` (fixa em 12 meses pra trás) — nesse caso `accountEffectiveStartDate` cai pra `created_at` e alguns meses do navegador (que só mostra 6 pra trás) podem aparecer `null` mesmo já tendo saldo real; caso raro (exige backdate de mais de um ano) e não tratado nesta rodada. Testado isolando a lógica de `buildFinancialTimeline`/`accountEffectiveStartDate` fora do browser (script Node reproduzindo os dois cenários — conta nova e saldo retroativo —, matemática conferida); não verificado num browser real porque o usuário já estava com o próprio `next dev` ativo na mesma porta (Next.js 16 bloqueia duas instâncias no mesmo projeto, mesma cautela já registrada em outras entradas). **Bug real no backfill desta migration, achado pelo usuário e corrigido pela entrada acima (096)**: o valor do lançamento retroativo usava o saldo ATUAL da conta em vez do saldo do dia da criação — só coincidem pra conta sem atividade real depois. Ver seção 7.20.
- **2026-09-08** — Login com Google bloqueado pra qualquer pessoa fora de uma lista de testadores manual, achado pelo usuário tentando entrar (`Erro 403: access_denied`, "app não concluiu o processo de verificação do Google"). Causa: a tela de consentimento OAuth do projeto Google (`goguide-507314`) estava em modo "Testando" — Google só libera login irrestrito ("Em produção") sem passar por revisão manual quando o app não pede nenhum escopo sensível, e `signInWithOAuth` em `/cadastro`/`/login` pedia `https://www.googleapis.com/auth/user.birthday.read` (People API, pra auto-preencher `profiles.birth_date` — ver 3.1) além dos escopos básicos. Confirmado com o usuário: prioridade é qualquer pessoa conseguir entrar com Google agora, não vale a pena esperar a verificação do Google (dias, exige política de privacidade etc.) só pelo auto-preenchimento de aniversário, que é um bônus, não algo essencial (o cadastro manual já cobria isso desde sempre). Removido o escopo das duas telas e apagado `syncGoogleBirthday()`/a chamada à People API em `src/app/auth/callback/route.ts` — sem o escopo, aquela chamada nunca teria êxito de novo (sempre 403, silenciosamente engolido), então virava uma requisição morta em todo login só gastando latência/quota à toa. Depois do deploy, usuário publica o app na tela "Público-alvo" do Google Cloud Console (Google Auth Platform) pra sair do modo "Testando" — passo manual fora do repo, não automatizável daqui. Seção 3.1 atualizada.
- **2026-09-04** — Deploy pra produção (push em `origin/main`) do trabalho acumulado: vínculo de conta pro Pix (ver entradas abaixo) + Open Finance/Pluggy + importação de extrato OFX (seções 7.32/7.33). Ao aplicar as migrations no Supabase depois do deploy, `089_transaction_import.sql` rodou sem problema, mas `088_open_finance.sql` retornou `42P07: relation "open_finance_items" already exists` — investigado direto via `service_role`: as duas tabelas (`open_finance_items`/`open_finance_accounts`), as duas colunas novas (`financial_accounts.is_open_finance`, `transactions.pluggy_transaction_id`) e o `CHECK` de `source` atualizado (confirmado aceitando `'open_finance'` via um insert de teste que falhou por FK, não por CHECK) já estavam **todos presentes e corretos** — a migration já tinha sido aplicada numa passada anterior que nunca foi registrada aqui, então o erro era só uma tentativa de reaplicar algo que já existia, sem nenhum dado quebrado ou inconsistente. Não precisou de nenhuma correção — só atualizar a documentação (seções 1, 7.32, 7.33), que estava desatualizada dizendo "não aplicada". Nota pra próxima vez: `088_open_finance.sql` não usa `IF NOT EXISTS` no `CREATE TABLE` (diferente das `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` no mesmo arquivo), então reaplicá-la sempre vai falhar assim que já tiver rodado uma vez — comportamento esperado, não é preciso "corrigir" a migration em si.
- **2026-09-04** — Correção same-day: campo Moeda passou a vir antes de Tipo em `PaymentMethodForm`, e o `<select>` de Tipo passou a filtrar Pix fora quando a moeda não é BRL (Pix é exclusivo do Brasil) — pedido do usuário depois de ver "Pix" listado junto com moedas internacionais. `PAYMENT_METHOD_CATALOG` ganhou um campo opcional `currency` por entrada (só `pix: 'BRL'` usa por enquanto); trocar de moeda troca o tipo selecionado sozinho pro primeiro compatível quando o atual deixa de fazer sentido, e voltar pra BRL reseleciona Pix automaticamente (não só reabre a opção na lista — usuário pediu esse reforço depois de notar que o tipo ficava travado no fallback mesmo voltando pra BRL). Verificado num browser real (Playwright, conta seed). Ver seção 7.11 (1-ter).
- **2026-09-04** — Correção same-day: criar conta pro Pix direto de Configurações > Pagamentos passou a abrir o `AccountWizard` real (as mesmas 6 etapas de `/dashboard/financeiro/contas`), a pedido do usuário ("deve aparecer os mesmos processos das etapas que aparecem [...] em adicionar conta") — a versão anterior (entrada abaixo) usava um mini-formulário simplificado (só nome), que ele rejeitou por não bater com o fluxo de criação de conta que já existe no resto do sistema. `AccountWizard` ganhou um prop `onCreated` pra devolver a conta recém-criada pro chamador auto-selecionar; o wizard abre como um segundo `<Dialog>` aninhado por cima do dialog do método de pagamento (Base UI empilha automaticamente, sem código extra de camada). **Bug real de key duplicada no React encontrado e corrigido testando**: a conta nova chegava duas vezes na lista mesclada do `<select>` — uma vez pelo callback local (`extraAccounts`), outra pelo `router.refresh()` que o próprio wizard já dispara (atualiza a prop `financialAccounts`) — corrigido filtrando `extraAccounts` pra excluir qualquer id que já esteja em `financialAccounts` no momento do merge. Verificado num browser real (Playwright, conta seed `ana.ferreira`, dados de teste restaurados ao final, incluindo as contas duplicadas geradas pelo bug): wizard aninhado abre e passa pelas 6 etapas reais, conta criada auto-seleciona no `<select>` do Pix sem duplicar e sem erro de console (confirmado com o bug presente e depois do fix). Ver seção 7.11.
- **2026-09-04** — Correção same-day: vínculo de conta pro Pix virou obrigatório **sem exceção**, a pedido do usuário ("não pode ser só um Pix sem vincular a conta nenhuma"). A versão anterior (entrada abaixo) deixava salvar um Pix sem `linked_account_id` quando o perfil ainda não tinha nenhuma `financial_accounts` — só mostrava um link pra ir criar uma no Financeiro depois. Trocado por criação inline (depois substituída pelo `AccountWizard` completo, ver entrada acima no mesmo dia): sem nenhuma conta ainda, o próprio campo "Conta vinculada" em `PaymentMethodForm` abria um mini-formulário (nome da conta) que criava a `financial_accounts` e já vinculava, sem sair do dialog nem perder o que já tinha sido digitado no Pix; com conta(s) existente(s), o mesmo botão "+ Criar nova conta" ficava disponível ao lado do `<select>` pra quem queria uma diferente das listadas. Resultado: não existe mais nenhum caminho — criar ou editar — que salve um Pix sem conta vinculada. Verificado num browser real (Playwright, conta seed `ana.ferreira`): tentativa de salvar sem conta é bloqueada pela validação nativa do `<select required>`; fluxo de criação inline testado ponta a ponta (cria conta, vincula, salva, aviso some); dados de teste restaurados ao final. Ver seção 7.11.
- **2026-09-04** — Vínculo de conta obrigatório pra Pix em Configurações > Pagamentos, a pedido do usuário: quando o método é Pix, `PaymentMethodForm` exige escolher uma `financial_accounts` pra vincular (ou mostra link pra criar uma, se ainda não existir nenhuma) — mesmo padrão que `payment_methods.linked_account_id` já usava só pra Stripe, agora também organizando manualmente qual conta cada chave Pix alimenta. Pix já cadastrados sem vínculo (inclusive os anteriores a esta mudança) ganham um aviso no card, direcionando pra Financeiro criar uma conta ou pra editar e escolher uma existente. Verificado num browser real (Playwright, conta seed). Ver seção 7.11. **Superada pela entrada acima no mesmo dia** — o "sem bloquear" foi removido a pedido do usuário.
- **2026-09-04** — Open Finance desativado por custo (Pluggy sai ~R$2.500/mês, inviável nesta fase sem receita — pesquisa do próprio usuário) + importação de extrato OFX como alternativa gratuita, a pedido do usuário logo após a implementação da 7.32: `contas/page.tsx` voltou a mostrar o botão "Gerenciar Open Finance" desabilitado (todo o código da 7.32 continua no repo, intacto, pronto pra reativar); nova migration `089_transaction_import.sql` (`transactions.import_uid`, dedup por `UNIQUE(account_id, import_uid)`, `'import'` no `CHECK` de `source`), parser próprio `src/lib/statement-import/ofx.ts` (sem dependência nova — regex sobre blocos `STMTTRN`, detecta charset do cabeçalho pra decodificar Windows-1252/ISO-8859-1 corretamente) e `ImportStatementDialog` (botão "Importar extrato" em `AccountsList`, preview antes de confirmar, só contas corrente/poupança — sinal de OFX de cartão de crédito varia por emissor, ficou de fora). Testado de ponta a ponta com um arquivo OFX sintético contra a conta seed (saldo bateu certinho, dados de teste apagados depois). Ver seção 7.33.
- **2026-09-04** — Open Finance (Pluggy) implementado de ponta a ponta, revertendo a decisão de escopo mantida desde 7.20/7.21/7.29, a pedido do usuário ("integrar automaticamente as contas, pro botão gerenciar open finance ficar clicável e utilizável"): migration `088_open_finance.sql` (`open_finance_items`/`open_finance_accounts`, `financial_accounts.is_open_finance`, `transactions.pluggy_transaction_id`+`source`), `src/lib/open-finance/{pluggy,sync}.ts`, rotas `src/app/api/open-finance/{connect-token,items,items/[itemId],items/[itemId]/sync,webhook}` + cron diário `open-finance-sync`, componente `OpenFinanceManageDialog` (substitui o botão desabilitado). Dependências novas: `pluggy-js`, `react-pluggy-connect`/`pluggy-connect-sdk`. Bug real de SSR encontrado e corrigido num browser real (Playwright): o widget escreve em `window` no import, quebrando a passada de servidor — resolvido com `next/dynamic({ssr:false})`. **Pendente do lado do usuário**: aplicar a migration no Supabase e criar a conta na Pluggy (`PLUGGY_CLIENT_ID`/`PLUGGY_CLIENT_SECRET` ainda vazios) — até lá, o botão funciona e mostra aviso de configuração pendente em vez de quebrar. Ver seção 7.32.
- **2026-09-04** — Redesign visual dos e-mails do cron `notification-emails` (os 6 tipos, incluindo o de oferta recusada), a pedido do usuário: novo `src/lib/email/template.ts` (`renderEmailTemplate()`, reutilizável) substitui a função `wrap()` minimalista por um layout em tabela com logo real, card branco, cor de acento por tipo (verde/âmbar/olive) e bloco destacado pro motivo da recusa. Testado enviando um preview de verdade pro e-mail do próprio usuário via o cron real. **Trabalho paralelo no mesmo dia** resolveu o mesmíssimo problema de outro jeito (`src/lib/email/email-header.ts`, `wrapEmail()`, ver entrada de "Oferta agendada" abaixo) — as duas implementações coexistem por ora, consolidar fica em aberto. Ver seção 7.2.
- **2026-09-04** — Migrations 085/087 aplicadas pelo usuário; enviado manualmente o e-mail retroativo da oferta rejeitada no dia anterior (o trigger só reage a eventos futuros, não alcançaria essa sozinho) — confirmado com o usuário antes, por afetar uma pessoa real. Ver seção 7.2.
- **2026-09-04** — E-mail de "oferta recusada" passou a alcançar também quem reportou sem conta (não só quem tem login): nova coluna `pledges.rejection_email_sent_at` (migration `087_pledge_rejection_email_anonymous.sql`, renumerada de `086` por colisão com trabalho paralelo — ver seção 7.2) + segunda fase no cron `notification-emails` que varre `pledges` direto por `reporter_email`, sem depender de `auth.users`. Doação anônima de verdade (`is_anonymous=true`) continua sem alcance, porque já não tem e-mail salvo. Ver seção 7.2.
- **2026-09-04** — Ajuste de UX em Conciliação, a pedido do usuário vendo a tela ao vivo: numa oferta recusada dentro da janela de reanálise, a caixa "Confirmar depósito" não abre mais direto — só um botão "Reanalisar" aparece, e clicar nele é que revela o formulário + Confirmar/Cancelar (mesmo padrão "revelar ao clicar" que Rejeitar já usa numa oferta pendente). Evita a sensação de "oferta recusada com botão Confirmar já pronto". Verificado num browser real. Ver seção 7.2.
- **2026-09-04** — Oferta agendada ("Quero ajudar, mas não agora"), a pedido do usuário: nova opção no wizard de parceria pra quem quer ajudar mas só numa data futura ("só posso dia 10") — sem coletar pagamento agora, só data + valor opcional, com conta obrigatória pra poder lembrar. Nova tabela `scheduled_pledges` (migration 086), novo cron diário `scheduled-pledge-reminders` (notificação in-app + e-mail leve, sem cobrança, com link de cancelamento), `PledgeForm` pré-preenche e marca `fulfilled` quando a pessoa completa pelo link do lembrete. Novo cabeçalho de e-mail compartilhado (`wrapEmail()`, texto "go→guide" centralizado em vez de logo por ora), usado só no e-mail novo por enquanto. **No mesmo dia**: link "Não posso agora, mas quero ajudar depois" também adicionado direto na página de cada projeto, logo abaixo do botão principal de contribuir — a pedido do usuário, pra não ficar só no menu geral de parceria do perfil, já que quem não pode ajudar agora tende a não clicar em CTA nenhum de pagamento. Ver seção 7.3-sexies.
- **2026-09-02** — Tela de agradecimento do `PledgeForm` ganhou botão "Ver perfil de {nome}" + redirecionamento automático em 8s pro perfil do missionário, a pedido do usuário — cancela sozinho se a pessoa navegar antes (ex. "quero ser parceiro fixo"). De quebra, corrigido um gap real do retorno do Stripe Checkout, que não mostrava nenhuma tela de confirmação. **Correção no mesmo dia**: o redirect usava `router.push`, que não fechava o `PartnershipModal` quando essa tela estava dentro dele (modal ficava por cima da página já navegada por baixo) — trocado por `window.location.href`. Ver seção 7.3-quinquies.
- **2026-09-03** — Rejeitar uma oferta deixou de ser definitivo na hora, a pedido do usuário: fica em destaque em Conciliação por 7 dias (o apoiador pode aparecer com o comprovante), depois arquivada por mais ~53 dias (total 60), ainda reconfirmável nas duas fases — tudo calculado em cima de `pledges.reviewed_at`, sem coluna nova nem cron. `ReconciliationTabs` novo (Pendentes/Arquivadas, substitui `ReconciliationQueue`); `PledgeReviewCard` mostra o motivo + prazo de reconsideração e esconde Rejeitar numa oferta já recusada. Nova notificação `pledge_rejected` pro doador com o motivo (migration `085_pledge_rejected_notification.sql`, **não aplicada por mim** — mesma limitação de DDL da 084, precisa rodar no SQL Editor do Supabase), roteada em `NotificationsBell` e somada ao cron de e-mail; `/dashboard/financeiro-parceiro` agora mostra o motivo da recusa pro doador. Verificado num browser real (Playwright) exceto o disparo do trigger em si, que depende da migration ser aplicada. Ver seção 7.2.
- **2026-09-03** — Conciliação sem nenhuma conta financeira cadastrada: primeiro corrigido pra pelo menos mostrar as ofertas pendentes (antes sumia a lista inteira, só um texto sem link); depois, a pedido do usuário ("já tenho Pix configurado, o sistema deveria entender isso"), revisto pra **criar a conta automaticamente** ao confirmar (nome/moeda a partir do método de pagamento da própria oferta), sem exigir passo manual nenhum — mesmo padrão que `payment_methods.linked_account_id` já usa pra Stripe, agora com auto-criação de verdade. Verificado num browser real (Playwright) de ponta a ponta. Ver seção 7.2.
- **2026-09-03** — Confirmado ao vivo que o `ALTER PUBLICATION supabase_realtime ADD TABLE public.notifications` (migration 084) já tinha sido aplicado pelo usuário: script Node avulso (autenticado como conta seed, insert via service role) recebeu o evento `postgres_changes` em ~800ms, sem precisar de F5. Realtime de notificações confirmado funcionando de ponta a ponta. Ver seção 7.10.
- **2026-09-03** — Correção: a tela de Conciliação (revisar oferta pendente) não tinha nenhum `<Label>` nos campos de valor/conta — os botões Confirmar/Rejeitar sempre funcionaram (verificado clicando de ponta a ponta), mas a falta de rótulo dava a impressão de tela quebrada/pouco intuitiva. Adicionado label nos dois campos + instrução no topo da página + seletor de conta agora filtra só contas na moeda da oferta. Ver seção 7.2.
- **2026-09-03** — Correção: notificação de "nova oferta" levava pra `/dashboard/financeiro/lancamentos`, que aparecia vazia (a oferta é `pledges` pendente, ainda não virou `transactions`). `NotificationsBell` agrupava `new_pledge`/`pledge_confirmed`/`partner_lapsed` numa única rota fixa; agora cada tipo vai pro lugar certo — `new_pledge` (missionário) → Conciliação, `pledge_confirmed` (parceiro que reportou) → `/dashboard/financeiro-parceiro`, `partner_lapsed` (missionário) → `/dashboard/parceiros`. Verificado num browser real. Ver seção 7.10.
- **2026-09-03** — Correção: notificações só apareciam recarregando a página, nunca em tempo real. Causa: a tabela `notifications` nunca tinha sido adicionada à publicação `supabase_realtime` — mesmo bug, mesma causa raiz, já corrigido antes pra `messages` (migration 030). Nova migration `084_notifications_realtime.sql` (precisa ser rodada manualmente no SQL Editor do Supabase — não há acesso de DDL direto neste ambiente). Ver seção 7.10.
- **2026-09-03** — Revisão do módulo financeiro inteiro via `/code-review high --fix`: corrigiu um bug de máscara de moeda no `AccountWizard` (ignorava a moeda escolhida) e sinalizou que "arquivar conta" (7.29) não se propagava pro resto do módulo. Corrigido manualmente: `FinancialDashboard`/Relatórios/Limites/Metas agora só somam saldo ou oferecem moeda de contas ativas, e o seletor de conta pra lançamento novo (avulso, recorrente, confirmação de oferta) some contas arquivadas — enquanto tabelas/listas que mostram dado já existente continuam vendo todas, pra não quebrar a exibição de lançamentos antigos numa conta hoje arquivada. Verificado num browser real. Ver seção 7.31.
- **2026-09-03** — Verificação end-to-end num browser real (Playwright, conta seed `ana.ferreira`) das três rodadas anteriores desta sessão (Categorias, Reserva de emergência, Contas): tudo confirmado funcionando exceto um bug real corrigido — o empty-state de `CategoryTree` mostrava "Nenhuma categoria ainda." (implica que não existe categoria nenhuma) quando na verdade era só um dos toggles de filtro zerando a lista; agora considera os toggles também. Dados de teste criados durante a verificação foram apagados e o estado seed original restaurado. Ver seção 7.30.
- **2026-09-03** — Tela de Contas bancárias, estilo GranaZen: cabeçalho novo + botão "Gerenciar Open Finance" (desabilitado, Open Finance segue fora de escopo), abas Ativas/Arquivadas ligando a coluna `financial_accounts.archived` que já existia sem UI, botão Arquivar/Reativar no card. "Adicionar conta" virou `AccountWizard` de 6 etapas — as 3 primeiras e a revisão seguem o wizard real do GranaZen (automática/manual, nome, saldo inicial+sinal), mas a etapa "conta padrão pro WhatsApp" do GranaZen não se aplica (sem bot de WhatsApp aqui) e foi substituída por duas etapas próprias pra tipo de conta/moeda/cartão/compartilhada, que o `AccountForm` (mantido só pra editar) já exigia. Ver seção 7.29.
- **2026-09-03** — Calculadora de "Reserva de emergência" na aba Metas, estilo GranaZen (revisita uma decisão da 7.25, que tinha deixado isso fora): card de entrada some quando já existe uma meta chamada "Reserva de emergência"; `EmergencyFundWizard` novo (6 passos — custo fixo mensal, tipo de renda define 6 ou 12 meses de reserva, valor guardado, valor por mês, revisão) com painel lateral "Sua proteção" recalculando em tempo real. Cria uma `FinancialGoal` normal ao final (sem tabela nova), reaproveitando toda a infra de metas da 7.25/7.21. Ver seção 7.28.
- **2026-09-03** — Toggles "Com limite de gasto"/"Com subcategoria" + botão "Relatório limite de gasto" na tela de Categorias, revisando a 7.26 (dessa vez com o HTML real da referência em mãos, não só prints): `Switch` novo (`src/components/ui/switch.tsx`, wrapping do `@base-ui/react/switch`), pills de filtro sempre visíveis, link de texto virou botão sólido com nossa cor primária no lugar do preto fixo do GranaZen. Ver seção 7.27.
- **2026-09-03** — Cor por categoria + integração com Limites de gastos, estilo GranaZen: coluna `transaction_categories.color` (existia desde o schema inicial, nunca usada) agora editável via `CategoryForm` novo (paleta de 10 cores) e exibida como bolinha na tela de Categorias, reescrita (`CategoryTree`) com busca, "Limite de gasto: RX" por categoria (join com `spending_limits`), badge de valor total do limite + link, botão de relatório linkando pros lançamentos filtrados. Deliberadamente fora: arquivar categoria (rota `/arquivadas` do GranaZen — tocaria muito código existente), toggles de filtro extras, menu de ações colapsado no mobile. **Não verificado num browser real desta vez** — usuário estava com o próprio servidor de dev ativo, evitei mexer nele de novo (Next.js 16 também bloqueia dois `next dev` no mesmo projeto). Ver seção 7.26.
- **2026-09-03** — "Visão geral" na aba Metas, estilo GranaZen: card com anel de composição (`DonutChart` novo, primeiro donut do app — parte-de-um-todo com número-herói no centro, exceção válida ao "não usar pizza" da dataviz skill) mostrando quanto cada meta representa do total guardado, escopado por moeda. Cada meta ganhou cor própria e estável (paleta `chart-3..8`, atribuída por ordem de criação), reaproveitada na bolinha do nome, na fatia do anel e na barra de progresso. Card de meta reestruturado (Já guardado/Valor da meta lado a lado, Faltam+percentual). De fora: avatar de quem contribuiu, página de detalhe "Ver meta", card promocional de reserva de emergência. Verificado num browser real (Playwright + dados seed) — matemática do anel conferida, sem sobreposição geométrica. Ver seção 7.25.
- **2026-09-03** — Primeira verificação de ponta a ponta num browser de verdade (`npm run dev` + Playwright headless + conta seed com dados reais, não só `tsc`/`build`) de toda a rodada do dashboard financeiro estilo GranaZen. Achou e corrigiu 2 bugs visuais que só apareciam com dado real: título truncando pra 3-4 letras em `MonthSummaryCards` (valor e título competindo pela mesma linha do grid — resolvido empilhando o valor abaixo em vez de do lado) e rótulos do eixo X colidindo no `FrequencyChart` (faltava a mesma proteção de colisão que o `TrendChart` já tinha). Ver seção 7.24.
- **2026-09-03** — Aba "Relatórios" virou analytics estilo GranaZen (Despesas/Receitas por categoria + `FrequencyChart` novo, Receitas×Despesas por dia/semana do mês) — a rota antiga de mesmo nome (histórico de prestação de contas, seção 7.10-quater) foi renomeada pra "Prestações" (`/dashboard/financeiro/prestacoes`), colisão de nome resolvida com o usuário. `FrequencyChart` usa a paleta `chart-1`/`chart-2` já validada, não o verde/vermelho do GranaZen — consistência com o `TrendChart` existente. De fora desta rodada: sub-abas Lançamentos pendentes/Fluxo de caixa/Conta bancária Open Finance e "Gerenciar Gráficos" (reordenar cards por drag-and-drop). Ver seção 7.24.
- **2026-09-02** — Renumera migrations 079-081 → 081-083 (colisão com trabalho paralelo: outra sessão pegou 079/080 pra `079_history_blocks_image_gallery.sql`/`080_profile_birth_date.sql`, sem relação com o dashboard financeiro). Mesmo tipo de colisão já resolvido antes (ver entrada de renumeração 074-077→075-078). Só renomeação de arquivo + atualização de comentários/doc citando o número — schema/lógica das migrations inalterados.
- **2026-09-02** — Quarta rodada do dashboard financeiro estilo GranaZen: `PeriodFilterBar` novo no topo da Visão Geral (←/→, "Esse mês", intervalo de datas, ocultar valores, Atualizar — faltava, tinha ficado de fora da 7.20 por engano); `MonthTransactionsPanel` ganhou botões Receita/Despesa (`defaultType` novo em `TransactionForm`), busca por valor além de descrição, empty state com ícone; `CategoryPanel` ganhou heading + intervalo de data por escopo, `CategoryBarChart` ganhou ícone no vazio. Dois ajustes finos no `MonthNavigator`: cartões de mês alargados (116→144px, cortavam texto maior) e linha conectora/bolinha realinhadas (bolinha caía fora da linha; trecho sólido ultrapassava o mês selecionado). Ver seção 7.23.
- **2026-09-02** — Correção: dashboard financeiro (Visão Geral) não renderizava nada além do card "Criar minha primeira conta" quando o missionário ainda não tinha `financial_accounts` — `page.tsx` tinha um `return` antecipado que cortava o resto da página. Virou uma faixa de aviso no topo; `FinancialDashboard` sempre renderiza (zerado, com `accounts=[]`), igual ao GranaZen. Ver seção 7.22.
- **2026-09-02** — Fidelidade visual do navegador de mês (`MonthNavigator`) e dos cards de resumo (`MonthSummaryCards`) no dashboard financeiro: usuário mandou o HTML real do GranaZen pra reproduzir mais de perto. Navegador ganhou `Card` wrapper, abas em pill (`bg-muted/60`), texto de descrição por métrica, cartões de mês maiores com borda/linha tracejada pra meses futuros e setas circulares sobrepostas. Cards de resumo ganharam barra de destaque colorida + ícone em badge circular + sublinhas em pílula (`CircleCheck`/`Clock3`, `text-success`/`text-warning`), paleta trocada de `chart-1/2` pra `primary/success/destructive` (mais adequada pra card de status que pra série de gráfico). Puramente visual, sem mudança de dado/comportamento. Ver seção 7.22 e 11.1.
- **2026-09-02** — Segunda rodada do dashboard financeiro estilo GranaZen: painel "Lançamentos" + "Gráficos" lado a lado na Visão Geral (`MonthTransactionsPanel`/`CategoryPanel`, cada um com suas próprias abas — Todas/Receitas/Despesas e Todas/Receitas/Despesas/Despesas Não Pagas), substituindo o card fixo "Por categoria" da rodada anterior. Dois módulos novos: `spending_limits` (migration `082_spending_limits.sql`, limite mensal por categoria com barra de progresso e aviso ao estourar, `/dashboard/financeiro/limites`) e `financial_goals` (migration `083_financial_goals.sql`, meta de economia com aporte manual — sem vínculo automático com transações, confirmado com o usuário — `/dashboard/financeiro/metas`). `FinanceSubNav` ganhou as duas abas. Deliberadamente fora: Investimentos e Comunidade/Indicar amigos do GranaZen (sem relação com o financeiro daqui), conexão bancária, date-range picker explícito. Ver seção 7.21.
- **2026-09-02** — Saldo Previsto no dashboard financeiro (`/dashboard/financeiro`), a pedido do usuário reproduzindo o modelo do GranaZen (outro produto do usuário, branch `master` deste repo): nova coluna `transactions.is_paid` (migration `081_transaction_paid_status.sql`, default `true` — preserva comportamento anterior) diferencia pago/recebido de pendente; `update_account_balance()` passou a considerar `is_paid` e a disparar em UPDATE também (corrige de brinde o risco operacional já documentado de edição não resincronizar saldo). Novo motor `src/lib/financial/timeline.ts` (`buildFinancialTimeline`) calcula saldo anterior/disponível/previsto mês a mês a partir do saldo real da conta. `MonthNavigator` (abas de métrica + linha do tempo de 13 meses, toggle ocultar valores) e `MonthSummaryCards` (4 cards: Saldo Anterior, Receitas recebido×a receber, Despesas pago×não pago, Saldo Disponível+Previsto) substituem o antigo `balance-summary.tsx`. `TransactionForm` ganhou checkbox "Já paguei/recebi"; `TransactionTable` ganhou badge de pendência + botão "Marcar como pago". Visão Geral ganhou botão de lançamento rápido. Deliberadamente fora do escopo: conexão bancária (Open Finance), date-range picker explícito, botões flutuantes de suporte (WhatsApp/dicas) do GranaZen. Ver seção 7.20.
- **2026-09-02** — Stripe billing da plataforma ativado (planos Pro/Missão + addon de gestores) + bypass de superadmin pra todo limite de plano, a pedido do usuário (motivado por uma conta pessoal presa em "0 de 0 gestores" sem nunca ter passado pelo Stripe). Financeiro e limite de posts/mês passaram a refletir o que `/planos` já prometia (mensagens/E2EE deliberadamente ficaram sem paywall). Webhook novo (`/api/billing/webhook`) grava plano/gestores extra a partir dos eventos do Stripe. Ver seção 7.19.
- **2026-09-02** — Três ajustes no funil de doação → parceria fixa, a pedido do usuário: (1) copy do card "quero ser parceiro fixo" (após oferta avulsa) agora deixa claro que virar parceiro(a) fixo(a) significa acompanhar tudo que o missionário publicar, não um compromisso mensal automático; (2) campo de telefone de `PledgeForm` ("Registrar oferta") trocou de `<Input>` sem máscara pra `PhoneInput` (DDI + máscara por país, sem limitar dígitos antes); (3) nova coluna `profiles.birth_date` (migration `080_profile_birth_date.sql`), coletada no `/cadastro` manual ou via Google People API (escopo `user.birthday.read`) no OAuth, copiada pra `partners.birth_date` quando a conta vira parceira fixa — alimenta `BirthdayReminders` do missionário sem exigir cadastro manual. Ver seção 7.3-quater.
- **2026-09-02** — Linha do tempo da história ganhou mídia por item, a pedido do usuário: cada item pode **vincular um post existente** (novo `TimelinePostPicker`, grade 3 colunas igual à `ProfilePostsGrid`, salva só `items[].post_id`) ou **anexar uma foto avulsa sem criar post** (`items[].image_url`, upload direto igual ao padrão já usado nas galerias de "Quem somos"/"Nosso chamado") — mutuamente exclusivos, sem migration (`timeline.items[]` já é JSONB livre). Item com post vinculado abre o `PostDetailViewer` de verdade ao tocar (em vez de só expandir o texto), reaproveitando a infra de posts — como o post vinculado às vezes não está entre os 20 mais recentes já carregados pra grade do perfil, novo helper `src/lib/history/linked-posts.ts` busca e enriquece (`enrichWithEngagement`) só os posts referenciados, chamado tanto por `[username]/historia/page.tsx` quanto por `[username]/page.tsx` (repassado por `ProfileContentTabs`). De quebra, dois ajustes na nav de pills adicionada na sessão anterior: pills passaram a usar sempre os rótulos fixos curtos (nunca mais o título livre da seção, que deixava algum pill enorme) e corrigido um bug real de corrida — a seção "Linha do tempo" (alta) continuava "intersectando" a zona de detecção mesmo com a página já no fim, e a própria notificação da `IntersectionObserver` desfazia o "cheguei ao fim, ativa o Convite" calculado por um listener de scroll separado; unificado num único callback que checa "chegou ao fim" primeiro, sempre. Ver seção 7.12.
- **2026-09-02** — Redesign visual da landing page do informativo (`/{username}/atualizacoes/[broadcastId]`): novos componentes em `src/components/partners/broadcast/` (stagger de entrada, stat tiles com contagem animada, gráfico de categoria animado reaproveitando a paleta `--chart-1..8` do dashboard, card de projeto com barra de progresso animada e placeholder gradiente quando não há foto), glow radial na cor de destaque do perfil. Gráfico de categoria passou a aparecer nos dois modos de privacidade (exato e percentual), não só no percentual. Ver seção 7.10-bis.
- **2026-09-02** — Correção de escopo + geração sem IA no compositor de atualização: os blocos "Prestação de contas"/"Projetos em destaque" estavam sem querer presos dentro do gate de IA (`<fieldset disabled={!aiEnabled}>`) — como `ANTHROPIC_API_KEY` nunca teve valor real em nenhum ambiente, isso significava que nenhum missionário conseguia usar esses blocos em produção. Corrigido: blocos sempre habilitados, e novo `generateTemplateUpdate()` (`src/lib/partners/generate-template-update.ts`) monta o texto por template determinístico (sem IA, sem custo) — `POST /api/ai/generate-partner-update` ganhou `mode: 'ai' | 'template'`. Botão "Montar informativo" sempre disponível ao lado de "Gerar com IA" (continua Pro-only). Ver seção 7.10-bis.
- **2026-09-02** — Dashboard financeiro com gráficos na Visão Geral (`/dashboard/financeiro`): primeiro gráfico do app — `--chart-1..8` em `globals.css` preenchidos com paleta categórica validada (antes placeholders cinza mortos), `TrendChart` (linha/área entradas×saídas por mês, mês clicável) + `CategoryBarChart` (barra horizontal por categoria, clique navega pra Lançamentos filtrado) em `src/components/ui/charts/`, ambos com toggle de tabela (acessibilidade) e crosshair/tooltip. `FinancialDashboard` (novo container) escopa os dois por período (3/6/12 meses) e moeda; `BalanceSummary` ganhou contagem animada. Primeiro uso de `framer-motion` no app (já era dependência, nunca usado). Ver seção 11.1.
- **2026-09-02** — Detecção de parceiro recorrente que parou de doar: nova coluna `recurring_pledges.lapsed_notified_at` (migration `078_recurring_pledge_lapse_detection.sql`) + cron diário `GET /api/cron/lapsed-donor-check` — recalcula do zero a cada execução se um compromisso recorrente ativo ficou 45+ dias sem nenhuma `pledges` confirmada, notifica o missionário (nunca o parceiro — reengajar fica manual, mesmo tom convidativo do `FundingProjectionCard`) e mostra um badge "Parece ter parado" em `/dashboard/parceiros`. Ver seção 3.1 (`recurring_pledges`).
- **2026-09-02** — Lançamentos recorrentes (aluguel, assinaturas, mensalidades): nova tabela `recurring_transactions` (migration `077_recurring_transactions.sql`, schema enxuto — sem parceiro/projeto, caso de uso é conta fixa pessoal) + cron diário `GET /api/cron/generate-recurring-transactions` que materializa direto em `transactions` (`source='recurring'`, novo valor no CHECK) e avança `next_due_date` em +1 mês, mesma técnica de `recurring-reminders/route.ts` mas sem confirmação humana no meio (dinheiro do próprio missionário, não de um parceiro). Nova aba "Recorrentes" em `/dashboard/financeiro`. Ver seção 3.1 (Financeiro).
- **2026-09-02** — Editor dedicado de prestação de contas: `SendBroadcastButton` ganhou prop `mode="report"` (usada pela aba Relatórios, `/dashboard/financeiro/relatorios`) em vez de um componente novo — bloco financeiro sempre ligado sem checkbox, sem bloco de projetos, assunto pré-preenchido, não manda e-mail por padrão; `mode="campaign"` (default, `/dashboard/parceiros`) continua idêntico a antes. `createBroadcast` ganhou um segundo `revalidatePath` e a tela de sucesso passou a chamar `router.refresh()`, pra a lista da aba Relatórios atualizar sozinha após publicar. Ver seção 7.10-quater.
- **2026-09-02** — Fundo restrito por categoria de orçamento: view `project_budget_progress` ganhou `spent_amount` (migration `076_budget_category_spent.sql`), somando `transactions.type='expense'` com aquele `budget_category_id` — até aqui só existia `raised_amount`, porque `budget_category_id` só era preenchido do lado de receita (`PledgeReviewCard`). `TransactionForm` (lançamento geral em `/dashboard/financeiro/lancamentos`) ganhou o seletor "Categoria do orçamento" que faltava pro lado de despesa; `BudgetBreakdown` ganhou prop `showSpent` (desligada por padrão, não muda a página pública de doação); novo painel "Financeiro do projeto" em `/dashboard/projetos/[id]` mostra arrecadado/gasto/disponível por categoria pro dono. Ver seção 7.7-bis.
- **2026-09-02** — Privacidade em camadas na prestação de contas: nova coluna `partner_broadcasts.financial_visibility` (`exact`/`percent_only`, migration `075_broadcast_financial_visibility.sql`) finalmente usa o grant `financial_summary` de `partner_visibility_grants` (existia desde a migration 011, nunca era checado em código nenhum) — em modo `percent_only`, tanto a narrativa gerada por IA quanto a landing page pública escondem valores exatos de quem não tem o grant, mostrando só percentual por categoria. Nova aba "Relatórios" em `/dashboard/financeiro` lista o histórico de prestações de contas já publicadas (antes só existiam enquanto o missionário guardasse o link). Ver seção 7.10-quater.
- **2026-09-02** — Aba "História" do missionário ganhou navegação por seção (scrollspy): pills fixas logo abaixo do título/header, sempre com rótulos curtos e fixos (Quem somos/Nosso chamado/Linha do tempo/Convite — nunca o título livre da seção, que ficava enorme no pill), sincronizadas com o mesmo hook de esconder-ao-rolar do header global (`useHideOnScroll`) pra não deixar um vão vazado quando o header some. "Quem somos"/"Nosso chamado" trocaram de 1 imagem única (`image_url`/`image_caption`) por uma galeria de fotos (`content.images: string[]`, sem legenda — migration `079_history_blocks_image_gallery.sql` migra dados antigos), com upload de arquivo de verdade (reaproveita `GalleryEditor` de `src/components/highlights/`, que ganhou labels i18n opcionais) em vez de colar link. Itens da linha do tempo agora colapsam por padrão (ano + 1 linha, expande ao toque) e o campo de texto virou textarea (aceita mais que uma linha). Editor (`/dashboard/configuracoes/historia`) espelha a mesma nav de pills. Novo componente compartilhado `src/components/history/section-nav.tsx`. A pedido do usuário, pensando em padrões de storytelling/captação de páginas modernas.
- **2026-09-01** — Bypass de superadmin pra todo limite de plano (parceiros, posts/mês, gestores, Financeiro) + ativação do Stripe billing da plataforma (planos + assentos extra de gestor) em modo teste, a pedido do usuário — motivado pela conta pessoal `familia-silva` (Free) sem conseguir adicionar gestores ("0 de 0"). No caminho, corrigidos dois gaps entre `/planos` e o código real: Financeiro estava liberado pra qualquer plano (agora exige Pro+, superadmin ignora) e o limite de post/mês do Free nunca era checado (agora é). Mensagens/E2EE ficaram deliberadamente fora do gate (decisão de escopo desta sessão, não confirmada com o usuário — paywall em cima de comunicação básica pareceu produto-hostil). 6 Prices criados no Stripe (teste) e webhook novo (`POST /api/billing/webhook`) sincronizando `profiles.plan`/`extra_manager_seats`/`subscriptions`. Falta o usuário configurar as mesmas env vars na Vercel (Production) pra funcionar de verdade lá; fluxo não testado num navegador real nesta sessão (sem credencial de login disponível). Ver seção 7.19 pro detalhe completo.
- **2026-09-01** — Encurtamento externo dos links curtos via terceiro (is.gd, depois TinyURL) — **tentado e revertido no mesmo dia**, a pedido do usuário: sem domínio curto próprio ainda (`mission-guide.vercel.app`), cogitou-se embrulhar `/l/[code]` num encurtador gratuito. is.gd bloqueia `*.vercel.app` de propósito (achado depurando `vercel logs` em produção — falhava 100% das vezes, não era instabilidade); TinyURL funcionava, mas força uma página de preview própria antes do redirect real pra link criado sem conta, sem opção de desligar. Revertido: "melhor ficar sem por enquanto do que encaminhar pra um site que não é o nosso" — link de doação passando por domínio de terceiro (ainda mais com uma tela extra) quebra confiança. Voltou a ser `/l/[code]` direto, mutação client-side (sem API route). Ver seção 7.18.
- **2026-09-01** — "Gerar link" desacoplado de "mandar por e-mail" no compositor de atualização, a pedido do usuário ("uma coisa é uma coisa, outra coisa é outra coisa"): checkbox opcional (ligado por padrão) — desligado, cria só o registro/landing page, sem tocar em `partners`/fila de e-mail nenhuma. Botão "Criar atualização" (não mais "Enviar atualização"), já que enviar por e-mail virou opcional. Ver seção 7.10-bis.
- **2026-09-01** — Landing page pública pra cada atualização criada em "Enviar atualização" (`/{username}/atualizacoes/{id}`, migration `072_broadcast_landing_page.sql`), a pedido do usuário: como não existe integração real de WhatsApp ainda, essa página é o jeito de "mandar por WhatsApp" — o missionário copia o link (botão novo na tela de sucesso do compositor) e cola numa conversa. Leitura pública só via function `SECURITY DEFINER` (`get_public_broadcast`), nunca a tabela direto — mesmo padrão que a outra sessão paralela usou pra `resolve_short_link()` (migration 070), útil ver os dois convergindo pra mesma solução. Nova coluna `financial_snapshot` guarda o resumo financeiro real (antes só alimentava o prompt da IA e era descartado) pra a página poder mostrar números reais em cartões, não só a prosa. Ver seção 7.10-bis.
- **2026-09-01** — Links curtos pra bio do Instagram/redes, pra perfil e pra projeto (migration `070_short_links.sql`, nova rota `GET /l/[code]`). Pedido do usuário: `/[username]/projetos/[slug]` é longo demais pra colar numa bio. Resolve o alvo por FK (não pelo path final), então sobrevive a troca de username/slug; RLS fechada pro dono, resolução pública passa só por uma function `SECURITY DEFINER` que nunca expõe `click_count` em bulk. Botão "Link curto" no menu do perfil público e no menu de ações do card de projeto. Ver seção 7.18.
- **2026-09-01** — Duas correções na geração de IA dos blocos inteligentes, a pedido do usuário logo depois de ver a primeira versão funcionando ("o texto gerado será... com ótima retenção de atenção? conterá imagens... e links clicáveis?"): (1) modelo trocado de Haiku pra `claude-sonnet-5` — só a narrativa mudou, é a única chamada de IA do app que não usa Haiku, decisão deliberada por qualidade de escrita; (2) cartões de projeto com foto de capa real, barra de progresso e botão linkando pro projeto de verdade — montados por **código** no momento do envio (não pela IA, que só escreve texto puro), migration `071_broadcast_highlight_cards.sql` guarda quais projetos foram escolhidos. Ver seção 7.10-bis.
- **2026-09-01** — Blocos inteligentes com IA no compositor de atualização (extensão da Fase 1 de campanhas, a pedido do usuário: "não de uma maneira cansativa quando o parceiro abrir a mensagem"). Dois blocos opcionais — prestação de contas (arrecadado/gasto do período, por moeda) e projetos em destaque (meta/progresso/o que falta) — viram contexto pra Claude Haiku tecer um texto único, caloroso, nunca um relatório de números; rascunho sempre editável antes de enviar. Reaproveita a mesma infraestrutura de IA já existente (créditos, `consume_ai_credits`, padrão de `/api/ai/translate`) em vez de criar algo novo. UI com dois gates independentes (infraestrutura configurada + plano do usuário inclui IA), cada um com mensagem própria. Descoberta na configuração: `ANTHROPIC_API_KEY` nunca teve valor real em nenhum ambiente — tradução por IA (feature já existente) nunca funcionou de verdade até hoje. Ver seção 7.10-bis.
- **2026-09-01** — Fase 1 de "campanhas pra parceiros" (item 4 do plano de e-mail em produção, retomado a pedido do usuário depois dos outros 3 itens no ar): envio avulso de atualização por e-mail pra rede de parceiros, com filtro por tipo. Decisão central: não criar uma entidade "campanha" separada — `highlights` já tem meta (`goal_amount`) e prazo (`funding_deadline`, migration 016) prontos e usados na página pública, então a Fase 2 (campanha automática com gatilhos de lançamento/lembrete/resultado, planejada mas não implementada ainda) vai só automatizar comunicação em cima do que já existe. Migration `069_partner_broadcasts.sql` (tabelas `partner_broadcasts`+`partner_broadcast_recipients`), novo cron `GET /api/cron/broadcast-sender` (5min, fila em vez de envio síncrono — evita timeout de request em listas grandes), botão "Enviar atualização" em `/dashboard/parceiros`. Reaproveita `update_emails_opt_in`/unsubscribe já existentes do `prayer-partner-updates`, mesma categoria de e-mail do ponto de vista de quem recebe. Ver seção 7.10-bis (nova) pro detalhe completo.
- **2026-09-01** — Gate de e-mail verificado no Financeiro (`/dashboard/financeiro`), último item do plano de e-mail em produção (ver entradas acima) — único bloqueio de verdade por e-mail não verificado no app inteiro (o resto do dashboard usa só o banner dispensável, ver seção 7.1 item 5). Duas decisões explícitas do usuário: bloqueia mesmo (não só avisa) e vale só pra quem recebe, não pro parceiro doador. `financeiro/layout.tsx` reaproveita `EmailVerificationBanner` pro botão de reenvio. Seção 7.1 atualizada.
- **2026-09-01** — Notificação por e-mail pra 4 dos 7 eventos que já geram notificação in-app (`new_message`, `pledge_confirmed`, `new_pledge`, `new_partner` — escolhidos pelo usuário, os outros 3 ficaram de fora por risco de spam), continuando o plano de e-mail em produção (ver entradas acima). Novo cron `GET /api/cron/notification-emails` a cada 5min (migration `068_notification_emails.sql` adiciona `notifications.email_sent_at` + índice parcial) — ver seção 7.10 pro detalhe completo. **Descoberta relevante nesta rodada**: `CRON_SECRET` nunca esteve configurado (nem local, nem produção) — todas as 4 rotas de cron do projeto, incluindo as 3 que já existiam antes de hoje, vinham devolvendo 401 e nunca rodando de verdade, sem ninguém perceber. Corrigido gerando e configurando um valor novo. Seção 7.10 atualizada.
- **2026-09-01** — Teste ponta a ponta da recuperação de senha em produção (primeira vez que esse fluxo era testado de verdade) revelou dois problemas, ambos corrigidos: (1) **e-mail caindo em spam** — SPF/DKIM/DMARC do domínio já estavam corretos (confirmado no cabeçalho técnico do Gmail, todos PASS), então não era autenticação de domínio; a causa real era o template padrão do Supabase ("Reset Password", sem marca nenhuma) combinado com reputação de remetente novo pra esse fluxo específico — trocado pelo mesmo estilo visual do e-mail de verificação (`send-verification-email.ts`: logo, cor da marca `#34390c`, botão), configurado direto no painel do Supabase (Authentication → Emails → Templates → Reset Password), sem mudança de código. (2) **Bug real, mais sério**: depois de resolver o e-mail, clicar no link levava direto pro `/dashboard` em vez da tela de nova senha — causa raiz no `proxy.ts` (seção 5.2): a regra "rota de auth + usuário já autenticado → redirect `/dashboard`" usa `pathname.startsWith('/recuperar-senha')`, e o clique no link de recovery já autentica uma sessão *antes* do redirect final pra `/recuperar-senha/nova-senha` — então a própria regra que existe pra tirar gente já logada da tela de login estava sequestrando exatamente o fluxo que precisa estar logado pra funcionar. Corrigido com uma exceção explícita pra esse path. Como o fluxo nunca tinha sido testado de ponta a ponta em produção antes de hoje, esse bug provavelmente sempre existiu desde que a página foi criada — sem forma de saber há quanto tempo a recuperação de senha estava quebrada de fato.
- **2026-09-01** — Início do plano "colocar e-mail em produção" (verificação de e-mail + recuperação de senha), a pedido do usuário depois de resolver o envio via Brevo (SMTP/API key configurados na Vercel, ver entradas acima). Passo 3 do plano (aviso de perda de E2EE na troca de senha) implementado nesta rodada — ver seção 7.5. Passos 1-2 (SMTP customizado do Supabase via Brevo pra tirar `resetPasswordForEmail` do limite de teste do remetente padrão, e teste ponta a ponta em produção) exigem o painel do Supabase, fora do alcance de execução direta — ficam com o usuário. Passo 4 (mecanismo completo de sobrevivência da chave E2EE a troca de senha) explicitamente adiado pelo usuário pra depois de tudo funcionando.
- **2026-09-01** — Revisão de segurança da E2EE de mensagens (pedido do usuário: "revisa essa configuração de segurança de criptografia, veja se tem alguma fragilidade"). Duas fragilidades reais de RLS corrigidas na migration `067_fix_messages_and_dek_grants_rls.sql`, ambas na tabela usada pelo próprio Postgres, não no lado criptográfico (a cifra em si — X25519/Argon2id/XSalsa20-Poly1305 — segue íntegra): (1) **spoofing de remetente em `messages`** — a policy única `FOR ALL USING (sender_id OR recipient_id = auth.uid())` valia também pro INSERT (o Postgres reusa `USING` como `WITH CHECK` quando não há um explícito), então qualquer autenticado conseguia inserir uma mensagem com `sender_id` de OUTRA pessoa contanto que `recipient_id` fosse ele mesmo — dava pra forjar, direto pelo client Supabase sem passar pela UI, uma "mensagem recebida" com texto arbitrário em claro (`is_encrypted=false`) como se tivesse vindo de qualquer usuário (ex.: phishing se passando pelo missionário que a pessoa apoia, pedindo Pix). Corrigida separando as policies — INSERT agora exige `sender_id = auth.uid()`; UPDATE removida (não usada por nenhum fluxo). (2) **`encrypted_dek_grants` com INSERT/UPDATE liberados pra qualquer autenticado**, sem checar relação com o recurso — não quebra confidencialidade (sem a DEK real ninguém forja um `wrapped_dek` válido), mas permitia negar acesso: revogar/corromper o grant de terceiros sem nenhuma relação com o recurso, ou "reservar" a linha (`resource_type`, `resource_id`, `grantee_user_id`) de outra pessoa antes da conversa existir de verdade — possível porque `resource_id` de conversa é um hash determinístico sem segredo (`conversationId`, seção 6.3), computável por qualquer client sabendo os dois `user_id`s + `profile_id` (visíveis em qualquer perfil público). Restringido a `grantee_user_id = auth.uid()` (bootstrap) `OR has_dek_grant(...)` (já ter grant ativo no recurso) — fecha o vetor de UPDATE por completo; o de INSERT via auto-grant de bootstrap fica mais estreito mas não 100% fechado (ver nota em 6.2). Migration ainda não aplicada no Supabase remoto (aplicação manual, mesmo padrão já registrado em outras entradas deste changelog). De quebra, a pedido do usuário, o aviso "🔒 Conversa cifrada ponta-a-ponta" saiu do cabeçalho da conversa aberta (`MessageThread`) — informação repetida sem necessidade dentro de cada conversa — e ficou só na área externa (lista `/dashboard/mensagens`). Seções 6.2 e 7.6 atualizadas.
- **2026-09-01** — Gate de verificação de e-mail deixou de bloquear acesso ao dashboard, a pedido explícito do usuário: "a pessoa não perde o acesso ao sistema, mas essa pendência fica aparecendo sempre pra pessoa até ela verificar o e-mail". Antes disso, cheguei a montar (e descartar antes de aplicar, a pedido do usuário assim que viu a tela) uma versão com modal bloqueante por cima do dashboard — também rejeitada por ainda impedir o uso normal do sistema. Removido `if (!profile.email_verified) redirect('/verificar-email/pendente')` de `dashboard/layout.tsx`; a página cheia `/verificar-email/pendente` foi deletada (só existia pra esse redirect) e o CTA de link expirado/inválido em `/verificar-email/[token]` agora aponta pra `/dashboard` em vez dela. No lugar, novo `EmailVerificationBanner` (`src/components/dashboard/email-verification-banner.tsx`) — card com o mesmo conteúdo/estilo da tela antiga (ícone, título, corpo, botão "Reenviar e-mail", mesmas chaves de tradução já existentes) — renderizado no topo de `{children}` em `dashboard/layout.tsx`, então aparece em **qualquer** rota do dashboard enquanto `profile.email_verified` for `false`, sem nenhuma lógica de dismiss (não é como o `SetupChecklistBanner`, que uma vez fechado some pra sempre via localStorage — esse aqui volta em toda visita até a pessoa clicar no link de verdade). Perguntei ao usuário quais áreas específicas deveriam ficar desabilitadas/esmaecidas até a confirmação (pedido original também incluía isso) — a conversa ficou no meio (usuário pediu que eu explicasse antes o "porquê" de precisar disso, recomendei gatear só o Financeiro pelos mesmos motivos que o SISGO verifica o e-mail de contato de escola: recuperação de conta e recibos/avisos de repasse dependem de um e-mail de verdade) — **nenhuma área foi desabilitada ainda**, só o banner; isso fica como próximo passo em aberto. Seção 7.1 atualizada.
- **2026-08-31** — Dois ajustes no `BackButton` (`‹`) da barra de abas principal do perfil público (`profile-tabs.tsx`), reportados pelo usuário testando ao vivo num iPhone real, link aberto direto do WhatsApp. (1) **Botão inclicável**: o botão ficava encostado no pixel 0 da tela (`x: 0`, sem nenhuma margem esquerda) — em iOS, a borda esquerda é reservada pro gesto do sistema de "voltar" (usado tanto pelo Safari quanto pelo WebView in-app do WhatsApp), que compete com o toque e engolia o clique; funcionava perfeitamente no desktop porque esse gesto de borda não existe lá. Corrigido com `pl-4` (16px de respiro), mesma distância já usada no outro `BackButton` deste arquivo (variante `/mensagens`/`/oracao`). Verificado com WebKit real via Playwright (não Chromium, que não reproduz esse bug): `elementFromPoint` confirmou que o botão é o elemento correto no topo antes e depois — a diferença real era só a posição (`x: 0` → `x: 16`) — e um `tap()` simulado passou a navegar corretamente. (2) **Destino sempre fixo em `/explorar`**: a pedido explícito do usuário ("esse botão deve encaminhar sempre pra área de 'explorar', esse botão serve só pra sair"), o botão deixou de usar a lógica padrão do `BackButton` compartilhado (voltar pro histórico real via `router.back()`, com `href` só como rede de segurança, condicional a `viewerUserId` pra escolher entre `/dashboard`/`/explorar`) — agora passa um `onClick={() => router.push('/explorar')}` direto, que tem prioridade sobre toda a lógica de histórico do componente. Essa mudança é só desta instância específica do `BackButton`; as outras ~7 no resto do app continuam com o comportamento padrão de "voltar de verdade", que é o desejado ali. Prop `viewerUserId` (só existia pra essa decisão condicional) removida de `ProfileTabs`/`Props` e do call site em `[username]/layout.tsx`; chave de tradução `backToDashboard`, que ficou órfã, removida das 3 línguas.
- **2026-08-31** — Menu "..." (estilo Instagram) na barra de abas do perfil público, a pedido do usuário: substitui o seletor de idioma compacto solto ("PT ▾") que ficava sozinho no canto superior direito por um `DropdownMenu` (`ProfileMoreMenu`, novo, `src/components/profile/profile-more-menu.tsx`) com "Compartilhar perfil", submenu "Idioma" e, só pra quem não é dono (`!canEdit`), "Denunciar perfil". `LanguageSwitcher` ganhou um terceiro modo, `submenu` (`DropdownMenuSub`/`DropdownMenuSubTrigger`/`DropdownMenuSubContent`, reaproveitando a mesma `handleSwitch`/`switching` dos modos existentes, sem duplicar lógica), pra poder ser embutido dentro do `DropdownMenu` do `ProfileMoreMenu` em vez de abrir o seu próprio. Como compartilhar/denunciar passaram a morar nesse menu, o rodapé duplicado que uma mudança anterior no mesmo dia (ver entrada de moderação/denúncias abaixo) tinha colocado em `ProfileCTA` (ícone `ShareButton` + `ReportProfileButton` lado a lado, abaixo dos links de navegação secundários) foi removido — `ProfileCTA` perdeu as props `profileId`/`displayName` (não sobrou nada que as usasse) e o único call site (`[username]/page.tsx`) foi ajustado; `ReportProfileButton` (`src/components/profile/report-profile-button.tsx`) ficou sem nenhum uso e foi deletado (`ShareButton`, componente compartilhado, continua em uso em `profile-owner-actions.tsx` e na página pública de projeto). Seções 5.6.1, 7.16 e 10 (`profile/`) atualizadas.
- **2026-08-31** — Reconciliação de duas implementações paralelas de verificação de e-mail que surgiram ao mesmo tempo em sessões diferentes: esta sessão implementou o padrão customizado (token + Brevo, entrada de 2026-08-28 abaixo) enquanto, em paralelo, `cadastro/page.tsx` no `origin/main` já tinha ganhado um fluxo baseado no "Confirm email" nativo do Supabase (`if (!data.session) setConfirmEmailSent(true)`, chaves `confirmEmailTitle`/`confirmEmailSubtitle`) — descoberto só na hora do merge (`git merge origin/main`), via os 25 commits que incluíam também telefone/WhatsApp no cadastro (migration `055_profile_phone.sql`, que colidiu de número com a migration de e-mail desta sessão — renumerada pra `063_email_verification.sql`), moderação, painel admin, estabilidade de URL, busca difusa e mais. Perguntado, o usuário confirmou manter o fluxo **customizado** desta sessão e descartar o nativo: removido o estado `confirmEmailSent`/early-return e o comentário sobre `!data.session` de `cadastro/page.tsx` (mantidos os outros dois pontos legítimos que vieram junto no mesmo arquivo — campo de telefone opcional e `window.location.href` no lugar de `router.push`), removidas as chaves `confirmEmailTitle`/`confirmEmailSubtitle` das 3 línguas (ficariam órfãs), e `handle_new_user()` final passou a inserir `phone` (da 055) **e** `email_verified` (desta sessão) juntos, já que as duas migrations redefiniam a mesma função via `CREATE OR REPLACE`. Consequência prática pro usuário: o fluxo de e-mail nativo do Supabase precisa ficar **desligado** no painel (Authentication → Settings → "Confirm email") — se continuar ligado, `signUp()` nunca devolve sessão e o fluxo customizado desta sessão (que depende de estar logado pra chamar `/api/auth/enviar-verificacao`) nunca dispara.
- **2026-08-28** — Verificação de e-mail obrigatória no cadastro por e-mail/senha, a pedido do usuário ("todo mundo que criar uma conta deve autenticar o e-mail, para usarmos em integrações futuras de envio de e-mail"), replicando um padrão já usado no projeto irmão SISGO. O SISGO tem dois padrões distintos de verificação de e-mail (nativo do Supabase Auth pro cadastro de usuário; token custom + Brevo pra verificar e-mail de contato de escolas já cadastradas) — investiguei os dois e o usuário escolheu explicitamente o **customizado com token + Brevo**, adaptado da migration `sisgo/supabase/migrations/015_contact_email_verification.sql` e de `sisgo/src/lib/email/sendVerificationEmail.ts`/`.../verificar-email/[token]/page.tsx`, para reaproveitar o helper `sendEmail()` já existente aqui (`src/lib/email/brevo.ts`) sem depender de configurar nada no painel do Supabase. Diferença importante em relação ao padrão de escolas do SISGO (onde a pessoa cadastra um e-mail de contato *depois* de já ter conta): aqui não existe conta sem e-mail, então o e-mail verificado é o mesmo do cadastro — o disparo é automático, sem etapa extra de "adicionar e-mail". `profiles` ganhou `email_verified`/`email_verification_token`/`email_verification_token_expires_at` (migration `063_email_verification.sql`, renumerada — ver entrada acima), e `handle_new_user()` já cria a conta com `email_verified=true` para contas Google (o provider já verifica) e `false` para e-mail/senha. A migration faz grandfathering (`update profiles set email_verified = true where email_verified = false`) logo após criar a coluna — a exigência vale só pra contas criadas a partir de agora; sem isso, todo usuário já existente cairia bloqueado no próximo acesso ao dashboard. Cadastro (`/cadastro`) dispara `POST /api/auth/enviar-verificacao` logo após o `signUp` (fire-and-forget, não bloqueia o redirect pro onboarding); a rota gera token de 24h e reaproveita a mesma rota tanto pro envio inicial quanto pro reenvio manual em `/verificar-email/pendente`. Página de confirmação `/verificar-email/[token]` usa `createServiceClient()` e também sincroniza `auth.users.email_confirmed_at` via `supabase.auth.admin.updateUserById()`, de graça, mesmo o fluxo sendo custom. Gate único em `dashboard/layout.tsx` (`if (!profile.email_verified) redirect('/verificar-email/pendente')`), no mesmo padrão do check de `!profile` já existente ali — não no middleware (`src/proxy.ts`, o `middleware.ts` renomeado pelo Next 16), que só verifica presença de `user`, nunca consultou a tabela `profiles`. `/onboarding` fica de fora do gate de propósito (rota separada de `/dashboard`). Migration ainda não aplicada no Supabase remoto (projeto não está linkado à CLI localmente, sem `config.toml`/`DATABASE_URL` — aplicação é manual, como já era o padrão aqui) e `BREVO_API_KEY` continua vazia em `.env.local`, então o envio de fato só funciona depois desses dois passos manuais. Seções 3.1 (`profiles`) e 7.1 atualizadas.
- **2026-08-31** — Clareza visual na página pública do projeto (`[username]/projetos/[slug]`), a pedido do usuário depois de comparar com um mockup do Google Stitch: reclamou que "o texto 'ainda falta' parece que é do item de baixo, mas é do de cima" na lista de categorias de orçamento. Causa: `BudgetBreakdown` (`src/components/highlights/budget-breakdown.tsx`) não tinha nenhuma fronteira visual entre itens (só `space-y-4` solto) — a linha "Faltam R$X" do fim de um item ficava a poucos pixels do título do próximo, mesmo peso de texto, sem card/borda separando. Corrigido com card próprio por categoria (`rounded-xl border p-3`). Além disso, a pedido do usuário, o texto "Faltam R$X" **some da lista no modo contribuir** (perfil público, `contributeBaseHref` presente) — só aparece no modo admin (`FinancialEditSection`, sem botão Contribuir) ou quando a categoria já está 100% financiada (🎉, em ambos os modos); no modo contribuir, o valor que falta só aparece depois de clicar "Contribuir", via o chip "Cobrir o que falta" do `PledgeForm` (já existente desde 2026-08-30). Valor principal arrecadado (`R$ X arrecadados`) ganhou destaque tipográfico real (`text-2xl md:text-3xl font-bold`, antes só `text-base font-semibold`, quase do mesmo peso que "Meta: X" ao lado). Seção de PIX passou a listar **todas** as chaves ativas do tipo `pix` (antes `pixMethod = .find(...)` pegava só a primeira; virou `pixMethods = .filter(...)`, cada chave no seu próprio bloco com "Em nome de X" quando o rótulo existe) — útil quando o casal/família tem uma chave cada. `tsc` limpo, verificado com screenshot real (Playwright) contra o projeto "Construindo uma Casa Missionária", que já tinha 2 chaves Pix cadastradas.
- **2026-08-31** — Ajustes finos no menu ⋮ da listagem pública de projetos, a pedido do usuário. (1) Item "Compartilhar" novo (mesma lógica do `ShareButton` — Web Share API no celular, copiar link no desktop — reimplementada inline em vez de reaproveitar o componente, que é feito pra ser um botão próprio, não um item de menu). (2) Menu "sufocado" (usuário reclamou de falta de respiro) — `DropdownMenuContent` ganhou `w-56` fixo (antes seguia a largura do gatilho de 28px via `w-(--anchor-width)`, forçando "Mover para cima/baixo" a quebrar em duas linhas) e todo item passou por um `ITEM_CLASS` compartilhado (`gap-2.5 px-2.5 py-2`, mais respiro que o `gap-2` padrão) — mudança só neste menu (`project-card-menu.tsx`), não no componente `dropdown-menu.tsx` compartilhado por todo o app. (3) Ícone de "Projetos arquivados" ao lado de "Criar projeto" no cabeçalho de `[username]/projetos` (só quando `canEdit`) — antes só existia o link equivalente em `/dashboard/projetos`.
- **2026-08-31** — Dois follow-ups da leva de gestão de projetos de ontem, reportados pelo usuário. (1) **Projeto arquivado continuava aparecendo** em 4 lugares que a leva anterior não cobriu: faixa de projetos do perfil público (`[username]/page.tsx`, 4 queries — contagem de ativos/concluídos, faixa em si, e o highlight "em destaque"), `/explorar` (feed de descoberta), `/[username]/trajetoria` e a checagem de `hasTrajectory` em `[username]/layout.tsx`. Todas ganharam `.is('archived_at', null)`. (2) **Fluxo pós-criação/edição de projeto**: usuário não queria mais cair na lista do dashboard (`/dashboard/projetos`, com ícones ▲▼👁↗✏️🗑 na frente de cada linha) depois de salvar — queria ir direto pra listagem pública (`[username]/projetos`, com o menu ⋮ novo). `HighlightForm`'s `backPath` nas duas rotas (`novo`/`[id]`) passou de `/dashboard/projetos` pra `/${username}/projetos`. Reordenar (▲▼), que só existia na lista do dashboard, foi replicado pro menu ⋮ da listagem pública — exigiu extrair `ProjectCard` da Server Component pra um novo `ProjectGrid` (client, `src/components/highlights/project-grid.tsx`) segurando o array local, já que mover um card depende do estado da lista inteira, não só do card clicado; `ProjectCardMenu` ganhou `onMoveUp`/`onMoveDown` opcionais. Como reordenar manual só faz sentido com `order_index` como critério de ordenação, a listagem pública voltou a ordenar por `order_index` em vez de `created_at DESC` (pedido de 2026-08-30, agora superado por este pedido — as duas coisas são incompatíveis: ordenação automática por data não convive com reordenar manual persistente). `tsc` limpo; não testado em navegador nesta rodada (pedido explícito do usuário de economizar créditos).
- **2026-08-30** — Leva grande de gestão de projetos, a pedido do usuário (série de pedidos na mesma sessão, "não encontrei onde eu posso excluir o projeto dentro do projeto" foi o pontapé). (1) **Confirmação de exclusão reforçada**: novo `DeleteProjectDialog` (`src/components/highlights/delete-project-dialog.tsx`) substitui o `window.confirm()` genérico em todo lugar que exclui projeto — exige digitar o nome exato do projeto antes de habilitar o botão "Excluir projeto" (`variant="destructive"`), com aviso explícito de que é irreversível e do efeito colateral real (posts/ofertas/lançamentos vinculados sobrevivem, só perdem a associação). Usado por `highlights-list.tsx`, `highlight-form.tsx` e os dois pontos novos abaixo. (2) **Menu de ações rápidas (⋮) na listagem pública** (`[username]/projetos`, novo `ProjectCardMenu`) — Abrir/Editar/Publicar-Despublicar/Arquivar/Excluir direto do card, sem entrar no dashboard; só aparece pra quem tem `canEdit`, fica fora do `<Link>` do card (irmão dele, posicionado por cima) pra nunca disparar a navegação ao abrir o menu. (3) **Excluir "de dentro do projeto"**: novo `DeleteProjectButton`, ícone de lixeira ao lado do botão de compartilhar na página pública do projeto (`[username]/projetos/[slug]`), só pra quem tem `canEdit` — era o pedido original, já resolvido também pelos outros dois pontos mas faltava especificamente aqui. (4) **Arquivar** (migration `062_highlights_archived.sql`, `highlights.archived_at TIMESTAMPTZ`) — alternativa não destrutiva a excluir, pra manter o histórico (transações/doações continuam ligadas ao projeto) sem ele aparecer na listagem nem na página pública; coluna separada de `status` de propósito (eixos independentes — projeto arquivado guarda o `status` que tinha pra, se restaurado, voltar exatamente como estava). Nova rota `/dashboard/projetos/arquivados` (`ArchivedProjectsList`) com Restaurar (zera `archived_at`) e Excluir definitivo; link "Ver projetos arquivados (N)" aparece em `/dashboard/projetos` só quando há pelo menos um. Consultas públicas (`[username]/projetos` e `[slug]` pra quem não é dono) ganharam `.is('archived_at', null)`. (5) **Skeleton de carregamento da página do projeto**: usuário reportou "sensação de travamento muito grande" ao abrir um projeto pela listagem — a rota `[username]/projetos/[slug]` não tinha `loading.tsx` próprio, herdava o esqueleto genérico de perfil (avatar+grid) de `[username]/loading.tsx`, bem diferente da forma real da página (capa grande + título + card financeiro). Novo `loading.tsx` dedicado, na mesma forma da página real, aparece imediatamente ao clicar. (6) **Editor de capa com zoom** — `HighlightForm` trocou o `CoverEditor` antigo (só arrastar, sem zoom) pelo `ImageCropEditor` (mesmo componente do composer de post e do wizard "novo projeto"), a pedido explícito do usuário ("faça do mesmo jeito que é pra inserir pra post... mudar o zoom, arrastar"); recorte assado (`bakeImage()`) em `1.91:1` fixo antes do upload, mesma proporção da capa em destaque. A ilha de edição inline de capa na página pública (`cover-title-edit-section.tsx`) continua no `CoverEditor` antigo — dívida conhecida, fora do escopo desta rodada. `tsc` limpo em toda a leva; não testado com clique numa sessão autenticada real (mesma limitação já registrada nas entradas anteriores — geração de sessão administrativa bloqueada pelo classificador de segurança do ambiente), exceto o menu/exclusão que o próprio usuário confirmou visualmente durante a sessão.
- **2026-08-30** — Editor de História ganhou suporte a tradução PT/EN/ES (migration `061_history_blocks_translations.sql`) — único editor de conteúdo do app sem isso até aqui, reportado pelo usuário direto na tela ("em nenhum lugar tem um botão para traduzir"). `LocaleContentTabs` reaproveitado para título/texto de Quem somos, Nosso chamado e Convite; itens da Linha do tempo ganharam tradução própria embutida em cada item (não array paralelo, pra sobreviver ao reorder por arrastar de horas antes) com UI compacta no padrão do `MilestonesEditor`. Lado público (`HistoryView`) passou a resolver o idioma certo via `resolveLocalizedText`, que ainda não era usado ali. Ato contínuo, a pedido do usuário ("traduza tudo desta página... de forma natural"), os 4 blocos reais do perfil "Família Silva" foram traduzidos pra EN/ES e gravados no banco. Dois ajustes de UX na mesma tela, mesmo dia: campo de texto longo trocou `field-sizing-content` (crescia sem limite pra caber tudo) por `field-sizing-fixed` + `resize-y` (altura inicial curta, redimensionável arrastando); e a tela, que estava colada à esquerda, ganhou `max-w-lg mx-auto` no wrapper certo (cabeçalho + formulário juntos). Ver "Editor de História" na seção 7.12 para o detalhe completo.
- **2026-08-30** — `PledgeForm` (oferta avulsa): seção "Forma de pagamento" subiu pra logo depois de "Valor enviado" (moeda), antes de "Seus dados" — a pedido do usuário, que reparou que o seletor de moeda (bandeirinha ao lado de "Valor enviado") ficava visualmente longe da seleção de método de pagamento mesmo as duas coisas sendo "totalmente conectadas" (moeda filtra/ordena os métodos disponíveis, `dropdownCurrencies`/`handleCurrencyChange`) — com "Seus dados" (nome/e-mail/telefone/mensagem) no meio, a rolagem entre as duas era longa, especialmente no mobile. Corrigido só trocando a ordem dos dois blocos JSX (nenhuma lógica mudou, `id="pledge-manual-form"`/`form="pledge-manual-form"` no rodapé continuam funcionando independente da posição visual). `RecurringPledgeForm` não precisou do mesmo ajuste — lá "Forma de pagamento" já vinha logo após o valor, sem "Seus dados" no meio (assinatura recorrente usa os dados da conta logada, não pede nome/e-mail de novo). Verificado com Playwright, sem erros de console.
- **2026-08-30** — Reordenar itens da "Linha do tempo" (Editor de História, `/dashboard/configuracoes/historia`) por arrastar, a pedido do usuário ("vai facilitar o trabalho") — antes só dava pra adicionar no fim/remover, sem reordenar (limitação deliberada da entrada de 2026-08-08, revertida aqui). Nova dependência `@dnd-kit/core`+`@dnd-kit/sortable`+`@dnd-kit/utilities` (nenhuma lib de drag-and-drop existia no projeto ainda — as outras listas reordenáveis do app, como `HighlightsList`/projetos, usam botões ▲▼ em vez de arrastar) — escolhida em vez de HTML5 drag-and-drop nativo especificamente por suporte real a toque via `PointerSensor` (o app é mobile-first). `TimelineItem` ganhou um `id` local (`crypto.randomUUID()`, só em memória — não persiste no JSONB salvo, que continua só `{year, text}`) pra servir de chave estável do `SortableContext`; novo componente `SortableTimelineItem` com alça de arrastar (`GripVertical`, `touch-none` — necessário pro `PointerSensor` capturar o gesto de toque em vez do scroll da página) à esquerda do ano. `KeyboardSensor` incluído de quebra para acessibilidade (setas movem o item focado). `tsc` limpo; não testado com clique numa sessão autenticada real nesta sessão (mesma limitação da entrada de exclusão de projeto abaixo — geração de sessão administrativa bloqueada pelo classificador de segurança do ambiente). De quebra, corrigida a linha da Stack técnica (seção 2) que ainda listava Stripe e Brevo como "planejado, não implementado" — os dois já estão implementados e em uso (Connect Express desde a seção 7.11, e-mails via Brevo no lembrete de recorrência e no acompanhamento de parceiro de oração) havia tempo; a nota nunca tinha sido atualizada. **Complemento, mesmo dia**: a pedido do usuário, `addTimelineItem` agora devolve o foco pro campo "Ano" (`newYearRef`, `useRef<HTMLInputElement>`) depois de inserir um item — antes o cursor ficava parado no campo "O que aconteceu", exigindo clicar de volta no "Ano" pra cadastrar o próximo item em sequência.
- **2026-08-30** — Excluir projeto ficou mais fácil de achar/entender, a pedido do usuário. Excluir já existia (`HighlightsList`, ícone de lixeira por linha em `/dashboard/projetos`), mas com confirmação genérica ("Excluir este destaque?", componente compartilhado com `/dashboard/destaques`) e nenhuma opção dentro da própria tela de edição — quem estivesse editando um projeto não tinha como excluí-lo sem voltar pra listagem e procurar o ícone entre os outros botões de ação. Ajustes: (1) `handleDelete` em `highlights-list.tsx` agora recebe o `Highlight` inteiro (não só o `id`) e mostra o título do projeto na confirmação, junto com uma frase sobre o efeito colateral real (publicações/ofertas/lançamentos vinculados continuam existindo, só perdem a associação com o projeto — confirmado direto nas migrations: `ON DELETE SET NULL` em `posts.project_id`/`financial_accounts.highlight_id`/`transactions.highlight_id`/`pledges.highlight_id`/`recurring_pledges.highlight_id`, `ON DELETE CASCADE` só em dados exclusivos do projeto como `milestones`/`project_gallery_images`/`project_budget_categories`/`project_members`). (2) Novo botão "Excluir projeto" (`Trash2`, `variant="ghost"` destrutivo, alinhado à esquerda com `mr-auto`) no rodapé de `HighlightForm`, ao lado de Cancelar/Salvar, só visível em modo edição (`highlight` presente) — mesmo padrão `window.confirm()` + `supabase.from('highlights').delete()` + toast já usado em todo o resto do app (posts, parceiros, categorias/lançamentos financeiros — não existe `AlertDialog`/modal de confirmação customizado neste código, só o padrão pesado de `/conta/excluir`, reservado pra exclusão de conta inteira). RLS (`highlights_owner_all`, `FOR ALL USING (is_profile_owner(profile_id))`) já cobria DELETE, sem mudança de política necessária. `tsc` limpo; não foi possível testar o clique numa sessão autenticada real nesta sessão (geração de link mágico administrativo via service role foi bloqueada pelo classificador de segurança do ambiente) — revisão de código + reuso exato do padrão já validado em outros pontos do app.
- **2026-08-30** — Parceiro de oração: correção de UX + e-mail automático de acompanhamento. Pedido do usuário: clicar em "Comprometa-se a orar regularmente" caía numa tela genérica "Como deseja se envolver?", como se o clique anterior não tivesse valido nada; e não havia nenhum retorno automático pro parceiro depois do compromisso. Investigado antes de implementar: o vínculo persistente já existia (`partners.type='prayer'`, CRM do missionário já mostrava esses parceiros com badge "Oração") — faltava (a) a UX não repetir a pergunta e (b) um canal de saída que alcançasse parceiro sem conta (maioria dos cadastros de oração é anônima). (a) `PartnershipForm` — título/CTA/placeholder da mensagem agora reagem ao `type`/`defaultType` (`title_prayer`/`cta_prayer`/`messagePlaceholder_prayer` etc., novas chaves nas 3 línguas), ver 7.3. (b) Novo cron diário `GET /api/cron/prayer-partner-updates` (migration `060_partner_update_emails.sql` → `partners.last_update_email_sent_at`/`update_emails_opt_in`), manda e-mail via Brevo ("obrigado por orar... acompanhe os desenvolvimentos de {nome}... obrigado pela parceria em oração") só quando há post/projeto novo desde o último envio, com link de descadastro — ver 7.3-tris (nova). De quebra, corrigida nota desatualizada na seção 7.10: o sistema de `notifications` in-app não era "só consumido sem produtor" como o doc dizia — os triggers de `014_notification_triggers.sql` já produzem notificação real desde aquela migration, só não tinham sido documentados certos. Verificado com Playwright (tela de oração mostra o título/CTA certos) e `tsc` limpo; cron em si não foi executado nesta sessão (depende do agendamento do Vercel em produção).
- **2026-08-30** — Estratificação do projeto "Gastos de Agosto — Família Silva" por categoria: 10 linhas em `project_budget_categories` (`category_type='other'` + `custom_label` com o texto/emoji original de cada categoria — nenhum dos tipos fixos do enum, pensados pra viagem/mudança, cobre despesa doméstica), `target_amount` = valor de cada categoria da planilha enviada pelo usuário, somando os mesmos R$ 4.218,79 da meta do projeto. Pedido explícito: "se tiver alguma coisa que está fora da estrutura, pode ajustar" — usado pra justificar `category_type='other'` uniforme em vez de forçar mapeamento pros tipos de viagem existentes.
- **2026-08-30** — Listagem pública de projetos (`[username]/projetos/page.tsx`) passou a ordenar por `created_at DESC` (mais novo primeiro) em vez de `status, order_index` — pedido do usuário. A separação visual "Em andamento"/"Concluídos" continua igual (feita em JS via `.filter(p => p.status === ...)` depois da query), então a mudança só afeta a ordem dentro de cada uma dessas duas seções.
- **2026-08-30** — Busca difusa/tolerante a acento no diretório público (`searchDirectory`, migration `059_fuzzy_directory_search.sql`) — ver seção 7.14 (detalhe completo) e linha `search-actions.ts` na seção 10. Motivado por bug real reportado pelo usuário: buscar "familia silva" em `/explorar` devolvia "Nada encontrado" para o perfil "Família Silva" (ILIKE sem tratamento de acento). Extensões `unaccent`/`pg_trgm` + índices GIN de trigrama; troca de `ILIKE` cru por duas funções SQL (`search_missionaries`/`search_highlights`) chamadas via `.rpc(...)`, que ranqueiam por similaridade e também retornam resultados "parecidos" (não só match exato) acima de um piso de confiança — pedido explícito do usuário, no padrão de busca de plataformas grandes.
- **2026-08-30** — Formulário de oferta avulsa (`PledgeForm`) ganhou um chip dinâmico "Cobrir o valor total"/"Cobrir o que falta" ao lado dos valores fixos (`AmountChips`), calculado a partir da meta do projeto (`highlights.goal_amount`/`current_amount`) ou, quando uma etapa específica do orçamento está selecionada (`BudgetCategorySelect`), da meta daquela etapa (`target_amount`/`raised_amount`) — pedido do usuário, pra dar ao parceiro a opção de cobrir o valor inteiro (ou o restante) de um clique só, tanto pro projeto geral quanto ao entrar via "Contribuir" numa etapa específica (`budget-breakdown.tsx` → `?category=ID`). Só aparece quando a moeda selecionada no formulário é a mesma do projeto (evita mostrar uma meta com a moeda errada). Também adicionada uma frase de incentivo fixa abaixo dos valores ("Nenhuma oferta é pequena demais. Seja um semeador...", nas 3 línguas) — pedido do usuário, pra reforçar que a oferta é sobre intencionalidade, não quantia. `get-partnership-data.ts` passou a buscar `goal_amount`/`current_amount` de `highlights` e repassar pelo `PartnershipWizard` só no caminho `choice === 'financial_once'` (doação geral não tem highlight). Verificado com Playwright: chip mostra o valor certo tanto pro projeto geral quanto por etapa, clique preenche o campo, sem erros de console.
- **2026-08-30** — Estabilidade de link ao renomear (seção 7.17): projeto parou de trocar `slug` a cada edição de título (só na criação); `profiles.username`/`highlights.slug` antigos passam a redirecionar pro atual em vez de 404 (`profile_username_history`/`highlight_slug_history`, escritas por trigger, migration `058_url_stability.sql`); reivindicar `@username` antigo de outra pessoa bloqueado (`checkUsernameAvailability`). Motivado por um 404 real reportado pelo usuário depois de renomear um projeto. Verificado direto contra o banco (rename + curl + revert), não só build.
- **2026-08-30** — Extensão da área de superadmin (seção 7.16): badge global "Área admin" (`AdminLinkBadge`, no `RootLayout`, visível em qualquer página, não só `/superadmin`), painel `/superadmin` com estatísticas do sistema (usuários por papel/gênero/visibilidade, pendências de moderação, atividade), lista `/superadmin/usuarios` com busca e Suspender/Reativar direto por conta, e `/superadmin/layout.tsx` centralizando o gate + nav de abas. Novo campo `profiles.gender` (opcional, pergunta no onboarding) — migration `057_admin_dashboard.sql`, com backfill manual dos 17 perfis de teste existentes por nome (pedido explícito do usuário). `profiles.verification_requested_at`/`account_status_changed_at` novos, pra "há quantos dias está pendente" não depender de `updated_at`.
- **2026-08-30** — Primeira infraestrutura de confiança/segurança do app (seção 7.16, nova): verificação manual de missionário (`profiles.verification_status`, gate em `becomeMissionary()`), moderação automática de texto de posts/comentários via Claude Haiku (`checkTextModeration`, fail-open), botão de denúncia (`reports`, nova tabela) em post/comentário/perfil com ocultação automática a partir de 2 denunciantes distintos — do item (`moderation_status`) e, escalando, da conta inteira do autor (`account_status`, decisão explícita do usuário: "se o usr receber 2+ denúncias a conta dele fica bloqueada, não somente o comentário/post") — e a primeira rota admin real do app, `/superadmin/moderacao` (pedido explícito do usuário), com filas de verificação pendente/conteúdo denunciado/contas denunciadas. Migration `056_moderation.sql`. `SUPERADMIN_EMAILS` ganhou `johnsonwel.assis@gmail.com`. Build e `tsc` limpos, gate server-side de `/superadmin/moderacao` confirmado (redirect pra `/dashboard`→`/login` sem sessão); fluxo completo de denúncia→fila→ação **não testado em navegador nesta sessão** (sem credencial de teste disponível) — recomendado validar manualmente antes de uso real.
- **2026-08-30** — Seletor de moeda do checkout (avulso e recorrente) refeito: novo `CurrencySelect` (`src/components/partners/currency-select.tsx`, sobre um `Popover` novo em `src/components/ui/popover.tsx`, Base UI) substitui o `<select>` nativo por um botão com bandeira + código, popover com busca (só aparece com mais de 6 moedas — não faz sentido pras 7 atuais, mas prepara terreno pra lista crescer) e lista com check no item ativo. Também corrigida a sincronização de moeda, que só funcionava num sentido — usuário reportou: "se a pessoa selecionar dólar, mas clicar em pix abaixo, a moeda deve mudar automaticamente pra real". Antes disso ser possível, o grid de métodos filtrava por moeda selecionada (`visibleOptions`), então um método de outra moeda nem aparecia pra poder ser clicado — contradição real com o pedido. Resolvido tirando esse filtro: o grid agora sempre mostra todos os métodos juntos (`allOptions`, Cartão + todos os manuais, de qualquer moeda), e a sincronia passa a valer nos dois sentidos — `handleCurrencyChange` (dropdown → método, já existia) e `handleOptionSelect`, novo (método → moeda: clicar num Pix/transferência de outra moeda muda a moeda selecionada pra dele; Cartão não força troca, aceita a moeda atual). `PaymentMethodCards` ganhou bandeirinha da moeda em cada card (`CURRENCY_FLAGS`, nova em `currency-mask.ts`), só quando o grid de fato mistura mais de uma moeda — evita ruído no caso comum de só uma moeda cadastrada. Chave de tradução `noMethodsForCurrency` (com `{currency}`) virou `noMethodsAvailable` (genérica, sem moeda — grid não fica mais vazio por causa de moeda, só se não houver método nenhum cadastrado). Verificado com Playwright: popover abre com busca, filtra corretamente, e o fluxo completo do pedido (selecionar USD → clicar Pix → moeda volta pra BRL) funciona ponta a ponta.
- **2026-08-30** — `RecurringPledgeForm` (assinatura mensal): Cartão via Stripe estava travado artificialmente na moeda padrão do projeto — usuário com Stripe conectado só via "BRL" no dropdown, sem chance de receber assinaturas em outra moeda. Não era limitação real: `checkout-recurring/route.ts` já monta a subscription com `price_data` dinâmico (mesmo padrão do `checkout-once`), então Stripe aceita qualquer moeda. Corrigido pra espelhar o `PledgeForm` avulso: Cartão isento do filtro por moeda (`o.method === 'stripe' || o.currency === selectedCurrency`), dropdown mostra a lista completa de `CURRENCIES` quando Stripe está conectado (em vez de só `paymentOptions` + moeda do projeto), e `handleStripeCheckout` manda `selectedCurrency` pro backend em vez de `projectCurrency` fixo. De quebra, achado e corrigido bug adjacente: `checkout-recurring/route.ts` recebia `budgetCategoryId` do formulário mas nunca desestruturava do body nem gravava em `recurring_pledges.budget_category_id` — categoria de orçamento escolhida numa assinatura via cartão era descartada silenciosamente.
- **2026-08-27** — Sugestão de localização do composer ganhou cascata de fallback Google Places → Mapbox → Photon (`src/lib/geocoding/`, nova seção 7.9-bis), a pedido do usuário depois de reparar que um mercado real perto dele não aparecia (confirmado: não estava cadastrado no OpenStreetMap, cobertura de comércio local no OSM é inconsistente por região). Pesquisei os providers reais antes de implementar e corrigi um número que eu tinha passado errado na conversa: a API de Geocoding "clássica" do Mapbox (100k grátis/mês) só devolve endereço/área administrativa, sem POI — não resolveria o problema; o produto certo é a Search Box API (`/forward`/`/reverse`, sem sessão), com cota grátis menor (50.000/mês, depois US$1,00/1.000). Google Places Nearby Search: 5.000/mês grátis (SKU "Pro", fieldMask mantido mínimo de propósito — campos como `rating`/`photos` reclassificam pro SKU "Enterprise" e derrubam a cota pra 1.000/mês), depois US$32/1.000. `getNearbyLocations` (GPS) usa os 3 tiers; `searchLocations` (busca digitada) usa só Mapbox → Photon, sem Google — o tier grátis-ilimitado do Autocomplete do Google exige "session tokens" que o composer não implementa (cada tecla é uma Server Action independente, sem Place Details), então usar Google ali provavelmente geraria cobrança à toa. `runCascade` pula um tier sem chave configurada ou com uso ≥90% da cota do mês (`geocoding_usage`, nova tabela — migration `054_geocoding_usage.sql`, contador `(provider, period)` sem cron, mês novo é só uma linha nova), cai pro próximo em caso de erro **ou resultado vazio** (decisão deliberada: o objetivo é achar o lugar, não só respeitar limite). Sem chave de Google/Mapbox configurada ainda (nenhuma das duas contas existe), então hoje o comportamento é idêntico a antes da cascata — só Photon. Testado nesta sessão com chaves falsas via script `tsx` (não commitado): confirmado que erro 401/403 real de cada API cai pro próximo tier sem erro visível, terminando no Photon com resultado, inclusive já rodando contra o Supabase real antes da migration `054` ter sido aplicada (`isOverGeocodingQuota` degrada pra "não sobre cota" quando a tabela ainda não existe, sem lançar). Seções 2, 7.9 e 7.9-bis (nova) atualizadas.
- **2026-08-27** — Sugestões de localização do composer de post passaram a priorizar lugares perto de onde o usuário está agora, estilo Instagram (a pedido do usuário: "deve haver um jeito do sistema entender a localização onde a pessoa está e já meio que deixar 'sugerido'... como o Instagram faz"). Nova Server Action `getNearbyLocations(lat, lon)` (`src/app/dashboard/publicacoes/actions.ts`) faz reverse geocoding no mesmo Photon já usado por `searchLocations` (`GET /reverse?lon=&lat=&limit=20`). Primeira versão só excluía `postcode`/endereço sem nome e ainda devolvia muita rua — o usuário pediu explicitamente estabelecimento de verdade ("ao invés de só sugerir nome de ruas, sugira locais próximos, ponto de estabelecimento"), então o filtro virou allow-list por `properties.osm_key` (`NEARBY_POI_KEYS`: `amenity`, `shop`, `office`, `leisure`, `tourism`, `craft`, `healthcare`, `historic` — cobre igreja, mercado/comércio, escola, hospital, restaurante, empresa etc.), excluindo de propósito `highway` (rua — sempre tem uma a ~0m de qualquer ponto, dominaria o resultado) e `place` (bairro/CEP/cidade, administrativo demais pra ser "o lugar" de um post). `limit=20` no fetch bruto porque a maioria dos 20 candidatos mais próximos costuma ser rua/endereço — sobra só um punhado de estabelecimento real depois do filtro, por isso o raio maior antes de cortar pros 5 primeiros. `StepDetails` pede a posição do browser (`navigator.geolocation.getCurrentPosition`, `enableHighAccuracy: false`, timeout 8s) assim que o passo monta; se o usuário negar a permissão ou o browser não suportar, o `catch`/callback de erro fica em silêncio (sem toast) e os chips caem só no histórico de `getLocationSuggestions`, como já era antes. As duas listas são combinadas (`nearbyLocations` primeiro, depois o histórico, deduplicado), com um rótulo pequeno ("Perto de você"/"Near you"/"Cerca de ti", chave `nearbyLocations` nas 3 línguas) só quando há resultado por GPS — o dropdown de busca ativa (`searchLocations`, enquanto o usuário digita) não muda. Verificado o parsing da resposta do Photon reverse com coordenadas reais (não deu pra testar `navigator.geolocation` num browser autenticado de verdade, já que o composer exige login como dono do perfil e não há credencial de teste disponível nesta sessão). Seção 7.9 atualizada.
- **2026-08-27** — Endereço exibido no post (`post.location`, ex. "Dourados, Mato Grosso do Sul, Brasil") virou um link clicável que abre o Google Maps numa aba nova, igual ao Instagram — a pedido do usuário. Como o app só guarda o endereço em texto livre (sem lat/lng nem place_id salvo — `searchLocations`/`getNearbyLocations` descartam as coordenadas que o Photon devolve, só usam o rótulo formatado), o link é uma busca por texto (`https://www.google.com/maps/search/?api=1&query=<location>`), não um pin exato. Implementado em `shared/post-card.tsx`: antes o header inteiro (avatar+nome+username+localização+hora) era um único `<Link>` pro perfil, o que impedia aninhar um segundo link só na localização (HTML não permite `<a>` dentro de `<a>`); quebrado em elementos irmãos (avatar, nome, `@username` como `Link`s separados pro perfil; localização como `<a target="_blank">` própria) mantendo o mesmo layout visual. `PostDetailViewer` não precisou de mudança (só reaproveita `PostCard`). Verificado com Playwright numa sessão real: clique na localização abre popup pro Maps com a query certa (URL-encoded), clique no username continua indo pro perfil, sem erros de console. Note-se que isso não contradiz a decisão de 2026-07-22 (abaixo) de tirar `target="_blank"` dos links de perfil/projeto do feed — aquela reversão foi sobre navegação *interna* (parecia estranho sair do app pra ver um perfil), enquanto abrir mapa é inerentemente externo, igual o próprio Instagram faz. Seção 7.1-bis (inventário de componentes) atualizada.
- **2026-08-27** — **Seletor de idioma (bandeiras 🇧🇷🇺🇸🇪🇸/"PT ▾") não tinha efeito nenhum para usuário logado** (reportado pelo usuário com screenshot do dropdown compacto em `/username`, aba Perfil). Causa raiz: `LanguageSwitcher.handleSwitch` só gravava o cookie `NEXT_LOCALE`; `src/i18n/request.ts` sempre prioriza `profiles.locale` (banco) por cima do cookie quando há sessão (seção 5.6) — então clicar numa bandeira setava o cookie e rodava `router.refresh()`, mas o request seguinte lia o locale de volta do banco, inalterado, como se o clique não tivesse feito nada. O código tinha até um comentário justificando isso como intencional ("não deve mudar o idioma da conta/dashboard"), o que contradizia a própria linha logo abaixo nesta seção (`e se houver usuário logado também atualiza profiles.locale`) — doc e código haviam divergido; o usuário confirmou que o comportamento esperado é mesmo o documentado (idioma escolhido vale pro usuário em qualquer lugar do sistema, dashboard incluso). Corrigido em `handleSwitch`: se `supabase.auth.getUser()` (client-side) retornar um usuário, também grava `profiles.locale` via `.eq('user_id', user.id)` antes do `router.refresh()` — mesma coluna que `request.ts` lê de volta, então o efeito é imediato e não depende de resolver o perfil ativo (`getActiveProfile()`/cookie de troca de conta gerenciada, seção 3.1); erro de gravação mostra toast (reaproveita `AccountForm.errorSaveLocale`, já traduzido nas 3 línguas). `handleSwitch` é compartilhado pelos 3 pontos de uso do componente (`SiteNav`, `profile-tabs.tsx`, `checkout-header.tsx`) — corrigido uma vez só, sem tocar nenhum dos 3 call sites. `AccountForm` (Configurações → Conta) não foi tocado, já fazia a gravação certa (por `profile.id`, resolvido server-side via `getActiveProfile()` — cobre o caso de gestor de conta vinculada, que `LanguageSwitcher` não precisa cobrir por não ter esse contexto disponível).

- **2026-08-27** — Campo de localização do composer de post ganhou autocomplete mundo afora estilo Instagram (a pedido do usuário: "deve aparecer as localizações do mundo todo, igualzinho como funciona no Instagram") — antes só sugeria locais que o próprio missionário já tinha usado (`getLocationSuggestions`, mantido como fallback quando o campo está vazio). Nova Server Action `searchLocations` (`src/app/dashboard/publicacoes/actions.ts`) consulta o Photon (`photon.komoot.io`, geocoder público sobre dados do OpenStreetMap) — decisão consciente do usuário entre 3 opções apresentadas (Photon gratuito sem chave vs. Google Places vs. Mapbox, os dois últimos exigindo cadastro/cobrança): sem chave de API nem custo, adequado ao porte do app. Busca com debounce de 300ms a partir de 2 caracteres (`StepDetails`, mesmo padrão de debounce do `TagPeoplePicker`), resultado num dropdown estilo Instagram (ícone de pin + nome formatado "Local, Cidade, País") que substitui os chips de "locais recentes" enquanto o usuário digita. **Bug na primeira tentativa**: a chamada incluía `&lang=pt`, mas o Photon só aceita `default`/`de`/`en`/`fr` — devolvia HTTP 400 silenciosamente (erro engolido, resultado virava lista vazia, dropdown nunca aparecia). Corrigido removendo o parâmetro — o modo "default" já retorna o nome de cada lugar no idioma local (ex. "São Paulo, Brasil"), que é o comportamento correto pra um app global de qualquer forma. Confirmado com sessão real + Playwright, incluindo teste do encoding UTF-8 dos acentos e do clique numa sugestão preenchendo o campo. Seção 7.9 atualizada.

- **2026-08-27** — Efeito colateral do painel lateral (entrada seguinte): `StepMediaSelect` era a única etapa do wizard sem seu próprio teto de largura (`StepTypePicker`/`StepDetails` já tinham `max-w-md`/`max-w-xl mx-auto`) — com o painel mais largo (`lg:w-[820px]`), o quadro do `ImageCropEditor` (`w-full`, sem cap) passou a herdar quase toda essa largura, e como ele deriva a altura da largura via `aspect-ratio` (`4:5` = altura 1.25× a largura), o quadro ficou desproporcionalmente enorme (chegava a ~985px de altura numa foto retrato, dominando o painel inteiro) — reportado pelo usuário com screenshot. Corrigido dando o mesmo `max-w-md mx-auto` que as outras etapas já tinham; confirmado com Playwright que o quadro volta a ~448×560px (proporção `4:5` normal) mesmo num painel de 820px/viewport de 1920px, sem afetar o mobile (painel já é mais estreito que 448px lá). `StepAdjust` não foi tocado — continua sem teto de propósito, porque precisa da largura toda do painel pro layout lado a lado (`md:flex-row`).

- **2026-08-27** — `WizardModal` (shell do composer de post e de projeto) deixou de ser tela cheia (`fixed inset-0`) e virou um painel ancorado na borda esquerda, altura cheia — a pedido do usuário, que viu por acidente essa aparência durante o bug de largura descrito abaixo (item 3 da entrada seguinte) e achou "mais legal" que tanto a versão fullscreen quanto uma primeira tentativa de caixa centralizada (que também foi feita e descartada nesta mesma sessão, a pedido dele: "esse pode ser aquele modal que abre na lateral igual tava"). No mobile continua ocupando a tela toda (sem espaço pra mostrar algo "ao lado"); a partir de `sm:` vira um painel de largura fixa (`sm:w-[600px] lg:w-[820px]`, mais largo que os outros modais do app de propósito — `StepAdjust` usa layout lado a lado `md:flex-row`, que é breakpoint de *viewport*, não do painel, e ficaria espremido num painel estreito) encostado à esquerda (`inset-y-0 left-0`, sem `top`/`left`/`translate` de centralização), cantos arredondados só do lado direito exposto (`sm:rounded-r-2xl`), resto do dashboard visível e escurecido pelo overlay à direita. Mesma armadilha do `sm:max-w-sm` da entrada anterior teve que ser reaplicada aqui com a largura nova (`sm:max-w-none` + `sm:w-[...]` explícito, não só `w-[...]`). Confirmado com Playwright em 430px/1280px: painel flush nas 3 bordas, StepAdjust lado a lado a partir de `sm:`, clique fora aciona a confirmação de descarte existente (`DiscardConfirmDialog`) normalmente. `PartnershipModal`/`ProjectStoryDialog` (caixa centralizada) não foram tocados — a preferência por painel lateral foi só pro composer, não virou padrão geral do app.

- **2026-08-27** — **Preview do composer de post não batia com o corte** (reportado pelo usuário: "deixo com fundo verde no corte, mas ao avançar some o fundo e a imagem aparece ampliada preenchendo tudo"). Reproduzido com sessão autenticada real (magic link administrativo + `verifyOtp`, sem senha) e upload de arquivo de verdade via Playwright em cada etapa. Três causas, todas corrigidas:
  1. `StepAdjust` (etapa "Ajustar") e `TagPeoplePicker` (foto na etapa "Detalhes") reimplementavam o preview com `object-cover` + `objectPosition` + `scale(zoom)` solto, em vez da técnica do `ImageCropEditor` (`object-contain` + `object-position` fixo em `50% 50%` + `translate(x-50%,y-50%) scale(zoom)` — a mesma fórmula que `bake-image.ts` usa pro resultado salvo). Mesmo `position`/`zoom` produzia enquadramentos diferentes em cada etapa. Corrigido replicando a fórmula exata nas duas.
  2. A sobra (quando a imagem não preenche o quadro) não tinha a cor de fundo em nenhuma das duas — ficava preta (`TagPeoplePicker`, hardcoded `bg-black`) ou cinza (`StepAdjust`, sem fundo próprio, aparecia o `bg-muted` do container). Primeira tentativa: cada uma calculava sua própria cor via `onLoad` da imagem — não funcionou (usuário reportou fundo preto mesmo depois da correção 1) porque `onLoad` não dispara de forma confiável quando a blob URL já está em cache do navegador (a mesma imagem já tinha sido montada na etapa de corte). Corrigido calculando a cor média **uma vez só**, em `StepMediaSelect.handleFiles` logo após `createMediaDraft` (nova `averageColorFromUrl()` em `bake-image.ts`), guardada em `MediaDraft.bgColor` (campo novo) e lida diretamente pelas duas etapas — sem depender de evento nenhum.
  3. Bug separado e pré-existente, que só ficou visível ao corrigir 1: `WizardModal` (shell fullscreen do composer inteiro) não sobrescrevia `sm:max-w-sm` (classe embutida no `DialogContent` base, `ui/dialog.tsx`, que só vale a partir de 640px) — em qualquer tela ≥640px o modal "tela cheia" ficava preso a 384px de largura (`max-w-none` sem prefixo de breakpoint não cancela uma variante `sm:` do mesmo grupo). Com `object-cover` isso passava despercebido; com `object-contain` a imagem aparecia pequena dentro de uma caixa grande vazia. `EditProfileDialog` já tinha a correção certa (`max-w-none sm:max-w-none`) — só faltava aplicar em `WizardModal`.

  Corrigido e revertido uma vez no meio do caminho (usuário pediu rollback ao ver o resultado intermediário só com a correção 2, sem a 3 — a imagem ficava pequena numa caixa vazia, pior que antes); reaplicado depois com as três causas resolvidas juntas e validado com Playwright nas 3 etapas × 2 larguras de viewport (430px/1280px), screenshot a screenshot. Seção 11 (subseção "Editor de recorte de mídia", novo parágrafo) e árvore de componentes atualizadas.

- **2026-08-26** — Botões "Seja Parceiro" e "Enviar Oração" do perfil público passaram a abrir `/parceria`/`/oracao` em modal em vez de navegar pra uma página nova, a pedido do usuário ("para que o parceiro não tenha a impressão de sair da página do missionário"). Implementado com intercepting + parallel routes do Next.js (`[username]/@modal/(.)parceria/page.tsx`, `[username]/@modal/(.)oracao/page.tsx` + slot `@modal` em `layout.tsx`), padrão já documentado pelo próprio framework pra esse exato caso de uso — não uma solução ad hoc. A busca de dados de `parceria/page.tsx` foi extraída pra `src/lib/partners/get-partnership-data.ts`, compartilhada entre a página cheia (acesso direto/deep link, inclusive o retorno do Stripe Checkout e o e-mail de lembrete de recorrência) e o novo `PartnershipModal`; `oracao` não precisou de extração (só um `getProfile`). `ProfileCTA` não mudou nada em nenhum dos dois (`<Link>` comum) — a interceptação acontece só pra navegação client-side de dentro do app; fechar reaproveita `router.back()` (`PartnershipModal` via o `BackButton` interno do wizard, que já preferia `router.back()`; `PrayerModal` com um `X` próprio, já que `/oracao` nunca teve cabeçalho fixo interno pra reaproveitar). `PartnershipWizard`/`PrayerRequestForm` tiveram zero mudança de lógica interna (só a `Props` do wizard virou `export`ada, `PartnershipWizardProps`, pra ser reutilizada pelos dois pontos de entrada). **Correção no mesmo dia**: a primeira versão do `PartnershipModal` era tela cheia (`fixed inset-0`, mesmo padrão do `WizardModal` de post/projeto) — usuário pediu "no meio da tela igual os outros" (o `ProjectStoryDialog` da história). Trocado pro mesmo padrão de caixa centralizada/cantos arredondados (`max-w`/`max-h` responsivos por cima da centralização padrão do `DialogContent`, sem tocar `top`/`left`/`translate` — mesma lição do Changelog do `ProjectStoryDialog`); cabeçalho/rodapé fixos internos do wizard continuam funcionando porque o `transform` que o `DialogContent` já aplica vira containing block dos descendentes `fixed`, então eles se ancoram e são recortados pela caixa do modal em vez do viewport inteiro — confirmado com screenshot real (Playwright) em mobile e desktop, incluindo a sub-tela de checkout (header+footer fixos) e o fechamento por clique fora. Seção 7.3-bis (nova), nota de modal em 7.6 (nova) e árvore de rotas atualizadas.
- **2026-08-26** — `PostDetailViewer`/`PostCard`: legenda garantida sempre visível no modal, sem voltar ao bug do `max-h-[50vh]` (entrada abaixo, mesmo dia). Pedido do usuário: "calcule essa proporção do modal para que sempre apareça a descrição". Primeira tentativa foi CSS puro (flex column com a mídia em `flex-1 min-h-0`) — descartada depois de reproduzir isolado (HTML mínimo, fora do app) que `flex-grow` não distribui espaço nenhum dentro de um container com `max-height` mas **sem altura definida** (o container só encolhe pro tamanho do conteúdo; não há "espaço sobrando" pro item flex-1 herdar, então ele fica em 0). Implementado em vez disso um cálculo real em JS (`PostDetailViewer`): mede o card sem teto (tamanho natural, via `requestAnimationFrame` depois de resetar), e só se ele estourar o orçamento de 85vh do modal, aplica um `mediaMaxHeightPx` (número, recalculado a cada troca de post/resize) como `max-height` em pixel direto no elemento com `aspect-ratio` (`MediaFrame`, `post-card.tsx`) — sem ambiguidade de container-auto-height, o navegador encolhe largura e altura **juntas** a partir desse teto (nunca corta/distorce a proporção) e `mx-auto` centraliza o resultado quando fica mais estreito que o card. Cabeçalho/ações/legenda nunca encolhem, só a mídia. Testado com Playwright em 3 alturas de viewport (900/640/560px): proporção 4:5 exata (`0.800`) preservada em todos, legenda sempre dentro dos limites do dialog, e testado também com post carrossel (3 fotos). `mediaMaxHeightClass` (a prop antiga baseada em classe CSS fixa, entrada abaixo) foi substituída por `mediaMaxHeightPx`/`mediaRef` (número calculado + ref pra medição). Seção 7.9 atualizada.
- **2026-08-26** — `PostDetailViewer` (modal de post na página pública, aberto ao clicar numa publicação no grid do perfil) deixou de limitar a mídia a `max-h-[50vh]` — usuário reparou que o modal não refletia de verdade a proporção escolhida no upload ("a visualização da imagem deve ficar exatamente como o usuário visualizou na hora de subir a foto"). Causa raiz: um elemento com `aspect-ratio` (a classe `aspect-[4/5]` etc. de `post.media_aspect_ratio`) e `max-height`, mas **sem largura explícita**, faz o navegador calcular a largura a partir da altura travada em vez de preencher o container — medido com Playwright: a caixa da mídia ficava com 360px de largura numa tela de 420px (webp devia ocupar os 420px inteiros). Removido o limite (`mediaMaxHeightClass`, prop agora sem uso, removida de `PostCard`/`PostMedia`/`PostDetailViewer`) — a mídia volta a ocupar 100% da largura do modal na proporção exata do post (1:1/4:5/1.91:1, a mesma lógica do feed normal em `PostMedia`), igual ao que o `ImageCropEditor` mostrou no momento do upload. O corte da legenda/curtir/comentar em telas baixas (motivo original do limite) continua coberto pelo `overflow-y-auto` que o `DialogContent` já tinha — testado num viewport de celular pequeno (390×640): a foto ocupa a tela e rolar revela as ações normalmente, mesmo padrão do Instagram numa foto alta. Seção 7.9 atualizada.

> Adicione uma entrada aqui (mais recente no topo) toda vez que este arquivo for atualizado por causa de uma mudança real no sistema. Formato: `AAAA-MM-DD — o que mudou no sistema — o que foi atualizado neste doc`.

- **2026-08-28** — `PledgeForm`: (1) tela de sucesso agora abre citando o que foi contribuído ("Você acaba de contribuir com o projeto X, de Y!") antes da frase de status, num único parágrafo mesclado; (2) novo campo opcional "WhatsApp/Telefone" em `pledges.reporter_phone` (migration 053), exibido pro missionário em `pledge-review-card.tsx` como link `wa.me`; (3) quem se identifica (nome/e-mail) mas contribui sem estar logado agora recebe e-mail de confirmação via Brevo (`/api/pledges/[id]/notify-guest`) — só existe notificação in-app hoje (trigger `trg_fn_notify_pledge_confirmed` grava em `notifications`, que exige `reporter_user_id`), então guest identificado não tinha nenhum retorno. O id da pledge é gerado no client (`crypto.randomUUID()`) em vez de usar `.select().single()` no insert, porque a policy de SELECT de `pledges` (`pledges_reporter_read`, `auth.uid() = reporter_user_id`) bloquearia o RETURNING pra um guest sem sessão — geraria falso erro mesmo com insert bem-sucedido. A rota de notificação não confia em nada do corpo da requisição: busca os dados reais da pledge pelo id (service role) e só envia se ela for identificada, sem `reporter_user_id` e com e-mail salvo, evitando virar relay de e-mail arbitrário.
- **2026-08-28** — Confirmado que `pledges.message` (campo "Mensagem para o missionário") já chegava corretamente pro lado do missionário — aparece em Financeiro > Conciliação, dentro de cada card de oferta pendente ([pledge-review-card.tsx](src/components/financial/pledge-review-card.tsx)). Não era um bug, só não documentado.
- **2026-08-28** — Tela de sucesso de `PledgeForm` (doação avulsa manual) deixou de prometer notificação genérica pra todo mundo — usuário percebeu que quem contribui anônimo, sem conta, nunca é avisado (`reporter_user_id`/`reporter_email` ficam null). Agora 3 variantes de `doneDescription` conforme o caso real: logado (`doneDescriptionNotified`, será avisado por notificação in-app já que `reporter_user_id` fica setado independente do anonimato pro missionário), guest identificado (`doneDescriptionGuestNamed`, avisa que não dá pra notificar sem conta), e anônimo/guest (`doneDescriptionAnonymous`, sem prometer aviso). `doneAsLoggedIn` capturado no momento do submit (`!!user` do Supabase Auth). `RecurringPledgeForm` não precisou de mudança — parceria recorrente sempre exige login, mensagem genérica já era verdadeira.
- **2026-08-28** — Migration `052_fix_missing_columns_028_043.sql`: causa raiz real do "Erro ao registrar oferta" era `pledges.budget_category_id` (migration 043) ausente no banco remoto — só foi possível confirmar depois de adicionar `console.error` nos catches de insert em `pledge-form.tsx`/`recurring-pledge-form.tsx` (o toast genérico escondia o erro real do Supabase). Escrito script (`curl` + REST API com a `service_role` key) que testa programaticamente toda coluna de toda migration 001-051 e toda tabela `CREATE TABLE` contra o schema cache real do PostgREST — achou também migration 028 (fatura de cartão de crédito) inteira não aplicada. Ambas reaplicadas de forma idempotente na 052. Também achado (mas não corrigido, tabela morta sem uso no código): `post_deliveries` (migration 005) nunca foi criada no remoto. Recomendação registrada: rodar esse mesmo script de verificação depois de qualquer nova migration até o banco remoto ser confirmado 100% sincronizado com o histórico local.
- **2026-08-27** — Migration `051_fix_missing_content_columns.sql`: banco remoto estava com migrations 047-049 não aplicadas (mesmo problema da 050/pledges) — erro real do usuário ao salvar capa/título: "Could not find the 'letter_translations' column of 'highlights'". Migration reaplica as colunas/constraints de 047-049 de forma idempotente (`IF NOT EXISTS`) + `NOTIFY pgrst, 'reload schema'` pra forçar o PostgREST a recarregar o cache de schema, e precisa ser rodada manualmente no SQL Editor do Supabase Dashboard (sem acesso a `psql` direto neste ambiente — mesma limitação documentada na 050).
- **2026-08-27** — Dropdown de moeda do checkout passou a refletir o que foi cadastrado em Configurações > Pagamentos, em vez da lista fixa de 7 moedas — pedido do usuário ("amarrar as duas coisas"). Se só existe um método em BRL, só BRL aparece pra escolher (com Cartão conectado, a lista completa continua disponível, já que ele aceita qualquer uma). Usuário citou iene como exemplo de "qualquer moeda" — documentado por que isso não é suportado hoje (moedas sem casas decimais quebrariam o cálculo de centavos usado em todo o sistema) e fica registrado como próximo passo, não implementado nesta rodada. Seção 7.11 (item 3-nonies) atualizada.
- **2026-08-27** — Grid de método de pagamento (checkout avulso e recorrente) agora filtra pela moeda escolhida — pedido do usuário: uma chave Pix em BRL não deveria aparecer se o doador quer mandar USD. Seletor de moeda virou sempre visível/editável (antes só aparecia condicionalmente) e passou a decidir quais métodos mostrar, não o contrário. Cartão via Stripe é isento do filtro na doação avulsa (aceita qualquer moeda), mas entra no filtro na recorrência (API só aceita a moeda fixa do projeto). Também encurtado "Transferência bancária" pra "Transf. bancária" no mini-card, que estava cortando o texto. Seção 7.11 (item 3-nonies) atualizada.
- **2026-08-27** — Mini-card de método de pagamento com mais de uma chave do mesmo tipo (ex. 2 chaves Pix) ganhou o primeiro nome do rótulo customizado como complemento ("Pix Silas") — sugestão do usuário, refinada pra só aplicar quando o rótulo é mesmo customizado (evita "Pix Pix" no caso comum de uma única chave sem rótulo próprio). Seção 7.11 (item 3-sexies) atualizada.
- **2026-08-26** — Terceira rodada do bug de "voltar" em `/parceria`: usuário reparou que vir da página do projeto direto pro formulário (pulando a lista de escolha) e clicar em "<" ainda caía na lista, não na página do projeto de onde veio de verdade. Causa raiz: `choice` era `useState`, e "voltar" sempre resetava esse state pra lista, ignorando a navegação real do usuário. Reescrito pra `choice` vir direto de `useSearchParams()` (a URL manda) com navegação por `router.push` (histórico real por passo), e os botões de voltar trocaram callback fixo por `backHref` (usa a lógica já existente do `BackButton`: `router.back()` de verdade quando há histórico in-app, `href` só como rede de segurança). Validado com testes reais de dois cenários (link direto da página do projeto vs. navegação pela lista) — confirmado funcionando, com uma nota sobre latência normal de navegação assíncrona do Next.js em dev que inicialmente parecia flakiness. Seção 7.11 (item 3-octies) atualizada.
- **2026-08-26** — Segunda metade do bug de voltar do Stripe: `success_url`/`cancel_url` das sessões de checkout (`checkout-once/route.ts`, `checkout-recurring/route.ts`), montadas no servidor, também não tinham `choice=` — usuário reparou passando o mouse sobre o link de cancelar de dentro da própria página da Stripe. Corrigido nas duas rotas. Validado com teste end-to-end real (Playwright chega em `checkout.stripe.com` de verdade e confere o link). Seção 7.11 (item 3-septies) atualizada.
- **2026-08-26** — Corrigido bug reportado pelo usuário: voltar da tela do Stripe Checkout caía na lista inicial de escolha de `/parceria` em vez da tela de doação em que ele estava. Causa: o passo do wizard (`choice`) só vivia em estado React, nunca sincronizado com a URL — `PartnershipWizard` ganhou um `useEffect` que reflete `choice` em `?choice=` via `router.replace`. Validado com teste real de navegação (Playwright simulando ida a um domínio externo + botão voltar do navegador). Seção 7.11 (item 3-septies) atualizada.
- **2026-08-26** — **Bug real do zoom no composer de post corrigido** (o passo adaptativo abaixo, feito antes disso, era só um sintoma tratado, não a causa): o zoom do `ImageCropEditor` estava efetivamente travado em `posts` — clicar em "+" nunca movia a imagem, em nenhuma foto, não só em flyers estreitos. Reproduzido com uma sessão real (magic link administrativo + `verifyOtp`, sem senha) e upload de arquivo de verdade via Playwright (meu teste inicial, com `useState` isolado, não pegava o bug — só reproduziu com o fluxo real de `StepMediaSelect`). Causa raiz: `ImageCropEditor.setZoom()` chama `onZoomChange()` e, na sequência síncrona, `onPositionChange()` (pra reenquadrar a posição junto). Em `step-media-select.tsx`, `updateActive()` calculava o novo array com `mediaFiles.map(...)` a partir do `mediaFiles` "fechado" na render atual — as duas chamadas synchronous partiam do mesmo array desatualizado, e a segunda (`position`) sempre sobrescrevia o zoom que a primeira tinha acabado de setar, revertendo pro valor antigo. `AvatarCropDialog` e `step-cover-title.tsx` (capa de projeto) nunca tiveram esse problema porque já usavam a forma funcional do `setState` (`setMedia(prev => ...)`) pra cada campo separado. Corrigido trocando `onMediaChange(mediaFiles.map(...))` por `onMediaChange(prev => prev.map(...))` em `updateActive`/`handleFiles`/`removeMedia` (tipo de `onMediaChange` em `StepMediaSelect` ampliado pra aceitar updater funcional, casando com `Dispatch<SetStateAction<MediaDraft[]>>` que `composer.setMediaFiles` já é). Verificado com Playwright que o zoom agora avança de fato a cada clique (1 → 1.24 → 1.48 → ... até cobrir o quadro). De quebra (mesmo commit), passo do zoom no `ImageCropEditor` virou proporcional a `scaleRatio` (`zoomStep = max(0.1, scaleRatio*0.15)`, `wheelStep = max(0.08, scaleRatio*0.1)`) em vez de fixo em `0.1`/`0.08` — numa foto bem estreita/alta tipo flyer, cobrir o quadro exige zoom até `scaleRatio` inteiro (ex.: ~2.4x pra 1:3 num recorte 4:5), o que com passo fixo levaria ~24 cliques mesmo já com o bug acima corrigido. Cheguei a prototipar um slider contínuo (`<input type="range">`) como alternativa a isso, mas revertido: o Changelog já registra (entrada de 2026-08-08 abaixo) que um slider fino foi removido antes por ser "difícil de acertar no celular", feedback direto do usuário — reintroduzir contrariaria essa decisão. De quebra também, confirmado (sem mudança de código) que posts já saem em WebP automaticamente: `bakeImage()` gera um JPEG intermediário em canvas, mas `compressImage()` (chamado logo em seguida em `use-post-composer.ts`) força a saída em `image/webp` antes do upload. Seção 11 (subseção "Editor de recorte de mídia") atualizada.
- **2026-08-26** — Padronização de proporção de imagem em posts, igual ao Instagram, motivado por bug real (foto do visualizador "cortando" ao tentar caber junto com curtir/comentar/legenda em telas baixas — a causa raiz era a opção "Original" permitir proporção arbitrária, que o visualizador não conseguia acomodar direito). (1) Removida "Original" das opções do seletor de recorte de posts (`ASPECT_OPTIONS` em `image-crop-editor.tsx`) — agora só Quadrado/Retrato(4:5)/Paisagem, nunca proporção livre; avatar e capa de perfil não usam esse seletor (proporção fixa própria), não afetados. (2) A proporção de paisagem trocou de `16:9` (1.78:1, aproximado) pra `1.91:1` exato — a mesma que o Instagram usa de verdade —, em `MediaAspectRatio` (`types/database.ts`), nos 3 mapeamentos de classe CSS (`post-card.tsx`, `media-editor/types.ts`, `bake-image.ts`), no default de capa de projeto (`use-project-composer.ts`) e na migration `049_media_aspect_ratio_191.sql` (troca a CHECK constraint de `posts.media_aspect_ratio`; nenhum post em produção usava `16:9`, sem necessidade de backfill). Chave de tradução do seletor precisou de uma função helper (`aspectLabelKey`) pra sanitizar `.` além de `:` (`"1.91:1"` → `aspect_1_91_1`), já que next-intl trata `.` como separador de aninhamento nas chaves de mensagem. (3) O único post em produção que ainda estava com `media_aspect_ratio='original'` (imagem 890×1582, ~9:16, bem mais alta que qualquer proporção padrão) foi migrado manualmente pra `4:5` — a opção que menos corta dentre as 3 disponíveis. Seções 3.1 e 7.9 (item `media`) atualizadas.
- **2026-08-26** — "História por trás do projeto" (página pública de projeto) virou modal em vez de seção sempre visível — a pedido do usuário, a partir de outra iteração do mockup do Stitch. Novo `ProjectStoryDialog` (mesmo padrão do `EditProfileDialog`); `ScrollToSectionLink` removido (ficou sem uso). Avaliei reorganizar o resto da página, mas a estrutura já seguia boas práticas de página de doação (identidade → contexto → pedido financeiro → alternativas → prova social) — mantida como está. Seção 7.7-quinquies (nova) atualizada.
- **2026-08-26** — Seletor de método de pagamento do checkout (`PledgeForm`/`RecurringPledgeForm`) virou um grid único de mini-cards (ícone + tipo), com Cartão (Stripe) como mais uma opção do mesmo grid em vez de aba separada — a pedido do usuário, de novo puxando de uma iteração do mockup do Stitch, buscando deixar "a pessoa sem dúvida nenhuma do que ela está fazendo". Selecionar um card revela na hora os detalhes daquele método (chave Pix, dados bancários, ou o botão de checkout do Stripe) no mesmo lugar. `PaymentModeTabs` (da rodada anterior) ficou sem uso, removido. Também auditei o resto do app pelo mesmo bug de cabeçalho preso num bloco centralizado (pedido do usuário) — nenhuma outra instância encontrada, ver seção 7.11 pra detalhe de por que cada tela candidata (auth, onboarding, /oracao) não se aplica. Seção 7.11 (itens 3-quinquies/3-sexies) atualizada.
- **2026-08-26** — Cabeçalho fixo estendido de `PledgeForm`/`RecurringPledgeForm` pra toda a jornada de `/parceria` (lista inicial, oração/embaixador/voluntário, estados terminais) — o usuário reparou que essas outras telas ainda centralizavam tudo verticalmente (cabeçalho incluso), então ele "descia" junto com o conteúdo em telas altas em vez de ficar fixo. Pesquisei o padrão de mercado (Material Design "top app bar") antes de aplicar — confirma cabeçalho fixo + conteúdo começando logo abaixo, nunca centralizado no meio. Extraído `CheckoutHeader` compartilhado. Seção 7.11 (item 3-quinquies) atualizada.
- **2026-08-25** — Header e footer fixos no checkout (`PledgeForm`/`RecurringPledgeForm`), a pedido do usuário citando o mockup do Stitch de novo — pesquisei prática de mercado (CTA fixo no rodapé é padrão comprovado, ~18% de lift de conversão em checkout mobile) antes de implementar. Ao fazer isso, descobri e corrigi uma colisão com a barra fixa global do perfil (`ProfileTabs`) que já existia em `/parceria` — agora essa rota é 100% autônoma da chrome do perfil, com sua própria barra de voltar+idioma em todas as telas do wizard. De quebra, conectei `DonationHero` ao `cover_position` do projeto (mesmo enquadramento que o editor de capa já grava), a pedido do usuário — ele reparou que a foto podia cortar mal dependendo do card. Ver detalhe completo na seção 7.11 (itens 3-ter/3-quater) e 7.12.
- **2026-08-25** — Redesenho do checkout de doação (`PledgeForm`/`RecurringPledgeForm`), a pedido do usuário a partir de um mockup próprio feito no Google Stitch: gostou da estrutura (resumo sempre visível do valor+projeto, abas de método de pagamento) e pediu pra aplicar o máximo possível no app real. Extraí a estrutura mantendo os tokens de cor da marca (não o terracota/Material do mockup) e sem replicar os campos de cartão em texto puro (risco de PCI — o pagamento com cartão continua sendo o Stripe Checkout hospedado). Depois, o usuário pediu explicitamente a lógica de foto de contexto: capa do projeto quando a doação está vinculada a um `highlight`, foto de perfil do missionário como fallback. Ver detalhe completo na seção 7.11 (itens 3-bis/3-ter).
- **2026-08-25** — Recoloriu a logomarca (imagens estáticas) pra acompanhar a troca de `--primary`, fechando o gap registrado mais abaixo no mesmo dia: `logo.png`, `logo-pill.png`, `icon-mark.png`, os 5 frames de `public/splash/*.webp`, `src/app/icon.png`/`apple-icon.png`/`favicon.ico` e `public/icons/icon-{192,512}.png` foram recoloridos de indigo pra `#34390c` via script ImageMagick (máscara de "tinta vs. preenchimento branco" — ver receita completa na seção 11); `logo-black.png`/`logo-white.png` ficaram de fora de propósito (monocromáticos por design). `manifest.ts` (`theme_color`) também atualizado. Motivado pelo usuário, que reparou o logo continuando roxo depois da troca de paleta. Verificado com screenshot real da splash e da navbar. Seção 11 atualizada.
- **2026-08-25** — Dois lugares com `text-violet-500`/`bg-violet-50` fixos no Tailwind (não o token `--primary`) ficaram roxos mesmo depois da troca de paleta, porque não usavam o design system: os ícones de créditos/tradução em `/dashboard/ia` e o stat "Créditos IA" em `/dashboard/painel`. Reportado pelo usuário via screenshot. Trocados pra `bg-primary/10`/`text-primary`.
- **2026-08-25** — Troca da cor primária da marca (`--primary`): do indigo/violeta genérico de SaaS (`oklch(0.53 0.21 277)`) para um verde-oliva terroso ("Colheita", tom Oliva — `oklch(0.33 0.065 116)` claro / `oklch(0.68 0.085 116)` escuro), com raiz missiológica em Mt 9:37 ("a seara é grande, mas os trabalhadores são poucos"). Motivado pelo usuário: o indigo não carregava nenhum significado próprio, era só o hue mais comum de SaaS recente. Processo: apresentei um estudo de psicologia das cores com 3 direções (Colheita/verde, Caminho/terracota, Águas Vivas/teal), cada uma com mockups reais (botão, chips, card de projeto) e comparativo — usuário escolheu Colheita e pediu um tom mais fechado/terroso; refinei em 3 variantes (Musgo/Oliva/Terra) e Oliva foi a escolhida. Estudo salvo em `design/paleta-missiologica.html` (editável) e `design/paleta-missiologica.pdf`. Aplicado em `globals.css` (`--primary`/`--primary-foreground`/`--ring`/`--sidebar-primary*` em claro e escuro) e no default de `profile.accent_color` (migration `046_brand_accent_color.sql`, `#6366f1` → `#34390c`, com backfill dos perfis que nunca customizaram a própria cor) e no fallback de `profile-posts-grid.tsx`. Verificado com screenshot real (Playwright + Chrome do sistema) na landing (claro/escuro) e tela de login — bom contraste nos dois temas. Seções 3.1 e 11 atualizadas.
- **2026-08-25** — Doação avulsa (`financial_once`/`financial_once_general`) ganhou pagamento com cartão via Stripe, a pedido do usuário — motivado por apoiadores internacionais, que não usam Pix/transferência local. Antes, o cartão via Stripe só existia no caminho recorrente (`RecurringPledgeForm`); `PledgeForm` só oferecia métodos manuais. Nova rota `POST /api/stripe/checkout-once` (sem exigir login, diferente de `checkout-recurring`) cria `stripe.checkout.sessions.create({mode:'payment', ...}, {stripeAccount: contaConectada})`, carregando todos os dados do formulário (perfil, projeto, categoria de orçamento, anônimo, nome, e-mail, mensagem) em `session.metadata` — o `pledges` só é criado depois, pelo webhook, no `checkout.session.completed` com `mode='payment'` (branch nova em `POST /api/stripe/webhook`, que resolve/cria `partners` se não-anônimo e cria `transactions` se houver `linked_account_id`, mesmo padrão do `invoice.payment_succeeded` já existente pra recorrência). `PledgeForm` ganhou toggle Stripe/manual igual ao `RecurringPledgeForm` (`showManual`, inicia `false` quando `stripeAvailable`); campos comuns (valor, categoria, anônimo, nome/e-mail, mensagem) saíram de dentro do `<form>` manual pra serem compartilhados pelos dois caminhos, e passo de método/data/comprovante ficou só no caminho manual. De quebra, `StripeConnectCard` (Configurações > Pagamentos) removeu a opção "não lançar automaticamente" do vínculo com `financial_accounts` — cobrança via Stripe é sempre confirmação automática (API), então agora vincula sozinho a primeira conta financeira existente assim que conectado, sem essa escolha manual (que só faz sentido pra métodos como Pix); sem nenhuma `financial_accounts` cadastrada, mostra aviso com link pra criar uma. Novas chaves i18n em `PledgeForm` (`stripeCta`/`preferManual`/`preferStripe`/`errorCheckout`) e troca de `stripeLinkedAccountNone` por `stripeLinkedAccountEmpty`/`stripeLinkedAccountEmptyLink` em `PaymentMethods`, nas 3 línguas. Seção 7.11 atualizada.
- **2026-08-25** — Removida a rota `/dashboard/publicacoes` (lista de posts do dono, com editar/excluir/arquivar), a pedido do usuário: editar um post agora abre o `PostComposerModal` direto de dentro do perfil (`/[username]`), sem navegar pra outra tela — mesmo padrão de "editar tudo pelo perfil" já usado pra projetos. `PostCard` (compartilhado por feed, `/explorar` e perfil) trocou o `router.push('/dashboard/publicacoes')` do menu "Editar" por `useOptionalComposer().openComposer(post)` — nova variante não-throwing de `useComposer()` (`post-composer-provider.tsx`) que retorna `null` quando não há provider na árvore (`/explorar`, feed de outros perfis), em vez de quebrar a página; o dropdown "Editar" só chama isso quando `canEdit` já é `true`. `[username]/page.tsx` e `[username]/projetos/[slug]/page.tsx` (que reaproveita `ProfilePostsGrid` pras "Atualizações" do projeto) passaram a montar `ProjectComposerProvider`+`PostComposerProvider` (mesmo par do `dashboard/layout.tsx`) em volta do grid **só quando `canEdit && viewerUserId`** — `usePostComposer` depende de `useProjectComposer()` incondicionalmente (fluxo "trocar pra Projeto" no passo de tipo), então o par vem sempre junto, mesmo o segundo provider raramente disparando em modo edição (que já pula esse passo). `[username]/page.tsx` também passou a marcar `new_post`/`highlight_update` como lidas ao carregar o próprio perfil (`markNotificationTypesRead`), reproduzindo o gatilho que antes vivia na página removida. Deletados `dashboard/publicacoes/{page,loading}.tsx`, `dashboard/publicacoes/nova/page.tsx` e `components/dashboard/posts-list.tsx`; mantidos `actions.ts`/`social-actions.ts`/`tag-search-actions.ts` na mesma pasta (Server Actions genéricas, ainda usadas pelo composer/`PostCard`/comentários — sem `page.tsx`, a URL antiga já retorna 404 automaticamente). Links/nav atualizados: `use-dashboard-nav.ts` (item "Publicações" removido), `dashboard/painel/page.tsx` (stat card + "ver todas" → `/${username}`), `dashboard/ia/page.tsx` (CTA de tradução → `/${username}`), `notifications-bell.tsx` (`AREA_HREF.content`/`.comments` → `/dashboard/feed`, fallback genérico — o link específico pro post via `?post=&comments=1` já existia e continua igual). Namespace i18n `PostsList` removido das 3 línguas (só usado pelo componente deletado); chave `PublicProfile.discardTitleEdit` adicionada (texto "Descartar edição?" no lugar de "Descartar publicação?" quando o diálogo de descarte abre durante edição de um post existente, não criação — `post-composer-modal.tsx`). Mesma leva: título da seção acima do seletor de abas do perfil (`ProfileContentTabs`) estava fixo em "Publicações" mesmo com a aba "Projetos" selecionada — virou dinâmico por aba (`postsHeading`/`projectsHeading`/sem título na aba História, que já tem headings próprios por bloco), e saiu de `[username]/page.tsx` (estava fora do componente de abas, não reagia à troca) pra dentro do próprio `ProfileContentTabs`. Seções 5.5.2, 7.9, 10 e `messages/*.json` atualizadas.
- **2026-08-23** — Tradução (PT/EN/ES) estendida a versículo, "história por trás do projeto" e título de cada marco — usuário reportou que só título/descrição do projeto tinham essa opção na tela de edição (nem no formulário completo, nem na edição inline da página pública). Migration `047_more_content_translations.sql` adiciona `scripture_translations`/`letter_translations` em `highlights` e `title_translations` em `milestones` (mesmo formato JSONB de `title_translations`/`description_translations` da 042). Extraído `src/lib/i18n/content-translations.ts` (`initialTranslations`/`initialSources`/`buildTranslationsPayload`/`translateContent`) reaproveitado por `highlight-form.tsx`, `use-project-composer.ts` e os componentes novos, eliminando três cópias quase idênticas dessa lógica que já existiam antes desta mudança. `HighlightSnapshot` (`section-types.ts`) ganhou `originalLocale` + os `*Translations` de todos os campos — **corrige também um bug pré-existente**: como o snapshot não carregava `titleTranslations`/`descriptionTranslations`, toda vez que uma seção de edição inline não relacionada (ex. salvar só a "história") era salva na página pública do projeto, a API (`POST /api/highlights`) zerava silenciosamente `title_translations`/`description_translations` no banco (`titleTranslations ?? {}` sem o campo no payload). `CoverTitleEditSection`, `LetterEditSection` e `MilestonesEditor`/`MilestonesEditSection` passaram a usar `LocaleContentTabs` (marcos: painel compacto por item, sem tabs, pra não pesar a lista). Leitura pública (`[username]/projetos/[slug]/page.tsx`) resolve idioma certo via `resolveLocalizedText` também pra versículo, história e cada marco. **Migration ainda não aplicada no Supabase remoto** — sessão sem rota IPv6 pro host direto (`db.<ref>.supabase.co`) e sem credencial de pooler correta; precisa ser aplicada manualmente antes de usar em produção. Seções 3.2, 5.6.1 e 7.12 atualizadas.
- **2026-08-08** — `ProfilePostsGrid` (compartilhado por `/explorar` e pelo perfil) ajustado pra bater com o grid real do Instagram, a pedido do usuário (mandou prints comparando lado a lado): células quadradas (`aspect-square`, era `aspect-[4/5]`) e posts só-texto agora usam a `accent_color` do perfil como fundo em vez de cinza chapado com texto cru — todo item da grade "é uma imagem" de verdade. `/explorar` ganhou breakpoints responsivos (estava com largura fixa de mobile mesmo em telas grandes). Sinalizado pelo usuário como próximo passo (não implementado ainda): tornar imagem obrigatória em todo post, não só no fallback visual. Seção 7.15 atualizada.
- **2026-08-08** — Duas mudanças em cima de `/explorar` (ver 7.15), a pedido do usuário: (1) `ExploreTabs` — projetos e publicações viraram abas estilo Instagram (mesmo padrão do `ProfileContentTabs` do perfil) em vez de duas seções empilhadas com uma sempre antes da outra. (2) `ProjectCoverFallback` — projetos antigos sem foto de capa (cards cinza vazios em `/explorar`) ganharam um preenchimento com gradiente + emoji da categoria; capa virou obrigatória na criação de projetos novos daqui pra frente (client + rede de segurança no server), sem afetar projetos antigos já publicados sem capa. `PROJECT_CATEGORIES` extraído de `highlight-form.tsx` pra `src/lib/highlights/project-categories.ts` (módulo neutro) pra poder ser importado em Server Components sem repetir o bug do `project-status.ts`. Seção 7.15 atualizada.
- **2026-08-08** — Nova página pública `/explorar` (ver 7.15), a pedido do usuário depois do fix do `BackButton` (entrada abaixo): fallback do botão de voltar pra visitante anônimo sem histórico real deixou de ser a landing de vendas (`/`) e virou essa página — feed de projetos ativos + posts recentes de perfis `privacy_mode='public'`, reaproveitando `ProfilePostsGrid`/`PostDetailViewer` do perfil e `SiteNav`/`SiteFooter` da landing (que já trazem os CTAs de Entrar/Criar conta). Seção 7.15 (nova) e 11 atualizadas.
- **2026-08-08** — `BackButton` dava tela em branco ao abrir um link de projeto compartilhado direto numa aba nova e clicar em voltar — reportado pelo usuário logo após a correção anterior do mesmo botão (ver entrada abaixo). Causa: `window.history.length > 1` não é confiável (aba nova às vezes já conta 2 por causa de uma entrada em branco do próprio navegador), então `router.back()` era chamado sem ter pra onde voltar de verdade. Novo `src/lib/navigation-tracker.ts` (`hasInAppNavigation()`) + `src/components/navigation-tracker.tsx` (montado no layout raiz) rastreiam se uma navegação client-side de verdade já aconteceu, em vez de confiar no tamanho do histórico do navegador. Seção 11 ("Botão de voltar") atualizada.

- **2026-08-08** — `GalleryEditor` (galeria de fotos do projeto) ganhou limite de 10 imagens (`MAX_IMAGES`, contador no botão "Adicionar fotos", aviso via toast se selecionar mais do que cabe), a pedido do usuário. Confirmado que a compressão pra WebP em qualidade reduzida (`compressImage`, `src/lib/media/compress.ts`) já era aplicada — nada mudou aí. Também corrigido `ProfileTabs`: trocar de aba (Perfil/História/Projetos) empilhava uma entrada de histórico por clique, então o `BackButton` (que usa `router.back()` real desde 2026-08-08) ficava saltando de aba em aba em vez de sair da seção — `<Link replace>` nos links da barra de abas resolve, ver seção 11 ("Botão de voltar").

- **2026-08-08** — Redesenho da página pública do projeto (ver 7.7-quater), planejado em plan mode com benchmark de GoFundMe/Vakinha/Kickante a pedido do usuário: as 6 seções principais (formas de apoiar, marcos, fotos, história, atualizações, trajetória) ganharam o mesmo tratamento de cartão do bloco financeiro; novo `FloatingSupportCta` + hook `useScrolledPastElement` (`IntersectionObserver` genérico, reutilizável) faz uma pill flutuante aparecer só depois que o cartão financeiro sai da tela, atalhando de volta pra ele (sem duplicar CTA/categorias); novo `ScrollToSectionLink` no topo pula direto pra "história por trás do projeto"; removida a entrada "Divulgue e traga apoiadores" (embaixador) de `SUPPORT_TYPES` — na prática abria um formulário de contato pra follow-up, redundante com o `ShareButton` já existente, e benchmark confirmou que plataformas maduras não têm esse botão separado numa página de projeto único (o sistema de parceiro-embaixador em si, fora dessa página, não foi tocado). Seção 7.7-quater (nova) atualizada.
- **2026-08-08** — Causa raiz encontrada pra "o cabeçalho recolhível não funciona" (reportado logo depois da entrada de baixo, que só tinha ajustado o *threshold* do JS — sintoma continuou porque a causa real era CSS): `html, body { overflow-x: hidden }` (regra de 2026-07-23, ver "Sem deslocamento lateral de página" na seção 11) fazia `overflow-y` de `body` computar pra `auto` (regra do CSS Overflow Module), transformando `body` num scroll container próprio — todo `position: sticky` dentro dele (`ProfileTabs`) parava de grudar de verdade, já que passava a calcular "stuck" relativo ao `scrollTop` do `body` (sempre `0`) em vez do `html` (`document.scrollingElement` real). Confirmado com um script Playwright headless (não só leitura de código) comparando `getBoundingClientRect().top` do elemento sticky contra `window.scrollY` durante scroll simulado. Corrigido movendo a regra só pra `html`. Nessa mesma leva: o pequeno lápis de editar (`EditPencilButton`, usado por **toda** seção editável da página do projeto — capa, descrição, marcos, galeria, história, financeiro, datas/status) só aparecia no hover em telas `sm+` (desktop), o que o usuário não descobriu sozinho em duas seções (Marcos, História) mesmo já tendo editado outras — virou sempre visível, em qualquer tamanho de tela, sem precisar de hover. Seção 11 atualizada.
- **2026-08-08** — Leva de ajustes pedidos em sequência pelo usuário testando ao vivo o projeto de construção (ver 7.7-tris): (1) nova galeria de fotos do projeto (`project_gallery_images`, migration `045_project_gallery_images.sql`) — `GalleryEditor`/`GalleryEditSection`, também em `HighlightForm`; (2) "Atualizações" trocou uma lista vertical sem link por `ProfilePostsGrid`/`PostDetailViewer` (grid clicável, mesmo componente do perfil), fechando a lacuna de não existir caminho de volta post→projeto; (3) chave PIX ganhou destaque visual (`CopyableValue` prop `emphasized`, cor `support`) tanto na página do projeto quanto em `PaymentMethodInstructions`; (4) `ProfileTabs` ganhou cabeçalho recolhível ao rolar (`src/hooks/use-hide-on-scroll.ts`), ajustado uma segunda vez pra reagir a qualquer rolagem pra cima (não só perto do topo) depois de feedback direto. Também: dados do projeto de construção do próprio usuário corrigidos direto no banco (título/descrição que estavam concatenados com "—" num único campo foram separados; as 3 categorias de orçamento antigas foram substituídas pelas 7 da tabela que o usuário já tinha pronta, com `goal_amount` recalculado) — não é mudança de schema/código, registrado aqui só por rastreabilidade. Seções 3.1 e 7.7-tris (novas) atualizadas.
- **2026-08-08** — `project_budget_categories` ganhou coluna `description TEXT` nullable (migration `044_budget_category_description.sql`, `project_budget_progress` recriada pra expor), a pedido do usuário: em projetos fora do domínio de viagem (ex. construção), toda categoria cai em `other` e só tinha espaço pra um título curto (`custom_label`), sem lugar pra detalhar "o que essa etapa inclui" (o usuário já fazia isso manualmente numa tabela solta). `BudgetCategoriesEditor` — cada linha virou um mini-card: seletor de tipo/título/valor na primeira linha, `Textarea` de descrição embaixo. `BudgetBreakdown` (tela pública) mostra a descrição como texto secundário abaixo do progresso, só quando presente. Threaded por todos os 4 caminhos de salvamento (`highlight-form.tsx`, `financial-edit-section.tsx`, `use-project-composer.ts`, `/api/highlights`) e pela leitura pública em `[username]/projetos/[slug]/page.tsx`. Seção 3.1 (`project_budget_categories`) atualizada.
- **2026-08-08** — Dois ajustes rápidos no badge de status do projeto, reportados pelo usuário logo depois da entrada anterior do mesmo dia: (1) o placeholder "Adicionar datas..." (mostrado só pro dono, quando as duas datas estão vazias) colava por baixo do lápis de editar — `pr-7` reserva o espaço do ícone. (2) o badge de status era só leitura; virou clicável (`StatusBadge`, dropdown, salva na hora ao escolher uma opção) — motivado por feedback direto: mostrar o status não bastava, precisava dar pra mudar sem abrir o formulário de datas. Nesse processo, `STATUS_OPTIONS` saiu de um arquivo `'use client'` pra um módulo neutro (`src/lib/highlights/project-status.ts`) depois de um erro real de runtime só detectado testando no navegador (não no `next build`) — ver seção 7.7-bis.

- **2026-08-08** — Contribuição por categoria de orçamento (migration `043_pledge_budget_category.sql`) + redesenho do bloco financeiro da página pública do projeto, planejado em plan mode com pesquisa de benchmark (padrão *designated giving*/*restricted funds* do terceiro setor, Vakinha/Kickante no Brasil) a pedido explícito do usuário. Fechada uma lacuna antiga: `project_budget_categories`/`project_budget_progress` (migration 010) nunca tiveram nenhum caminho de código escrevendo `transactions.budget_category_id` — a "categoria" era só leitura, nunca usada de verdade. Agora `pledges`/`recurring_pledges` ganharam `budget_category_id` (nullable, `NULL`=projeto geral), com seletor "Onde investir" em `PledgeForm`/`RecurringPledgeForm` (só fluxo manual/Pix — Stripe recorrente ficou de fora desta rodada a pedido do usuário) e categoria pré-preenchida (editável) na conciliação manual. `BudgetBreakdown` ganhou botão "Contribuir" por categoria (só na tela pública, não pro dono) linkando com `?category=ID`, validado contra o `highlight_id` antes de pré-selecionar. Mesma leva, tudo a pedido direto do usuário: `FundingProjectionCard` trocou o tom de alarme (vermelho "não deve alcançar a meta") por convite acionável com CTA; badge de status passou a aparecer sempre junto do título (antes só existia dentro do form de edição); "Data da viagem" virou "Data de início" (nem todo projeto é viagem); corrigido bug do lápis de editar "flutuando" solto quando a seção de datas está vazia; novo `ShareButton` compartilhado (extraído de `profile-owner-actions.tsx`) usado também num botão de compartilhar projeto, visível pra qualquer visitante; PIX ganhou nome do titular (reaproveita a coluna `label` já existente); `[username]/projetos/[slug]` passou a mostrar a barra de abas normal do perfil (antes era tratada como fluxo de ação sem chrome, igual mensagens/oração/parceria). Seções 7.7-bis (nova) e 7.12 atualizadas.
- **2026-08-08** — Editor de História, a pedido do usuário: `history_blocks` tinha RLS de escrita e um componente de leitura (`HistoryView`) mas nenhuma UI de edição em lugar nenhum — a tabela nunca chegou a ser populada. Novo `history-editor-form.tsx` + rota `/dashboard/configuracoes/historia` cobrem só os 4 tipos com heading conhecido pelo `HistoryView` (Quem somos/Nosso chamado/Linha do tempo/Convite) como seções fixas, deliberadamente sem CRUD genérico de blocos (reordenar, tipos repetidos) — escopo enxuto pra uma feature com zero adoção até aqui. Alcançável por um link no fim do `ProfileForm` e por um botão na própria página pública `[username]/historia` quando `canEdit` (mesmo padrão de `getProfileViewerContext`). Mesma leva: `[username]/projetos` ganhou botão "Criar projeto" (→ `/dashboard/projetos/novo`) pra `canEdit`, que essa página ainda não checava. Seção 7.12 atualizada.
- **2026-08-08** — Corrigido `BackButton` (`src/components/ui/back-button.tsx`) pra sempre voltar pra tela anterior de verdade via `router.back()`, em vez de navegar pra um destino fixo — reportado pelo usuário: o botão de voltar da tela de Seguidores tinha `href` fixo (`/dashboard`/`/`, herdado do fallback genérico de `profile-tabs.tsx`) e ignorava que a pessoa tinha vindo do perfil. `href` virou só uma rede de segurança (`router.push`) pro caso raro de não haver histórico (`window.history.length <= 1`), então todo consumidor existente (perfil público: seguidores/história/trajetória/projeto/mensagens/oração/parceria; `dashboard/projetos/[id]/equipe`) foi corrigido automaticamente sem precisar tocar em cada tela — `onClick` continua existindo à parte pra casos que não são navegação real (fechar dialog, voltar passo de wizard). Fixado como padrão obrigatório na seção 11.
- **2026-08-08** — Causa raiz real do "deslocamento lateral" que voltava a acontecer mesmo depois do `overflow-x: hidden`/`overscroll-behavior-x: none` em `html, body` (entradas abaixo, mesmo dia): esses dois só protegem o documento inteiro, não um container interno com scroll próprio. Achado: pelo CSS Overflow Module, um `div` com `overflow-y-auto` mas **sem** `overflow-x` explícito tem o eixo X recomputado de `visible` pra `auto` — ou seja, qualquer filho um pouco mais largo (imagem, texto sem truncate, badge) já torna aquele container arrastável de lado, e o scroll fica preso onde soltar o dedo (diferente do bounce do body). Varredura em todo `src/` achou 12 containers nessa situação, incluindo o primitivo `DialogContent` (`src/components/ui/dialog.tsx` — herdado por **todo** `Dialog` do sistema) e o `<main>` de `dashboard/layout.tsx` (cobre toda página do dashboard): `EditProfileDialog`, `PostDetailViewer`, `PostCommentsSheet`, `WizardModal` (composer de post/projeto), `AccountMenuDrawer`, `MessageThread`, sidebar desktop, dropdown de busca do header, `TagPeoplePicker`, menu mobile do `SiteNav`. Todos ganharam `overflow-x-hidden` explícito ao lado do `overflow-y-auto`; carrosséis/tabs com `overflow-x-auto` de propósito não foram tocados. Fixado como padrão obrigatório na seção 11 ("Sem deslocamento lateral de página").
- **2026-08-08** — Corrigida lentidão real reportada pelo usuário ("primeira vez que clico em Perfil demora muito, segunda vez é rápido") + auditoria geral de transições de tela: `generateMetadata` de `[username]/page.tsx` fazia uma query `profiles` própria e não cacheada, duplicando a mesma busca que `getProfile()` (já `cache()`-wrapped) faz logo depois no `page.tsx` — trocado pra reaproveitar `getProfile()`, elimina um round-trip inteiro antes até de começar a montar o HTML. `RecentPosts` em `dashboard/painel/page.tsx` (a tela mais visitada do dashboard) rodava fora de `Suspense`, bloqueando a página inteira até a query terminar; agora está em `Suspense` com skeleton próprio, e a rota ganhou seu próprio `loading.tsx` (antes herdava o skeleton de feed do `dashboard/loading.tsx`, com formato bem diferente do conteúdo real — trocado por um formato compatível: stat cards + lista). Adicionado `loading.tsx` também em `dashboard/ia`, `meus-projetos`, `financeiro-parceiro`, `financeiro/{categorias,conciliacao,contas,lancamentos}` (reaproveitando os componentes de `src/components/ui/skeleton.tsx`), rotas que antes só tinham o fallback herdado de `dashboard/loading.tsx` (formato de feed, incompatível com tabelas/formulários/grids dessas telas).
- **2026-08-08** — Mais um ajuste no `ImageCropEditor` no mesmo dia: o usuário reportou que, depois da correção de direção do arrastar, batia numa "parede invisível" bem antes do que no editor antigo — o alcance máximo do `translate` estava fixo em ±50% do quadro, independente do zoom, enquanto o comportamento anterior (baseado em `object-position`/`cover`) crescia naturalmente com o zoom. Corrigido calculando o alcance por eixo em função do zoom (`panRange`/`clampPosition`, proporcional a `|zoom/localRatio - 1|`, mesma referência geométrica do `scaleRatio` já usado pro teto de zoom) — cada eixo só ganha alcance a partir do ponto em que ele de fato ultrapassa o quadro, mas cresce livremente dali em diante. Mudar o zoom agora também reenquadra a posição atual dentro do novo limite (`setZoom`), evitando que a posição fique "presa" fora do alcance ao reduzir o zoom. Seção 11 atualizada.
- **2026-08-08** — Depois do modelo contínuo de zoom (entrada abaixo, mesmo dia), o usuário reportou mais dois problemas testando de novo: o arrastar só respondia num eixo (o outro sempre travado) e a direção estava invertida (mouse pra direita movia a imagem pra esquerda). Causa: o `object-position` só reposiciona dentro da "sobra" do `object-fit:contain` — fisicamente correto pra evitar recorte indesejado, mas trava um eixo inteiro sempre que ele já bate exato com o quadro (comum, ex. foto retrato numa capa 21:9 larga), e o sinal da fórmula (herdado da época em que o "arrastar" só existia no modo `cover` antigo) tinha a semântica oposta à do `contain`. Resolvido abandonando `object-position` pro posicionamento (fica fixo em `50% 50%`) e passando a mover a imagem só via `transform: translate(x%, y%) scale(zoom)` — o `translate`, aplicado depois do `scale` na composição CSS, é um deslocamento livre nos dois eixos que não é afetado pela escala, e a imagem passa a seguir o cursor 1:1 (sinal corrigido) em qualquer direção, em qualquer zoom. `bakeImage()` espelha a mesma composição (`drawX = (canvas.width-drawWidth)/2 + panX`). Nessa mesma leva, a pedido do usuário ("gostei desse formato, pode colocar o mesmo padrão na foto de perfil"), `AvatarCropDialog` (que tinha sua própria implementação de crop, ~150 linhas, zoom só por slider) foi reescrito pra reaproveitar o `ImageCropEditor`/`bakeImage` com a nova prop `round` (quadro circular, botões de zoom viram fileira abaixo do círculo em vez de overlay interno) — mesma experiência de posicionamento (arrastar livre + zoom por botão/scroll/pinça) em avatar, capa, post e projeto. Chave `ProfileForm.zoom` (do slider antigo) removida das 3 línguas, sem mais uso. Seção 11 atualizada.
- **2026-08-08** — Reescrito o zoom/arrastar do `ImageCropEditor` pra um modelo contínuo único (elaborado em plan mode a pedido do usuário, referenciando a experiência do composer do Instagram), corrigindo 3 problemas reportados numa rodada de teste em produção: (1) no desktop o cursor virava "mãozinha aberta" mas nunca "fechava" (`grabbing`) e o arraste lateral não tinha efeito perceptível numa foto retrato numa capa 21:9 — causa raiz era a alternância entre `object-fit: contain`/`cover` (ver entrada abaixo, do mesmo dia) deixando o comportamento inconsistente na fronteira entre os dois modos; (2) o primeiro clique no zoom fazia a imagem "pular" de uma vez, sem transição gradual — causa raiz confirmada: a troca discreta de modo `contain`→`cover` no primeiro incremento de zoom, não o tamanho do passo. Corrigido eliminando os dois modos: agora é sempre `object-fit: contain` + `transform: scale(zoom)` contínuo, com `bakeImage()` usando a mesma fórmula (sem mais branch condicional `contain` vs `cover`, sem mais `RATIO_TOLERANCE`/checagem de tolerância — a fórmula única já degenera corretamente pro caso "proporção bate"). Novo teto de zoom proporcional (`scaleRatio * 3`, calculado a partir da proporção natural da imagem) em vez de um `3` fixo, e novo controle de zoom por scroll do mouse (desktop), somando aos botões +/- e à pinça de dois dedos já existentes — mais livre, sem depender só de acertar um botão pequeno. Seção 11 (subseção "Editor de recorte de mídia") atualizada com o novo modelo.
- **2026-08-08** — Três ajustes na edição da foto de capa (`ImageCropEditor`, `src/components/shared/media-editor/`), reportados pelo usuário testando em produção: (1) o preview não batia com o resultado salvo — o editor sempre mostrava a foto recortada preenchendo o quadro (`object-cover`), mas ao salvar com zoom=1 e proporção incompatível o `bakeImage()` entrava no modo "contida" (mantém a foto inteira, preenche a sobra com a cor média dela), dando a impressão de que a imagem encolhia depois de fechar; corrigido calculando no editor a mesma proporção/cor média do bake, alternando o preview pro modo contido nessa situação (arrastar fica desativado ali, sem efeito no resultado final; volta ao normal assim que dá zoom). (2) Zoom por slider fino foi trocado por botões +/- (mouse/desktop) + pinça de dois dedos (touch) — o slider era difícil de acertar no celular. (3) A tela travava e não dava pra rolar até o botão "Salvar alterações" enquanto editava a capa — o listener de `touchmove` no `window` chamava `preventDefault()` incondicionalmente; agora só intercepta quando o usuário está de fato arrastando/pinçando dentro do editor. Nessa mesma leva, reforçado o padrão "sem deslocamento lateral de página" (já registrado em 2026-07-23 com `overflow-x: hidden`, que sozinho não bastou) com `overscroll-behavior-x: none` em `html, body` — deliberadamente **não** via `touch-action: pan-y` global, que por spec bloquearia também carrosséis horizontais intencionais. Novas chaves `MediaEditor.zoomIn`/`zoomOut` (substituindo `zoom`) nas 3 línguas — seção 11 atualizada com duas novas subseções ("Sem deslocamento lateral de página" e "Editor de recorte de mídia").
- **2026-07-25** — Redesenho do fluxo de criar/ver publicações, inspirado (não copiado) no fluxo de composição do Instagram, pedido explícito do usuário. Banco: migration `038_post_engagement.sql` adiciona `posts.media_aspect_ratio`/`posts.location`, tabelas `post_tags` (marcação real de perfis na foto), `post_likes`, `post_comments` e o helper `can_view_post()`; `scheduled_at` ganhou uso real via cron `GET /api/cron/publish-scheduled-posts` (`vercel.json`, a cada 5min, plano Vercel Pro). Composer: `PostEditor` (formulário único) foi substituído por um wizard fullscreen em `src/components/dashboard/post-composer/` (tipo Post/Projeto → seleção+recorte/zoom/proporção/grade dos terços com carrossel reordenável → filtros/ajustes com imagem sempre visível → legenda+marcação+localização+agendar), sem bibliotecas novas (crop/pan hand-rolled estendendo o padrão de `CoverEditor`, filtros via CSS `filter` "assado" em canvas — `src/lib/media/bake-image.ts`). Feed: `feed-post-card.tsx` e `publications-feed.tsx` (divergentes, perfil público não tinha carrossel/vídeo) foram unificados em `shared/post-card.tsx` — mesma tela pro missionário e pro parceiro, com legenda truncada + "...mais", bolinhas de carrossel, curtir/comentar/compartilhar (`src/app/dashboard/publicacoes/social-actions.ts`) e pinos de marcação sob demanda; `enrichWithEngagement()` (`src/lib/posts/enrich-with-engagement.ts`) resolve curtidas/comentários/marcações em lote. Composição do `+` ("Post" vs. "Projeto") continua exclusiva do missionário (`ComposeBar` já escondia pra `role='partner'`); curtir/comentar ficou liberado pros dois papéis. Seções 3.1, 7.9, 10 e `messages/*.json` (`PostComposer`, `Feed`) atualizadas.
- **2026-07-25** — Passada de performance de carregamento, motivada por relato real de usuário (perfil público travando em branco no celular). Causa raiz: `src/proxy.ts` chamava `supabase.auth.getUser()` em toda rota (inclusive públicas), e `[username]/layout.tsx`/`dashboard/layout.tsx` faziam múltiplos `await`s sequenciais antes de `{children}` — como `loading.tsx` só cobre `page.tsx`, nada aparecia enquanto isso não resolvia. Correções: `proxy.ts` só autentica em rotas protegidas/de auth; `[username]/layout.tsx` isolou a busca da barra de abas numa `<Suspense>` própria, com `{children}` como irmão fora dela; `dashboard/layout.tsx` paralelizou `getTranslations` com o check de auth; novo `src/app/loading.tsx` como rede de segurança global; novo helper `getProfile()` (`src/lib/profile/get-profile.ts`, `cache()`) deduplicando a busca de `profiles` que se repetia em cada rota de `[username]/*`; `[username]/page.tsx`/`historia/page.tsx` passaram a streamar as seções pesadas (projetos, publicações, blocos de história) atrás de `<Suspense>` com skeleton, com as contagens do header em `Promise.all` dedicado; queries sequenciais-mas-independentes viraram `Promise.all` em várias páginas do dashboard (`configuracoes`, `financeiro`, `mensagens`, `parceiros`, `oracoes`, `publicacoes`), incluindo tirar `markNotificationTypesRead()` do caminho bloqueante; avatar do perfil ganhou `preload` (Next 16 renomeou `priority`→`preload`). Nova seção 9.1-bis documenta o padrão como convenção permanente.
- **2026-07-23** — Mais dois ajustes reportados pelo usuário: (1) rótulos da bottom nav mobile do apoiador/parceiro ("Meus projetos", "Minhas doações") continuavam grandes demais mesmo depois do fix de alinhamento — `useBottomNavItems` (`use-dashboard-nav.ts`) passou a usar `myProjectsShort`/`myGivingShort` ("Projetos"/"Doações") só na bottom nav; `useNav` (sidebar desktop + `AccountMenuDrawer`, que têm mais espaço) continua com o texto completo `myProjects`/`myGiving`, mesmo padrão já usado por `posts`/`postsShort`. (2) Página "andando" pros lados ao toque no mobile — causa clássica é algum elemento passando 1px+ da largura da viewport e o `html`/`body` não travando overflow horizontal; adicionado `overflow-x: hidden` em `html, body` no `@layer base` do `globals.css` (não havia nenhuma regra de overflow ali antes). Novas chaves `DashboardNav.myProjectsShort`/`myGivingShort` nas 3 línguas.
- **2026-07-23** — Dois ajustes reportados pelo usuário: (1) bottom nav mobile (`MobileBottomNav`, `sidebar.tsx`) tinha ícones desalinhados entre si — o container centraliza verticalmente o bloco ícone+texto de cada aba, e como só alguns rótulos quebram em 2 linhas ("Meus projetos", "Minhas doações") enquanto outros ficam em 1 ("Início", "Perfil"), cada bloco tinha altura diferente, empurrando os ícones pra alturas diferentes. Corrigido reservando `min-h-[2.2em]` (2 linhas) no `<span>` do rótulo sempre, independente do texto real precisar de 1 ou 2 linhas — todos os blocos ficam com a mesma altura e os ícones alinham. (2) `profiles.cover_url` (coluna já existia, usada desde 22/07 na grade de descoberta de missionários) não tinha nenhum campo de upload em lugar nenhum — `ProfileForm` (`dashboard/settings/profile-form.tsx`) ganhou uma seção "Foto de capa" (banner `aspect-[21/9]`, sem crop/reposicionamento, ao contrário do avatar que usa `AvatarCropDialog`) espelhando o padrão de upload do avatar (mesmo bucket `avatars`, path `${user_id}/cover.webp`, `compressImage` antes de subir). Novas chaves `ProfileForm.coverPhoto`/`editCover`/`coverHint` nas 3 línguas.
- **2026-07-23** — Splash screen (`splash-screen.tsx`) trocou os 3 frames de "quique de CSS" por um walk-cycle real de 5 poses desenhadas pelo usuário (`public/splash/walk-1.png` a `walk-5.png`, recortadas de uma folha `goGuide.png` gerada externamente — script cortou 5 colunas iguais, removeu a legenda numérica de cada frame e o fundo branco por flood-fill, mantendo o texto "Go guide" na mesma posição em todos os frames pra ficar parado enquanto só as pernas se movem). Fase de "GO piscando" foi removida (o novo frame já traz o lockup completo) — loop único de 220ms por frame desde o primeiro paint. Tamanho subiu de `h-20` (80px) pra `h-40`/`sm:h-48` (160-192px), pedido do usuário pra "ver melhor". `--keyframes splash-blink/splash-step` removidas de `globals.css` (não usadas mais).
- **2026-07-23** — CTA "virar missionário" (`becomeMissionary()`, já existia) deixou de estar escondido só em Configurações → Conta — motivado por feedback direto do usuário achando que não havia "fluxo claro" pra isso. Extraído pra componente reutilizável `src/components/dashboard/become-missionary-card.tsx` (mesmo padrão de `Card` + botão + `usePendingAction`, sem duplicar o que já existia em `account-form.tsx`, que passou a importar esse componente) e plugado em mais 3 lugares que o usuário pediu: card em `/dashboard/meus-projetos` (só quando `profile.user_role === 'partner'`, via `getActiveProfile()`), item de menu em `DashboardSidebar`/`AccountMenuDrawer` (rodapé, ao lado de "Ver perfil"/"Sair"), e banner dentro de `DiscoverMissionaries` quando o feed está vazio (`FeedScreen` passa `showBecomeMissionary={role === 'partner'}`). Todos os 4 pontos chamam a mesma server action, que já fazia o flip de `user_role` + redirect pro `/onboarding` — nenhuma lógica nova, só mais pontos de entrada.
- **2026-07-23** — Três ajustes no papel parceiro/apoiador, motivados por feedback direto do usuário testando com contas reais ("não fica claro que tipo de conta a pessoa está criando", "não tem lugar mostrando o papel dela depois de logada", "perfil de apoiador mostra abas de História/Projetos como se fosse missionário"): (1) `/cadastro` (`src/app/(auth)/cadastro/page.tsx`) mostra uma nota explicando "conta de apoiador" quando o `redirect` não aponta pra `/onboarding` — esse é o caso de quem vira parceiro pelo fluxo "apoiar este missionário" (`RecurringPledgeForm` → `criar conta`), que pula o passo explícito de escolha de papel do onboarding e por isso nunca via essa clareza. (2) Selo de papel (`profiles.user_role`) ao lado do nome, em `DashboardSidebar` (desktop) e `AccountMenuDrawer` (mobile) — antes só mostravam o plano (`free`/`pro`/`mission`), sem indicar se a conta é de missionário ou apoiador. (3) `ProfileTabs` (`profile-tabs.tsx`) só mostra as abas História/Projetos/Trajetória quando `isMissionary` — antes apareciam pra qualquer perfil público, incluindo o de um apoiador, que não tem jornada/projetos pra mostrar. Novas chaves `Auth.signupPartnerNote`, `DashboardNav.roleBadgePartner`/`roleBadgeMissionary` nas 3 línguas — seções 7.1-bis, 8 e 10 atualizadas.
- **2026-07-23** — Redesign visual pedido pelo usuário: a paleta era 100% monocromática ("carbon", só `--destructive` tinha chroma) e os cantos eram discretamente arredondados — não combinava com um produto de missionários/apoiadores, que precisa de calor humano e confiança, não cara de painel administrativo. Mudanças: (1) paleta intencional baseada em psicologia das cores — `--primary` virou indigo/violeta (reaproveitando o hue que já era default de `profile.accent_color`), `--support` (coral, novo) isolado só em CTAs de doação (`Button variant="support"`: submits de `pledge-form`/`recurring-pledge-form`, CTA "Faça parte deste projeto" na página pública do projeto, ícones dos 3 cards financeiros do `PartnershipWizard`), `--success` (verde, novo) na barra de arrecadação (`ui/progress.tsx`, era `bg-primary`) e em badges de "Meta atingida 🎉" (`highlights-list.tsx` e `[username]/projetos/[slug]`), `--warning` (âmbar, novo) reservado sem uso ainda; dark mode com chroma reduzido (mais sóbrio à noite, pedido do usuário). (2) `--radius` subiu de `0.625rem` pra `1.125rem` — como toda a escala deriva dessa variável via `calc()`, botões padrão (`h-8`/`h-9`) ficaram naturalmente em formato pílula sem tocar em componente nenhum. (3) Engajamento: `discover-missionaries.tsx` virou grade editorial 2 colunas com foto de capa (`profiles.cover_url`, query em `getDiscoverMissionaries` ganhou `cover_url`/`location`/`show_location`) em vez de lista simples; `feed-post-card.tsx` ganhou uma borda esquerda fina na cor `--support` quando o post está ligado a um projeto (`post.highlight`), sinalizando "isso é sobre apoiar" sem tingir o card inteiro. (4) Nova logomarca — pernas cruzadas em passo com "GO" encaixado no cruzamento dos pés e "guide" ao lado, gerada pelo usuário numa ferramenta de imagem externa (prompt escrito nesta sessão) e recortada em 4 variantes PNG com fundo transparente (`public/logo.png`, `logo-white.png`, `logo-black.png`, `icon-mark.png` + `src/app/icon.png`/`favicon.ico`) — plugada em `site-nav.tsx` e `dashboard/sidebar.tsx` (`MobileHeader`, que perdeu a dependência de `profile.accent_color` pro ícone provisório "M"). Ver seção 11 (Tema) pros valores exatos de token e seção 3.1 (`accent_color`). Reordenação do cabeçalho do perfil público (bio antes das estatísticas) foi avaliada e **descartada** por ora — usuário não confirmou, padrão atual (stats primeiro, estilo Instagram) mantido.
- **2026-07-23** — Quatro ajustes pontuais pedidos pelo usuário na sequência do redesign: (1) `HeaderSearch` (`src/components/dashboard/header-search.tsx`, novo) — barra de busca fixa no header desktop (`hidden md:block`), estilo Instagram web, com dropdown de resultados ao vivo sobre a página (fecha em Esc/clique fora, Enter leva pra `/dashboard/buscar?q=`); benchmark confirmou que o app do Instagram usa uma aba dedicada em vez de barra persistente (mobile não muda, continua só o ícone -> tela cheia). Lógica de debounce/busca extraída pra `src/hooks/use-directory-search.ts`, reaproveitada por `HeaderSearch` e por `/dashboard/buscar` (que ganhou suporte a `?q=` via `useSearchParams`, dentro de `Suspense`). (2) Ícone do app recentralizado (o corte anterior tinha a arte deslocada pro canto) e passou a existir também como `apple-icon.png` (iOS) e `manifest.ts` com `icons` 192/512 (`public/icons/`) pro Android/Chrome "adicionar à tela inicial" — antes só existia `icon.png`/`favicon.ico`, que o iOS/Chrome não usam pra atalho de tela inicial. (3) Removida a aba "Seguidores" de `ProfileTabs` (`profile-tabs.tsx`) — redundante com a linha "X seguidores · Y seguindo" que já fica abaixo do avatar (`ProfileHeader`) e leva pro mesmo `/[username]/seguidores`. (4) Corrigido bug real do menu ☰ (`AccountMenuDrawer`) que aparecia como uma faixa fina no topo em vez de gaveta cobrindo a tela: o wrapper de `ProfileTabs` usa `backdrop-blur` (CSS `filter`), que cria containing block pra descendentes `position:fixed` — a gaveta renderizava presa dentro da faixa fina da barra de abas. Corrigido renderizando a gaveta via `createPortal` direto no `document.body`, escapando esse containing block (mesmo padrão que bibliotecas de modal usam por padrão). De quebra, trocado mais um logo placeholder "M" esquecido em `DashboardSidebar` pela logomarca real.
- **2026-07-23** — Nova página de busca `/dashboard/buscar` (pessoas + projetos, resultado instantâneo com debounce de 300ms) e ícone de lupa fixo em `dashboard/layout.tsx`, motivado por feedback direto do usuário (não havia lugar visível pra procurar pessoas/projetos — `DiscoverMissionaries` só aparece no estado vazio do feed e só lista missionários). Benchmark contra Instagram/TikTok (busca como slot fixo na bottom nav) vs. LinkedIn/X (ícone de lupa no cabeçalho) — adotado o segundo padrão porque a bottom nav daqui já está com os 5 slots ocupados. Nova server action `searchDirectory` (`src/app/dashboard/feed/search-actions.ts`) com sanitização do termo de busca antes de montar a string do `.or()` do PostgREST (evita injeção de cláusula de filtro). Nova chave `DashboardNav.search` + namespace `Search` nas 3 línguas — seções 7.14 (nova) e 10 atualizadas.
- **2026-07-22** — Removido o `FollowButton` de dentro dos cards de post do feed (`FeedPostCard`), motivado por bug real reportado pelo usuário (print mostrando o mesmo autor com posts diferentes exibindo "Seguindo"/"Seguir" ao mesmo tempo): cada `FollowButton` tinha `useState` local, então clicar num card não sincronizava os outros cards do mesmo autor. Benchmark contra Instagram/X (botão de seguir só aparece em conteúdo de descoberta, nunca no feed principal, que já é só de quem você segue) confirmou que, como `getFeedPage` já filtra por quem o usuário segue-ou-apoia, o botão ali era redundante e a fonte do bug — removido de `feed-post-card.tsx`/`feed-list.tsx`/`feed-screen.tsx` (que também parou de buscar `getFollowedProfileIds()` sem uso). `FollowButton` continua só em `discover-missionaries.tsx` e na nova `FollowsList` do perfil — ver 7.1-bis e 10.
- **2026-07-22** — Nova aba Seguidores/Seguindo no perfil público, só para `user_role='missionary'`, pedida pelo usuário como "distinção sutil" entre quem o missionário segue e por quem é seguido (incluindo o caso mútuo) — benchmark rápido contra Twitter/X (badge "Follows you"), Instagram (abas + "Follow Back"), LinkedIn (Conexões vs. Followers, confirma manter `follows` separado de `partners`) e GitHub (baseline sem badge de mútuo); adotado abas estilo Instagram + badge "Segue você" estilo Twitter + label "Seguir de volta" estilo Instagram. Fechada a lacuna de RLS documentada desde a migration 033 (`follows_owner_read` nova, migration `037_follows_visibility.sql`), mais `following_count`, `get_followers`/`get_following` (RPCs paginadas com checagem de `privacy_mode` embutida). Nova rota `[username]/seguidores/page.tsx`, componente `FollowsList` (`src/components/profile/follows-list.tsx`), server actions em `src/app/dashboard/feed/follows-list-actions.ts`, `FollowButton` ganhou prop opcional `followsViewer`. Contadores de seguidores/seguindo em `ProfileHeader` (linha própria, só quando `user_role='missionary'`) e aba nova em `ProfileTabs` (gated por `isMissionary`, thread desde `[username]/layout.tsx`). Novo namespace i18n `FollowsList` + chaves novas em `Feed`/`PublicProfile`, nas 3 línguas — seções 3.1, 7.12 e 10 atualizadas.
- **2026-07-22** — Revertida a decisão de 2026-07-16 (ver changelog daquela data) de abrir em nova aba os links de perfil/projeto dentro do card do feed (`FeedPostCard`, `src/components/dashboard/feed/feed-post-card.tsx`) — motivado por feedback direto do usuário achando estranho sair do app pra ver um perfil a partir do feed. `target="_blank"` removido dos dois links (avatar/nome do autor e título do projeto vinculado); navegação passa a ser integrada como o resto do dashboard, igual ao link "Ver meu perfil" do sidebar (7.12).
- **2026-07-22** — Padronizado o tratamento de scroll em modais (motivado por bug real: a barra de rolagem do `EditProfileDialog` sobrepunha o final dos campos no desktop). Testado e descartado `scrollbar-gutter: stable` (não reservou espaço de forma confiável); o padrão fixado é `padding` simétrico manual — `max-h-[85vh] overflow-y-auto` + `px-5 py-4` movidos pro primitivo `DialogContent` (`src/components/ui/dialog.tsx`, era `p-4` sem cap de altura/scroll; `DialogFooter` acompanhou com `-mx-5`), então os 9 modais que só usam `className="max-w-sm"` herdaram o comportamento automaticamente, sem editar cada um. `EditProfileDialog` (layout próprio, fullscreen no mobile) cancela o novo default explicitamente (`max-h-full overflow-y-hidden`, com `md:max-h-[85vh]` restaurando o cap no desktop) pra não quebrar a tela cheia. Ver seção 11 (novo bloco "Modais roláveis").
- **2026-07-20** — Botão "voltar" unificado nas 7 páginas do perfil público + edição inline por seção na página de projeto, motivado por feedback direto do usuário (prints mostrando botões de voltar duplicados, e a pergunta "por que não dá pra editar direto aqui?"). (1) Botão de voltar: `ProfileTabs` ganhou `BackToProfile` (ícone só, `fixed top-3 left-3`, ao lado do `LanguageSwitcher` que já ficava em `top-3 right-3`) nas 4 rotas sem barra de abas (`mensagens`/`oracao`/`parceria`/`projetos/[slug]`), substituindo os links "← Voltar ao perfil de X" que cada uma renderizava no próprio conteúdo; nas 3 rotas com barra de abas (`historia`/`trajetoria`/`projetos` lista), o link inline equivalente foi só removido, já que a aba "Perfil" da barra já cobria a mesma função — ver 7.12 e 10 (`profile/`). (2) Desambiguado o CTA "Faça parte deste projeto" das cards de "Outras formas de apoiar" em `[username]/projetos/[slug]`: nenhum dos links passava `?choice=` pro wizard de parceria, então todos caíam no mesmo menu genérico, dando a impressão (correta) de que eram redundantes — `SUPPORT_TYPES` ganhou um campo `choice` mapeado pros 6 valores que o `PartnershipWizard` já aceita — ver 7.3. (3) Edição inline por seção: dono/gestor (`canEdit`, via `getProfileViewerContext`) agora edita capa/título/versículo, descrição, formas de apoio, financeiro, marcos, história e datas/status direto na própria página pública do projeto, sem modal e sem navegar pro dashboard — 7 novos componentes client "ilha" em `src/components/highlights/*-edit-section.tsx`, reaproveitando lógica extraída do `HighlightForm` (ver 7.7) e um hook novo (`useHighlightSectionSave`, 5.5.1-bis) — ver 7.12 e 10. Pré-requisito de segurança fechado na mesma mudança: `POST /api/highlights` (5.3) não validava posse do `profileId` no servidor — a documentação já citava esse fechamento como feito (na entrada de papéis abaixo), mas o código real ainda não tinha `assertProfileAccess`; nota registrada aqui pra não repetir esse tipo de drift entre este arquivo e o código (ver aviso da seção 3.3 sobre o mesmo risco já ter acontecido com RLS). Novo namespace i18n `PublicProject` + chave `PublicProfile.backToProfile` nas 3 línguas — seções 3, 5.3, 5.5.1-bis, 7.3, 7.7, 7.12 e 10 atualizadas.
- **2026-07-20** — Fase 1 de papéis (parceiro vs. missionário): até aqui todo cadastro criava um `profiles` idêntico e caía sempre na visão de gestão do missionário, sem distinção nenhuma entre apoiador e missionário. Nova coluna `profiles.user_role` (migration `032_profile_user_role.sql`, default `'partner'`, com backfill pra `'missionary'` de quem já tinha projeto/post/parceiro cadastrado) passa a decidir isso. `/dashboard/page.tsx` deixou de ser a visão de gestão e virou o **feed universal** (estilo Instagram, pros dois papéis, sem branch de conteúdo por papel); o conteúdo antigo (checklist/aniversários/stats/atalhos) foi recortado pra `/dashboard/painel/page.tsx` sem mudar a lógica. Nav do dashboard (`useNav`/`useBottomNavItems`, `src/hooks/use-dashboard-nav.ts`) passou a ser consciente de papel — parceiro vê só Feed/Mensagens/Meus projetos/Minhas doações/Configurações, sem nenhuma ferramenta de gestão. Novo "seguir" leve (`follows`, migration `033_follows_and_feed_events.sql`) deliberadamente separado do compromisso formal em `partners` — feed lê a união de `follows` e `partners.user_id=auth.uid()` (essa segunda parte já funcionava com a RLS existente, zero mudança de schema aí). Heurística de ranking v1 transparente (`src/lib/feed/rank.ts`: recência + segue + pledge confirmado + parceiro fixo + afinidade de `highlights.category`, coluna nova da migration `034_highlights_category.sql`, multi-select em `HighlightForm`) — já grava `feed_events` (mesma migration 033, write-only nesta fase) pra permitir treinar um modelo de verdade no futuro sem mudar o resto do pipeline. Área nova do parceiro: `/dashboard/meus-projetos` (projetos que apoia, somente leitura), `/dashboard/financeiro-parceiro` (histórico de doações entre missionários, filtro por missionário, usando RLS de self-read que já existia) e `GET /api/partner/tax-export` (comprovante agregado CSV/PDF — `pdfkit` novo na stack — explicitamente não é documento fiscal oficial). Onboarding ganhou um passo `role` novo antes dos 4 passos de missionário (`OnboardingWizard` migrou de `useState` numérico pra união de strings tipada); escolher "parceiro" pula recebimento/projeto e vai direto pro feed. Conversão "virar missionário" (`becomeMissionary()`, card em Configurações → Conta) reaproveita o mesmo wizard, pulando o passo `role` porque o flip já aconteceu antes do redirect. Modo de pré-visualização pra `SUPERADMIN_EMAILS` (`SuperadminRoleSwitcher`, cookie `preview_role`) permite ver as duas experiências com uma conta só, sem impersonar ninguém nem contornar RLS. De quebra, corrigida a checagem de posse ausente em `POST /api/highlights` (`assertProfileAccess`, gap já documentado aqui) e uma nota de design desatualizada na seção 7.12 (seguir o missionário já não é mais "não implementado"). Novos namespaces i18n `Feed`, `PartnerFinance`, `BecomeMissionary`, `Superadmin` + chaves novas em `Onboarding`/`DashboardNav`, nas 3 línguas — seções 2, 3.1, 5.3, 7.1, 7.1-bis (nova), 7.12, 8 e 10 atualizadas.
- **2026-07-19** — Seletor de idioma discreto + tela de editar perfil em tela cheia, benchmark contra Instagram/WhatsApp/TikTok/X (nenhum expõe troca de idioma na tela principal): `src/i18n/request.ts` ganhou detecção de `Accept-Language` (primeira visita, sem cookie ainda) como novo passo antes do fallback fixo `pt` — antes um visitante estrangeiro sem cookie sempre caía em português. `LanguageSwitcher` ganhou modo `compact` (texto "PT ▾" em vez de 3 bandeiras), usado em `profile-tabs.tsx` no lugar do badge flutuante anterior; `SiteNav` mantém o modo padrão. `EditProfileDialog` virou tela cheia no mobile (seta voltar + título centralizado, estilo Instagram, `showCloseButton={false}`) mantendo o popup central no desktop — `ProfileForm` em si não foi alterado, então a aba Perfil em Configurações continua idêntica. Nova chave `PublicProfile.closeEdit` nas 3 línguas — seções 5.6 e 10 (`marketing/`, `profile/`) atualizadas.
- **2026-07-16** — Feed do dashboard ganhou uma faixa de "stories" no topo, estilo Instagram: em vez de fotos de perfil efêmeras, mostra projetos ativos das pessoas que o usuário segue/apoia com atualização recente (motivado por print do Instagram enviado pelo usuário). Nova tabela `project_story_views` (migration `035_project_story_views.sql`, seção 3.1) rastreia visto/não visto — anel vira gradiente quando há atualização nova (`hasUnseen`), estático quando já visto. Não existe conteúdo de "story" separado: a faixa reaproveita 100% os `posts` já usados no feed principal e na página do projeto (`getFollowedProjectStories`, seção 7.13). Tocar num círculo abre `ProjectStoryViewer`, tela cheia tap-to-advance (sem timer automático) entre as atualizações daquele projeto, com "Ver projeto completo" fixo linkando pra `/[username]/projetos/[slug]`. Header mobile do dashboard também mudou: logo passou a ficar centralizado (`dashboard/layout.tsx`, grid de 3 colunas) em vez de alinhado à esquerda, replicando o cabeçalho do Instagram. Novo namespace i18n `ProjectStories` nas 3 línguas — seções 3.1, 7.13 e 10 (`dashboard/feed/`) atualizadas.
- **2026-07-16** — Bottom nav mobile do dashboard redesenhado estilo Instagram, na sequência direta da mudança anterior de "perfil dono vs visitante": o 5º slot (antes um botão "Menu" que abria a gaveta de navegação completa) virou uma miniatura do próprio avatar, linkando pra `/[username]` — mesmo padrão do Instagram/Threads/TikTok, onde a foto do usuário é sempre a última aba do rodapé. A gaveta de navegação completa (Visão Geral/Publicações/Projetos/Parceiros/Orações/Mensagens/Financeiro/IA Copiloto/Configurações/Sair) não desapareceu: foi extraída pra um componente self-contained (`AccountMenuDrawer`, seção 10 `dashboard/`) e seu gatilho migrou pra dentro da própria tela de perfil (ícone ≡ em `ProfileTabs`, só quando `canEdit`) — igual ao Instagram, onde o menu de configurações mora na tela de perfil, não na tab bar. **Trade-off consciente**: no mobile, chegar em Financeiro/Configurações/etc. agora leva 2 toques (aba avatar → ≡) em vez de 1; aceito porque replica um padrão já validado em escala por essas plataformas, e é reversível (o `AccountMenuDrawer` já está pronto pra ganhar um segundo gatilho se isso incomodar na prática). `useNav`/`useBottomNavItems`/`useSignOut` foram extraídos de `sidebar.tsx` pra `src/hooks/use-dashboard-nav.ts` (seção 5.5.2) porque o `AccountMenuDrawer` precisa deles fora da árvore `/dashboard`. Novo `CreateContentFab` (botão flutuante circular, estilo X/Twitter — essa plataforma usa FAB em vez de um slot na tab bar) oferece "Nova publicação"/"Novo projeto" a partir de qualquer tela do dashboard mobile, oculto para `role='partner'`; gesto de arrastar para criar conteúdo foi cogitado e descartado (risco de colidir com o gesto nativo de "voltar" do Safari/Chrome mobile e com o scroll horizontal da tab bar de abas do perfil). Novas chaves i18n `DashboardNav.profileTab` e namespace `CreateContent` nas 3 línguas — seções 5.5.2, 10 (`dashboard/`, `profile/`) atualizadas.
- **2026-07-16** — Perfil público (`[username]`) ganhou noção de dono/gestor, unificando a experiência de "ver meu perfil" com a de um visitante comum (motivado por feedback do usuário comparando com o Instagram: no app antigo, o dono via a mesma UI genérica de visitante e "Ver meu perfil" abria numa aba separada, sem afinidade nenhuma com o dashboard). Novo helper `getProfileViewerContext` (`src/lib/profile/viewer-context.ts`, seção 7.12) centraliza a detecção de `isOwner`/`canEdit` (considerando também `profile_managers`) e substitui checagens de auth espalhadas. `[username]/layout.tsx` ganhou uma barra de abas persistente (`ProfileTabs`, oculta em telas de ação/conversão); `page.tsx` troca `ProfileCTA` por `ProfileOwnerActions` ("Editar perfil"/"Compartilhar perfil") quando `canEdit`; "Editar perfil" abre o `ProfileForm` já existente (reaproveitado, sem duplicar lógica de upload/tradução/username) dentro de um `Dialog`, que ganhou um `onSaved` opcional pra fechar sozinho ao salvar. `mensagens/page.tsx` redireciona o dono/gestor de volta pro perfil. De quebra, corrigido um bug pré-existente: dono de perfil `privacy_mode='private'` ficava trancado do próprio perfil por não ter vínculo em `partners` consigo mesmo. Link "Ver meu perfil" do sidebar parou de abrir em nova aba (`target="_blank"` removido, ícone trocado de `ExternalLink` pra `UserCircle`); links pra perfis de outras pessoas no feed do dashboard mantêm nova aba. Novas chaves i18n em `PublicProfile` nas 3 línguas — seções 7 (nova 7.12), 8 e 10 atualizadas.
- **2026-07-14** — Recebimento automático via Stripe Connect + recorrência real na página pública (migration `031_stripe_connect_and_recurring_pledges.sql`): `payment_methods` ganhou tipo `'stripe'` (conta conectada do próprio missionário, dinheiro cai direto nela) e `linked_account_id` (deposita a cobrança confirmada numa `financial_accounts` automaticamente); nova tabela `recurring_pledges` representa o compromisso de recorrência em si (distinto de cada `pledges` gerado mês a mês) e **exige conta** (`reporter_user_id NOT NULL`) — diferente do pledge avulso anônimo. Na página de parceria, "Ser parceiro fixo" trocou `PledgeForm` por `RecurringPledgeForm`: se deslogado, manda pra `/cadastro`/`/login` (que ganharam suporte a `?redirect=`, antes só existia em `/login`) e volta exatamente pro formulário via `?choice=financial_ongoing`; se o missionário tem Stripe conectado, oferece assinatura automática (`POST /api/stripe/checkout-recurring` → Stripe Checkout `mode=subscription` na conta conectada, sem `application_fee`); sempre disponível o caminho manual (qualquer método, incluindo `bank_transfer`) com opção de lembrete mensal por e-mail. Webhook novo (`POST /api/stripe/webhook`, secret de Connect separado do de billing) confirma automaticamente cada cobrança (`invoice.payment_succeeded` → `pledges` já `status='confirmed'` + `transactions` se houver `linked_account_id`) e reflete cancelamento (`customer.subscription.deleted`). Lembrete manual usa **Brevo** (`src/lib/email/brevo.ts`, chave ausente = desativado) via cron diário (`vercel.json` + `GET /api/cron/recurring-reminders`, protegida por `CRON_SECRET`), com link de descadastro. De quebra, `bank_transfer` no `PaymentMethodForm` genérico ganhou campos estruturados (titular, banco, conta/IBAN, SWIFT, routing, país) em vez de um textarea livre (`src/lib/payment-methods/bank-details.ts`). Registrada como nota de design (não implementada): granularidade de "seguir projeto" via `highlight_followers`, só faz sentido quando existir algum produtor real de notificação — ver seção 7.10. Novo namespace i18n `RecurringPledge` + chaves novas em `PaymentMethods` nas 3 línguas — seções 3.1, 7.1, 7.3, 7.10, 7.11 e 10 atualizadas.
- **2026-07-14** — Stripe Connect trocado de OAuth Standard para **Express + Account Links**: o motivo é que Standard exigia o missionário já ter uma conta Stripe própria pra logar e o dono da plataforma configurar `STRIPE_CONNECT_CLIENT_ID` (fluxo de OAuth registration na Stripe), enquanto Express deixa a própria plataforma criar a conta conectada e a Stripe hospeda um formulário de onboarding guiado (dados bancários/identidade) sem exigir nenhum client_id — mais perto do "cole seus dados de recebimento aqui, o sistema te guia" que existe pros outros métodos manuais (Pix etc.), mas sem o risco de guardar credencial sensível (é a própria Stripe que coleta, nunca o app). `GET /api/stripe/connect/start` agora cria `stripe.accounts.create({type:'express', country, ...})` (país escolhido antes, num `<select>` no `StripeConnectCard`) + `stripe.accountLinks.create` e redireciona pro onboarding hospedado; `GET /api/stripe/connect/callback` não recebe mais `code`/`state` (não é OAuth) — confere `details_submitted`/`charges_enabled` da conta pra decidir se marca `payment_methods.is_active=true` ou deixa pendente (`stripe=incomplete`, card mostra "Continuar configuração"); "Desconectar" trocou `stripe.oauth.deauthorize` por `stripe.accounts.del`. `STRIPE_CONNECT_CLIENT_ID` removido do `.env.example` (não é mais usado); `STRIPE_CONNECT_WEBHOOK_SECRET` continua necessário (webhook de Connect não muda). Novo `src/lib/stripe/connect-countries.ts` com subconjunto de países suportados pelo Express. Card ganhou nota de segurança fixa (`stripeSecurityNote`) explicando que nenhum dado bancário/senha fica salvo no app, traduzida nas 3 línguas — seções 2, 3 (`payment_methods`) e 7.11 atualizadas.
- **2026-07-13** — CTA "Quero ser parceiro de oração" (`PrayerRequestForm`) passou a linkar `/{username}/parceria?choice=prayer` em vez de `/{username}/parceria` puro — `PartnershipWizard` ganhou suporte a abrir direto numa sub-tela via `?choice=` (novo prop `initialChoice`, validado em `page.tsx` contra `VALID_CHOICES`), pra não obrigar mais um clique extra em "Comprometer-me em oração" depois de já ter clicado no CTA — seção 7.3 atualizada.
- **2026-07-13** — Formulário público de oração (`PrayerRequestForm`, `[username]/oracao`) ganhou CTA pra virar parceiro contínuo de oração: após enviar (`done=true`), um segundo card convida a "ser parceiro de oração" com link pra `/{username}/parceria`, mesmo padrão do card `onBecomePartner` do `PledgeForm` — motivado por a tela só oferecer o envio avulso, sem convite pra oração recorrente. Componente passou a receber `username` como prop; as duas strings novas foram pra uma nova namespace i18n `PrayerRequestForm` nas 3 línguas (resto do componente segue hardcoded em PT, só o texto novo foi migrado) — seção 7.6 atualizada.
- **2026-07-13** — Corrigidos 2 links "voltar" empilhados na página `/parceria` ao entrar em qualquer sub-tela do wizard (o fixo de `page.tsx`, "← Voltar ao perfil de X", ficava por cima do "← Voltar" interno do `PartnershipWizard`). O link pro perfil foi movido pra dentro do `PartnershipWizard` (agora recebe `username` como prop) e só aparece na tela inicial de escolha; nas sub-telas, some e só resta o "← Voltar" interno — um único link de voltar por vez — seção 7.3 atualizada.
- **2026-07-13** — Formulário público de parceria (`PartnershipForm`, acessado ao clicar em "Comprometer-me em oração"/"Divulgar e trazer apoiadores"/"Oferecer apoio pessoal" no `PartnershipWizard`) ganhou seletor de DDI + máscara no campo de telefone, reaproveitando o `PhoneInput` já usado no `AddPartnerButton` (antes era um `<Input>` livre com placeholder `+55 11 99999-9999`, sem máscara nem seleção de país). Label do campo virou "WhatsApp/Telefone" (existem lugares no mundo onde só se usa "telefone", não "WhatsApp") e essa string nova foi para a nova namespace i18n `PartnershipForm` (`whatsappLabel`) nas 3 línguas — resto do componente segue hardcoded em PT (só o texto tocado nesta mudança foi migrado, seguindo a regra de nunca introduzir texto novo hardcoded) — seções 7.3 e 10 (`ui/`) atualizadas.

- **2026-07-08** — Criada `/dashboard/ia` (Copiloto IA) — era um link de nav órfão (gap já documentado aqui). Mostra saldo de `ai_credits`, explica o único recurso de IA existente hoje (tradução de conteúdo, 1 crédito/uso, com atalhos pra Publicações/Perfil), nudge pra `/planos` no plano Free, e histórico via `ai_credit_transactions`. Deliberadamente **não** monta um fluxo de compra avulsa de créditos (`STRIPE_PRICE_CREDITS_20/50/100` do `.env.example` continuam sem uso em código — mesmo estado "schema/env pronto, sem integração" já descrito na seção 2, não é gap novo). Traduzido nas 3 línguas (namespace `AICopilot`) — seção 8 atualizada.
- **2026-07-08** — Mensagens diretas: 3 bugs corrigidos. (1) Bug de segurança/dados: `ensureResourceKey` usava `upsert` pra conceder acesso à DEK da conversa — com RLS, isso exige visibilidade (via policy de SELECT) de uma eventual linha conflitante de outra pessoa, travando silenciosamente a concessão de acesso a qualquer destinatário que não o próprio remetente (inclusive a primeiríssima mensagem de qualquer conversa nova). Trocado para `insert` simples + ignorar erro de duplicata (23505) — seção 6.2. (2) Descoberto que as policies de `encrypted_dek_grants` em produção estavam desalinhadas do que os arquivos de migration (015/018) já descreviam havia tempo — reaplicadas + nova migration `030_fix_dek_grants_rls_and_realtime.sql` pra não perder essa correção de novo; ver aviso novo na seção 3.3 sobre esse risco de drift migration-vs-banco. (3) `messages` nunca tinha sido adicionada à publicação `supabase_realtime` — respostas só apareciam recarregando a página; corrigido na mesma migration 030. Também corrigida a caixa de mensagens do dashboard (`/dashboard/mensagens` e `/dashboard/mensagens/[userId]`), que assumia `profile_id` sempre igual ao próprio perfil ativo e por isso nunca mostrava conversas iniciadas por mim como parceiro de outro missionário — seção 7.6 atualizada.
- **2026-07-08** — Criptografia ponta-a-ponta (E2EE) deixou de exigir qualquer configuração manual: a chave privada agora é derivada (Argon2id) da própria **senha de login**, desbloqueada automaticamente por `login/page.tsx`/`cadastro/page.tsx` logo após autenticar — funciona em qualquer dispositivo só de logar normalmente, sem código de recuperação pra guardar (mesmo modelo do Bitwarden/1Password). Chave também passou a ser cacheada em `localStorage` por dispositivo, então nem recarregar a página pede desbloqueio de novo. Contas sem senha (Google OAuth) continuam com um código aleatório como fallback, mostrado de forma discreta e não bloqueante (antes era uma tela cheia obrigatória "Ativar criptografia" com checkbox de confirmação). `E2EEGate` simplificado de 5 estados pra 2 (`checking → needs_password | ready`). Motivado por feedback direto: usuário não deveria precisar entender ou configurar nada pra mandar uma mensagem — seções 6.1, 6.2 e 7.5 atualizadas.
- **2026-07-08** — Página pública de mensagens (`[username]/mensagens`) parou de exigir ser parceiro pra poder mandar mensagem — a RLS de `messages` nunca exigiu isso (só `sender_id`/`recipient_id` = quem está autenticado), era uma trava adicional só na UI que forçava "Faça parte" antes de simplesmente poder conversar — seção 7.6 atualizada.
- **2026-07-08** — Página pública de oração (`[username]/oracao`) invertida de sentido: virou "enviar uma oração de apoio ao missionário" em vez de "pedir que o missionário ore por mim" — motivado por ficar destoante do tom convidativo do resto da área pública (parceria + oração = funil de participação). Mudança só de copy (`PrayerRequestForm`, título/subtítulo da página), sem alterar schema/inbox — ver nota na seção 7.6.
- **2026-07-08** — Wizard de parceria (`PartnershipWizard`) ganhou opção de doação avulsa **sem** precisar vir de um link de campanha específica (`financial_once_general`) — antes, sem `?highlight_id=` na URL, a única opção financeira era virar parceiro fixo mensal. `PledgeForm`, após confirmar uma doação avulsa, agora convida o doador a virar parceiro fixo (card com CTA que volta o wizard pra `financial_ongoing`) — seção 7.3 atualizada. Também adicionado link discreto "← Voltar ao perfil" nas páginas `parceria`, `oracao` e `mensagens` (não existia jeito de voltar sem usar o botão do navegador).
- **2026-07-07** — Corrigida sensação de "travamento" pós-`router.refresh()` em ~20 componentes: novo hook compartilhado `usePendingAction` (`src/hooks/use-pending-action.ts`, seção 5.5.1) envolve a ação em `startTransition` (React 19) em vez de zerar o loading local logo após a chamada síncrona de `router.refresh()`/`router.push()` — que não espera o Server Component terminar de re-buscar/re-renderizar. Motivado pelo caso mais perceptível (troca de idioma em Configurações → Conta, onde o `RootLayout` inteiro precisa re-renderizar para o `NextIntlClientProvider` atualizar), mas aplicado em todos os lugares com o mesmo padrão estrutural: `account-form.tsx`, `language-switcher.tsx`, `profile-form.tsx`, `privacy-form.tsx`, `payment-method-form.tsx`, `payment-method-card.tsx`, `category-tree.tsx`, `transaction-table.tsx`, `transaction-form.tsx`, `financial/account-form.tsx`, `manage-members-dialog.tsx`, `pledge-review-card.tsx`, `project-team-panel.tsx`, `highlight-form.tsx`, `post-editor.tsx`, `login/page.tsx`, `recuperar-senha/nova-senha/page.tsx` — seção 5.5.1 nova.
- **2026-07-07** — Redesenho da aba Pagamentos em Configurações: as 4 colunas fixas de `profiles` (`pix_key`/`paypal_url`/`wise_url`/`external_donation_url`) foram substituídas por uma nova tabela `payment_methods` (migration `029_payment_methods.sql`, com backfill dos dados existentes e RLS própria), suportando múltiplos métodos por perfil com um catálogo de 14 tipos agrupados por contexto (Pix/Mercado Pago, PayPal/Wise/transferência internacional/Revolut, Zelle/Venmo/Cash App, Alipay/WeChat Pay, M-Pesa, carteira cripto, "outro" com label livre) em `src/lib/payment-methods/catalog.ts` — motivado por feedback de que a tela só cobria Brasil/EUA. UI trocou o formulário fixo `payment-form.tsx` por `PaymentMethodsList`/`PaymentMethodForm`/`PaymentMethodCard` (padrão dialog criar/editar + grid de cards, igual a `financial/account-form.tsx`/`account-card.tsx`). Atualizados para ler da nova tabela: página de parceria, página pública de projeto, `ProfileHeader`, checklist do dashboard e passo 2 do onboarding (que continua simples: só Pix/PayPal/Wise, com aviso de que mais opções ficam em Configurações). `pledges.payment_method` teve o `CHECK` ampliado para o mesmo catálogo. Novo namespace i18n `PaymentMethods` (substitui `PaymentForm`) nas 3 línguas — seções 3.1, 7.1 e 10 atualizadas.
- **2026-07-07** — Cartão de crédito no módulo financeiro: `financial_accounts` ganhou `closing_day`/`due_day`/`card_brand`/`archived` e `transactions` ganhou `fatura_date`/`fatura_paid` (migration `028_credit_card_fatura.sql`); `AccountForm`/`AccountCard` mostram limite/bandeira/fatura atual para contas `account_type='credit'`, `TransactionForm` atribui a fatura automaticamente a partir do dia de fechamento. Nova página `dashboard/financeiro/cambio` (conversor de moedas client-side via API pública `open.er-api.com`, sem tabela própria), adicionada ao `FinanceSubNav`. Modelo e lógica de fatura reaproveitados do projeto GranaZen (branch `master` deste repo — histórico git não relacionado ao de `main`, era um app pessoal de finanças anterior); UI foi reconstruída do zero no padrão shadcn já usado aqui, não copiada. Fora do escopo desta rodada: parcelamento de compra e baixa manual de fatura (`fatura_paid=true`) — seção 5 (Financeiro) atualizada.
- **2026-07-06** — Campo opcional `partners.birth_date` (migration `027_partners_birth_date.sql`), preenchível no modal `AddPartnerButton` e no formulário público `PartnershipForm`. Novo `src/lib/partners/birthdays.ts` (`getUpcomingBirthdays`) calcula em memória quem faz aniversário nos próximos 14 dias e novo `BirthdayReminders` (Server Component, `src/components/dashboard/birthday-reminders.tsx`) mostra um banner no topo do dashboard com atalho para parabenizar (chat interno se o parceiro tem conta, senão `wa.me` se tem WhatsApp). É um cálculo feito a cada carregamento da página — não há job agendado (`pg_cron`), então só "lembra" o missionário se ele abrir o dashboard. Nova namespace i18n `Birthdays` em `messages/{pt,en,es}.json` — seções 4 (`partners`), 7.1 e 10 atualizadas.
- **2026-07-06** — Modal "Novo parceiro" (`AddPartnerButton`) ganhou seletor de DDI com bandeira + máscara por país: novo componente `src/components/ui/phone-input.tsx` (não-shadcn, lista curada de países), usado em dois campos — WhatsApp (`phone`, já existia) e Telefone opcional (`phone_alt`, coluna nova, migration `026_partners_phone_alt.sql`) — seções 4 (`partners`) e 10 (`ui/`) atualizadas.
- **2026-07-06** — Perfil público (`[username]/page.tsx`) ganhou feed de publicações: query em `posts` (`is_draft=false`, `order by published_at desc`, join com `highlights(title, slug)` via `project_id`) renderizada pelo novo `publications-feed.tsx`. `projects-section.tsx` foi redesenhado de lista de cards para faixa horizontal de "destaques" estilo Instagram (bolinha com anel na cor de destaque + título/progresso abaixo) — seção 7.9 (componentes de `profile/`) atualizada.
- **2026-07-07** — `LocaleContentTabs` ganhou um aviso não bloqueante de cobertura de idioma (lista os idiomas sem tradução e avisa que parceiros nesses idiomas verão só o texto original) e passou a abrir por padrão na aba do idioma da conta (`preferredLocale`), não mais na aba do idioma original do conteúdo; componente também migrado para `next-intl` (estava com "Traduzir com IA" hardcoded desde que foi criado) — seção 5.6.1 atualizada.
- **2026-07-07** — Tela de Configurações 100% traduzida (PT/EN/ES): `SettingsTabs`, `ProfileForm`, `PaymentForm`, `PrivacyForm`, `SensitiveDataForm`, `AccessManagersForm` e `AccountForm` migrados de texto hardcoded para `next-intl` (namespaces `SettingsPage`/`ProfileForm`/`PaymentForm`/`PrivacyForm`/`SensitiveDataForm`/`AccessManagersForm`/`AccountForm`). Motivado por bug relatado pelo usuário: trocar o idioma em Configurações não tinha efeito porque essas telas nunca usaram `next-intl`. Regra permanente registrada: toda mudança de UI daqui pra frente deve considerar as 3 línguas — seção 5.6 atualizada.
- **2026-07-07** — Implementada a recuperação de senha (`/recuperar-senha` + `/recuperar-senha/nova-senha`), fechando um gap conhecido (a rota era referenciada no login mas não existia): `resetPasswordForEmail` reaproveita o mesmo `GET /auth/callback` (troca de `code` por sessão via PKCE) já usado no login OAuth; página de nova senha confere `getSession()` no mount para tratar link inválido/expirado; novas chaves em `messages/{pt,en,es}.json` (namespace `Auth`) — seções 5.2, 8 e 13 atualizadas. Também corrigido bug de idioma: `src/i18n/request.ts` agora prioriza `profiles.locale` sobre o cookie `NEXT_LOCALE` para usuário logado (antes o cookie sempre vencia, então trocar idioma na tela pública "vazava" pro dashboard); `LanguageSwitcher` da tela pública parou de gravar em `profiles.locale`, só afeta o cookie do visitante. E ajustes de UX no login/cadastro: logo com link para `/` no `AuthLayout` (antes não havia como voltar à área pública) e `tabIndex={-1}` no link "Esqueceu a senha?" (antes o Tab ia parar nele em vez de ir para o campo de senha).
- **2026-07-06** — Trocado o provedor de e-mail transacional planejado de Resend para Brevo: dependência `resend` removida, `@getbrevo/brevo` instalada; `RESEND_API_KEY`/`RESEND_FROM_EMAIL` renomeadas para `BREVO_API_KEY`/`BREVO_FROM_EMAIL` (`.env.example`, `.env.local`). Nenhum código de envio existia ainda (era só dependência/placeholder, sem uso em `src/`), então é só a troca do scaffold — seções 2 e 5.6/pendências não mudam de status ("planejado, sem integração ainda") — seção 2 atualizada.
- **2026-07-05** — Conteúdo multi-idioma (PT/EN/ES) em posts e bio, via tradução assistida por IA: migration 025 adiciona `posts.original_locale`/`translations` e `profiles.bio_locale`/`bio_translations` (JSONB, só as traduções — o texto original continua só em `content`/`bio`) + função `consume_ai_credits` (débito atômico de `profiles.ai_credits`); primeira integração real de IA do projeto (`src/lib/ai/`, usa `@anthropic-ai/sdk` com `claude-haiku-4-5`) via `POST /api/ai/translate`; novo componente `LocaleContentTabs` (abas de idioma, tradução manual sempre grátis + botão opcional "Traduzir com IA") em `PostEditor` e `ProfileForm`; gravação de posts migrou de insert/update direto do client para a Server Action `savePost` (`src/app/dashboard/publicacoes/actions.ts`); renderização pública (`profile-header.tsx`, `[username]/projetos/[slug]/page.tsx`) resolve o idioma certo para o visitante via `resolveLocalizedText` — seções 3.1, 3.2, 5.3, 5.6, 7.9 e 10 atualizadas.
- **2026-07-05** — Internacionalização (i18n): `ProfileCTA`, `ProfileHeader` e `[username]/page.tsx` (metadata + `PrivateProfileScreen`) traduzidos — os CTAs e o cabeçalho do perfil público principal agora respeitam o idioma; motivado pelo usuário testar a bandeira e ver os botões "Seja Parceiro"/"Enviar Oração" etc. ainda em português. Demais subpáginas de `[username]/*` (historia, trajetória, parceria, oração, mensagens, projetos) seguem pendentes.
- **2026-07-05** — Internacionalização (i18n) Fase 2 (parcial): `OnboardingWizard` + `AccountTypeSelector` (agora com hook `useAccountTypeCopy`) e o "shell" do dashboard (sidebar desktop/mobile, notificações com mensagens interpoladas, `SetupChecklistBanner`, visão geral) traduzidos; novo `src/app/[username]/layout.tsx` adiciona `LanguageSwitcher` flutuante (topo direito) em todas as páginas públicas de perfil, mesmo antes delas serem totalmente traduzidas — parceiros internacionais já conseguem trocar o idioma da navegação. Restante do dashboard (publicações/projetos/parceiros/financeiro/orações/mensagens/configurações) e o conteúdo das páginas `[username]/*` continuam em português.
- **2026-07-05** — Internacionalização (i18n) Fase 1: `next-intl` instalado (sem prefixo de URL), locale resolvido por cookie `NEXT_LOCALE` → `profiles.locale` (nova coluna, migration 024) → default `pt`; `messages/{pt,en,es}.json` com o site público inteiro traduzido (landing, `/planos`, `SiteNav`/`SiteFooter`, login, cadastro); `LanguageSwitcher` com bandeiras no `SiteNav` e em Configurações → Conta; `src/lib/modules.ts` e `src/lib/pricing.ts` refatorados para guardar só `id`/ícone/cor/preço, texto migrado para os `messages/*.json`. Onboarding e dashboard inteiro ainda em português — Fase 2 planejada, não escondida — seções 2, 3.1, 5.6 e 10 atualizadas.
- **2026-07-05** — Contas vinculadas estilo Instagram: nova tabela `profile_managers` (migration 023, roles `manager`/`viewer`) + `profiles.extra_manager_seats`; `is_profile_owner()`/`is_profile_viewer_or_above()` (SQL) estendidas para reconhecer gestores, e as políticas de RLS que ainda faziam check inline (posts, highlights, prayer_requests, transaction_categories, subscriptions, ai_credit_transactions, whatsapp_config, history_blocks) migradas para os helpers; convite por e-mail via RPC `invite_profile_manager` com limite de assentos (`planLimits().managersIncluded` + pacotes pagos `MANAGER_ADDONS`); troca de conta ativa via cookie (`getActiveProfile()`, `src/lib/profile/active-profile.ts`) + dropdown na sidebar; nova aba "Acesso" nas Configurações (`AccessManagersForm`); dezenas de páginas do dashboard migradas do lookup `profiles.eq('user_id', ...)` para `getActiveProfile()` — seção 3.1 atualizada.
- **2026-07-05** — Nova coluna `profiles.account_type` (migration 022, `individual`/`family`/`organization`, default `individual`): seletor em `AccountTypeSelector` (`src/components/profile/account-type-selector.tsx`), usado no passo 1 do `OnboardingWizard` e em `ProfileForm`, personaliza placeholder/dica de nome e bio conforme o tipo; ao escolher "organização", mostra dica recomendando conta separada (perfil público + Pix/PayPal próprios), já que hoje um `highlight` (projeto) sempre herda os dados de recebimento do perfil-dono — seção 3.1 atualizada.
- **2026-07-05** — Hero da landing (`/`) virou 2 colunas: texto+CTA à esquerda, foto (`public/hero-missionario.png`, gerada por IA a pedido do autor) num card arredondado com badge flutuante à direita — reforça a mensagem "financeiro/parceiros/comunicação resolvidos, foco na missão". `useSignOut()` (`src/components/dashboard/sidebar.tsx`) passou a redirecionar para `/` em vez de `/login` após sair.
- **2026-07-05** — Nova coluna `profiles.show_location` (migration 021, default `true`): checkbox "Mostrar minha localização no perfil público" no passo 1 do `OnboardingWizard` e em `ProfileForm` (configurações); `ProfileHeader` só exibe `location` quando `privacy_mode === 'public'` **e** `show_location` for `true`. Bio ganhou texto de orientação (quem/onde/por quê) em ambos os formulários — seção 3.1 atualizada.
- **2026-07-05** — Landing page redesenhada com inspiração no benchmark de layout da stripe.com/br (nav com mega-menu, bento grid de módulos, seção "deep-dive" dos 3 módulos mais estratégicos, tira de diferenciais honestos — sem depoimentos/números fabricados): novo `src/lib/modules.ts` como fonte única dos 7 módulos (ícone/descrição/bullets/cor) consumida por `SiteNav`, `SiteFooter` e a própria landing, para a página pública não ficar defasada conforme o produto cresce — seção 10 atualizada.
- **2026-07-05** — Landing page reformulada (tour dos módulos + "como funciona" + teaser de planos), onboarding guiado de 4 passos após o cadastro (`OnboardingWizard`, cada etapa pulável), `SetupChecklistBanner` substituindo o banner de perfil incompleto (agora cobre perfil/recebimento/primeiro projeto), nova página pública `/planos` com preços e CTA de assinatura, e scaffold de checkout Stripe (`src/lib/stripe/client.ts` + `POST /api/billing/checkout`, inativo até `STRIPE_SECRET_KEY`/`STRIPE_PRICE_*` serem preenchidos) — seções 2, 7.1, 8 e 10 atualizadas.
- **2026-07-05** — Criação inicial deste documento, a partir de varredura completa do estado do repositório (migrations 001–017, `src/lib`, `src/app`, `src/components`, `src/proxy.ts`, `src/types`).
